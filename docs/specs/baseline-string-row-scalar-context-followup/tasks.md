# Baseline String Row-Scalar Context Follow-up Tasks

- [x] Specify the context-routing follow-up and compatibility limits.
- [x] Admit the selected existing string functions in parser retry contexts.
- [x] Add MySQL-runtime expectation probes for `ORDER BY` and `UPDATE`.
- [x] Add focused C runtime coverage for `ORDER BY` and `UPDATE`.
- [x] Update string-function compatibility notes.
- [x] Run focused runtime tests and MySQL expectation scripts.
- [x] Run diff checks, workflow checks, review, commit, and push.
