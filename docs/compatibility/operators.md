# Operators

Operators, predicates, assignment forms, and SQL expression syntax that MySQL lists alongside built-ins.

| Function or operator | Status | Notes |
| --- | --- | --- |
| `&` | 🟡 | Limited no-source/`DUAL` unsigned-64 numeric scalar bitwise projection and limited `DO` expression execution over the current signed-64 scalar arithmetic operand domain; no table-backed expression support or binary-string bit operations |
| `>` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `>>` | 🟡 | Limited no-source/`DUAL` unsigned-64 numeric scalar right-shift projection and limited `DO` expression execution over the current signed-64 scalar arithmetic operand domain; shift counts greater than or equal to 64 produce `0`; no table-backed expression support or binary-string bit operations |
| `>=` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `<` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `<>, !=` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible `ENUM` labels or ordinals, compatible `SET` member lists or bitmaps, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `<<` | 🟡 | Limited no-source/`DUAL` unsigned-64 numeric scalar left-shift projection and limited `DO` expression execution over the current signed-64 scalar arithmetic operand domain; shift counts greater than or equal to 64 produce `0`; no table-backed expression support or binary-string bit operations |
| `<=` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `<=>` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible `ENUM` labels or ordinals, compatible `SET` member lists or bitmaps, string/`ENUM`/`SET` `NULL`, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 scalar NULL-safe equality projection and limited `DO` expression execution |
| `%, MOD` | 🟡 | Limited no-source/`DUAL` signed-64 scalar modulo projection, limited `DO` expression execution, and limited constant integer expression-default evaluation with `%`, infix `MOD`, and `MOD(left, right)`; no table-backed expression support |
| `*` | 🟡 | Limited no-source/`DUAL` signed-64 scalar arithmetic projection, limited `DO` expression execution, limited constant integer expression-default evaluation, limited source-free constant integer `UPDATE` assignment expressions for integer-family targets, and limited single-table descriptor-backed signed integer projection over signed integer columns and integer/boolean/`NULL` operands; no unsigned, decimal, string, division/modulo, predicate, ordering-expression, or general table-backed expression support |
| `+` | 🟡 | Limited no-source/`DUAL` signed-64 scalar arithmetic projection, limited `DO` expression execution, limited constant integer expression-default evaluation, limited source-free constant integer `UPDATE` assignment expressions for integer-family targets, limited same-column integer `UPDATE` assignment `column = column + unsigned_integer_literal` for non-key, non-auto-increment integer-family targets, and limited single-table descriptor-backed signed integer projection over signed integer columns and integer/boolean/`NULL` operands; no unsigned, decimal, string, division/modulo, predicate, ordering-expression, or general table-backed expression support |
| `-` (binary) | 🟡 | Limited no-source/`DUAL` signed-64 scalar arithmetic projection, limited `DO` expression execution, limited constant integer expression-default evaluation, limited source-free constant integer `UPDATE` assignment expressions for integer-family targets, limited same-column integer `UPDATE` assignment `column = column - unsigned_integer_literal` for non-key, non-auto-increment integer-family targets, and limited single-table descriptor-backed signed integer projection over signed integer columns and integer/boolean/`NULL` operands; no unsigned, decimal, string, division/modulo, predicate, ordering-expression, or general table-backed expression support |
| `-` (unary) | 🟡 | Limited no-source/`DUAL` signed-64 scalar unary arithmetic projection, limited `DO` expression execution, limited constant integer expression-default evaluation with unary `+` and unary `-`, and limited source-free constant integer `UPDATE` assignment expressions for integer-family targets; no table-backed expression support or unsigned-width expression results |
| `/` | 🟡 | Limited no-source/`DUAL` top-level scalar division and limited `DO` expression execution over signed-64 scalar arithmetic operands, returning four fractional digits for integer/integer inputs; no nested slash composition, table-backed expression support, decimal/float/string/hex/bit conversion, or expression metadata |
| `:=` | 🟡 | Limited to user-variable assignment inside supported `SET @name := value` assignment lists; assignment expressions elsewhere remain unsupported |
| `=` (assignment) | 🟡 | One unqualified descriptor-column single-table `UPDATE` assignment to a supported decimal integer literal, limited source-free constant integer arithmetic expression for integer-family targets, compatible bit literal for `BIT` targets, fixed decimal literal for decimal targets, compatible `ENUM` label or ordinal for enum targets, compatible `SET` member list or bitmap for set targets, compatible canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` string literal for temporal targets, binary string or hex literal for binary string targets, `TRUE`, `FALSE`, `NULL`, descriptor-resolved `DEFAULT`, limited scalar subquery values, or limited same-column integer `+`/`-` arithmetic for non-key targets; limited supported system-variable `SET` assignments; limited user-variable `SET @name = value` assignments; general table-backed expression assignments remain unsupported |
| `=` (comparison) | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with non-`NULL` decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible `ENUM` labels or ordinals, compatible `SET` member lists or bitmaps, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands; limited descriptor `column = NULL`, signed-64 scalar-literal equality, and signed-64 literal-left descriptor equality are also supported in `WHERE`; string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `^` | 🟡 | Limited no-source/`DUAL` unsigned-64 numeric scalar bitwise XOR projection and limited `DO` expression execution over the current signed-64 scalar arithmetic operand domain; distinct from keyword logical `XOR`; no table-backed expression support or binary-string bit operations |
| `ABS()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `SIGN()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct exact-decimal integer literals, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `CEIL()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals up to 81 significant digits, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no decimal/float rounding, table-backed expression support, string/hex/bit conversion, or expression metadata |
| `CEILING()` | 🟡 | Synonym for the limited `CEIL()` subset |
| `FLOOR()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals up to 81 significant digits, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no decimal/float rounding, table-backed expression support, string/hex/bit conversion, or expression metadata |
| `ROUND()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals up to 81 significant digits, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; `ROUND(value, places)` additionally supports signed-64 negative-place rounding plus nonnegative places over the current one-argument envelope; no decimal/float rounding, table-backed expression support, string/hex/bit conversion, unsigned-boundary negative-place rounding, or expression metadata |
| `BIN()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned/signed envelopes, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; returns unsigned 64-bit base-2 text; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `OCT()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned/signed envelopes, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; returns unsigned 64-bit base-8 text; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `CONV()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution for `CONV(value, from_base, to_base)` over integer/boolean/`NULL`, direct decimal integer literals in the admitted signed/unsigned value envelope, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise value operands; base arguments are signed-64 scalar arithmetic values with absolute range `2..36`; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `SQRT()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned-64 magnitude envelope, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; negative inputs return `NULL` without warnings; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `ACOS()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned-64 magnitude envelope with optional sign, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; values outside `-1..1` return `NULL` without warnings; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `ASIN()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned-64 magnitude envelope with optional sign, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; values outside `-1..1` return `NULL` without warnings; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `SIN()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned-64 magnitude envelope with optional sign, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `COS()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned-64 magnitude envelope with optional sign, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `TAN()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned-64 magnitude envelope with optional sign, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `COT()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over the same operand domain as `COS()`; nonzero inputs return MySQL-shaped double text, zero-valued inputs report `1690 / 22003`; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `ATAN()` / `ATAN2()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution for one-argument `ATAN(value)` / `ATAN2(value)` plus two-argument `ATAN(y, x)` / `ATAN2(y, x)` over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned-64 magnitude envelope with optional sign, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; two-argument forms short-circuit when `y` is `NULL`; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `DEGREES()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned-64 magnitude envelope with optional sign, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `RADIANS()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, direct decimal integer literals in the admitted unsigned-64 magnitude envelope with optional sign, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no table-backed expression support, string/decimal/float/hex/bit conversion, or expression metadata |
| `AND, &&` | 🟡 | Limited descriptor-backed `WHERE` boolean expressions over existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate atoms, including the limited scalar-literal predicate atoms; `AND` binds tighter than `XOR` and `OR`, `&&` is accepted with deprecation warning 1287, and limited no-source/`DUAL` scalar logical projection plus limited `DO` expression execution admit keyword `AND` only |
| `BETWEEN ... AND ...` | 🟡 | Limited descriptor-backed `WHERE` range predicates for existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate contexts with one descriptor column and two decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string bounds; also supported over one synthetic `INFORMATION_SCHEMA` metadata column with supported metadata literals; string ranges use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation for base tables and metadata-declared ASCII collations for `INFORMATION_SCHEMA`; `TIME` ranges use signed total-second semantics; no expression operands, non-ASCII/full-Unicode string collation parity, row ranges, or full expression-level operator support |
| `BINARY` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `SELECT` `CAST(value AS BINARY)`, `CONVERT(value, BINARY)`, and `CONVERT(value USING BINARY)` over admitted scalar values plus descriptor integer-family and nonbinary string-family row operands with bare `BINARY` only; no length-bearing binary casts, deprecated unary `BINARY`, binary string or `BIT` row operands, predicates, DML assignments, embedded-NUL scalar delivery, or binary comparison semantics |
| `BIT_COUNT()` | 🟡 | Limited no-source/`DUAL` scalar projection and limited `DO` expression execution over integer/boolean/`NULL`, signed-64 scalar arithmetic, and limited unsigned-64 numeric bitwise operands; no table-backed expression support or binary-string bit counting |
| `CASE` | 🟡 | Limited no-source and `FROM DUAL` top-level scalar searched and simple `CASE` projection plus limited `DO` expression execution whose children may use existing non-`CASE` signed-64 scalar expressions; no nested `CASE`, `CASE` as a larger scalar operand, table-backed evaluation, predicates, DML assignments, string/collation behavior, subqueries, full result type aggregation, or expression metadata |
| `DIV` | 🟡 | Limited no-source/`DUAL` signed-64 scalar integer division projection, limited `DO` expression execution, and limited constant integer expression-default evaluation; no table-backed expression support |
| `EXISTS()` | 🟡 | Limited descriptor-driven `SELECT ... WHERE EXISTS (subquery)` predicate slice for one outer base table and tableless/`DUAL` or one-table inner subqueries with optional limited predicate, simple correlated integer equality/null-safe equality, and `LIMIT row_count`; also supports uncorrelated `[NOT] EXISTS` filters for no-source/`FROM DUAL` row-scalar `INSERT ... SELECT` sources; no scalar projection, other DML predicates, nested subqueries, derived tables, joins, arbitrary expressions, or optimizer parity |
| `IN()` | 🟡 | Limited descriptor-backed `WHERE` membership predicates for existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate contexts with one descriptor column and a nonempty list of decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` strings, or `NULL` values; also supports limited descriptor-driven `SELECT ... WHERE descriptor_column IN (SELECT descriptor_column FROM descriptor_table [WHERE ...])` subqueries over compatible integer-family or ASCII nonbinary string-family columns in the current single-table `SELECT` predicate envelope, plus literal-list `INFORMATION_SCHEMA` metadata predicates; string membership uses MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation for base tables and metadata-declared ASCII collations for `INFORMATION_SCHEMA`; `TIME` membership uses signed total-second semantics; no expression operands, metadata predicate subqueries, subqueries outside the documented `SELECT` predicate slice, row constructors, non-ASCII/full-Unicode string collation parity, or full expression-level operator support |
| `IS` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates for descriptor-column `IS NULL`, `IS TRUE`, `IS FALSE`, and `IS UNKNOWN`, plus limited signed-64 scalar-literal `WHERE` truth tests for the same forms; limited no-source/`DUAL` signed-64 scalar projection and limited `DO` expression execution for `IS NULL`, `IS TRUE`, `IS FALSE`, and `IS UNKNOWN`; no table-backed expression projection, string truth conversion, or unparenthesized scalar `IS` chaining |
| `IS NOT` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates for descriptor-column `IS NOT NULL`, `IS NOT TRUE`, `IS NOT FALSE`, and `IS NOT UNKNOWN`, plus limited signed-64 scalar-literal `WHERE` truth tests for the same forms; limited no-source/`DUAL` signed-64 scalar projection and limited `DO` expression execution for `IS NOT NULL`, `IS NOT TRUE`, `IS NOT FALSE`, and `IS NOT UNKNOWN`; no table-backed expression projection, string truth conversion, or unparenthesized scalar `IS` chaining |
| `IS NOT NULL` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates over admitted integer, decimal, string, binary string, `BIT`, limited `ENUM`, limited `SET`, and temporal descriptor columns, plus limited signed-64 scalar-literal `WHERE` operands; limited no-source/`DUAL` scalar projection and limited `DO` expression execution over admitted signed-64 scalar operands |
| `IS NULL` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates over admitted integer, decimal, string, binary string, `BIT`, limited `ENUM`, limited `SET`, and temporal descriptor columns, plus limited signed-64 scalar-literal `WHERE` operands; limited no-source/`DUAL` scalar projection and limited `DO` expression execution over admitted signed-64 scalar operands |
| `LIKE` | 🟡 | Limited descriptor-backed `WHERE` predicate for `CHAR`, `VARCHAR`, and baseline `TEXT` family columns with an ASCII string pattern literal in existing filtered `SELECT`, aggregate-source, `DELETE`, and `UPDATE` contexts, plus one-column synthetic `INFORMATION_SCHEMA` metadata predicates; default mode supports `%`, `_`, and backslash wildcard escaping, while `NO_BACKSLASH_ESCAPES` disables backslash pattern escaping for this slice; metadata predicates use metadata-declared ASCII collations; no explicit `ESCAPE`, non-ASCII collation parity, expression operands, parameter patterns, binary strings, or general expression-level pattern matching |
| `NOT, !` | 🟡 | Limited keyword `NOT` over descriptor-backed `WHERE` boolean expressions for existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate atoms; limited no-source/`DUAL` scalar logical projection and limited `DO` expression execution admit keyword `NOT`; symbolic `!` remains unsupported until MyLite owns its higher-precedence expression semantics and deprecation warning behavior |
| `NOT BETWEEN ... AND ...` | 🟡 | Limited negation of the descriptor-backed `BETWEEN` predicate subset; no expression operands, non-ASCII/full-Unicode string collation parity, row ranges, or full expression-level operator support |
| `NOT EXISTS()` | 🟡 | Limited negation of the descriptor-driven `EXISTS` predicate slice through existing `NOT` predicate semantics |
| `NOT IN()` | 🟡 | Limited negation of the descriptor-backed `IN` predicate subset, including MySQL three-valued `NULL` list and subquery semantics for the documented `SELECT` predicate slice; no expression operands, subqueries outside that slice, row constructors, non-ASCII/full-Unicode string collation parity, or full expression-level operator support |
| `NOT LIKE` | 🟡 | Limited negation of the descriptor-backed `LIKE` predicate subset in existing filtered `SELECT`, aggregate-source, `DELETE`, and `UPDATE` contexts; `NULL` column values remain unmatched like MySQL |
| `NOT REGEXP` | 🟡 | Limited negation of the descriptor-backed `REGEXP` predicate subset in existing filtered `SELECT`, aggregate-source, `DELETE`, and `UPDATE` contexts; `NULL` column values remain unmatched like MySQL |
| `OR, \|\|` | 🟡 | Limited descriptor-backed `WHERE` boolean expressions over existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate atoms, including the limited scalar-literal predicate atoms; `OR` binds looser than `XOR` and `AND`, and `\|\|` is accepted as logical OR with deprecation warning 1287 in predicate contexts when `PIPES_AS_CONCAT` is inactive. With `PIPES_AS_CONCAT`, limited `\|\|` string concatenation is supported for no-source/`DUAL` scalar projection, `DO`, and current single-table row-scalar `SELECT` projection; no predicate-level concatenation truth evaluation, scalar logical `\|\|` projection outside this mode, or general expression-level boolean/string semantics |
| `REGEXP` | 🟡 | Limited descriptor-backed `WHERE` predicate for `CHAR`, `VARCHAR`, and baseline `TEXT` family columns with an ASCII regular-expression string literal in existing filtered `SELECT`, aggregate-source, `DELETE`, and `UPDATE` contexts; no expression operands, parameter patterns, non-ASCII pattern literals, or full regex syntax |
| `RLIKE` | 🟡 | Synonym for the limited descriptor-backed `REGEXP` predicate subset |
| `SOUNDS LIKE` | ❌ | Compare sounds |
| `XOR` | 🟡 | Limited descriptor-backed `WHERE` boolean expressions over existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate atoms, including the limited scalar-literal predicate atoms; `XOR` binds looser than `AND` and tighter than `OR`, propagates `NULL` for the admitted predicate subset, and has limited no-source/`DUAL` scalar logical projection plus limited `DO` expression execution support |
| `\|` | 🟡 | Limited no-source/`DUAL` unsigned-64 numeric scalar bitwise OR projection and limited `DO` expression execution over the current signed-64 scalar arithmetic operand domain; no table-backed expression support or binary-string bit operations |
| `~` | 🟡 | Limited no-source/`DUAL` unsigned-64 numeric scalar bitwise inversion projection and limited `DO` expression execution over the current signed-64 scalar arithmetic operand domain; no table-backed expression support or binary-string bit operations |

[Back to compatibility overview](../../COMPATIBILITY.md)
