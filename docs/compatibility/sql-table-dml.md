# SQL table DML

Table-shaped data modification, import, replace, and direct table access
statements.

Currently supported single-table `INSERT`, `REPLACE`, `UPDATE`, and `DELETE`
forms plus the limited joined `DELETE` and joined `UPDATE` forms that target
the synthetic `information_schema` schema are rejected before catalog lookup with
MySQL-compatible `1044 / 42000` access denied diagnostics. This does not imply
a privilege engine or writable system views.

The shared descriptor `WHERE` subset used by supported single-table `UPDATE`
and `DELETE` also admits limited ASCII `FIND_IN_SET(value, list)` predicates
over flat scalar values and descriptor-backed integer, exact `DECIMAL`,
nonbinary string, baseline `TEXT`, `ENUM`, `YEAR`, and temporal values, plus
limited `REGEXP_LIKE(value, pattern[, match_type])` predicates over nonbinary
string descriptor values and scalar literal patterns. `SET` column
member-ordinal behavior and full ICU regular-expression semantics remain
unsupported.

The current generated `AUTO_INCREMENT` DML subset honors session-local
`auto_increment_increment` and `auto_increment_offset` values for supported
persistent and temporary tables when `1 <= offset <= increment <= 65535`.
Mutable global values and the unclear MySQL edge case where offset exceeds the
increment remain deferred.

Ordinary non-strict DML has a limited implicit-default adjustment surface. When
neither `STRICT_TRANS_TABLES` nor `STRICT_ALL_TABLES` is active, supported
`INSERT` / `REPLACE` `VALUES` and `SET` statements warn and materialize the
existing descriptor implicit values for omitted no-explicit-default
non-`AUTO_INCREMENT` columns and explicit `DEFAULT` values targeting no-default
columns. Omitted `AUTO_INCREMENT` columns still use generated values. Supported
single-table `UPDATE` statements warn and materialize those same values for
matched rows when assigning `NULL` to `NOT NULL` columns or `DEFAULT` to
no-default columns; no-match updates and `LIMIT 0` do not record these
conversion warnings. Explicit `NULL` in ordinary `INSERT` / `REPLACE` still
errors outside `IGNORE`, and broader non-strict conversion remains deferred.

The current numeric DML storage conversion also admits exact quoted numeric
strings for descriptor-backed `INSERT`, `REPLACE`, admitted duplicate-key
assignment values, and single-table `UPDATE`: quoted decimal integers for
integer-family targets, quoted fixed decimal strings for exact decimal targets,
and quoted finite decimal or scientific-notation strings for approximate
targets. This does not imply general expression coercion, selected-row
conversion, prefix numeric string scanning, whitespace trimming, or broader
non-strict conversion.

| Feature | Status | Notes |
| --- | --- | --- |
| `DELETE` (single-table) | 🟡 | Limited persistent or shadowing session temporary base-table `DELETE FROM table` with optional baseline descriptor `WHERE` predicate boolean expression, including keyword `NOT`, `AND`/`&&`, `XOR`, `OR`/`\|\|`, limited `BETWEEN` range predicates, limited `IN` membership predicates, compatible `BIT` literal predicates, descriptor-column `IS [NOT] TRUE` / `FALSE` / `UNKNOWN`, limited ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string-literal equality/null-safe equality/inequality/range/membership predicates plus limited `LIKE` / `NOT LIKE` pattern predicates, limited `REGEXP` / `RLIKE` / `NOT REGEXP` / `NOT RLIKE` ASCII regular-expression predicates, and limited `REGEXP_LIKE()` predicates over nonbinary string descriptor values and scalar literal patterns, and limited `YEAR`, canonical `DATE` / `TIME`, and canonical plus limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string-literal predicates, one unqualified integer, `BIT`, `YEAR`, `DATE`, `TIME`, `DATETIME`, `TIMESTAMP`, or ASCII nonbinary string (`CHAR`, `VARCHAR`, baseline `TEXT` family) descriptor `ORDER BY` column, optional `ASC`/`DESC`, exact affected rows, `LIMIT row_count` using unsigned decimal literals in signed 64-bit range, parent-side rejection for rows referenced by supported `RESTRICT` / `NO ACTION` foreign-key descriptors, direct non-recursive `ON DELETE CASCADE` child removal, and direct non-recursive `ON DELETE SET NULL` child-key nulling for supported foreign-key descriptors without changing parent affected-row counts; admitted string comparisons, ranges, membership, and ordering use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, and `TIME` range predicates and ordering use signed total-second semantics; no aliases, partitions, modifiers, `USING`, non-ASCII string predicate literals or full Unicode string ordering, full ordering, LIMIT offset forms, triggers, recursive actions beyond the current direct FK action subset, or privilege semantics |
| `DELETE` (multi-table) | 🟡 | Limited one-target two-source joined deletes through `DELETE target FROM left JOIN right ...` and `DELETE FROM target USING left JOIN right ...`, over persistent or shadowing session temporary base tables, with descriptor-driven target/source/alias/schema resolution, the current two-source `JOIN` / `INNER JOIN` / `CROSS JOIN` / `LEFT JOIN` envelope, current joined `ON` equality and joined `WHERE` predicate subsets, exact affected rows for target rows deleted once, and direct non-recursive parent-side FK actions for deleted target rows; no multiple targets, `target.*`, comma table references, nested or derived table references, subqueries, CTEs, joined-delete `ORDER BY` / `LIMIT`, partitions, modifiers, triggers, privileges, recursive FK actions, or broader joined-source grammar |
| `HANDLER` | ❌ | HANDLER OPEN, READ, and CLOSE cursor-like table access |
| `INSERT ... VALUES` | 🟡 | Limited single- and multi-row `INSERT [LOW_PRIORITY \| HIGH_PRIORITY \| DELAYED] [IGNORE] [INTO] table_name ... VALUES ...` into persistent or shadowing session temporary base tables, including explicit or omitted empty all-default rows with `VALUES ()`, `VALUE ()`, or `VALUES ROW()`, and nonempty row lists with ordinary `VALUES`, `VALUE`, or homogeneous `VALUES ROW(...)` syntax, with optional unqualified column lists, integer/fixed decimal/approximate numeric/`NULL`/`TRUE`/`FALSE`/`DEFAULT` values plus ordinary string literals for `CHAR`, `VARCHAR`, baseline `TEXT` family, and `YEAR` and canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` targets plus zero-fractional `CURRENT_DATE` / `UTC_DATE` for `DATE`, `CURRENT_TIME` / `UTC_TIME` for `TIME`, and `UTC_TIMESTAMP` for `DATETIME` / `TIMESTAMP` targets, ordinary string or hex literals for binary string targets, and compatible integer/boolean/bit/string/hex literals for `BIT` targets, descriptor-driven assignment, omitted columns and explicit `DEFAULT` values filled from descriptor numeric/string/temporal/`YEAR`/`BIT` defaults or effective `NULL` defaults, limited generated values for the current indexed persistent and session-temporary auto-increment subset, strict range/nullability/no-default/string-length/binary-length/temporal diagnostics including decimal rounding notes, approximate range checks, and default-mode `CHAR` trimming, `VARCHAR` trailing-space truncation notes, and limited non-strict and `IGNORE` `CHAR`/`VARCHAR` nonspace truncation warnings, duplicate enforcement for the current supported primary-key and unique-index subset, child-side checks for supported foreign-key descriptors, affected rows, all-or-nothing statement behavior, no-op `LOW_PRIORITY` / `HIGH_PRIORITY`, warning-producing deprecated `DELAYED`, and limited `IGNORE` warning demotion for omitted no-explicit-default columns, explicit `DEFAULT` for no-default columns, explicit `NULL` into numeric, string, binary string, `BIT`, `YEAR`, or temporal `NOT NULL`, integer/decimal range clipping, `BIT` over-width clipping, binary string truncation, invalid temporal-to-zero adjustment, `TIME` endpoint clipping, and duplicate key rows; no-column-list nonempty rows target visible descriptor columns only, empty rows target zero explicit columns and materialize all descriptor defaults, while explicit column lists may name invisible columns; nullable dropped-default columns warn and store `NULL`, numeric `NOT NULL` no-default columns warn and store target zero values, binary string `NOT NULL` no-default columns warn and store the MySQL-compatible implicit binary value, `BIT NOT NULL` no-default columns warn and store zero, `YEAR NOT NULL` no-default columns warn and store `0000`, `DATE NOT NULL` no-default columns warn and store `0000-00-00`, `TIME NOT NULL` no-default columns warn and store `00:00:00`, `DATETIME NOT NULL` and `TIMESTAMP NOT NULL` no-default columns warn and store `0000-00-00 00:00:00`, and string `NOT NULL` no-default columns warn and store empty strings; non-ASCII values in admitted string key columns remain non-ignorable errors; no mixed ordinary and `ROW(...)` value sources, mixed-mode multi-row auto-increment gap emulation, `DEFAULT(column_name)` outside the limited baseline function subset, TEXT-family overlength string truncation warning demotion, unsupported `ON DUPLICATE KEY UPDATE` shapes outside the limited duplicate-key tail, quoted numeric strings outside the exact numeric subset, string-to-temporal/`YEAR` conversion outside canonical strings, or `SELECT` form |
| `INSERT ... SET` | 🟡 | Limited one-row `INSERT [LOW_PRIORITY \| HIGH_PRIORITY \| DELAYED] [IGNORE] [INTO] table_name SET column_name = value[, ...]` into persistent or shadowing session temporary base tables with unqualified assignment columns, supported decimal integer/fixed decimal/approximate numeric/`NULL`/`TRUE`/`FALSE`/`DEFAULT` values plus ordinary string literals for `CHAR`, `VARCHAR`, baseline `TEXT` family, and `YEAR` and canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` targets plus zero-fractional `CURRENT_DATE` / `UTC_DATE` for `DATE`, `CURRENT_TIME` / `UTC_TIME` for `TIME`, and `UTC_TIMESTAMP` for `DATETIME` / `TIMESTAMP` targets, ordinary string or hex literals for binary string targets, and compatible integer/boolean/bit/string/hex literals for `BIT` targets, descriptor-driven assignment including invisible columns when explicitly named, omitted columns and explicit `DEFAULT` values filled from descriptor numeric/string/temporal/`YEAR`/`BIT` defaults or effective `NULL` defaults, limited generated values for the current indexed persistent and session-temporary auto-increment subset, strict required-column/range/nullability/string-length/binary-length/temporal diagnostics including decimal rounding notes, approximate range checks, and default-mode `CHAR` trimming, `VARCHAR` trailing-space truncation notes, and limited non-strict and `IGNORE` `CHAR`/`VARCHAR` nonspace truncation warnings, duplicate enforcement for the current supported primary-key and unique-index subset, child-side checks for supported foreign-key descriptors, one affected row, all-or-nothing statement behavior, no-op `LOW_PRIORITY` / `HIGH_PRIORITY`, warning-producing deprecated `DELAYED`, and limited `IGNORE` warning demotion for omitted no-explicit-default columns, explicit `DEFAULT` for no-default columns, explicit `NULL` into numeric, string, binary string, `BIT`, `YEAR`, or temporal `NOT NULL`, integer/decimal range clipping, `BIT` over-width clipping, binary string truncation, invalid temporal-to-zero adjustment, `TIME` endpoint clipping, and duplicate key rows; nullable dropped-default columns warn and store `NULL`, numeric `NOT NULL` no-default columns warn and store target zero values, binary string `NOT NULL` no-default columns warn and store the MySQL-compatible implicit binary value, `BIT NOT NULL` no-default columns warn and store zero, `YEAR NOT NULL` no-default columns warn and store `0000`, `DATE NOT NULL` no-default columns warn and store `0000-00-00`, `TIME NOT NULL` no-default columns warn and store `00:00:00`, `DATETIME NOT NULL` and `TIMESTAMP NOT NULL` no-default columns warn and store `0000-00-00 00:00:00`, and string `NOT NULL` no-default columns warn and store empty strings; non-ASCII values in admitted string key columns remain non-ignorable errors; no table-qualified assignment targets, expression assignments outside the limited `DEFAULT(column_name)` function subset, TEXT-family overlength string truncation warning demotion, aliases, partitions, unsupported `ON DUPLICATE KEY UPDATE` shapes outside the limited duplicate-key tail, quoted numeric strings outside the exact numeric subset, string-to-temporal/`YEAR` conversion outside canonical strings, or `SELECT` form |
| `INSERT ... SELECT` | 🟡 | Limited descriptor-backed `INSERT [LOW_PRIORITY \| HIGH_PRIORITY \| DELAYED] [INTO] target [(column_name[, ...])] SELECT ... FROM source` for persistent base tables, reusing the existing single-table source `SELECT` subset with optional table alias, `WHERE`, one-column integer, `YEAR`, `DATE`, `TIME`, `DATETIME`, `TIMESTAMP`, or ASCII nonbinary string (`CHAR`, `VARCHAR`, baseline `TEXT` family) `ORDER BY`, `LIMIT`, and simple no-op source locking clauses; table-backed targets may now carry supported primary-key, unique-index, foreign-key child, and indexed auto-increment descriptors, including same-table persistent source/target materialization, strict duplicate-key and missing-parent rollback, limited non-`AUTO_INCREMENT` `INSERT IGNORE ... SELECT` duplicate-key and supported missing-parent warning demotion, and selected-row-order auto-increment allocation for non-`IGNORE` statements; additionally supports one-row row-scalar sources as `INSERT ... SELECT select_item_list` and `INSERT ... SELECT select_item_list FROM DUAL [WHERE [NOT] EXISTS (...)]` for persistent or shadowing session temporary targets, including primary-key, unique-index, foreign-key child, and auto-increment targets through the normal insert path; additionally supports unparenthesized compound sources `SELECT ... UNION [DISTINCT\|ALL] SELECT ...` over the current descriptor-backed single-table branch subset or current no-source/`DUAL` row-scalar branch subset, materialized through SQLite temporary storage before validation and insertion, with MySQL-style duplicate removal/preservation, same-table persistent source/target materialization, duplicate-key rollback, zero-row omitted-column behavior, and affected-row reporting; target column lists are descriptor-driven, omitted targets receive descriptor defaults or effective nullable `NULL` only for rows actually inserted, source/target `*` and omitted target lists use visible columns where a descriptor source exists, explicit target columns may name invisible columns, selected descriptor values and admitted row-scalar values are validated against target descriptors or adjusted for selected `NULL` into `NOT NULL`, selected integer range violations, and selected `CHAR`/`VARCHAR` truncation for compatible descriptors when non-strict mode or an admitted `IGNORE` form allows it, `LOW_PRIORITY` / `HIGH_PRIORITY` are no-ops, limited non-`AUTO_INCREMENT` table-backed `IGNORE` adjusts omitted no-default targets, selected `NULL` into `NOT NULL` targets, selected integer range violations, selected `CHAR`/`VARCHAR` truncation, duplicate-key rows, and supported missing-parent rows with warnings, and `DELAYED` records MySQL's deprecated-delayed warning; non-`AUTO_INCREMENT` targets may carry the limited duplicate-key tail with current ODKU assignment, warning, affected-row, and duplicate-key semantics; no row-scalar or compound-source `IGNORE`, table-backed `IGNORE` on `AUTO_INCREMENT` targets, partitions, unsupported `ON DUPLICATE KEY UPDATE` shapes outside the limited duplicate-key tail, `TABLE`, row constructors, joins, CTEs, `REPLACE ... SELECT` key-bearing/scalar/compound sources, wildcard `DUAL`, general `DUAL WHERE` expressions, source ordering/limits for row-scalar sources, compound global or branch ordering/limits, parenthesized query expressions, triggers, generated columns, locking options beyond current no-ops, `TEXT` truncation warning demotion, string/decimal/approximate/temporal/`YEAR`/binary conversion outside compatible descriptors, selected integer conversion outside compatible integer descriptors, broader selected-row warning demotion outside selected `NULL`, integer range, and `CHAR`/`VARCHAR` truncation adjustment, broad set-operation type aggregation, or general expression conversion |
| `INSERT ... ON DUPLICATE KEY UPDATE` | 🟡 | Limited `INSERT [LOW_PRIORITY \| HIGH_PRIORITY \| DELAYED] [INTO] table_name ... VALUES ... ON DUPLICATE KEY UPDATE column_name = value[, ...]`, including all-default empty `VALUES ()` rows, one-row `INSERT ... SET ... ON DUPLICATE KEY UPDATE column_name = value[, ...]`, and current non-`AUTO_INCREMENT` `INSERT ... SELECT` source envelopes for persistent base tables, with one or more distinct unqualified duplicate assignment targets, including non-`AUTO_INCREMENT` primary-key and unique-key columns when they are not referenced parent foreign-key parts; supported descriptor insert value literals include `NULL`, `TRUE`, `FALSE`, `DEFAULT`, decimal/fixed decimal/string/temporal literals already admitted by `INSERT`, and deprecated same-target `VALUES(column_name)` references to the proposed row with warning `1287` per `VALUES()` occurrence; duplicate conflicts are handled when the target has no enforced key or one or more enforced primary or unique key descriptors, including supported single-column, composite, and prefix key parts with first-conflicting descriptor selection for multiple keys; composite unique rows with a proposed SQL `NULL` key part insert normally; reassigned primary and unique key tuples receive descriptor-driven second-order duplicate checks before physical mutation; successful affected rows follow MySQL's inserted-row `1`, changed-duplicate `2`, unchanged-duplicate `0` behavior, generated auto-increment duplicate attempts advance the next generated value without replacing `LAST_INSERT_ID()`, and supported in-range duplicate updates preserve statement atomicity and file-backed persistence; `LOW_PRIORITY` and `HIGH_PRIORITY` are no-ops and `DELAYED` records warning `3005`; no `IGNORE`, auto-increment `INSERT ... SELECT` targets, unsupported `INSERT ... SELECT` source envelopes, aliases, partitions, duplicate assignment targets, qualified assignment targets, qualified or cross-column `VALUES()` references, auto-increment or referenced parent foreign-key key assignments, expression assignments outside the limited `DEFAULT(column_name)` function subset, column-to-column assignments, non-strict string truncation warning demotion, general expression conversion, broad foreign-key referential actions for ODKU key reassignment, triggers, cascades, generated columns, or privilege semantics |
| `INSERT IGNORE` | 🟡 | Limited `INSERT IGNORE ... VALUES` and `INSERT IGNORE ... SET` warning demotion for descriptor integer/decimal/approximate/year/date/time/datetime/timestamp/binary-string/`BIT`/`NULL`/`DEFAULT` inputs, admitted string, binary-string, and `BIT` null/default adjustments, generated values for the current indexed auto-increment subset, duplicate rows for the current supported primary-key and unique-index subset, and missing-parent rows for supported foreign-key child descriptors; nonconflicting rows continue and affected rows count rows actually inserted. Omitted no-explicit-default columns and explicit `DEFAULT` for no-default columns warn and store target zero values for numeric `NOT NULL`, zero-padded bytes for `BINARY NOT NULL`, zero-length bytes for `VARBINARY` and BLOB family `NOT NULL`, `0000-00-00` for `DATE NOT NULL`, `00:00:00` for `TIME NOT NULL`, `0000-00-00 00:00:00` for `DATETIME NOT NULL` or `TIMESTAMP NOT NULL`, empty string for string `NOT NULL`, or `NULL` for nullable dropped-default columns, explicit `NULL` into numeric `NOT NULL` stores the target zero value, explicit `NULL` into binary string `NOT NULL` stores the same target implicit binary value, explicit `NULL` into `YEAR NOT NULL` stores `0000`, explicit `NULL` into `DATE NOT NULL` stores `0000-00-00`, explicit `NULL` into `TIME NOT NULL` stores `00:00:00`, explicit `NULL` into `DATETIME NOT NULL` or `TIMESTAMP NOT NULL` stores `0000-00-00 00:00:00`, explicit `NULL` into string `NOT NULL` stores empty string, missing-parent foreign-key child rows are skipped, and all of those adjustments record warnings; out-of-range integer and decimal values clip to the supported descriptor range with warnings, overlength `CHAR`/`VARCHAR` strings truncate to descriptor character length with warnings, overlength binary strings truncate to descriptor length with warnings, over-width `BIT` values clip to all ones, out-of-range `TIME` values clip to `-838:59:59` or `838:59:59`, invalid `YEAR` values store `0000`, invalid temporal values store the target zero temporal value with warnings, and non-ignorable syntax/name/shape errors plus unsupported non-ASCII string-key values remain errors; also accepts non-`AUTO_INCREMENT` table-backed `INSERT IGNORE ... SELECT` rows with omitted no-default target adjustment, selected `NULL` adjustment, selected integer range clipping, duplicate-key skipping, and supported missing-parent skipping; no row-scalar `INSERT IGNORE ... SELECT`, table-backed `INSERT IGNORE ... SELECT` on `AUTO_INCREMENT` targets, partitions, `DEFAULT(col_name)`, TEXT-family overlength string warning demotion, quoted numeric strings outside the exact numeric subset, string-to-temporal/`YEAR` conversion outside canonical strings, general expression conversion, or full non-strict mode behavior |
| `INSERT DELAYED` | 🟡 | Limited modifier support on currently supported `INSERT ... VALUES`, `INSERT ... SET`, and descriptor-backed `INSERT ... SELECT` forms; executes as ordinary `INSERT` and records warning `3005`; `VALUES` and `SET` may combine `DELAYED IGNORE` with delayed warning before adjustment warnings; no delayed queues, background writes, storage scheduling, or `DELAYED IGNORE ... SELECT` |
| `INSERT LOW_PRIORITY` / `HIGH_PRIORITY` | 🟡 | Limited no-op priority modifier support on currently supported `INSERT ... VALUES`, `INSERT ... SET`, and descriptor-backed `INSERT ... SELECT` forms; `VALUES`, `SET`, and clean non-`AUTO_INCREMENT` table-backed `INSERT ... SELECT` may combine one priority modifier before `IGNORE`; no table-lock scheduling semantics, multiple modifiers, `DELAYED IGNORE ... SELECT`, row-scalar `INSERT IGNORE ... SELECT`, table-backed `INSERT IGNORE ... SELECT` on `AUTO_INCREMENT` targets |
| `LOAD DATA INFILE` | ❌ | Server-side import syntax |
| `LOAD DATA LOCAL INFILE` | ❌ | LOCAL INFILE flow and security |
| `LOAD XML INFILE` | ❌ | XML import syntax |
| `LOAD XML LOCAL INFILE` | ❌ | Client-side XML import request behavior diagnostics |
| `REPLACE ... VALUES` | 🟡 | Limited `REPLACE [LOW_PRIORITY \| DELAYED] [INTO] table_name [(column_name[, ...])] VALUES (value[, ...])[, ...]` path for persistent base tables, including explicit or omitted empty all-default rows with `VALUES ()`, `VALUE ()`, or `VALUES ROW()`, and nonempty row lists with ordinary `VALUES`, `VALUE`, or homogeneous `VALUES ROW(...)` syntax, with the same descriptor value conversion/default/nullability/range/string/temporal/binary/`BIT`/`ENUM`/`SET`/JSON subset as current `INSERT ... VALUES`; no-key targets remain insert-equivalent, while key-bearing targets use descriptor-driven delete-then-insert replacement for the current primary-key and unique-index subset, including composite keys, admitted ASCII string and prefix unique keys, skipped conflicts for unique-key `NULL` parts, multiple unique conflicts, generated and explicit `AUTO_INCREMENT` values, child-side and parent-side foreign-key checks, exact no-op affected-row behavior, all-or-nothing statement behavior, no-op `LOW_PRIORITY`, and warning-producing deprecated `DELAYED`; no mixed ordinary and `ROW(...)` value sources, partitions, `DEFAULT(column_name)` outside the limited baseline function subset, generated columns, triggers, cascades, `IGNORE`-style warning demotion, TEXT-family overlength string truncation warning demotion, quoted numeric strings outside the exact numeric subset, string-to-temporal/`YEAR` conversion outside canonical strings, or query forms |
| `REPLACE ... SET` | 🟡 | Limited one-row `REPLACE [LOW_PRIORITY \| DELAYED] [INTO] table_name SET column_name = value[, ...]` path for persistent base tables with unqualified descriptor assignment targets and the same descriptor conversion/default/nullability/range/string/temporal/binary/`BIT`/`ENUM`/`SET`/JSON subset as current `INSERT ... SET`; no-key targets remain insert-equivalent, while key-bearing targets use descriptor-driven delete-then-insert replacement for the current primary-key and unique-index subset, including composite keys, admitted ASCII string and prefix unique keys, skipped conflicts for unique-key `NULL` parts, generated and explicit `AUTO_INCREMENT` values, child-side and parent-side foreign-key checks, exact no-op affected-row behavior, all-or-nothing statement behavior, no-op `LOW_PRIORITY`, and warning-producing deprecated `DELAYED`; no partitions, qualified assignment targets, expression assignments outside the limited `DEFAULT(column_name)` function subset, `VALUES`, `SELECT` forms, generated columns, triggers, cascades, TEXT-family overlength string truncation warning demotion, or quoted numeric strings outside the exact numeric subset, string-to-temporal/`YEAR` conversion outside canonical strings |
| `REPLACE ... SELECT` | 🟡 | Limited no-key-descriptor `REPLACE [LOW_PRIORITY \| DELAYED] [INTO] target [(column_name[, ...])] SELECT ... FROM source` insert-equivalent path for persistent base tables without primary-key or unique-index descriptors, reusing the existing single-table source `SELECT` subset with optional table alias, `WHERE`, one-column integer, `YEAR`, `DATE`, `TIME`, `DATETIME`, `TIMESTAMP`, or ASCII nonbinary string (`CHAR`, `VARCHAR`, baseline `TEXT` family) `ORDER BY`, `LIMIT`, and simple no-op source locking clauses; target column lists are descriptor-driven, omitted targets receive descriptor numeric/string/temporal/`YEAR`/`BIT` defaults or effective nullable `NULL` only for rows actually inserted, source/target `*` and omitted target lists use visible columns, explicit columns may name invisible columns, and selected integer/`NULL`, compatible decimal text, compatible approximate `REAL` values, compatible canonical `YEAR` text and compatible canonical date/time/datetime/timestamp text admitted by the current limited SQL-mode temporal rules, compatible string `TEXT` values, plus compatible binary string or `BIT` `BLOB` values are validated against target descriptors; key-bearing targets are still rejected until `REPLACE ... SELECT` delete-insert streaming semantics are specified, and there are no partitions, `TABLE`, joins, CTEs, literal/expression projection sources, generated values, locking options, warning demotion, string/integer/decimal/approximate/temporal/`YEAR`/binary conversion outside compatible descriptors, or insert ids |
| `REPLACE LOW_PRIORITY` / `DELAYED` | 🟡 | Limited modifier support on currently supported `REPLACE` forms: `LOW_PRIORITY` is accepted as a no-op and `DELAYED` executes as ordinary `REPLACE` with warning `3005`; duplicate-key replacement semantics are supported only for the current `VALUES` and `SET` key-bearing subset; no multiple modifiers, `HIGH_PRIORITY`, delayed queues, storage scheduling changes, or key-bearing `REPLACE ... SELECT` |
| `UPDATE` (single-table) | 🟡 | Limited persistent or shadowing session temporary base-table `UPDATE table_name SET column_name = value[, column_name = value ...]` with one or more unqualified assignment columns, where multiple-assignment lists are limited to distinct non-key, non-auto-increment constant assignments, supported decimal integer/fixed decimal/approximate numeric/`NULL`/`TRUE`/`FALSE`/`DEFAULT` assignment values plus ordinary string literals for `CHAR`, `VARCHAR`, baseline `TEXT` family, `YEAR` and canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` targets admitted by the current limited SQL-mode temporal rules, ordinary string or hex literals for binary string targets, compatible integer/boolean/bit/string/hex literals for `BIT` targets, zero-fractional `CURRENT_TIMESTAMP` / `NOW()` / `UTC_TIMESTAMP` synonyms for `DATETIME` and `TIMESTAMP` targets, zero-fractional `CURRENT_DATE` / `UTC_DATE` synonyms for `DATE` targets, and zero-fractional `CURRENT_TIME` / `UTC_TIME` synonyms for `TIME` targets, descriptor-driven automatic `ON UPDATE CURRENT_TIMESTAMP` columns when another value changes, limited same-column integer arithmetic assignments `column = column + unsigned_integer_literal` and `column = column - unsigned_integer_literal` for non-key, non-auto-increment integer-family columns within the current signed-64 delta/result envelope, and limited uncorrelated table-backed scalar subquery assignments of one descriptor source column copied through compatible descriptor families including binary-to-binary and `BIT`-to-`BIT` copies; optional baseline `WHERE` predicate boolean expression including keyword `NOT`, `AND`/`&&`, `XOR`, `OR`/`\|\|`, limited integer, `BIT`, `YEAR`, and canonical temporal string-literal plus limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning `DATE`/`DATETIME`/`TIMESTAMP` `BETWEEN` range predicates, limited integer, `BIT`, `YEAR`, and canonical temporal string-literal plus limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning `DATE`/`DATETIME`/`TIMESTAMP` `IN` membership predicates, limited ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string-literal equality/null-safe equality/inequality/range/membership predicates plus limited `LIKE` / `NOT LIKE` pattern predicates, limited `REGEXP` / `RLIKE` / `NOT REGEXP` / `NOT RLIKE` ASCII regular-expression predicates, and limited `REGEXP_LIKE()` predicates over nonbinary string descriptor values and scalar literal patterns, descriptor-column `IS [NOT] TRUE` / `FALSE` / `UNKNOWN` for integer columns, and `IS NULL` / `IS NOT NULL` for integer, decimal, approximate, string, binary string, `BIT`, `YEAR`, or temporal columns, optional one-column integer, `BIT`, `YEAR`, `DATE`, `TIME`, `DATETIME`, `TIMESTAMP`, or ASCII nonbinary string (`CHAR`, `VARCHAR`, baseline `TEXT` family) `ORDER BY` with `ASC`/`DESC`, optional `LIMIT row_count` using unsigned decimal literals in signed 64-bit range, validated no-op target `USE`/`FORCE`/`IGNORE INDEX|KEY` hints with optional `FOR JOIN` / `FOR ORDER BY` / `FOR GROUP BY` scopes, duplicate-key errors when updating the current supported primary-key or unique-index subset to an existing value, child-side and parent-side checks for supported foreign-key descriptors, direct non-recursive `ON UPDATE CASCADE` child-key updates, direct non-recursive `ON UPDATE SET NULL` child-key nulling when supported parent key columns change without changing parent affected-row counts, and changed-row affected counts after decimal/approximate/`YEAR`/temporal canonicalization, binary padding/truncation, `BIT` canonicalization, and default-mode `CHAR` trimming, `VARCHAR` trailing-space truncation notes, and limited non-strict `CHAR`/`VARCHAR` nonspace truncation warnings; arithmetic assignment evaluates only for matched non-`NULL` source rows, skips no-match/`LIMIT 0`/all-`NULL` oversized deltas, preserves `NULL` as unchanged, and reports changed rows; scalar assignment subqueries reuse the current single-table descriptor `SELECT` source envelope, evaluate only when the outer update matches rows, return `NULL` for empty scalar results, return `1242 / 21000` for matched multi-row scalar results, and reject same-table source reads with `1093 / HY000`; admitted string comparisons, ranges, membership, and ordering use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, and `TIME` range predicates and ordering use signed total-second semantics rather than raw text ordering, assignment, predicate, and ordering columns resolve through descriptors and may explicitly name invisible columns, and non-ASCII values in admitted string key columns are rejected before mutation; no aliases, table-qualified assignment targets, duplicate assignment targets, multi-assignment scalar subqueries or arithmetic assignments, multi-assignment primary-key, unique-key, or auto-increment targets, expression assignments outside the limited current date/time/timestamp, same-column integer arithmetic, and scalar subquery assignment subset, arithmetic assignment to primary-key, unique-key, or auto-increment columns, implicit non-`NULL` string/integer/decimal/approximate/temporal/`YEAR`/binary conversion outside compatible descriptors except the limited `DEFAULT(column_name)` same-descriptor-family subset, `DEFAULT(column_name)` outside the limited baseline function subset, partitions, modifiers, joined updates, multi-table updates, correlated subqueries, subquery predicates, derived tables, binary string comparisons/order keys outside `BIT`, non-ASCII string predicate literals or full Unicode string ordering, decimal or approximate ordering/comparison predicates, table-qualified/order expression/ordinal/multiple-key ordering, physical optimizer behavior for hints, LIMIT offset forms, triggers, recursive actions beyond the current direct FK action subset, generated columns, or privilege semantics |
| `UPDATE` (multi-table / joined) | 🟡 | Limited one-target two-source joined update through `UPDATE left [AS alias] JOIN right [AS alias] ... SET target_column = constant_value`, with `JOIN`, `INNER JOIN`, `CROSS JOIN`, `LEFT JOIN`, and `LEFT OUTER JOIN`, current joined `ON` equality and joined `WHERE` predicate subsets, explicit `AS` aliases only, descriptor-driven unqualified/alias-qualified/table-qualified/schema-table-qualified assignment target resolution, one assignment to one resolved source table, current single-table constant assignment conversion for the resolved target descriptor, duplicate join matches updating each target row at most once, changed-row affected counts, zero warnings for supported in-range updates, close/reopen persistence, and direct non-recursive parent-side `ON UPDATE CASCADE` / `SET NULL` actions for updated target rows; joined `ORDER BY` and `LIMIT` are accepted syntactically only to return MySQL-compatible `1221 / HY000` wrong-usage diagnostics; no updating multiple tables, multiple assignments, bare aliases, comma table references, nested/derived table references, `USING` joins, CTEs, scalar-subquery/arithmetic/column-to-column/general expression assignments, predicates beyond the current joined predicate subset, partitions, modifiers, triggers, privileges, recursive FK actions, generated columns, or broader MySQL joined-source grammar |
| `ENUM` row values | 🟡 | Limited enum DML in `INSERT ... VALUES`, `INSERT ... SET`, `REPLACE ... VALUES`, `REPLACE ... SET`, and supported `UPDATE` assignments: matching string labels store the canonical definition label, in-range decimal integer ordinals store the corresponding label, quoted decimal strings fall back to ordinal assignment only when no label matches, `NULL` follows nullability, `DEFAULT` resolves through the descriptor, and omitted `NOT NULL` enum columns without an explicit default use the first label. Supported enum predicates are `=`, `<=>`, `<>`, `!=`, `<=> NULL`, `IS NULL`, and `IS NOT NULL`; successful strict conversions report zero warnings. No enum `INSERT IGNORE` invalid-value demotion, enum values in `INSERT ... SELECT`, scalar subquery assignment into enum columns, enum ordering/grouping/distinct, enum key participation, invalid index-0 storage, non-strict conversion, or general expression conversion |
| `SET` row values | 🟡 | Limited set DML in `INSERT ... VALUES`, `INSERT ... SET`, `REPLACE ... VALUES`, `REPLACE ... SET`, and supported `UPDATE` assignments: matching string member lists store the canonical definition-order display string, nonnegative decimal integer bitmaps store the selected members, quoted decimal strings fall back to bitmap assignment only when no member matches, `NULL` follows nullability, `DEFAULT` resolves through the descriptor, and omitted or explicit `DEFAULT` for `NOT NULL` set columns without an explicit default fails under the current strict mode. Supported set predicates are `=`, `<=>`, `<>`, `!=`, `<=> NULL`, `IS NULL`, and `IS NOT NULL`; successful strict conversions report zero warnings. No set `INSERT IGNORE` invalid-value demotion, set values in `INSERT ... SELECT`, scalar subquery assignment into set columns, set ordering/grouping/distinct, set key participation, compact bitmap storage, non-strict conversion, or general expression conversion |
| `JSON` row values | 🟡 | Limited JSON DML in `INSERT ... VALUES`, `INSERT ... SET`, `REPLACE ... VALUES`, `REPLACE ... SET`, supported `UPDATE` assignments, compatible `INSERT ... SELECT` / `REPLACE ... SELECT`, and compatible scalar-subquery `UPDATE`: SQL string literals containing the verified JSON subset store canonical JSON text, SQL `NULL` follows nullability, `DEFAULT` resolves through the descriptor, omitted `JSON NOT NULL` without an explicit default fails in strict mode, and `INSERT IGNORE` for omitted/default or explicit `NULL` into `JSON NOT NULL` stores the JSON value `null` with warnings. Supported predicates are `IS NULL` and `IS NOT NULL`; successful in-range conversions report zero warnings. No JSON comparison, ordering, grouping, path extraction, functions, generated/path indexes, expression assignments, or general expression conversion |
| `UPDATE` (multi-table) | 🟡 | Same limited one-target two-source joined update subset as `UPDATE` (multi-table / joined): explicit `AS` aliases only, current joined-source/predicate envelope, one resolved target source and one assignment, no joined-update `ORDER BY` / `LIMIT`, no multiple targets, and no broader joined-source grammar |
| SQL safe updates variable | 🟡 | Limited scalar `@@sql_safe_updates` reads report fixed disabled value `0`; no mutable safe-updates state, `sql_select_limit`, `max_join_size`, key-aware DML checks, or changed `UPDATE`/`DELETE` behavior |
| SQL warnings variable | 🟡 | Limited scalar `@@sql_warnings` reads report fixed disabled value `0`; limited non-strict and `INSERT IGNORE ... VALUES` / `SET` string truncation plus `INSERT IGNORE` adjustment warnings are recorded in diagnostics like MySQL; no mutable warning-reporting state, broader warning-producing insert conversions beyond documented string-truncation and `INSERT IGNORE` subsets, or single-row insert information strings |

[Back to compatibility overview](../../COMPATIBILITY.md)
