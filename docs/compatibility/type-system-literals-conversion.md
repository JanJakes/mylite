# Type system, literals, and conversion

Ordinary non-strict DML currently has a narrow MySQL-compatible adjustment
surface: when neither `STRICT_TRANS_TABLES` nor `STRICT_ALL_TABLES` is active,
omitted no-explicit-default non-`AUTO_INCREMENT` `INSERT` / `REPLACE` target
columns and explicit `DEFAULT` values for no-default columns use the same
implicit descriptor values as the existing `INSERT IGNORE` subset, except that
no-explicit-default `ENUM NOT NULL` omitted/default values use the first label
without warning, and matched single-table `UPDATE` assignments of `NULL` to
`NOT NULL` columns or `DEFAULT` to no-default columns adjust with warnings.
Adjusted explicit `NULL` into `ENUM NOT NULL` stores the empty enum error value.
Descriptor-backed `INSERT ...
SELECT` additionally adjusts omitted no-default targets, selected `NULL` into
`NOT NULL` targets, selected integer range violations for compatible
integer-family targets, and selected overlength `CHAR` / `VARCHAR` values for
compatible string descriptors. Supported duplicate-key update assignments
additionally adjust no-default `DEFAULT`, character truncation, and multi-row
literal `NULL` assignments to integer `NOT NULL` targets. Omitted
`AUTO_INCREMENT` columns continue to generate values. Explicit `NULL` in
ordinary `INSERT` / `REPLACE` `VALUES` and `SET` forms remains an error outside
`IGNORE`, and broader non-strict string, numeric, temporal, range, and
expression coercions remain outside the current subset. Ordinary non-strict
`INSERT` / `REPLACE`, matched single-table `UPDATE`, supported duplicate-key
updates, and `INSERT IGNORE ... VALUES` / `SET` truncate overlength `CHAR` /
`VARCHAR` string literals with MySQL-compatible
diagnostics; `VARCHAR` values whose only excess characters are trailing spaces
record notes, while `CHAR` keeps the current silent default-mode trailing-space
trimming.

Descriptor-backed `INSERT`, `REPLACE`, admitted duplicate-key assignment, and
single-table `UPDATE` numeric targets also accept quoted numeric strings and
ordinary hexadecimal literals: integer-family targets support leading ASCII
whitespace, numeric-prefix scanning, decimal/exponent rounding, hexadecimal
magnitudes, and non-strict or `INSERT IGNORE` warning adjustment within the
current physical range; `DECIMAL` family targets accept fixed and exponent
strings plus hexadecimal magnitudes with descriptor scale rounding, strict
incorrect-decimal diagnostics, and non-strict or `INSERT IGNORE` warning
adjustment; and approximate targets accept finite decimal/scientific strings
plus hexadecimal magnitudes with descriptor range adjustment and `FLOAT`
single-precision rounding. Integer-family column defaults still accept only
exact quoted decimal integer strings within the current physical range. General
expression coercion, broad selected-row conversion, bit-string numeric
conversion, quoted decimal/approximate defaults, and full unsigned `BIGINT`
storage above MyLite's signed-64 physical range remain deferred.

Descriptor-backed string and binary string DML targets accept compatible
string, numeric, boolean, and hexadecimal literal values in those same row-value
paths. Nonbinary string targets require valid non-`NUL` UTF-8 after decoding;
binary string and BLOB-family targets preserve byte values subject to length
conversion rules.

Baseline `STRCMP()` uses the same admitted scalar literal envelope as the
current string-search functions: ASCII, `NUL`-free ordinary string literals,
signed-64 decimal integer literals, `TRUE`, `FALSE`, and `NULL`, plus
descriptor-backed text-convertible row values in its row-scalar projection
slice, except for `TIME` descriptor columns whose direct MySQL `STRCMP()`
coercion is deferred. It does not add binary, non-ASCII collation, approximate
numeric, parameter, or arbitrary expression conversion.

Row-scalar `CAST()` / `CONVERT()` conversion is limited to one-base-table
projection lists over descriptor integer-family columns, nonbinary
string-family columns, scalar literals, and supported folded session scalar
values. It supports binary, character, signed integer, unsigned integer,
`USING BINARY`, and `USING utf8mb4`/`utf8`/`utf8mb3`/`latin1` targets with
MyLite-owned value conversion and metadata while SQLite streams the source rows.
Supported constant `CAST()` / `CONVERT()` forms are also admitted in
descriptor-backed `INSERT`, `REPLACE`, supported duplicate-key assignment, and
single-table `UPDATE` value positions before target descriptor conversion.
It does not add general selected-row conversion, predicates, ordering/grouping
expressions, DML assignment casts outside that constant scalar subset, binary
string or `BIT` operands, decimal/approximate/temporal/JSON/spatial operands,
or general transcoding.

## Numeric types

| Feature | Status | Notes |
| --- | --- | --- |
| `TINYINT` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion with MyLite's current descriptor-range literal check, and single-column sort support; only signed `TINYINT(1)` is persisted/rendered with width, other widths normalize away with warning 1681; no `ZEROFILL`, repeated/combined attributes, aliases, general expression defaults beyond the limited parenthesized constant integer subset, general expression semantics, compact physical storage, or protocol-grade result metadata |
| `SMALLINT` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion with MyLite's current descriptor-range literal check, and single-column sort support; display widths normalize away with warning 1681; no `ZEROFILL`, repeated/combined attributes, aliases, general expression defaults beyond the limited parenthesized constant integer subset, general expression semantics, compact physical storage, or protocol-grade result metadata |
| `MEDIUMINT` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion with MyLite's current descriptor-range literal check, and single-column sort support; display widths normalize away with warning 1681; no `ZEROFILL`, repeated/combined attributes, aliases, general expression defaults beyond the limited parenthesized constant integer subset, general expression semantics, compact physical storage, or protocol-grade result metadata |
| `INT` / `INTEGER` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion, and single-column sort support; display widths normalize away with warning 1681; no general expression defaults beyond the limited parenthesized constant integer subset, general expression semantics, `ZEROFILL`, repeated/combined attributes, or protocol-grade result metadata |
| `BIGINT` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion, and single-column sort support; display widths normalize away with warning 1681; `BIGINT UNSIGNED` defaults and row values are capped at the signed 64-bit SQLite integer range in this slice; no `ZEROFILL`, repeated/combined attributes, general expression defaults beyond that integer subset, or protocol-grade result metadata |
| Integer type aliases | 🟡 | Limited `INT1`, `INT2`, `INT3`, `INT4`, and `INT8` aliases normalize to existing integer-family descriptors, with optional single `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, descriptor-driven DDL/DML/conversion/introspection coverage, and normalized type rendering; only signed `INT1(1)` persists/renders as `tinyint(1)`, other alias widths normalize away with warning 1681; no `ZEROFILL`, combined/repeated attributes, general expression defaults beyond that integer subset, casts, or protocol-grade metadata |
| `DECIMAL` / `NUMERIC` | 🟡 | Limited exact fixed-point descriptors, optional deprecated `UNSIGNED`, exact canonical `TEXT` storage/readback, decimal defaults, row DML conversion with scale padding, half-away-from-zero rounding, truncation notes, range diagnostics, descriptor-owned quoted string prefix scanning with strict and adjusted diagnostics for supported `INSERT`/`REPLACE`/`UPDATE` storage paths, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no decimal comparison/order/math, `ZEROFILL`, decimal primary keys, auto-increment, quoted string conversion outside DML storage strings, float/hex/bit conversion, casts, or protocol-grade numeric metadata |
| `FIXED` | 🟡 | Limited alias for the supported `DECIMAL` descriptor subset, normalized to `decimal(M,D)` metadata and storage; same gaps as `DECIMAL` |
| `FLOAT` | 🟡 | Limited approximate numeric descriptors stored as SQLite `REAL`, including bare `FLOAT`, `FLOAT(0..24)`, deprecated `FLOAT(M,D)` display metadata with warning 1681, and `FLOAT4`, optional deprecated `UNSIGNED` with warning 1681, `DEFAULT NULL` or compatible numeric literal defaults, row DML conversion from integer/fixed-decimal/approximate/boolean/`NULL`/`DEFAULT` literals and quoted numeric storage strings with descriptor-owned prefix scanning and strict or adjusted diagnostics, single-precision rounding/readback text, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no scaled `FLOAT(M,D)` storage rounding/range clipping, `ZEROFILL`, `SIGNED`, quoted string conversion outside DML storage strings, hex/bit conversion, comparison/order/math, indexes, primary keys, auto-increment, casts, or protocol-grade numeric metadata |
| `DOUBLE` / `REAL` | 🟡 | Limited approximate numeric descriptors stored as SQLite `REAL`, including bare `DOUBLE`, deprecated `DOUBLE(M,D)` display metadata with warning 1681, `DOUBLE PRECISION`, default-mode `REAL`, `FLOAT(25..53)`, and `FLOAT8`, optional deprecated `UNSIGNED` with warning 1681, `DEFAULT NULL` or compatible numeric literal defaults, row DML conversion from integer/fixed-decimal/approximate/boolean/`NULL`/`DEFAULT` literals and quoted numeric storage strings with descriptor-owned prefix scanning and strict or adjusted diagnostics, double readback text, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, `REAL_AS_FLOAT` mapping of later `REAL` column definitions to the supported `FLOAT` descriptor subset, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no scaled `DOUBLE(M,D)` storage rounding/range clipping, `REAL(M,D)`, `DOUBLE PRECISION(M,D)`, `ZEROFILL`, `SIGNED`, quoted string conversion outside DML storage strings, hex/bit conversion, comparison/order/math, indexes, primary keys, auto-increment, casts, or protocol-grade numeric metadata |
| `FLOAT4` / `FLOAT8` | 🟡 | Limited aliases: `FLOAT4` normalizes to the supported `FLOAT` descriptor subset and `FLOAT8` normalizes to the supported `DOUBLE` descriptor subset, with the same DDL, DML, metadata, and unsupported-operation limits |
| `BIT` | 🟡 | Limited `BIT` / `BIT(1..64)` descriptors stored as fixed-width big-endian `BLOB` values, with default width `1`, byte-safe result readback, compatible decimal integer/`TRUE`/`FALSE`/bit/string/hex row-value assignment, `NULL`, `DEFAULT`, strict width/range/nullability diagnostics, `INSERT IGNORE` over-width clipping and not-null adjustment, compatible descriptor defaults including limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... ADD COLUMN`, descriptor cloning/copying, limited scalar-subquery assignment, descriptor-backed comparison/`BETWEEN`/`IN`/`IS NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `BIT()`/`BIT(0)`/`BIT(65+)`, bit indexes, `ALTER MODIFY`/`CHANGE` conversion, bit arithmetic, casts, aggregate arguments, grouping/distinct, or protocol-grade metadata |
| `BOOL` / `BOOLEAN` | 🟡 | Bare column-type aliases normalize to signed `TINYINT(1)` descriptors with integer/`NULL`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, plus limited `TRUE`/`FALSE` DDL/DML/conversion/introspection coverage and no display-width warning; no `BOOL(1)`, `BOOLEAN(1)`, `SIGNED`, `UNSIGNED`, `ZEROFILL`, general expression defaults beyond that integer subset, general expression truth semantics, casts, or protocol-grade metadata |
| `SERIAL` | 🟡 | Limited `CREATE TABLE` column-type alias expanding to `BIGINT UNSIGNED AUTO_INCREMENT` plus a generated unique key, with implicit `NOT NULL` unless explicit `NULL`; descriptor metadata renders the expanded type/key/auto-increment shape and DML uses the current indexed auto-increment allocator. No separate `SERIAL DEFAULT VALUE` attribute after explicit integer types, no ALTER column-attribute support, and `BIGINT UNSIGNED` row values remain capped at the current signed-64 physical range |

## Temporal types

`TIME(0)`, `DATETIME(0)`, and `TIMESTAMP(0)` are accepted in supported column
definition paths and normalize to the existing seconds-only descriptors. MyLite
still rejects nonzero temporal fractional precision before schema mutation, and
`fsp > 6` uses MySQL's precision-limit diagnostic.

| Feature | Status | Notes |
| --- | --- | --- |
| `DATE` | 🟡 | Limited canonical `YYYY-MM-DD` descriptors stored as `TEXT`, with valid range `1000-01-01` through `9999-12-31`, canonical and admitted limited relaxed date-string defaults, parenthesized generated current-date defaults, `INSERT`/`REPLACE`/`UPDATE` assignment including explicit zero-fractional current-date and UTC-date values, limited SQL-mode-aware full-zero, partial-zero, and `ALLOW_INVALID_DATES` conversion for row values/defaults/compatible descriptor copies/predicates, `INSERT IGNORE` invalid/zero/`NULL` adjustments to `0000-00-00`, readback, descriptor cloning/copying, `IS NULL` / `IS NOT NULL`, canonical plus limited datetime-shaped string-literal storage/default conversion and comparison/`BETWEEN`/`IN` predicates, single-column ordering with MySQL-compatible `NULL` placement, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no relaxed date strings outside the documented storage, default, and predicate slices, pre-1000 non-full-zero dates, runtime distinction for parser-admitted `DATE '...'` temporal literal introducers, time parts outside the admitted storage/default/predicate literals, casts, date arithmetic, general expression defaults beyond admitted current-date defaults, current-date/UTC-date assignment to other target families, date primary keys, auto-increment, time-zone shifting outside documented `DATE` storage/default offset normalization, or protocol-grade temporal metadata |
| `TIME` | 🟡 | Limited canonical `[-]HH:MM:SS` / `[-]HHH:MM:SS` descriptors stored as `TEXT`, with valid range `-838:59:59` through `838:59:59`, canonical time-string defaults, parenthesized generated current-time defaults, `INSERT`/`REPLACE`/`UPDATE` assignment including explicit zero-fractional current-time and UTC-time values, `INSERT IGNORE` invalid/out-of-range/`NULL` adjustments to `00:00:00` or clipped endpoint values, nonempty `ALTER TABLE ... ADD TIME NOT NULL` zero-time backfill, readback, descriptor cloning/copying, `IS NULL` / `IS NOT NULL`, string-literal comparison/`BETWEEN`/`IN` predicates using signed total-second semantics, single-column ordering with MySQL-compatible `NULL` placement and signed time ordering, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering with `DATETIME_PRECISION = 0`; no fractional precision, relaxed time strings, runtime distinction for parser-admitted `TIME '...'` temporal literal introducers, numeric time conversion, general expression defaults beyond admitted current-time defaults, current-time/UTC-time assignment to other target families, casts, time primary keys, auto-increment, or protocol-grade temporal metadata |
| `DATETIME` | 🟡 | Limited canonical `YYYY-MM-DD HH:MM:SS` descriptors stored as `TEXT`, with valid range `1000-01-01 00:00:00` through `9999-12-31 23:59:59`, canonical and admitted limited relaxed datetime-string defaults plus zero-fractional `CURRENT_TIMESTAMP` defaults, `ON UPDATE CURRENT_TIMESTAMP`, `INSERT`/`REPLACE`/`UPDATE` assignment including explicit current timestamp and UTC timestamp values, limited SQL-mode-aware full-zero, partial-zero, and `ALLOW_INVALID_DATES` conversion for row values/defaults/compatible descriptor copies/predicates, `INSERT IGNORE` invalid/zero/`NULL` adjustments to `0000-00-00 00:00:00`, readback, descriptor cloning/copying, `IS NULL` / `IS NOT NULL`, canonical plus limited `T`-separator, numeric-offset, single trailing-`Z`/`z` truncation-warning, and date-only one-/two-digit month/day string-literal comparison/`BETWEEN`/`IN` predicates, canonical plus limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` string-literal storage/default conversion, single-column ordering with MySQL-compatible `NULL` placement, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering with `DATETIME_PRECISION = 0`; no fractional precision, relaxed datetime strings outside the documented storage, default, and predicate slices, pre-1000 non-full-zero datetimes, runtime distinction for parser-admitted `DATE` and `TIMESTAMP` temporal literal introducers, mutable time-zone conversion beyond documented storage/default and predicate offset conversion, temporal functions beyond the limited statement-current/UTC synonyms, casts, datetime arithmetic, datetime primary keys, auto-increment, or protocol-grade temporal metadata |
| `TIMESTAMP` | 🟡 | Limited fixed-UTC canonical `YYYY-MM-DD HH:MM:SS` descriptors stored as `TEXT`, with valid range `1970-01-01 00:00:01` through `2038-01-19 03:14:07`, canonical and admitted limited relaxed timestamp-string defaults plus zero-fractional `CURRENT_TIMESTAMP` defaults, `ON UPDATE CURRENT_TIMESTAMP`, `INSERT`/`REPLACE`/`UPDATE` assignment including explicit current timestamp and UTC timestamp values, limited SQL-mode-aware full-zero conversion for row values/defaults/compatible descriptor copies/predicates, `INSERT IGNORE` invalid/zero/out-of-range/`NULL` adjustments to `0000-00-00 00:00:00`, nonempty `ALTER TABLE ... ADD TIMESTAMP NOT NULL` zero backfill, readback, descriptor cloning/copying, `IS NULL` / `IS NOT NULL`, canonical plus limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning string-literal storage/default conversion and comparison/`BETWEEN`/`IN` predicates, single-column ordering with MySQL-compatible `NULL` placement, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering with `DATETIME_PRECISION = 0`; no fractional precision, relaxed timestamp strings outside the documented storage, default, and predicate slices, partial-zero timestamp storage, `ALLOW_INVALID_DATES` timestamp widening, runtime distinction for parser-admitted temporal literal introducers, mutable time-zone storage/readback conversion, temporal functions beyond the limited statement-current/UTC synonyms, casts, timestamp arithmetic, timestamp primary keys, auto-increment, or protocol-grade temporal metadata |
| `SYSDATE()` temporal assignment | 🟡 | Limited zero-fractional execution-time `SYSDATE()` values convert only to compatible `DATETIME` and `TIMESTAMP` descriptors in current `INSERT`, `REPLACE`, and single-table `UPDATE` row-value paths. The value is formatted as canonical text after applying the limited session `time_zone` offset and ignoring the limited session `timestamp` override; no fractional precision, defaults, `ON UPDATE`, `DATE` / `TIME`, string, numeric, `YEAR`, `BIT`, `ENUM`, `SET`, `JSON`, binary, spatial, or general expression coercion |
| `YEAR` | 🟡 | Limited `YEAR` / deprecated `YEAR(4)` descriptors stored as canonical four-digit `TEXT`, with `YEAR(4)` warning 1287 and invalid explicit widths rejected with 1818, represented values `0000` and `1901..2155`, MySQL-compatible numeric, string, boolean, `NULL`, and `DEFAULT` row-value conversion including two-digit rules, canonical descriptor defaults including limited parenthesized constant integer/`NULL` expression defaults, `INSERT`/`REPLACE`/`UPDATE` assignment, `INSERT IGNORE` invalid/out-of-range/`NULL`/missing-default adjustment to `0000`, nonempty `ALTER TABLE ... ADD YEAR NOT NULL` zero backfill, readback, descriptor cloning/copying, limited scalar-subquery assignment, `IS NULL` / `IS NOT NULL`, comparison/`BETWEEN`/`IN` predicates including MySQL-compatible multi-value `IN` list coercion, single-column ordering with MySQL-compatible `NULL` placement, supported nonunique and full-column unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `YEAR()`/`YEAR(0)`/`YEAR(2)`/other widths, `YEAR UNSIGNED`/`SIGNED`/`ZEROFILL`, relaxed year strings, temporal functions, casts, year primary keys, auto-increment, or protocol-grade temporal metadata |

## String and binary types

| Feature | Status | Notes |
| --- | --- | --- |
| `CHAR` | 🟡 | Limited bare `CHAR`, `CHAR(0..255)`, `CHARACTER`, and `CHARACTER(0..255)` descriptors with descriptor-owned row-size checks for create/add-column paths, UTF-8 non-`NUL` `TEXT` storage/readback in MySQL's default trimmed `CHAR` shape, silent excess-trailing-space truncation, strict nonspace overlength errors, limited non-strict and `INSERT IGNORE` nonspace DML truncation warnings, literal descriptor defaults plus limited parenthesized constant string/`NULL` generated defaults in full column-definition DDL, `NULL` and empty-string behavior including non-key `CHAR(0)`, optional explicit `utf8mb4` or `ascii` column charset/collation metadata and standalone `BINARY` binary-collation shorthand on admitted create/add/modify/change paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique full/prefix secondary-index declaration/metadata, ASCII `CHAR(1..255)` primary-key participation in single-column or table-level composite primary keys plus single-column or table-level composite full-column unique-key participation and prefix unique-key participation including composite prefix key parts through MyLite's fixed ASCII subset of `utf8mb4_0900_ai_ci`, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no warning-producing overlength default truncation, general character expression defaults beyond the limited generated string/`NULL` subset, `PAD_CHAR_TO_FULL_LENGTH`, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, `CHAR(0)` keys, primary prefix keys, non-ASCII primary/unique key values, full Unicode collation-aware index semantics, or protocol-grade metadata |
| `VARCHAR` | 🟡 | Limited `VARCHAR(0..16383)`, `CHARACTER VARYING(0..16383)`, and `CHAR VARYING(0..16383)` descriptors with descriptor-owned row-size checks for create/add-column paths, UTF-8 non-`NUL` `TEXT` storage/readback, `NULL` and empty-string behavior, strict nonspace overlength errors, strict trailing-space truncation notes, limited non-strict and `INSERT IGNORE` nonspace DML truncation warnings, literal descriptor defaults plus limited parenthesized constant string/`NULL` generated defaults in full column-definition DDL, optional explicit `utf8mb4` or `ascii` column charset/collation metadata and standalone `BINARY` binary-collation shorthand on admitted create/add/modify/change paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique full/prefix secondary-index declaration/metadata, ASCII `VARCHAR(1..255)` primary-key participation in single-column or table-level composite primary keys plus single-column or table-level composite full-column unique-key participation and prefix unique-key participation including composite prefix key parts through MyLite's fixed ASCII subset of `utf8mb4_0900_ai_ci`, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no warning-producing overlength default truncation, general character expression defaults beyond the limited generated string/`NULL` subset, lengths above 16383, charset conversion, full Unicode collation comparison, grouping/distinct, string aggregates beyond the current `MIN()` / `MAX()` descriptor subset, non-admitted column charset attributes, `VARCHAR(0)` keys, primary prefix keys, non-ASCII primary/unique key values, full Unicode collation-aware index semantics, or protocol-grade metadata |
| `CHARACTER` / `CHARACTER VARYING` | 🟡 | Limited aliases for the supported `CHAR` and `VARCHAR` descriptor subsets: `CHARACTER` / `CHARACTER(n)` normalize to `char(n)`, while `CHARACTER VARYING(n)` and `CHAR VARYING(n)` normalize to `varchar(n)`; same current CHAR length, wider VARCHAR length, DDL, DML, metadata, collation, and key limits as the normalized target descriptors |
| `NCHAR` / `NATIONAL CHAR` | 🟡 | Limited aliases for the supported `CHAR` descriptor subset: `NCHAR`, `NCHAR(n)`, `NATIONAL CHAR[(n)]`, and `NATIONAL CHARACTER[(n)]` store as SQLite `TEXT`, preserve a national logical descriptor, emit warning 3720 when explicitly declared, render as `char(n) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci` in `SHOW CREATE TABLE`, report utf8mb3 metadata in `SHOW FULL COLUMNS`, `INFORMATION_SCHEMA.COLUMNS`, and result-column metadata, and reuse current `CHAR` DDL/DML/default/key/clone/persistence behavior. No general column charset attributes, national string literals, charset conversion, utf8mb3 collation semantics, `PAD_CHAR_TO_FULL_LENGTH`, or protocol-grade metadata |
| `NVARCHAR` / `NATIONAL VARCHAR` | 🟡 | Limited aliases for the supported `VARCHAR` descriptor subset: `NVARCHAR(n)`, `NATIONAL VARCHAR(n)`, `NCHAR VARCHAR(n)`, `NCHAR VARYING(n)`, `NATIONAL CHAR VARYING(n)`, and `NATIONAL CHARACTER VARYING(n)` store as SQLite `TEXT`, preserve a national logical descriptor, emit warning 3720 when explicitly declared, render as `varchar(n) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci` in `SHOW CREATE TABLE`, report utf8mb3 metadata in `SHOW FULL COLUMNS`, `INFORMATION_SCHEMA.COLUMNS`, and result-column metadata, and reuse current `VARCHAR` DDL/DML/default/key/clone/persistence behavior within the current `VARCHAR(0..16383)` envelope. No bare `NVARCHAR`, lengths above 16383, general column charset attributes, national string literals, charset conversion, utf8mb3 collation semantics, or protocol-grade metadata |
| `BINARY` | 🟡 | Limited `BINARY` / `BINARY(0..255)` descriptors stored as SQLite `BLOB`, with zero-byte right-padding, byte-safe result readback, string, hex, and bit-literal row-value assignment, limited literal string/hex defaults, `NULL`, strict length diagnostics, `INSERT IGNORE` overlength truncation and not-null adjustment, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no binary comparisons, ordering/grouping/distinct, indexes, casts into table-backed expressions, streaming I/O, or protocol-grade metadata |
| `VARBINARY` | 🟡 | Limited `VARBINARY(0..65535)` descriptors stored as SQLite `BLOB`, with current row-size cap at `65532` for one wide column, byte-safe result readback, string, hex, and bit-literal row-value assignment, limited literal string/hex defaults, `NULL`, strict length diagnostics, `INSERT IGNORE` overlength truncation and not-null adjustment, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no bare `VARBINARY`, binary comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `CHAR BYTE` | 🟡 | Limited alias behavior for `CHAR BYTE` and `CHAR(n) BYTE`, normalized to `BINARY(1)` and `BINARY(n)` descriptors; same DDL, DML, metadata, padding, and unsupported-operation limits as `BINARY` |

## Large object types

| Feature | Status | Notes |
| --- | --- | --- |
| `TINYBLOB` | 🟡 | Limited bare `TINYBLOB` descriptors stored as SQLite `BLOB`, with byte-safe result readback, string, hex, and bit-literal row-value assignment, `NULL`, strict and `INSERT IGNORE` length/nullability handling, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, limited parenthesized hex/`NULL` generated defaults in supported column-definition DDL, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no bare non-`NULL` defaults, string or general expression defaults, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `BLOB` | 🟡 | Limited bare `BLOB` plus `BLOB(M)` normalization to the smallest compatible BLOB family descriptor, with byte-safe result readback, string, hex, and bit-literal row-value assignment, `NULL`, strict and `INSERT IGNORE` length/nullability handling, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, limited parenthesized hex/`NULL` generated defaults in supported column-definition DDL, and metadata rendering; no bare non-`NULL` defaults, string or general expression defaults, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `MEDIUMBLOB` | 🟡 | Limited bare `MEDIUMBLOB` descriptors stored as SQLite `BLOB`, with byte-safe result readback, string, hex, and bit-literal row-value assignment, `NULL`, strict and `INSERT IGNORE` length/nullability handling, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, limited parenthesized hex/`NULL` generated defaults in supported column-definition DDL, and metadata rendering; no bare non-`NULL` defaults, string or general expression defaults, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `LONGBLOB` | 🟡 | Limited bare `LONGBLOB` descriptors stored as SQLite `BLOB`, with byte-safe result readback, string, hex, and bit-literal row-value assignment, `NULL`, strict and `INSERT IGNORE` length/nullability handling, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, limited parenthesized hex/`NULL` generated defaults in supported column-definition DDL, and metadata rendering; no bare non-`NULL` defaults, string or general expression defaults, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `TINYTEXT` | 🟡 | Limited bare `TINYTEXT` descriptors with descriptor-owned row-size checks for create/add-column paths and supported `ALTER CHANGE` / `MODIFY` targets from compatible nonbinary string/text-family source descriptors, UTF-8 non-`NUL` `TEXT` storage/readback, strict 255-byte row-value checks plus limited non-strict and `IGNORE` overlength DML truncation, `NULL` and empty-string behavior, optional explicit `utf8mb4` or `ascii` column charset/collation metadata and standalone `BINARY` binary-collation shorthand on admitted create/add/modify/change paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique and unique prefix-index declaration/metadata plus full-column secondary/unique indexes through the documented WordPress bridge, `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering, and parenthesized constant string/`NULL` defaults in supported column-definition DDL; no `TINYTEXT(M)`, bare string defaults, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, full-column text indexes outside the documented WordPress bridge, primary indexes, or protocol-grade metadata |
| `TEXT` | 🟡 | Limited bare `TEXT` descriptors plus `TEXT(M)` normalization to the smallest compatible text or binary family descriptor, with descriptor-owned row-size checks for create/add-column paths and supported `ALTER CHANGE` / `MODIFY` targets from compatible nonbinary string/text-family source descriptors, UTF-8 non-`NUL` `TEXT` storage/readback, strict 65535-byte row-value checks plus limited non-strict and `IGNORE` overlength DML truncation, `NULL` and empty-string behavior, optional explicit `utf8mb4` or `ascii` column charset/collation metadata and standalone `BINARY` binary-collation shorthand on admitted create/add/modify/change paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique and unique prefix-index declaration/metadata plus full-column secondary/unique indexes through the documented WordPress bridge, `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering, and parenthesized constant string/`NULL` defaults in supported column-definition DDL; no decimal/signed/quoted `TEXT(M)` lengths, bare string defaults, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, full-column text indexes outside the documented WordPress bridge, primary indexes, or protocol-grade metadata |
| `MEDIUMTEXT` | 🟡 | Limited bare `MEDIUMTEXT` descriptors with descriptor-owned row-size checks for create/add-column paths and supported `ALTER CHANGE` / `MODIFY` targets from compatible nonbinary string/text-family source descriptors, UTF-8 non-`NUL` `TEXT` storage/readback, strict 16777215-byte row-value checks plus limited non-strict and `IGNORE` overlength DML truncation, `NULL` and empty-string behavior, optional explicit `utf8mb4` or `ascii` column charset/collation metadata and standalone `BINARY` binary-collation shorthand on admitted create/add/modify/change paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique and unique prefix-index declaration/metadata plus full-column secondary/unique indexes through the documented WordPress bridge, `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering, and parenthesized constant string/`NULL` defaults in supported column-definition DDL; no `MEDIUMTEXT(M)`, bare string defaults, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, full-column text indexes outside the documented WordPress bridge, primary indexes, or protocol-grade metadata |
| `LONGTEXT` | 🟡 | Limited bare `LONGTEXT` descriptors with descriptor-owned row-size checks for create/add-column paths and supported `ALTER CHANGE` / `MODIFY` targets from compatible nonbinary string/text-family source descriptors, UTF-8 non-`NUL` `TEXT` storage/readback, strict 4294967295-byte row-value checks within process memory limits plus limited non-strict and `IGNORE` overlength DML truncation, `NULL` and empty-string behavior, optional explicit `utf8mb4` or `ascii` column charset/collation metadata and standalone `BINARY` binary-collation shorthand on admitted create/add/modify/change paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique and unique prefix-index declaration/metadata plus full-column secondary/unique indexes through the documented WordPress bridge, `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering, and parenthesized constant string/`NULL` defaults in supported column-definition DDL; no `LONGTEXT(M)`, bare string defaults, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, full-column text indexes outside the documented WordPress bridge, primary indexes, streaming I/O, or protocol-grade metadata |
| `LONG` / `LONG VARCHAR` | 🟡 | Limited aliases normalized to the supported `MEDIUMTEXT` descriptor path in create/add-column/clone/CTAS and row DML, with optional admitted `utf8mb4` or `ascii` column charset/collation metadata, standalone `BINARY` binary-collation shorthand, inherited `MEDIUMTEXT` predicate and ordering subset, and normalized `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `LONG VARCHAR(length)`, `LONG TEXT`, bare string defaults, general expression defaults beyond inherited parenthesized constant string/`NULL` defaults, alias spelling preservation, grouping/distinct, full-column text indexes outside the documented WordPress bridge, primary indexes, streaming I/O, or protocol-grade metadata |
| `LONG VARBINARY` | 🟡 | Limited alias normalized to the supported `MEDIUMBLOB` descriptor path in create/add-column/clone/CTAS and row DML, with byte-safe readback, inherited limited parenthesized hex/`NULL` generated defaults, and normalized `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `LONG VARBINARY(length)`, bare non-`NULL` defaults, string or general expression defaults, charset/collation attributes, alias spelling preservation, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |

## Enumerated types

| Feature | Status | Notes |
| --- | --- | --- |
| `ENUM` | 🟡 | Limited descriptor-owned `ENUM('label'[, ...])` columns with valid non-`NUL` UTF-8 labels, trailing-space normalization at definition time, ASCII case-insensitive duplicate detection and label matching, physical `TEXT` storage of the canonical definition label, string-label defaults, nullable `NULL` defaults, omitted/default no-explicit-default `NOT NULL` first-label behavior without warning, adjusted explicit `NULL` to the empty enum error value in current non-strict/`IGNORE` paths, strict `INSERT`/`REPLACE`/supported `UPDATE` assignment conversion from matching labels, in-range decimal ordinal literals, quoted ordinal fallback when no label matches, `NULL`, and `DEFAULT`, descriptor-backed `=`, `<=>`, `<>`, `!=`, `<=> NULL`, `IS NULL`, and `IS NOT NULL` predicates, `SHOW` / `INFORMATION_SCHEMA.COLUMNS` / result-column metadata, and descriptor cloning. No compact ordinal storage, invalid index-0 storage beyond the adjusted explicit-`NULL` empty enum value, non-strict or `IGNORE` invalid-value demotion, enum sorting/grouping/distinct, enum keys/indexes, enum foreign keys, expression conversion, `ALTER MODIFY`/`CHANGE`, `INSERT ... SELECT`, scalar subquery assignment, full collation semantics, or 65,535-label envelope |
| `SET` | 🟡 | Limited descriptor-owned `SET('member'[, ...])` columns with nonempty valid non-`NUL` UTF-8 members, trailing-space normalization at definition time, comma-containing member rejection, ASCII case-insensitive duplicate detection and member matching, physical `TEXT` storage of the canonical comma-separated definition-order display string, string-list defaults, nullable `NULL` defaults, catalog-only `ALTER COLUMN SET DEFAULT` and `ALTER COLUMN DROP DEFAULT`, strict `INSERT`/`REPLACE`/supported `UPDATE` assignment conversion from matching member lists, nonnegative decimal bitmap literals, quoted-bitmap fallback when no member matches, `NULL`, and `DEFAULT`, descriptor-backed `=`, `<=>`, `<>`, `!=`, `<=> NULL`, `IS NULL`, and `IS NOT NULL` predicates, `SHOW` / `INFORMATION_SCHEMA.COLUMNS` / result-column metadata, and descriptor cloning. No compact bitmap storage, empty-string set members, invalid value warning demotion, set sorting/grouping/distinct, set keys/indexes, set foreign keys, expression conversion, `ALTER MODIFY`/`CHANGE`, `INSERT ... SELECT`, scalar subquery assignment, full collation semantics, or more than 64 members |

## Structured and spatial types

| Feature | Status | Notes |
| --- | --- | --- |
| `JSON` | 🟡 | Limited descriptor-owned `JSON` columns stored as canonical SQLite `TEXT`, with validation/canonicalization for the verified subset of JSON `null`, booleans, strings, signed integer numbers, arrays, and ASCII-key objects; supports `CREATE TABLE`, `CREATE TABLE ... LIKE`, `CREATE TABLE ... SELECT`, `ALTER TABLE ... ADD COLUMN`, `INSERT`/`REPLACE`/supported `UPDATE` assignment, compatible `INSERT ... SELECT`/`REPLACE ... SELECT` and scalar-subquery `UPDATE`, SQL `NULL`, `DEFAULT`, `INSERT IGNORE` implicit JSON `null`, `IS NULL` / `IS NOT NULL`, limited `JSON_VALID()` checks, limited simple-path `JSON_EXTRACT()` / `JSON_VALUE()` / `JSON_UNQUOTE()` / `JSON_LENGTH()` / `JSON_TYPE()` / `->` / `->>` read projections, limited `JSON_ARRAY()` / `JSON_OBJECT()` construction, limited scalar/row-scalar `JSON_SET()` / `JSON_INSERT()` / `JSON_REPLACE()` / `JSON_REMOVE()` document mutation projections, `SHOW COLUMNS`, `SHOW CREATE TABLE`, `INFORMATION_SCHEMA.COLUMNS`, descriptor result-column metadata, and limited scalar/row-scalar result-column metadata for current JSON function projections. No stored partial JSON mutation, full path grammar, comparison/order/grouping semantics, binary JSON storage, decimal/exponent/non-ASCII JSON normalization beyond the verified subset, expression defaults, generated/path indexes, direct JSON indexes, `ALTER MODIFY`/`CHANGE`, casts, or general expression conversion |
| `GEOMETRY` | 🟡 | Limited descriptor-owned nullable or `NOT NULL` spatial column metadata in persistent `CREATE TABLE`, `CREATE TABLE ... LIKE`, limited `SHOW` / `INFORMATION_SCHEMA.COLUMNS`, and limited `INFORMATION_SCHEMA.ST_GEOMETRY_COLUMNS`, with SQL `NULL` storage/readback and metadata-only one-column `SPATIAL` index participation when `NOT NULL`; no non-`NULL` geometry values, WKB/WKT parsing, SRID attributes, validity checks, spatial functions, spatial predicates, or coordinate access |
| `POINT` | 🟡 | Limited descriptor-owned nullable or `NOT NULL` spatial column metadata with SQL `NULL` storage/readback and metadata-only one-column `SPATIAL` index participation when `NOT NULL`; no non-`NULL` point values, WKB/WKT parsing, SRID attributes, validity checks, spatial functions, spatial predicates, or coordinate access |
| `LINESTRING` | 🟡 | Limited descriptor-owned nullable or `NOT NULL` spatial column metadata with SQL `NULL` storage/readback and metadata-only one-column `SPATIAL` index participation when `NOT NULL`; no non-`NULL` LineString values, WKB/WKT parsing, SRID attributes, validity checks, spatial functions, spatial predicates, or coordinate access |
| `POLYGON` | 🟡 | Limited descriptor-owned nullable or `NOT NULL` spatial column metadata with SQL `NULL` storage/readback and metadata-only one-column `SPATIAL` index participation when `NOT NULL`; no non-`NULL` Polygon values, WKB/WKT parsing, SRID attributes, validity checks, spatial functions, spatial predicates, or coordinate access |
| `MULTIPOINT` | 🟡 | Limited descriptor-owned nullable or `NOT NULL` spatial column metadata with SQL `NULL` storage/readback and metadata-only one-column `SPATIAL` index participation when `NOT NULL`; no non-`NULL` MultiPoint values, WKB/WKT parsing, SRID attributes, validity checks, spatial functions, spatial predicates, or coordinate access |
| `MULTILINESTRING` | 🟡 | Limited descriptor-owned nullable or `NOT NULL` spatial column metadata with SQL `NULL` storage/readback and metadata-only one-column `SPATIAL` index participation when `NOT NULL`; no non-`NULL` MultiLineString values, WKB/WKT parsing, SRID attributes, validity checks, spatial functions, spatial predicates, or coordinate access |
| `MULTIPOLYGON` | 🟡 | Limited descriptor-owned nullable or `NOT NULL` spatial column metadata with SQL `NULL` storage/readback and metadata-only one-column `SPATIAL` index participation when `NOT NULL`; no non-`NULL` MultiPolygon values, WKB/WKT parsing, SRID attributes, validity checks, spatial functions, spatial predicates, or coordinate access |
| `GEOMETRYCOLLECTION` | 🟡 | Limited descriptor-owned nullable or `NOT NULL` spatial column metadata, rendered as `geomcollection` in MySQL-style DDL, with SQL `NULL` storage/readback and metadata-only one-column `SPATIAL` index participation when `NOT NULL`; no non-`NULL` GeometryCollection values, WKB/WKT parsing, SRID attributes, validity checks, spatial functions, spatial predicates, or coordinate access |

## Literals

Standalone `VALUES` row values currently admit the documented limited numeric,
ordinary string, `NULL`, `TRUE`, and `FALSE` literal subset from
[SQL query expressions](sql-query-expressions.md); broader expression,
temporal, hex, bit, decimal, floating, and `DEFAULT` row values remain outside
that standalone statement subset.

| Feature | Status | Notes |
| --- | --- | --- |
| Numeric literals | 🟡 | Decimal integer literals with optional unary sign as supported no-source/`DUAL` literal projection values with MySQL-shaped result-column metadata, no-source/`DUAL` signed-64 arithmetic projection operands, limited table-backed signed integer arithmetic projection operands, limited no-source/`DUAL`/`DO` numeric functions, limited no-source/`DUAL`/`DO` `GET_FORMAT()` format arguments, limited no-source/`DUAL`/`DO` `SEC_TO_TIME()` second, `FROM_UNIXTIME()` epoch-second, `FROM_DAYS()` day-number, `MAKEDATE()` year/day, and `MAKETIME()` part arguments, limited no-source/`DUAL` bare `CAST(value AS BINARY)` inputs, limited no-source/`DUAL`/`DO` `CONVERT(value, BINARY)` and scalar `CONVERT(value USING utf8mb4\|utf8\|utf8mb3\|latin1)` inputs with `latin1` limited to ASCII scalar values, limited no-source/`DUAL`/`DO` DATE interval-unit `DATE_ADD()` / `DATE_SUB()` / `ADDDATE()` / `SUBDATE()` operands, limited same-column single-table `UPDATE` DATE interval assignment operands, and interval-second `TIMESTAMPADD()` operands, limited no-source/`DUAL`/`DO` `ELT()` integer arguments, limited no-source/`DUAL`/`DO` and row-scalar control-flow, `CONCAT_WS()`, `FIELD()`, `GREATEST()`, `LEAST()`, `INTERVAL()`, `REPLACE()`, `INSERT()`, `JSON_ARRAY()`, `JSON_OBJECT()`, and `JSON_VALID()` integer arguments, limited no-source/`DUAL`/`DO` and row-scalar string length, string slice, string search, string replacement/insertion, string padding, ASCII string case, `HEX()`, and `QUOTE()` function integer arguments, no-source/`DUAL` keyword scalar logical operands, limited `DO` scalar-expression operands including top-level `/` division, integer column default values including exact quoted decimal integer strings, limited parenthesized constant integer expression defaults, `INSERT ... VALUES`, `INSERT ... SET`, `REPLACE ... VALUES`, `REPLACE ... SET`, limited no-argument `UNIX_TIMESTAMP()` plus/minus signed integer or `NULL` DML value deltas for non-`AUTO_INCREMENT` integer-family and nonbinary string insert/replace/duplicate-update and single-table `UPDATE` assignment targets, limited source-free constant integer `UPDATE` arithmetic assignment expressions, and limited unsigned same-column integer `UPDATE` and `VALUES`/`SET` duplicate-update arithmetic assignment deltas plus supported filtered `SELECT`/`DELETE`/`UPDATE` predicate right operands, scalar-literal truth/comparison/`IS` predicates, literal-left descriptor comparisons, `BETWEEN` bounds, and `IN` list values; fixed decimal literals with optional unary sign only for compatible decimal and approximate column defaults plus row DML assignments and limited no-source/`DUAL`/`DO` `FORMAT()` / `TRUNCATE()` value arguments plus limited no-source/`DUAL`/`DO` and row-scalar `QUOTE()` value arguments, with exact decimal canonicalization for decimal targets and finite range checks for approximate targets; approximate numeric literals with optional unary sign are admitted only for compatible approximate column defaults and row DML assignments; `INSERT IGNORE ... VALUES` and `INSERT IGNORE ... SET` clip out-of-range descriptor integer, decimal, and approximate inputs to the current supported range with warnings; unsigned decimal integer literals for supported `SELECT` `LIMIT`/`OFFSET` and `DELETE`/`UPDATE LIMIT`; hexadecimal and bit literals are admitted as byte-safe no-source/`DUAL` scalar projections, hexadecimal and bit literals remain admitted as binary-string row values, hexadecimal literals are admitted as numeric magnitudes for compatible integer, decimal, and approximate row DML assignments, hexadecimal literals are admitted as numeric operands for the supported integer-family predicate right-operand, scalar-literal truth/comparison, literal-left descriptor comparison, `BETWEEN`, and `IN` subsets, and hexadecimal literals remain admitted as limited no-source/`DUAL`/`DO` `CRC32()` and `HEX()` inputs; no table-backed expression-level numeric semantics outside the limited table-backed signed integer arithmetic projection subset, limited source-free constant update arithmetic assignment subset, limited same-column update and `VALUES`/`SET` duplicate-update arithmetic assignment subset, limited `UNIX_TIMESTAMP()` insert/replace/duplicate-update and single-table update assignment arithmetic subset, limited same-column single-table update DATE interval assignment subset, documented hexadecimal DML storage and integer-predicate slices, and limited row-scalar control-flow value subset, bit-string numeric conversion, non-decimal numeric formats as numeric values outside the documented hexadecimal DML and predicate slices, approximate scalar projection, approximate predicates beyond `IS NULL`, or quoted decimal/approximate defaults |
| Boolean literals | 🟡 | `TRUE` and `FALSE` are accepted as `1` and `0` only in supported no-source/`DUAL` literal projection values with MySQL-shaped result-column metadata, limited no-source/`DUAL` signed-64 scalar arithmetic projection operands including unary `+` and unary `-`, limited table-backed signed integer arithmetic projection operands, limited no-source/`DUAL`/`DO` `GET_FORMAT()` format arguments, limited no-source/`DUAL` `ABS()`, `SIGN()`, `CEIL()`/`CEILING()`/`FLOOR()`, limited integer-domain `ROUND()`, `BIT_COUNT()`, `BIN()`, `OCT()`, `CONV()`, `SQRT()`, `DEGREES()`, `RADIANS()`, `ACOS()`, `ASIN()`, `SIN()`, `COS()`, `TAN()`, `COT()`, `ATAN()`, `ATAN2()`, `EXP()`, `LN()`, `LOG()`, `LOG10()`, `LOG2()`, `POW()`, `POWER()`, `CRC32()`, `HEX()`, `FORMAT()`, `TRUNCATE()`, `SEC_TO_TIME()`, `FROM_UNIXTIME()`, `FROM_DAYS()`, `MAKEDATE()`, and `MAKETIME()` operands, limited no-source/`DUAL` bare `CAST(value AS BINARY)` inputs, limited no-source/`DUAL`/`DO` `CONVERT(value, BINARY)` and scalar `CONVERT(value USING utf8mb4\|utf8\|utf8mb3\|latin1)` inputs with `latin1` limited to ASCII scalar values, limited no-source/`DUAL`/`DO` `ELT()` boolean arguments, limited no-source/`DUAL`/`DO` and row-scalar control-flow, `CONCAT_WS()`, `FIELD()`, `GREATEST()`, `LEAST()`, `INTERVAL()`, `REPLACE()`, `INSERT()`, `JSON_ARRAY()`, `JSON_OBJECT()`, and `JSON_VALID()` boolean arguments, limited no-source/`DUAL`/`DO` and row-scalar string length, string slice, string search, string replacement/insertion, string padding, ASCII string case, `HEX()`, and `QUOTE()` function boolean arguments, no-source/`DUAL` keyword scalar logical operands, limited `DO` scalar-expression operands including top-level `/` division, column default values, integer row-value, `INSERT ... SET`, `REPLACE ... VALUES`, `REPLACE ... SET`, single-table `UPDATE` assignment and limited source-free constant integer `UPDATE` arithmetic assignment expressions, filtered `SELECT`/`DELETE`/`UPDATE` predicate right-operand, scalar-literal truth/comparison/`IS` predicates, literal-left descriptor comparisons, `BETWEEN` bound, and `IN` list positions, `COUNT(TRUE)` / `COUNT(FALSE)` aggregate argument positions, and descriptor-column `IS [NOT] TRUE` / `FALSE` truth-test targets; no general boolean expressions outside the limited scalar and descriptor-backed subsets, scalar truth metadata beyond the literal projection metadata subset, general expression evaluation, or `LIMIT TRUE` / `LIMIT FALSE` |
| String literals | 🟡 | Ordinary single- and double-quoted string literals with decoded quote and backslash escapes are supported only in admitted `CHAR`, `VARCHAR`, baseline `TEXT` family, limited `ENUM`, limited `SET`, limited `JSON`, and binary string row-value positions, limited ASCII string, `ENUM`, and `SET` equality predicate right operands, limited ASCII string range/membership predicate operands, limited ASCII `LIKE` and `REGEXP` pattern predicate right operands, canonical `TIME` defaults/row values plus canonical temporal predicates and canonical or limited relaxed `DATE`, `DATETIME`, and `TIMESTAMP` defaults/row values and predicates, plus literal `CHAR`/`VARCHAR`/`ENUM`/`SET` defaults and limited parenthesized generated `CHAR`/`VARCHAR`/`TEXT` default strings, limited no-source/`DUAL` ordinary string literal projection values with byte-safe result access and MySQL-shaped result-column metadata, limited no-source/`DUAL` bare `CAST(value AS BINARY)` scalar inputs and limited no-source/`DUAL`/`DO` `CONVERT(value, BINARY)`, `CONVERT('string' USING BINARY)`, and scalar `CONVERT(value USING utf8mb4\|utf8\|utf8mb3\|latin1)` inputs with `latin1` limited to ASCII scalar values, limited no-source/`DUAL`/`DO` `CRC32()`, `HEX()`, and `QUOTE()` string arguments, limited no-source/`DUAL`/`DO` DATE interval-unit `DATE_ADD()` / `DATE_SUB()` / `ADDDATE()` / `SUBDATE()` date/datetime string inputs plus interval-second `TIMESTAMPADD()` date/datetime string inputs, limited no-source/`DUAL`/`DO` `ADDTIME()` / `SUBTIME()` canonical datetime/time string inputs, limited no-source/`DUAL`/`DO` and row-scalar `GET_FORMAT()` format-name string arguments, limited no-source/`DUAL`/`DO` and row-scalar `DATE_FORMAT()` / `TIME_FORMAT()` value/format string arguments, limited no-source/`DUAL`/`DO` and row-scalar `DATEDIFF()`, `TIMEDIFF()`, and `TIMESTAMPDIFF()` value string arguments plus `TIMESTAMP()` value/time string arguments, limited no-source/`DUAL`/`DO` and row-scalar `UNIX_TIMESTAMP()` value string arguments, limited no-source/`DUAL`/`DO` and row-scalar temporal extractor, `TIME_TO_SEC()`, `TO_DAYS()`, and `TO_SECONDS()` string arguments, limited no-source/`DUAL`/`DO` `ELT()` string arguments, limited no-source/`DUAL`/`DO` and row-scalar control-flow, `CONCAT_WS()`, `FIELD()`, `GREATEST()`, `LEAST()`, `REPLACE()`, `INSERT()`, `JSON_ARRAY()`, `JSON_OBJECT()`, `JSON_VALID()`, `JSON_TYPE()`, and `JSON_LENGTH()` string arguments, limited no-source/`DUAL`/`DO` and row-scalar string length, string slice, string replacement/insertion, and string padding function string arguments, limited ASCII NUL-free no-source/`DUAL`/`DO` and row-scalar string search function string arguments, limited ASCII no-source/`DUAL`/`DO` and row-scalar string case function string arguments, limited row-scalar `QUOTE()` and `HEX()` function string arguments, and existing alias contexts; parser-admitted charset introducers before string, hex, and bit literals are also accepted in descriptor-backed DML value positions, preserve the literal token bytes, and do not transcode; `ANSI_QUOTES` makes later double-quoted tokens identifiers instead of strings, and `NO_BACKSLASH_ESCAPES` disables backslash escape decoding for later admitted string literals; admitted `CHAR` row values are canonicalized to MySQL's default trimmed readback shape, admitted `ENUM` row values canonicalize to the matching definition label, admitted `SET` row values canonicalize to comma-separated definition-order member display, admitted `JSON` row values canonicalize to the current verified JSON display subset, binary string row values may contain decoded embedded `NUL` bytes and are exposed through byte-safe result accessors, while other admitted row values preserve ordinary `\%` and `\_` backslash escapes like MySQL outside pattern matching; no charset introducer conversion/collation semantics, national strings, adjacent literal concatenation, parameters, table-backed expression values outside admitted predicates, row-scalar control-flow projection, and row-scalar `QUOTE()` / `HEX()` string inputs, arbitrary scalar projection beyond listed slices, embedded `NUL` in nonbinary text storage outside scalar `HEX()` and `QUOTE()` inputs and the explicit `DATEDIFF()`, `TIMEDIFF()`, `TIMESTAMP()`, `TIMESTAMPDIFF()`, `TO_DAYS()`, and `TO_SECONDS()` unsupported-literal diagnostics, non-ASCII string predicate, string search, string case, or `ELT()`/`FIELD()`/`GREATEST()`/`LEAST()` literals outside the current enum/set label-matching subset, relaxed temporal strings outside the documented storage, default, and predicate slices, or broader SQL mode behavior |
| Temporal literals | 🟡 | Canonical quoted `YYYY-MM-DD`, `[-]HH:MM:SS` / `[-]HHH:MM:SS`, and `YYYY-MM-DD HH:MM:SS` strings are admitted only where the limited `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` descriptor subsets explicitly allow them plus limited `DATE`, `DATETIME`, and `TIMESTAMP` storage, default, and predicate strings with a `T` date/time separator, a numeric `+HH:MM` / `-HH:MM` offset, or a single trailing `Z` / `z` suffix with truncation warning, the limited no-source/`DUAL`/`DO` DATE interval-unit `DATE_ADD()` / `DATE_SUB()` / `ADDDATE()` / `SUBDATE()` scalar slice plus interval-second `TIMESTAMPADD()` scalar slice, limited no-source/`DUAL`/`DO` `ADDTIME()` / `SUBTIME()` and `TIMESTAMP()` datetime/time scalar operands, limited `DATE_FORMAT()`, `TIME_FORMAT()`, `DATEDIFF()`, `TIMEDIFF()`, `TIMESTAMP()`, `TIMESTAMPDIFF()`, `UNIX_TIMESTAMP()`, `TIME_TO_SEC()`, `TO_DAYS()`, and `TO_SECONDS()` value arguments, and limited temporal extractor arguments including `MICROSECOND()` and `EXTRACT(MICROSECOND FROM ...)` fractional time/datetime strings plus date-only strings with warning 1292; parser-admitted standard `DATE '...'`, `TIME '...'`, and `TIMESTAMP '...'` literal introducers preserve the quoted literal token and are accepted in descriptor-backed DML value positions before the target descriptor applies the same storage conversion as ordinary quoted temporal strings; `DATEDIFF()`, `TIMESTAMPDIFF()`, `TO_DAYS()`, and `TO_SECONDS()` additionally admit verified complete date strings before year 1000 in their string-argument subsets; `DATE` / `DATETIME` / `TIMESTAMP` descriptor positions apply the current limited SQL-mode zero-temporal rules; no runtime distinction for parser-admitted standard temporal literal introducers outside that DML value admission, relaxed temporal strings outside the documented storage, default, and predicate slices, broader trailing-garbage temporal truncation, pre-1000 non-full-zero temporal dates outside the verified `DATEDIFF()`, `TIMESTAMPDIFF()`, `TO_DAYS()`, and `TO_SECONDS()` string-argument subsets, general intervals, fractional seconds outside the limited microsecond extractor string-argument subsets, temporal offsets outside the documented temporal storage, default, and predicate slices, or temporal expression coercion |
| JSON path literals | 🟡 | Limited simple path string literals for `JSON_EXTRACT()`, `JSON_VALUE()`, `JSON_LENGTH()`, `JSON_SET()`, `JSON_INSERT()`, `JSON_REPLACE()`, `JSON_REMOVE()`, and `->` / `->>`: `$`, ASCII identifier members, quoted ASCII members, and nonnegative array indexes, with no-leading-zero indexes still required by `JSON_EXTRACT()`, `JSON_VALUE()`, `JSON_LENGTH()`, and path operators. `JSON_REMOVE()` rejects root `$` for mutation. No wildcards, recursive descent, array ranges, `last`, filters, non-ASCII path names, or general path expression support |

## Variables and conversion

| Feature | Status | Notes |
| --- | --- | --- |
| User variables | 🟡 | Limited handle-local `@name` variables with case-insensitive 1..64-character names, nonpersistent session retention, `SET @name = value` / `SET @name := value`, limited deprecated scalar assignment expressions `@name := expression` with warning `1287`, top-level supported `SELECT ... INTO @var[, ...]`, scalar `SELECT`/`DO` reads, uninitialized `NULL`, SQL-level prepared statement source/`USING` values, and text/`NULL` storage for admitted integer, fixed-decimal, floating, string, hex, bit, charset-introduced, boolean, system-variable, user-variable, and selected-row assignment values. Existing limited scalar-expression functions may consume user variables when their current operand conversion accepts the stored text/`NULL` value. No stored-program local variables, table-backed predicates/order/DML values outside prepared execution expansion, metadata tables, binary values with embedded `NUL`, table-row assignment-expression execution, direct parameters, `SET` subqueries, `OUTFILE`/`DUMPFILE`, charset conversion for introduced literals, or durable storage |
| Local variables | ❌ | Stored-program variable typing, scope, and diagnostics |
| Type conversion | 🟡 | Limited strict conversion for supported integer/decimal/approximate/canonical `YEAR`/canonical temporal/binary string/limited `ENUM`/limited `SET`/`NULL`/`TRUE`/`FALSE` defaults plus inserted, replaced, and updated values, including exact decimal canonicalization, scale padding, half-away-from-zero rounding, truncation notes, range diagnostics for decimal descriptors, finite range checks and `FLOAT` single-precision rounding for approximate descriptors, canonical four-digit text storage for `YEAR` descriptors, canonical `YYYY-MM-DD` validation for `DATE` descriptors, canonical `[-]HH:MM:SS` / `[-]HHH:MM:SS` validation for `TIME` descriptors, canonical `YYYY-MM-DD HH:MM:SS` validation for `DATETIME` and `TIMESTAMP` descriptors, limited session SQL-mode zero-temporal behavior for full-zero, partial-zero, and `ALLOW_INVALID_DATES` `DATE` / `DATETIME` plus full-zero `TIMESTAMP`, binary string byte decoding, hex decoding, fixed-length padding, and byte-length checks, `ENUM` label/ordinal conversion to canonical definition-label text, and `SET` member-list/bitmap conversion to canonical definition-order display text; strict UTF-8 non-`NUL` string-literal conversion for admitted `CHAR`, `VARCHAR`, baseline `TEXT` family, binary string, `BIT`, `ENUM`, and `SET` row values plus limited generated character/text defaults; ASCII-only descriptor-driven string-literal conversion for admitted string, `ENUM`, and `SET` equality predicates; default-mode `CHAR` trailing-space canonicalization; descriptor resolution for limited DML `DEFAULT` keyword values in supported `INSERT`/`REPLACE` `VALUES` and `SET` paths plus supported `UPDATE` assignments; limited `INSERT IGNORE ... VALUES` / `SET` adjustment for omitted no-explicit-default columns, explicit `DEFAULT` for no-default columns, explicit `NULL` into numeric/string/`YEAR`/temporal/binary string `NOT NULL`, out-of-range descriptor integer, decimal, `YEAR`, and `TIME` inputs, invalid `YEAR` and temporal inputs, overlength string values including text-family byte truncation, and overlength binary string values, descriptor-driven existing-row validation for supported integer `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]`, descriptor-driven integer/boolean/`YEAR`/date/time/datetime/timestamp predicate conversion for `SELECT`/`DELETE`/`UPDATE` comparisons, literal-left descriptor comparisons, `BETWEEN` bounds, and non-`NULL` `IN` list values including limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning `DATE`/`DATETIME`/`TIMESTAMP` predicate strings, signed-64 scalar-literal conversion for limited scalar truth/comparison/`IS` predicates, descriptor-preserving `NULL` comparison and `IN` list values, descriptor-driven ASCII `CHAR`/`VARCHAR`/baseline `TEXT` family plus limited `ENUM` and `SET` predicate conversion for `=`, `<=>`, `<>`, and `!=`, descriptor-driven ASCII `CHAR`/`VARCHAR`/baseline `TEXT` family predicate conversion for `<`, `<=`, `>`, `>=`, `BETWEEN`, and `IN`, and unsigned signed-64 range conversion for supported `SELECT` `LIMIT`/`OFFSET` plus `DELETE`/`UPDATE LIMIT` literals only |
| Aggregate numeric result envelope | 🟡 | Limited descriptor-backed integer `SUM(column)` results use MyLite's signed-64 text result envelope; MySQL's exact decimal widening for integer sums beyond signed 64 bits is not yet implemented |
| Collation coercibility | 🟡 | Limited `COERCIBILITY()` metadata introspection for admitted scalar arguments and descriptor-backed columns; no coercibility-driven comparison, ordering, grouping, distinct, set-operation, conversion, or full expression collation resolution |

[Back to compatibility overview](../../COMPATIBILITY.md)
