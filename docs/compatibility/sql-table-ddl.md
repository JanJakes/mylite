# SQL table DDL

Table lifecycle, table-specific DDL, CREATE TABLE options, and ALTER TABLE
actions.

## Statement surface

Currently supported table DDL, index DDL, `TRUNCATE`, and rename forms that
target the synthetic `information_schema` schema are rejected before catalog
lookup with MySQL-compatible `1044 / 42000` access denied diagnostics. This is
an access-diagnostic baseline only; MyLite does not implement writable system
views or a privilege engine.

| Feature | Status | Notes |
| --- | --- | --- |
| `ALTER TABLE` | 🟡 | Limited single-action persistent base-table `ALTER TABLE ... RENAME [TO\|AS]`, single-column `ALTER TABLE ... ADD [COLUMN] ... [FIRST\|AFTER column_name]`, `ALTER TABLE ... ADD PRIMARY KEY (column[, ...])`, `ALTER TABLE ... ADD UNIQUE [INDEX\|KEY] [name] (column[, ...])`, `ALTER TABLE ... ADD CONSTRAINT [symbol] UNIQUE [INDEX\|KEY] [name] (column[, ...])`, `ALTER TABLE ... ADD INDEX\|KEY [name] (key_part[, ...])`, `ALTER TABLE ... ADD FULLTEXT [KEY\|INDEX] [name] (column[, ...])`, named or unnamed integer-family `ALTER TABLE ... ADD [CONSTRAINT name] FOREIGN KEY [index_name] (child_column[, ...]) REFERENCES parent(parent_column[, ...]) [ON DELETE action] [ON UPDATE action]` with `CASCADE`, `RESTRICT`, `NO ACTION`, or `SET NULL`, `ALTER TABLE ... DROP FOREIGN KEY name`, `ALTER TABLE ... DROP PRIMARY KEY`, `ALTER TABLE ... DROP INDEX\|KEY index_name`, `ALTER TABLE ... RENAME INDEX\|KEY old_name TO new_name`, `ALTER TABLE ... ALTER INDEX index_name VISIBLE\|INVISIBLE`, `ALTER TABLE ... AUTO_INCREMENT [=] N`, single-column `ALTER TABLE ... DROP [COLUMN]`, single-column `ALTER TABLE ... RENAME COLUMN ... TO ...`, single-column `ALTER TABLE ... MODIFY [COLUMN] ...`, single-column `ALTER TABLE ... CHANGE [COLUMN] old_col new_col ...`, single-column `ALTER TABLE ... ALTER [COLUMN] ... SET DEFAULT ...` / `DROP DEFAULT`, single-column `ALTER TABLE ... ALTER [COLUMN] ... SET VISIBLE\|INVISIBLE`, descriptor metadata `ALTER TABLE ... [DEFAULT] CHARSET` / `CHARACTER SET` with admitted `utf8mb4` or `ascii` plus matching `COLLATE` values, physical row-order rebuild `ALTER TABLE ... ORDER BY ...`, and physical table rebuild `ALTER TABLE ... FORCE` forms; limited comma-separated `ALGORITHM` / `LOCK` option tails are admitted only on selected supported single-action ALTER forms; see [ALTER TABLE actions](#alter-table-actions) |
| `CREATE TABLE` | 🟡 | Limited persistent base-table creation: optional `IF NOT EXISTS`, explicit integer-family columns (`TINYINT`, `SMALLINT`, `MEDIUMINT`, `INT`/`INTEGER`, `BIGINT`), the `INT1`/`INT2`/`INT3`/`INT4`/`INT8` plus `BOOL`/`BOOLEAN` and `SERIAL` aliases, limited exact `DECIMAL`/`DEC`/`NUMERIC`/`FIXED` columns, limited approximate `FLOAT`/`FLOAT4`/`FLOAT8`/`DOUBLE`/`DOUBLE PRECISION`/`REAL` columns, canonical `YEAR`, `DATE`, `TIME`, `DATETIME`, and `TIMESTAMP` columns, limited bare `CHAR`, `CHAR(0..255)`, `CHARACTER`, `CHARACTER(0..255)`, national `CHAR` aliases, `VARCHAR(0..16383)`, `CHARACTER VARYING(0..16383)`, `CHAR VARYING(0..16383)`, and national `VARCHAR` aliases, bare `TINYTEXT`/`TEXT`/`MEDIUMTEXT`/`LONGTEXT` columns plus `LONG`/`LONG VARCHAR` aliases normalized to `MEDIUMTEXT`, optional explicit column `CHARACTER SET`/`CHARSET` metadata for admitted `utf8mb4` or `ascii` and matching `COLLATE` values plus explicit `CHARACTER SET`/`CHARSET binary` or `COLLATE binary` normalization on `CHAR`, `VARCHAR`, and bare `TEXT` family columns, limited `BINARY`, `BINARY(0..255)`, `VARBINARY(0..65535)`, `CHAR BYTE`, `CHAR(n) BYTE`, bare BLOB family columns plus `BLOB(M)` normalization and `LONG VARBINARY` alias normalized to `MEDIUMBLOB`, limited `BIT` / `BIT(1..64)` columns, limited `ENUM('label'[, ...])` columns, limited `SET('member'[, ...])` columns, and limited `JSON` columns, optional deprecated display width `0..255` before optional single `SIGNED` or `UNSIGNED` for integer-family and `INT*` aliases, optional deprecated `UNSIGNED` for decimal and approximate descriptors, `NULL`/`NOT NULL`, optional `DEFAULT NULL`, optional descriptor-owned integer, decimal literal, approximate literal, `TRUE`/`FALSE`, limited parenthesized constant integer/`NULL` expression, literal `CHAR`/`VARCHAR` string, matching `ENUM`/`SET` string defaults, canonical year-string, canonical date-string, canonical time-string, canonical datetime-string, canonical timestamp-string, parenthesized current-date/current-time, or current-timestamp defaults, or compatible `BIT` defaults within the current physical ranges, optional single-column inline integer-family or ASCII `CHAR(1..255)` / `VARCHAR(1..255)` `PRIMARY KEY`, optional table-level primary keys over integer-family and ASCII `CHAR(1..255)` / `VARCHAR(1..255)` key parts with optional `ASC` / `DESC` direction metadata, including composite string and mixed `PRIMARY KEY (column[, ...])` forms, optional `AUTO_INCREMENT` on one integer-family column backed by the current single-column primary, unique, or nonunique secondary-key subset, optional inline single-column or table-level full-column `UNIQUE`/`UNIQUE KEY`/`UNIQUE INDEX` definitions, including named table-level `CONSTRAINT name UNIQUE [KEY|INDEX]` forms, on integer-family, exact `DECIMAL`, canonical `YEAR`, canonical `DATE`, canonical `TIME`, canonical `DATETIME`, canonical `TIMESTAMP`, and ASCII `CHAR(1..255)` / `VARCHAR(1..255)` descriptors plus table-level unique prefix keys including composite prefix key parts on `CHAR`, `VARCHAR`, and bare `TEXT` family descriptors with optional `ASC` / `DESC` direction metadata, optional table-level nonunique `KEY`/`INDEX` definitions with ordered unqualified full key parts on supported descriptors and prefix key parts on `CHAR`, `VARCHAR`, and bare `TEXT` family descriptors with optional `ASC` / `DESC` direction metadata, optional table-level metadata-only `FULLTEXT [KEY|INDEX] [name]` definitions over unqualified `CHAR(1..255)`, `VARCHAR(1..16383)`, and bare `TEXT` family descriptor columns with positive prefixes parsed and ignored, optional table-level one-column or composite integer-family `FOREIGN KEY [index_name]` constraints referencing same-schema compatible primary or unique parent keys with optional `ON DELETE` / `ON UPDATE` `CASCADE`, `RESTRICT`, `NO ACTION`, or `SET NULL`, optional explicit `ENGINE [=] InnoDB`, optional `AUTO_INCREMENT=N` initial counter option, optional admitted `utf8mb4` and `ascii` table charset/collation options plus limited `binary` table charset/collation inheritance for non-national `CHAR`, `VARCHAR`, and bare `TEXT` family descriptors, limited empty descriptor cloning with `CREATE TABLE ... LIKE`, and limited descriptor-inferred row copying with `CREATE TABLE ... SELECT`; `SERIAL` expands to `BIGINT UNSIGNED AUTO_INCREMENT` plus a generated unique key with implicit `NOT NULL` unless explicit `NULL`; display width plus decimal and approximate `UNSIGNED` emit warning 1681, decimal and temporal values store as exact canonical text, approximate values store as physical `REAL`, `YEAR` values store as canonical four-digit text, string aliases normalize to `char(n)` or `varchar(n)` descriptors; national aliases preserve `NCHAR(n)` or `NVARCHAR(n)` descriptors and emit warning 3720 on explicit declarations, binary string and `BIT` descriptors store as physical `BLOB`, `ENUM`, `SET`, and `JSON` descriptors store canonical text, wide `VARCHAR` and `VARBINARY` definitions are checked against MyLite's descriptor-owned MySQL row-size envelope, and only signed `TINYINT(1)`/`INT1(1)` plus `BOOL`/`BOOLEAN` persist as `tinyint(1)` metadata; existing-table `IF NOT EXISTS` is a no-op with `Note 1050`; no other options, `SERIAL DEFAULT VALUE`, approximate indexes/keys, binary string indexes, `ENUM`/`SET` indexes, direct JSON indexes, non-ASCII string primary/unique key values, spatial indexes, full-text search behavior, primary prefix parts, non-integer foreign keys, recursive foreign-key actions, `SET DEFAULT` FK actions, general named constraints outside the current named UNIQUE, FK, and CHECK subsets, `CREATE TABLE` auto-increment defaults, binary string defaults, explicit `TEXT`-family string defaults, string/hex defaults for `BIT`, bare JSON defaults, JSON expression defaults, numeric or expression defaults for `ENUM`/`SET`, empty-string members for `SET`, warning-producing overlength string-default truncation, general expression defaults beyond the admitted parenthesized integer and current date/time/timestamp subsets, `YEAR` expression defaults, `YEAR UNSIGNED` / `YEAR SIGNED` / `YEAR ZEROFILL`, `FLOAT(M,D)`/`DOUBLE(M,D)`, `ZEROFILL`, `TEXT(M)`, `LONG VARCHAR(length)`, `LONG VARBINARY(length)`, `LONG TEXT`, `LONG BINARY`, `TINYBLOB(M)`, `MEDIUMBLOB(M)`, `LONGBLOB(M)`, `CHARACTER BYTE`, other non-admitted column charsets, column charset/collation attributes on other type families, `PAD_CHAR_TO_FULL_LENGTH`, `VISIBLE`/`INVISIBLE` column definitions, temporary tables, or generated invisible primary keys |
| `CREATE TEMPORARY TABLE` | 🟡 | Limited session-scoped explicit `CREATE TEMPORARY TABLE [IF NOT EXISTS] table_name (...)` using the current table-definition subset for columns, defaults, primary/unique/nonunique keys, prefix keys, charset/collation, `ENGINE=InnoDB`, and session-local `AUTO_INCREMENT`, except `FULLTEXT`, plus limited `CREATE TEMPORARY TABLE [IF NOT EXISTS] table_name LIKE source` and parenthesized `(LIKE source)` cloning from visible persistent or temporary source tables including reset auto-increment counters and excluding source fulltext, foreign-key, or check-constraint runtime support, and limited `CREATE TEMPORARY TABLE [IF NOT EXISTS] table_name [AS] SELECT ... FROM source` row copying through the current descriptor-backed CTAS source subset; unqualified creation requires a selected schema, qualified creation requires an existing schema, same-schema temporary tables shadow persistent tables for supported descriptor-driven `SELECT`/`INSERT`/`UPDATE`/`DELETE` and `SHOW COLUMNS`/`SHOW INDEX`/`SHOW CREATE TABLE`, and descriptors are cleaned up on close. No temporary explicit-column CTAS, temporary ALTER/RENAME/TRUNCATE, standalone index DDL on temporary tables, full-text indexes, generated columns, foreign keys, triggers, privileges, or temporary DDL inside an active user transaction |
| `CREATE TABLE ... LIKE` | 🟡 | Limited empty persistent base-table descriptor clone for MyLite-owned integer/null, exact `DECIMAL`, approximate `FLOAT`/`DOUBLE`, canonical `YEAR`, canonical `DATE`, canonical `TIME`, canonical `DATETIME`, canonical `TIMESTAMP`, `CHAR(0..255)`, national `CHAR` aliases, `VARCHAR(0..16383)`, national `VARCHAR` aliases, bare `TEXT` family, admitted binary string, `BIT`, `ENUM`, `SET`, and `JSON` descriptors, including nullability, defaults, visibility, column order, current supported primary-key descriptors, supported unique and nonunique secondary indexes, metadata-only full-text indexes, and the current indexed auto-increment attribute with the target counter reset to `1`; supports optional `IF NOT EXISTS`, parenthesized `(LIKE source)` form, and visible temporary-table sources; no row copying, views, generated columns, unsupported index attributes, constraints beyond the current primary key and supported secondary indexes, table options beyond the reset counter, or full MySQL metadata cloning |
| `CREATE TABLE ... SELECT` | 🟡 | Limited descriptor-backed `CREATE TABLE [IF NOT EXISTS] target [AS] SELECT ... FROM source` for persistent base tables plus `CREATE TEMPORARY TABLE [IF NOT EXISTS] target [AS] SELECT ... FROM source` for session-local temporary targets, reusing the current single-table source `SELECT` subset with optional table alias, `WHERE`, one-column `ORDER BY`, and `LIMIT`; target descriptors are inferred from selected source descriptor columns, including canonical `YEAR`, temporal, admitted binary string, `BIT`, and `JSON` descriptors, preserve type/nullability/default metadata, make explicitly selected invisible source columns visible in the destination, do not copy primary keys or other indexes, copy rows atomically, and report inserted-row affected counts; no explicit destination column definitions, expression/literal projection, joins, indexes, constraints, table options, or full CTAS inference |
| `DROP TABLE` | 🟡 | Limited base-table drop with comma-separated target lists and optional `IF EXISTS`; a session temporary table with the effective name is dropped before the persistent fallback, missing-table and missing-explicit-schema `IF EXISTS` forms are no-ops with `Note 1051`, duplicate targets fail before mutation, and non-`IF EXISTS` missing targets fail atomically; no `RESTRICT` or `CASCADE`, and dropping temporary tables inside an active user transaction is rejected |
| `DROP TEMPORARY TABLE` | 🟡 | Limited `DROP TEMPORARY TABLE [IF EXISTS] table_name[, ...]` for session temporary descriptors only; persistent tables are ignored, missing temporary targets produce MySQL-compatible unknown-table errors or `Note 1051` for `IF EXISTS`, and successful drops report zero affected rows. No persistent fallback, `RESTRICT`, `CASCADE`, or temporary DDL inside an active user transaction |
| `RENAME TABLE` | 🟡 | Limited persistent base-table rename with one or more left-to-right pairs from `baseline-table-rename-lifecycle` and `baseline-multi-table-rename-lifecycle`, including unqualified, schema-qualified, and cross-schema names plus statement rollback on pair failure; no temporary tables, views, triggers, metadata locks, foreign keys, or privilege semantics |
| `TRUNCATE TABLE` | 🟡 | Limited persistent base-table `TRUNCATE [TABLE] table_name` for unqualified and schema-qualified descriptor targets; empties physical rows, preserves descriptors, resets the current auto-increment counter subset to `1`, reports zero affected rows, rejects tables participating in supported foreign-key descriptors, and follows the limited user-transaction implicit commit subset; no temporary tables, partitions, cascades, triggers, locks, privileges, or physical storage rebuild semantics |
| Atomic DDL | 🟡 | Catalog descriptor rows and generated physical SQLite table/index changes commit or roll back atomically for the limited create/create-like/drop/rename/truncate and single-action alter rename/add-column/add-primary-key/add-index/add-check/drop-primary-key/drop-index/drop-check/alter-check/drop-column/rename-column/modify-column/change-column/order-by/force subsets only |
| Implicit commit boundaries | 🟡 | Limited MySQL-style implicit commit before current supported DDL/object lifecycle statements when a MyLite user transaction is active; no complete coverage for future DDL, administrative statements, locking statements, or full protocol state |
| `IMPORT TABLE` | ❌ | Transportable tablespace import syntax, diagnostics |

## CREATE TABLE and table options

| Feature | Status | Notes |
| --- | --- | --- |
| Column definition grammar | 🟡 | Limited integer-family type, `INT1`/`INT2`/`INT3`/`INT4`/`INT8` aliases, bare `BOOL`/`BOOLEAN` aliases, `SERIAL`, exact `DECIMAL`/`DEC`/`NUMERIC`/`FIXED`, approximate `FLOAT`/`FLOAT(0..53)`/`FLOAT4`/`FLOAT8`/`DOUBLE`/`DOUBLE PRECISION`/`REAL`, canonical `YEAR`, canonical `DATE`, canonical `TIME`, canonical `DATETIME`, canonical `TIMESTAMP`, limited bare `CHAR`, `CHAR(0..255)`, `CHARACTER`, `CHARACTER(0..255)`, national `CHAR` aliases, `VARCHAR(0..16383)`, `CHARACTER VARYING(0..16383)`, `CHAR VARYING(0..16383)`, and national `VARCHAR` aliases, bare `TINYTEXT`/`TEXT`/`MEDIUMTEXT`/`LONGTEXT` plus `LONG`/`LONG VARCHAR` aliases normalized to `MEDIUMTEXT`, optional explicit column `CHARACTER SET`/`CHARSET` metadata for admitted `utf8mb4` or `ascii` and matching `COLLATE` values plus explicit `CHARACTER SET`/`CHARSET binary` or `COLLATE binary` normalization on `CHAR`, `VARCHAR`, and bare `TEXT` family definitions, limited `BINARY`, `BINARY(0..255)`, `VARBINARY(0..65535)`, `CHAR BYTE`, `CHAR(n) BYTE`, and bare BLOB family types plus `BLOB(M)` normalization and `LONG VARBINARY` alias normalized to `MEDIUMBLOB`, limited `BIT` / `BIT(1..64)`, limited `ENUM('label'[, ...])`, limited `SET('member'[, ...])`, and limited `JSON`, optional unsigned decimal display width before optional single `SIGNED` or `UNSIGNED` for integer-family and `INT*` aliases, optional deprecated `UNSIGNED` for decimal and approximate descriptors, nullability, optional trailing `DEFAULT NULL`, compatible integer/decimal/approximate/`TRUE`/`FALSE` default literal for numeric columns, compatible integer/boolean/bit-literal defaults for `BIT`, matching string defaults for `ENUM`/`SET`, limited parenthesized constant integer/`NULL` expression defaults for integer-family columns, string default literal for `CHAR`/`VARCHAR`, canonical year-string default literal for `YEAR` columns, canonical date-string default literal or parenthesized current-date default for `DATE` columns, canonical time-string default literal or parenthesized current-time default for `TIME` columns, canonical datetime-string default literal or zero-fractional current-timestamp default for `DATETIME` columns, or canonical timestamp-string default literal or zero-fractional current-timestamp default for `TIMESTAMP` columns, optional `AUTO_INCREMENT` for one integer-family column backed by the current single-column primary, unique, or nonunique secondary-key subset, optional trailing `PRIMARY KEY` for one integer-family or ASCII `CHAR(1..255)` / `VARCHAR(1..255)` column, and optional trailing `UNIQUE` / `UNIQUE KEY` for supported unique-index target columns including canonical `YEAR` and ASCII `CHAR(1..255)` / `VARCHAR(1..255)`; no `SERIAL DEFAULT VALUE`, binary string defaults, explicit `TEXT`-family string defaults, string/hex defaults for `BIT`, bare JSON defaults, JSON expression defaults, numeric or expression defaults for `ENUM`/`SET`, empty-string members for `SET`, warning-producing overlength string-default truncation, general expression defaults beyond the admitted parenthesized integer and current date/time/timestamp subsets, `YEAR` expression defaults or numeric attributes, fractional temporal precision, `FLOAT(M,D)`/`DOUBLE(M,D)`, `ZEROFILL`, approximate, binary string, or `ENUM`/`SET` keys/indexes or direct JSON indexes, non-ASCII string primary/unique key values, `TEXT(M)`, `LONG VARCHAR(length)`, `LONG VARBINARY(length)`, `LONG TEXT`, `LONG BINARY`, `TINYBLOB(M)`, `MEDIUMBLOB(M)`, `LONGBLOB(M)`, `CHARACTER BYTE`, other non-admitted column charsets, column charset/collation attributes on other type families, auto-increment defaults, inline column references, named constraints, or column-definition composite keys |
| `ENUM` column type | 🟡 | Limited `ENUM('label'[, ...])` definitions in `CREATE TABLE`, `CREATE TABLE ... LIKE`, and `ALTER TABLE ... ADD COLUMN`, with string-literal labels, trailing-space normalization, duplicate detection under MyLite's current ASCII case-insensitive default-collation subset, optional `NULL` / `NOT NULL`, `DEFAULT NULL`, and matching string-label defaults. Existing rows backfill nullable enum columns as `NULL`, `NOT NULL` no-explicit-default enum columns as the first label, and explicit defaults as the canonical definition label. No enum keys/indexes, enum foreign keys, expression or numeric enum defaults, enum `ALTER MODIFY`/`CHANGE`, enum column charset/collation attributes, invalid index-0 storage, full non-ASCII collation semantics, or 65,535-label envelope |
| `SET` column type | 🟡 | Limited `SET('member'[, ...])` definitions in `CREATE TABLE`, `CREATE TABLE ... LIKE`, and `ALTER TABLE ... ADD COLUMN`, plus catalog-only `ALTER COLUMN SET DEFAULT` and `ALTER COLUMN DROP DEFAULT`, with nonempty string-literal members, trailing-space normalization, comma-containing member rejection, duplicate detection under MyLite's current ASCII case-insensitive default-collation subset, optional `NULL` / `NOT NULL`, `DEFAULT NULL`, and matching string-list defaults. Existing rows backfill nullable set columns as `NULL`, `NOT NULL` no-explicit-default set columns as the empty string, and explicit defaults as the canonical definition-order display string. No empty-string set members, set keys/indexes, set foreign keys, expression or numeric set defaults, set `ALTER MODIFY`/`CHANGE`, set column charset/collation attributes, compact bitmap storage, full non-ASCII collation semantics, or more than 64 members |
| `JSON` column type | 🟡 | Limited `JSON` definitions in `CREATE TABLE`, `CREATE TABLE ... LIKE`, `CREATE TABLE ... SELECT`, and `ALTER TABLE ... ADD COLUMN`, with optional `NULL` / `NOT NULL` and nullable `DEFAULT NULL`; values are stored as canonical text after MyLite-owned validation of the verified JSON subset. Existing rows backfill nullable JSON columns as SQL `NULL` and `JSON NOT NULL` no-explicit-default columns as the JSON value `null` while metadata remains no-explicit-default. Direct JSON indexes fail with MySQL-compatible diagnostics. No JSON expression defaults, bare literal defaults, generated/path indexes, direct JSON indexes, JSON functions, binary JSON storage, `ALTER MODIFY`/`CHANGE`, charset/collation attributes, comparison/order/grouping semantics, or general expression conversion |
| Silent column specification changes | ❌ | Automatic column rewrites |
| Default expressions | 🟡 | Limited descriptor-owned defaults in supported integer-family, decimal, approximate, `CHAR`, `VARCHAR`, limited `ENUM`, limited `SET`, `YEAR`, `DATE`, `TIME`, `DATETIME`, and `TIMESTAMP` column definitions plus `ALTER TABLE ... ALTER [COLUMN] column_name SET DEFAULT ...` / `DROP DEFAULT`, and compatible integer/boolean/bit-literal defaults for `BIT` in `CREATE TABLE` and `ADD COLUMN`: `DEFAULT NULL` for nullable columns, decimal integer literals with optional unary sign and `TRUE`/`FALSE`, plus parenthesized constant integer/`NULL` expressions with unary/binary arithmetic, `%`, `MOD`, and `DIV`, within the current MyLite integer physical range, fixed decimal literals for compatible decimal or approximate descriptors, approximate numeric literals for compatible approximate descriptors, string literals for compatible `CHAR`/`VARCHAR`, matching `ENUM` label descriptors, and matching `SET` member-list descriptors, canonical year-string literals for `YEAR` descriptors, canonical date-string literals for `DATE` descriptors, canonical time-string literals for `TIME` descriptors, canonical datetime-string literals for `DATETIME` descriptors, canonical timestamp-string literals for `TIMESTAMP` descriptors, parenthesized zero-fractional `CURDATE()` / `CURRENT_DATE` synonyms for `DATE`, parenthesized zero-fractional `CURTIME()` / `CURRENT_TIME` synonyms for `TIME`, and zero-fractional `CURRENT_TIMESTAMP` / `NOW()` synonyms for `DATETIME` and `TIMESTAMP` descriptors admitted by the current limited SQL-mode temporal rules, with dropped-default metadata preserved distinctly from implicit nullable defaults; supported by `CREATE TABLE`, `ADD COLUMN`, `ALTER COLUMN SET DEFAULT`, omitted-column `INSERT`, limited DML `DEFAULT` keyword values for current `INSERT`/`REPLACE` `VALUES` and `SET` paths plus supported `UPDATE` assignments, `SHOW COLUMNS`, `SHOW CREATE TABLE`, and limited `INFORMATION_SCHEMA.COLUMNS`; `ALTER ... SET DEFAULT` on an `AUTO_INCREMENT` column follows MySQL's hidden-default behavior for omitted/default DML values without rendering a visible metadata default; no `TEXT`-family string defaults, numeric or expression defaults for `ENUM`/`SET`, string/hex defaults for `BIT`, warning-producing overlength string-default truncation, `ALTER COLUMN SET DEFAULT` for `BIT`, general expression defaults beyond the admitted integer and current date/time/timestamp subsets, `YEAR` expression defaults, relaxed temporal defaults, fractional current date/time/timestamp defaults, or `DEFAULT(col_name)` |
| Generated columns | ❌ | Generated column metadata |
| Invisible columns | 🟡 | Limited descriptor visibility for existing integer-family columns through `ALTER TABLE ... ALTER [COLUMN] column_name SET VISIBLE\|INVISIBLE`; invisible columns are omitted from `SELECT *` and no-column-list `INSERT ... VALUES`, remain explicitly addressable by supported `SELECT`, `INSERT`, `INSERT ... SET`, `UPDATE`, and `DELETE` forms, `DROP COLUMN` preserves at least one visible column, supported `MODIFY`/`CHANGE` definitions without visibility attributes make the target visible, and visibility renders in `SHOW COLUMNS`, `SHOW CREATE TABLE`, and limited `INFORMATION_SCHEMA.COLUMNS.EXTRA` metadata; no `CREATE`/`ADD`/`MODIFY`/`CHANGE` invisible column definitions, generated hidden columns, protocol flags, views, or index visibility through column syntax |
| Generated invisible primary keys | ❌ | Invisible primary key metadata and hidden `my_row_id` table creation; limited scalar `@@sql_generate_invisible_primary_key` reads expose the fixed disabled baseline only |
| Primary key requirement enforcement | ❌ | `@@sql_require_primary_key` DDL effects are not implemented; limited scalar reads expose the fixed disabled baseline only |
| AUTO_INCREMENT columns | 🟡 | Limited `CREATE TABLE` and `CREATE TEMPORARY TABLE` support for one integer-family auto-increment column when it is backed by the current single-column primary, unique, or nonunique secondary-key subset, including `SERIAL` expansion to `BIGINT UNSIGNED AUTO_INCREMENT` plus a generated unique key; descriptor-owned durable or session-local counters, `CREATE TABLE ... AUTO_INCREMENT=N`, `CREATE TEMPORARY TABLE ... AUTO_INCREMENT=N`, limited persistent `ALTER TABLE ... AUTO_INCREMENT [=] N` counter changes, MySQL-compatible hidden defaults from existing `ALTER ... SET DEFAULT` syntax, preservation after persistent `ALTER TABLE ... DROP PRIMARY KEY` only when a supported secondary index keeps the column indexed, `CREATE TABLE ... LIKE` attribute cloning and counter reset, `TRUNCATE` counter reset for persistent tables, limited `NO_AUTO_VALUE_ON_ZERO` storage of explicit zero in current `INSERT` paths, `SHOW COLUMNS`, `SHOW CREATE TABLE`, `SHOW TABLE STATUS` for persistent tables, and limited persistent `INFORMATION_SCHEMA.TABLES.AUTO_INCREMENT` / `INFORMATION_SCHEMA.COLUMNS.EXTRA` metadata; temporary counters are connection-local and omitted from durable metadata listings. No `CREATE TABLE` auto-increment defaults, `SERIAL DEFAULT VALUE`, `ALTER TABLE` add/drop/modify of the column attribute, temporary `ALTER TABLE`, generated invisible primary keys, mixed-mode allocation gaps, exact InnoDB-style `NO_AUTO_VALUE_ON_ZERO` counter reservation gaps, or replication lock modes |
| Table options: engine | 🟡 | Optional explicit `ENGINE [=] InnoDB` only; no alternate engines, engine substitution, or durable per-table engine metadata |
| Table options: charset/collation | 🟡 | Optional `CHARSET` / `CHARACTER SET` options for admitted `utf8mb4` and `ascii` table charsets plus admitted matching table `COLLATE` options for the limited persistent `CREATE TABLE` subset plus limited `CREATE TABLE` / `CREATE TEMPORARY TABLE` table-default `CHARSET` / `CHARACTER SET binary` and `COLLATE binary` inheritance that normalizes inherited non-national `CHAR`, `VARCHAR`, and bare `TEXT` family descriptors to existing binary string/BLOB descriptors while preserving explicit admitted column overrides and leaving `ENUM`/`SET` descriptors unchanged; mutable descriptor metadata remains limited to `ALTER TABLE ... [DEFAULT] CHARSET` / `CHARACTER SET` with admitted `utf8mb4` or `ascii` and admitted `ALTER TABLE ... [DEFAULT] COLLATE` values on persistent base tables, with matching static `SHOW CHARACTER SET` / limited `SHOW COLLATION` rows and limited `INFORMATION_SCHEMA.CHARACTER_SETS` / `COLLATIONS` / `COLLATION_CHARACTER_SET_APPLICABILITY` including `binary`; explicit column charset/collation attributes are limited to admitted `utf8mb4`/`ascii` metadata and `binary` normalization for string column definitions; limited `CHAR`, `VARCHAR`, and `TEXT` family storage validates UTF-8 but no `ALTER TABLE ... DEFAULT CHARSET=binary`, binary string defaults, binary string indexes, other non-admitted defaults, conversion, full collation comparison semantics, or full charset/collation catalogs |
| Table options: storage | ❌ | Storage option metadata |
| Table options: statistics | ❌ | Statistics option metadata |
| Table options: misc | ❌ | Misc table options |
| NDB comment options | ❌ | NDB comment syntax |
| Temporary table metadata | 🟡 | Limited session-local temporary table, column, index, and current auto-increment counter descriptors with negative internal IDs, generated SQLite temporary physical names, same-schema name shadowing for supported DML and selected SHOW statements, durable metadata omission from `SHOW TABLES`, `SHOW TABLE STATUS`, and `INFORMATION_SCHEMA`, and close-time cleanup; no durable catalog rows, cross-handle visibility, or full MySQL data-dictionary parity |

## ALTER TABLE actions

| Feature | Status | Notes |
| --- | --- | --- |
| `ADD COLUMN` | 🟡 | Limited `ALTER TABLE table_name ADD [COLUMN] column_name column_type [NULL\|NOT NULL] [DEFAULT NULL\|DEFAULT integer_literal\|DEFAULT decimal_literal\|DEFAULT approximate_literal\|DEFAULT TRUE\|DEFAULT FALSE\|DEFAULT canonical_year_string\|DEFAULT canonical_date_string\|DEFAULT canonical_time_string\|DEFAULT canonical_datetime_string\|DEFAULT canonical_timestamp_string\|DEFAULT CURRENT_TIMESTAMP\|DEFAULT parenthesized_current_date_or_time_expression\|DEFAULT (integer_expression)\|DEFAULT string_literal_for_CHAR_or_VARCHAR\|DEFAULT matching_string_literal_for_ENUM_or_SET\|DEFAULT bit_literal_for_BIT] [FIRST\|AFTER column_name]` for persistent base tables; supports `TINYINT`, `SMALLINT`, `MEDIUMINT`, `INT`/`INTEGER`, `BIGINT`, `INT1`/`INT2`/`INT3`/`INT4`/`INT8` aliases, bare `BOOL`/`BOOLEAN` aliases, exact `DECIMAL`/`DEC`/`NUMERIC`/`FIXED`, approximate `FLOAT`/`FLOAT4`/`FLOAT8`/`DOUBLE`/`DOUBLE PRECISION`/`REAL`, canonical `YEAR`, canonical `DATE`, canonical `TIME`, canonical `DATETIME`, canonical `TIMESTAMP`, limited bare `CHAR`, `CHAR(0..255)`, `CHARACTER`, `CHARACTER(0..255)`, national `CHAR` aliases, `VARCHAR(0..16383)`, `CHARACTER VARYING(0..16383)`, `CHAR VARYING(0..16383)`, and national `VARCHAR` aliases, bare `TEXT` family types plus `LONG`/`LONG VARCHAR` aliases normalized to `MEDIUMTEXT`, optional explicit column `CHARACTER SET`/`CHARSET` metadata for admitted `utf8mb4` or `ascii` and matching `COLLATE` values plus explicit `CHARACTER SET`/`CHARSET binary` or `COLLATE binary` normalization on `CHAR`, `VARCHAR`, and bare `TEXT` family additions, limited `BINARY`, `BINARY(0..255)`, `VARBINARY(0..65535)`, `CHAR BYTE`, `CHAR(n) BYTE`, and bare BLOB family types plus `BLOB(M)` normalization and `LONG VARBINARY` alias normalized to `MEDIUMBLOB`, limited `BIT` / `BIT(1..64)`, limited `ENUM`, and limited `SET`, with optional `ON UPDATE CURRENT_TIMESTAMP` for `DATETIME` and `TIMESTAMP`, optional deprecated display width `0..255` before optional single `SIGNED` or `UNSIGNED` for integer-family and `INT*` aliases, optional deprecated `UNSIGNED` for decimal and approximate descriptors, and optional `FIRST` / `AFTER column_name` logical descriptor positioning; existing rows backfill descriptor defaults including statement-time current date/time/timestamp defaults, nullable no-default columns backfill `NULL`, integer, decimal, and approximate not-null no-default columns backfill target zero values, `YEAR NOT NULL` no-default columns backfill `0000`, binary string and `BIT` not-null no-default columns backfill the MySQL-compatible implicit value, `DATE NOT NULL` and `DATETIME NOT NULL` no-default columns succeed only for empty tables and otherwise fail on the implicit zero temporal value under the current strict mode, `TIME NOT NULL` no-default columns backfill `00:00:00`, `TIMESTAMP NOT NULL` no-default columns backfill `0000-00-00 00:00:00` even for existing rows in the fixed-UTC baseline, and string, `ENUM`, and `SET` not-null no-default columns backfill their current MySQL-compatible implicit values while catalog metadata remains no-explicit-default; widened `VARCHAR` and `VARBINARY` add-column definitions are checked against MyLite's descriptor-owned MySQL row-size envelope; admits the limited comma-separated `ALGORITHM` / `LOCK` option tail; no multiple actions, parenthesized lists, binary string defaults, explicit `TEXT`-family string defaults, string/hex defaults for `BIT`, bare JSON defaults, JSON expression defaults, numeric or expression defaults for `ENUM`/`SET`, warning-producing overlength string-default truncation, general expression defaults beyond the admitted parenthesized integer and current date/time/timestamp subsets, `YEAR` expression defaults or numeric attributes, fractional current date/time/timestamp defaults, `FLOAT(M,D)`/`DOUBLE(M,D)`, other non-admitted column charsets, column charset/collation attributes on other type families, generated/invisible/auto-increment columns, added primary keys or other keys, constraints, broader online-DDL algorithm/lock semantics, temporary tables, or views |
| `ADD PRIMARY KEY` | 🟡 | Limited single-action `ALTER TABLE table_name ADD PRIMARY KEY (column_name [ASC\|DESC][, ...])` for persistent base tables and existing unqualified integer-family plus ASCII `CHAR(1..255)` / `VARCHAR(1..255)` descriptor columns, including composite string and mixed key tuples. Supported key parts may specify `ASC` or `DESC`; omitted direction is ascending. Existing rows must contain no `NULL` values in any key part, no duplicate key tuples, no unsupported string-key values, and no aggregate key-length overflow; success reports zero affected rows/warnings, makes every key-part descriptor column `NOT NULL`, normalizes implicit/explicit `NULL` defaults to no explicit default, preserves supported defaults and secondary indexes, creates one generated SQLite unique index, and drives existing DML, `SHOW`, `CREATE TABLE ... LIKE`, and limited `INFORMATION_SCHEMA` metadata from ordered descriptors. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail; no `ADD CONSTRAINT`, primary prefix parts, non-ASCII string key values, key options beyond that tail, multi-action `ALTER`, auto-increment conversion, broader online-DDL algorithm/lock semantics, temporary tables, or views |
| `ADD UNIQUE` | 🟡 | Limited single-action `ALTER TABLE table_name ADD UNIQUE [INDEX\|KEY] [index_name] (key_part[, ...])` and `ALTER TABLE table_name ADD CONSTRAINT [symbol] UNIQUE [INDEX\|KEY] [index_name] (key_part[, ...])` for persistent base tables and existing unqualified supported descriptor columns, including integer-family, exact `DECIMAL`, canonical `YEAR`, canonical `DATE`, canonical `TIME`, canonical `DATETIME`, canonical `TIMESTAMP`, ASCII `CHAR(1..255)` / `VARCHAR(1..255)` full columns, and `CHAR` / `VARCHAR` / bare `TEXT` family prefixes including composite prefix tuples. Named constraint forms lower to the visible unique-index descriptor and do not create a separate unique-constraint descriptor. Supported key parts may specify `ASC` or `DESC`; omitted direction is ascending. Existing non-`NULL` duplicate values, prefixes, or full-column tuples fail before mutation, duplicate `NULL` values and tuples with any `NULL` part are allowed, success reports zero affected rows/warnings for non-redundant supported forms, creates descriptor-owned unique secondary-index metadata plus one generated SQLite unique index, supports omitted-name suffixing for name collisions, preserves rows and column descriptors, and is visible to `SHOW`, `CREATE TABLE ... LIKE`, limited `INFORMATION_SCHEMA`, DML duplicate checks, `ALTER TABLE ... DROP INDEX\|KEY`, and standalone `DROP INDEX`. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail; no multiple actions, primary-prefix/functional/table-qualified key parts, index options beyond that tail, comments, visibility, redundant-index warnings, fulltext/spatial indexes, broader online-DDL algorithm/lock semantics, temporary tables, or views |
| `ADD INDEX` / `ADD KEY` | 🟡 | Limited single-action `ALTER TABLE table_name ADD INDEX\|KEY [index_name] (key_part[, ...])` for persistent base tables and existing unqualified supported descriptor columns, including nonunique composite indexes on supported full key parts and nonunique `CHAR`, `VARCHAR`, and bare `TEXT` family prefix key parts. Full key parts include integer-family, exact `DECIMAL`, canonical `YEAR`, canonical `DATE`, canonical `TIME`, canonical `DATETIME`, canonical `TIMESTAMP`, and `CHAR` / `VARCHAR` descriptors. Supported key parts may specify `ASC` or `DESC`; omitted direction is ascending. Success reports zero affected rows/warnings, creates descriptor-owned nonunique secondary-index metadata plus one generated SQLite index, supports omitted-name suffixing, preserves rows and column descriptors, and is visible to `SHOW`, `CREATE TABLE ... LIKE`, limited `INFORMATION_SCHEMA.STATISTICS`, DML, and auto-increment key checks. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail; no multiple actions, primary prefix parts, functional/table-qualified key parts, index options beyond that tail, comments, creation-time visibility options, fulltext/spatial indexes, broader online-DDL algorithm/lock semantics, temporary tables, or views |
| `ADD FULLTEXT` | 🟡 | Limited single-action metadata-only `ALTER TABLE table_name ADD FULLTEXT [KEY\|INDEX] [index_name] (column[, ...])` for persistent base tables and existing unqualified nonzero-length character or text descriptor columns. Positive prefixes are parsed and ignored, omitted names use first-column suffixing, the first fulltext add to a table emits MySQL warning 124 only when durable fulltext document-id state has not already been introduced, success stores descriptor-owned metadata without creating a SQLite index or FTS table, and the descriptor is visible to `SHOW`, `CREATE TABLE ... LIKE`, limited `INFORMATION_SCHEMA.STATISTICS`, drop, and rename. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail with MySQL-compatible rejection of `ALGORITHM=INSTANT` and `LOCK=NONE`; no multiple actions, temporary tables, parser options, explicit direction, expression/table-qualified key parts, full-text search behavior, broader online-DDL algorithm/lock semantics, or views |
| `ADD CONSTRAINT CHECK` | 🟡 | Limited single-action `ALTER TABLE table_name ADD [CONSTRAINT [name]] CHECK (expression) [[NOT] ENFORCED]` for persistent base tables using the current descriptor-owned CHECK expression subset. Omitted names use the first currently available table-local generated CHECK ordinal and still participate in the schema-wide CHECK namespace. Enforced adds validate existing rows and report the scan count; not-enforced adds update descriptors only and report zero rows. No multi-action forms, broader expressions, `DROP CONSTRAINT`, `ALTER CONSTRAINT`, temporary tables, algorithms, locks, or views |
| `ADD CONSTRAINT FOREIGN KEY` | 🟡 | Limited named single-action `ALTER TABLE child ADD CONSTRAINT name FOREIGN KEY [index_name] (child_column[, ...]) REFERENCES parent(parent_column[, ...]) [ON DELETE action] [ON UPDATE action]` plus unnamed `ALTER TABLE child ADD FOREIGN KEY [index_name] ...` for persistent base tables and compatible integer-family child/parent column lists in the same schema; action values are `CASCADE`, `RESTRICT`, `NO ACTION`, and `SET NULL`. Existing child rows must contain a `NULL` in any FK part or match the parent key tuple, `SET NULL` requires every child FK column to be nullable, the parent columns must exactly match a primary or unique key descriptor, matching child indexes are reused or auto-created with MySQL-compatible constraint/index-symbol naming for the admitted subset, success reports zero affected rows/warnings, and metadata/enforcement use descriptor-owned ordered FK rows. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail only for MySQL-compatible `DEFAULT` / `COPY` forms while rejecting `INSTANT`, `INPLACE`, and `LOCK=NONE` under MyLite's fixed enabled foreign-key checks policy; no non-integer keys, prefix/expression/descending parts, cross-schema references, recursive actions, `SET DEFAULT`, `MATCH`, temporary tables, views, broader online-DDL algorithm/lock semantics, or multi-action ALTER |
| `DROP FOREIGN KEY` | 🟡 | Limited single-action `ALTER TABLE table_name DROP FOREIGN KEY foreign_key_name` for persistent base tables with descriptor-owned integer-family FKs. Success removes FK descriptor rows, preserves child indexes and row data, performs no physical SQLite DDL, reports zero affected rows/warnings, and updates `SHOW CREATE TABLE`, `SHOW INDEX`, limited information-schema metadata, and supported DML enforcement. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail. Missing FKs fail with MySQL-compatible `1091 / 42000`; no `IF EXISTS`, `DROP CONSTRAINT`, multi-action ALTER, broader online-DDL algorithm/lock semantics, temporary tables, views, triggers, or recursive cascade cleanup |
| `DROP PRIMARY KEY` | 🟡 | Limited single-action `ALTER TABLE table_name DROP PRIMARY KEY` for persistent base tables. Success deletes primary-key descriptor rows and the generated SQLite unique index, preserves row values, column descriptors, supported secondary indexes, and allowed auto-increment descriptors, leaves former key columns `NOT NULL`, updates descriptor-driven DML, `SHOW`, `CREATE TABLE ... LIKE`, and limited `INFORMATION_SCHEMA` metadata, reports the current table row count, and follows the limited user-transaction implicit commit subset. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail. Tables without a primary key fail, dropping the only key on an auto-increment column is rejected, and dropping a parent key referenced by a supported FK is rejected; no `DROP CONSTRAINT`, `DROP INDEX PRIMARY`, multi-action `ALTER`, broader online-DDL algorithm/lock semantics, temporary tables, views, triggers, or cascades |
| `DROP INDEX` / `DROP KEY` | 🟡 | Limited single-action `ALTER TABLE table_name DROP INDEX\|KEY index_name` for descriptor-owned unique, nonunique secondary, and metadata-only fulltext indexes on persistent base tables. Success deletes secondary-index descriptor rows and the generated SQLite index when one exists, preserves row values, column descriptors, primary-key descriptors, other supported secondary indexes, allowed auto-increment descriptors, and durable fulltext document-id state, updates descriptor-driven DML, `SHOW`, `CREATE TABLE ... LIKE`, and limited `INFORMATION_SCHEMA` metadata, reports zero affected rows/warnings, and follows the limited user-transaction implicit commit subset. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail. Unknown indexes fail with MySQL-compatible `1091`, dropping the last key on an auto-increment column is rejected, and dropping an index used by a supported FK is rejected; no primary-key drops through `DROP INDEX`, multi-action `ALTER`, broader online-DDL algorithm/lock semantics, temporary tables, views, triggers, or cascades |
| `DROP CHECK` | 🟡 | Limited single-action `ALTER TABLE table_name DROP CHECK check_name` for descriptor-owned CHECK constraints on persistent base tables. Success removes the descriptor, rebuilds only when an enforced physical CHECK must be removed, reports zero affected rows/warnings, updates descriptor-driven metadata, and follows the limited user-transaction implicit commit subset. Unknown CHECK names fail with MySQL-compatible `3821 / HY000`; no `DROP CONSTRAINT`, multi-action forms, temporary tables, algorithms, locks, triggers, or cascades |
| `ALTER CHECK ENFORCED` / `NOT ENFORCED` | 🟡 | Limited single-action `ALTER TABLE table_name ALTER CHECK check_name ENFORCED\|NOT ENFORCED` for descriptor-owned CHECK constraints on persistent base tables. Enforcing validates existing rows through a descriptor-built physical rebuild and reports the scan count; unenforcing removes physical enforcement and reports zero rows; idempotent toggles are metadata no-ops. Unknown CHECK names fail with MySQL-compatible `3821 / HY000`; no `ALTER CONSTRAINT`, multi-action forms, temporary tables, algorithms, locks, triggers, or cascades |
| `RENAME INDEX` / `RENAME KEY` | 🟡 | Limited single-action `ALTER TABLE table_name RENAME INDEX\|KEY old_name TO new_name` for descriptor-owned unique, nonunique secondary, and metadata-only fulltext indexes on persistent base tables. Success updates the logical index descriptor name, preserves row values, column descriptors, primary-key descriptors, secondary key parts, uniqueness, visibility, generated SQLite physical index names when present, durable fulltext document-id state, and supported foreign-key references by index id, updates descriptor-driven DML, `SHOW`, `CREATE TABLE ... LIKE`, and limited `INFORMATION_SCHEMA` metadata, reports zero affected rows/warnings, and follows the limited user-transaction implicit commit subset. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail. Missing old indexes fail with MySQL-compatible `1176`, duplicate new names fail with `1061`, quoted `PRIMARY` names fail with `1280`, and exact same-name/case-only renames are accepted; no primary-key renames, multi-action `ALTER`, broader online-DDL algorithm/lock semantics, temporary tables, views, triggers, or cascades |
| `ALTER INDEX VISIBLE` / `INVISIBLE` | 🟡 | Limited catalog-only `ALTER TABLE table_name ALTER INDEX index_name VISIBLE\|INVISIBLE` for descriptor-owned unique, nonunique secondary, and metadata-only fulltext indexes on persistent base tables. Success preserves physical SQLite indexes and row data, reports zero affected rows/warnings except `ALGORITHM=COPY` reports the current row count, updates descriptor-driven `SHOW INDEX`, `SHOW CREATE TABLE`, `CREATE TABLE ... LIKE`, and limited `INFORMATION_SCHEMA.STATISTICS`, and follows the limited implicit-commit subset. Missing indexes fail with `1176`, primary indexes fail with `3522`, and `ALGORITHM=INSTANT` is rejected; no `ALTER KEY`, creation-time visibility options, multi-action `ALTER`, temporary tables, views, optimizer-use semantics, triggers, or cascades |
| `AUTO_INCREMENT` table option | 🟡 | Limited `CREATE TABLE ... AUTO_INCREMENT=N` and `CREATE TEMPORARY TABLE ... AUTO_INCREMENT=N` initial counter support plus single-action `ALTER TABLE table_name AUTO_INCREMENT [=] nonnegative_integer_literal` for persistent base tables. Temporary create options initialize the session-local next counter; persistent alter options update the durable next counter to the greater of the requested value and the next value after the current maximum row value. `AUTO_INCREMENT=0` normalizes to the lower bound, lowered counters after deletes are supported for persistent `ALTER`, generated inserts and metadata observe the new counter, and persistent tables without an auto-increment column are accepted no-ops. No signed, decimal, string, hex, bit, expression, parameter, duplicate-option, or multi-action forms; no add/drop/modify of the auto-increment column attribute, algorithms, locks, temporary `ALTER TABLE`, or views |
| `DROP COLUMN` | 🟡 | Limited single-column `ALTER TABLE table_name DROP [COLUMN] column_name` for persistent base tables; removes the MyLite descriptor column, compacts later descriptor ordinals, preserves remaining row values, deletes one-part primary, unique, nonunique, prefix, and metadata-only fulltext key descriptors over the dropped column, shrinks composite primary, unique, nonunique, and prefix keys by removing the dropped key part, validates narrowed unique and primary keys for duplicate remaining tuples, removes supported nonunique-key-backed auto-increment columns, and emits MySQL-compatible diagnostics for unknown columns, attempts to drop the last column, attempts to drop the last visible column when other columns are invisible, duplicate narrowed keys, and supported child/parent foreign-key column dependencies; admits the limited comma-separated `ALGORITHM` / `LOCK` option tail; no multiple actions, table-qualified drop targets, CHECK dependency rewrites, generated columns, broader online-DDL algorithm/lock semantics, temporary tables, or views |
| `RENAME COLUMN` | 🟡 | Limited single-action `ALTER TABLE table_name RENAME COLUMN old_col TO new_col` for persistent base tables; preserves column id, ordinal position, type, nullability, defaults, visibility, and row values, supports exact same-name no-op and case-only spelling changes, and lets existing primary-key, unique, nonunique, prefix, metadata-only fulltext, and supported integer-family foreign-key descriptors follow the renamed column by descriptor id; rejects duplicate and unknown columns and still rejects CHECK-constrained tables because CHECK expression dependency rewriting is deferred. Admits the limited comma-separated `ALGORITHM` / `LOCK` option tail. No multiple actions, table-qualified column names, type changes, generated-column dependency rewrites, broader online-DDL algorithm/lock semantics, temporary tables, or views |
| `CHANGE COLUMN` | 🟡 | Limited single-action `ALTER TABLE table_name CHANGE [COLUMN] old_col new_col replacement_type [NULL\|NOT NULL] [compatible DEFAULT ...] [FIRST\|AFTER column_name]` for persistent base tables, including keyed tables when existing key descriptors remain valid after replacement; supports the current integer-family replacement subset plus limited `CHAR`, `VARCHAR`, national `CHAR`, national `VARCHAR`, `DATETIME`, and `TIMESTAMP` descriptor replacements with compatible defaults, `ON UPDATE CURRENT_TIMESTAMP` for admitted temporal replacements, optional explicit column `CHARACTER SET`/`CHARSET` with admitted `utf8mb4` or `ascii`, matching `COLLATE` values, and explicit `CHARACTER SET`/`CHARSET binary` or `COLLATE binary` normalization on non-national `CHAR` and `VARCHAR` replacements; preserves column id and existing primary-key, unique, nonunique, prefix, and metadata-only full-text key descriptors, replaces name/type/nullability/default/explicit charset/collation from the replacement definition, updates descriptor ordinal position for admitted `FIRST`/`AFTER`, treats omitted nullability as nullable except that current primary-key parts remain `NOT NULL`, makes the target visible because visibility attributes are not admitted, validates existing integer/text/temporal/`NULL` row values and key compatibility inside the catalog mutation before descriptor replacement, keeps integer display-width-only, bool-alias range-equivalent, and default-only changes metadata-only except for physical column renames and position rebuilds, and rebuilds descriptor-backed physical tables plus non-fulltext physical indexes when type range, text descriptor, temporal descriptor, nullability, or position changes require it; rebuilds require at least one unshadowed SQLite rowid alias and report MySQL-runtime-verified row counts for the admitted subset. No non-admitted replacement types, key descriptor rewriting such as prefix-length shrink adjustment, non-admitted column charsets beyond admitted `binary` normalization, column charset/collation attributes on other type families, multiple actions, table-qualified column names, generated/invisible/auto-increment columns, added primary keys or other keys, foreign-key or CHECK-constrained target tables, algorithms, locks, temporary tables, or views |
| `MODIFY COLUMN` | 🟡 | Limited single-action `ALTER TABLE table_name MODIFY [COLUMN] column_name replacement_type [NULL\|NOT NULL] [compatible DEFAULT ...] [FIRST\|AFTER column_name]` for persistent base tables, including keyed tables when existing key descriptors remain valid after replacement; supports the current integer-family replacement subset plus limited `CHAR`, `VARCHAR`, national `CHAR`, national `VARCHAR`, `DATETIME`, and `TIMESTAMP` descriptor replacements with compatible defaults, `ON UPDATE CURRENT_TIMESTAMP` for admitted temporal replacements, optional explicit column `CHARACTER SET`/`CHARSET` with admitted `utf8mb4` or `ascii`, matching `COLLATE` values, and explicit `CHARACTER SET`/`CHARSET binary` or `COLLATE binary` normalization on non-national `CHAR` and `VARCHAR` replacements; preserves column id and existing primary-key, unique, nonunique, prefix, and metadata-only full-text key descriptors, replaces nullability/default/explicit charset/collation exactly as specified, applies MySQL-compatible omitted-nullability handling for primary-key parts, updates descriptor ordinal position for admitted `FIRST`/`AFTER`, makes the target visible because visibility attributes are not admitted, supports exact same-definition no-op and case-only spelling changes, validates existing integer/text/temporal/`NULL` row values and key compatibility inside the catalog mutation before descriptor replacement, keeps integer display-width-only, bool-alias range-equivalent, and default-only changes metadata-only when no position rebuild is required, and rebuilds descriptor-backed physical tables plus non-fulltext physical indexes when type range, text descriptor, temporal descriptor, nullability, or position changes require it; rebuilds require at least one unshadowed SQLite rowid alias and report MySQL-runtime-verified row counts for the admitted subset. No non-admitted replacement types, key descriptor rewriting such as prefix-length shrink adjustment, non-admitted column charsets beyond admitted `binary` normalization, column charset/collation attributes on other type families, multiple actions, table-qualified column names, generated/invisible/auto-increment columns, added primary keys or other keys, foreign-key or CHECK-constrained target tables, algorithms, locks, temporary tables, or views |
| `ALTER COLUMN SET DEFAULT` | 🟡 | Limited catalog-only `ALTER TABLE table_name ALTER [COLUMN] column_name SET DEFAULT value` for persistent base tables and one unqualified integer-family, decimal, approximate, `CHAR`, `VARCHAR`, limited `ENUM`, limited `SET`, `YEAR`, `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` descriptor column; `value` may be `NULL` for nullable columns, an in-range decimal integer literal with optional unary sign, `TRUE`, or `FALSE`, or a limited parenthesized constant integer/`NULL` expression for integer descriptors, a compatible fixed decimal literal for decimal or approximate descriptors, an approximate numeric literal for approximate descriptors, a string literal for `CHAR`/`VARCHAR` descriptors, a matching string literal for `ENUM`/`SET` descriptors, a canonical year-string literal for `YEAR` descriptors, a canonical date-string literal for `DATE` descriptors, a canonical time-string literal for `TIME` descriptors, a canonical datetime-string literal for `DATETIME` descriptors, a canonical timestamp-string literal for `TIMESTAMP` descriptors, a parenthesized zero-fractional current-date synonym for `DATE`, a parenthesized zero-fractional current-time synonym for `TIME`, or a zero-fractional current-timestamp synonym for `DATETIME` / `TIMESTAMP`; preserves existing row values and physical SQLite schema, reports zero affected rows and warnings, and updates descriptor defaults for later omitted-column inserts; defaults on `AUTO_INCREMENT` columns are hidden from `SHOW COLUMNS` / `SHOW CREATE TABLE` like MySQL, while ordinary column defaults render normally; no general expression defaults beyond the admitted parenthesized integer and current date/time/timestamp subsets, `YEAR` expression defaults, binary string defaults, explicit `TEXT`-family string defaults, `BIT` defaults, numeric or expression defaults for `ENUM`/`SET`, warning-producing overlength string-default truncation, fractional current date/time/timestamp defaults, table-qualified columns, multiple actions, temporary tables, or views |
| `ALTER COLUMN DROP DEFAULT` | 🟡 | Limited catalog-only `ALTER TABLE table_name ALTER [COLUMN] column_name DROP DEFAULT` for persistent base tables and one unqualified descriptor column; preserves existing row values and physical SQLite schema, reports zero affected rows and warnings, stores a dropped-default descriptor state distinct from implicit nullable `DEFAULT NULL`, makes later omitted-column inserts fail, and renders MySQL-compatible `SHOW COLUMNS` / `SHOW CREATE TABLE` metadata; no table-qualified columns, multiple actions, temporary tables, or views |
| `ALTER COLUMN SET VISIBLE` / `SET INVISIBLE` | 🟡 | Limited catalog-only `ALTER TABLE table_name ALTER [COLUMN] column_name SET VISIBLE\|INVISIBLE` for persistent base tables and one unqualified descriptor column; preserves row values and physical SQLite schema, reports zero affected rows and warnings, prevents making every column invisible with MySQL error 4028, and drives `SELECT *`, implicit insert mapping, `SHOW COLUMNS`, and `SHOW CREATE TABLE`; no table-qualified columns, multiple actions, column-definition visibility syntax, generated invisible primary keys, protocol flags, temporary tables, or views |
| `RENAME TO` | 🟡 | Limited single-action persistent base-table `ALTER TABLE ... RENAME [TO\|AS]` through descriptor rename; supports unqualified, schema-qualified, cross-schema, and same-object no-op forms; no combined actions, temporary tables, views, triggers, options, locks, algorithms, privileges, or metadata side effects |
| `ORDER BY` | 🟡 | Limited single-action `ALTER TABLE table_name ORDER BY column_name [ASC\|DESC][, ...]` for persistent base tables without primary-key or secondary-index descriptors; resolves unqualified, target-table-qualified, and target-schema-table-qualified descriptor columns, rebuilds the physical SQLite table with `INSERT INTO temp SELECT ... ORDER BY ...`, reports copied rows, and leaves descriptors unchanged; no expression or ordinal order keys, temporary tables, mixed ALTER actions, indexes, clustering guarantees, storage-engine algorithm controls, locks, or privileges |
| `CONVERT TO CHARACTER SET` | ❌ | Column/table charset and collation conversion semantics |
| `DEFAULT CHARACTER SET` / `COLLATE` | 🟡 | Limited metadata-only `ALTER TABLE table_name [DEFAULT] CHARSET` / `CHARACTER SET` with admitted `utf8mb4` or `ascii` and admitted `ALTER TABLE table_name [DEFAULT] COLLATE` values for persistent base tables; stores descriptor table defaults, preserves rows and physical SQLite schema, and reports zero affected rows/warnings. Explicit column charset/collation attributes are tracked separately for admitted string column definitions. No conversion, non-admitted charsets, collation comparison semantics, multiple actions, temporary tables, views, or full charset/collation catalogs |
| `FORCE` | 🟡 | Limited single-action `ALTER TABLE table_name FORCE` for persistent base tables without primary-key or secondary-index descriptors; performs a descriptor-driven SQLite-side physical rebuild, preserves row values and logical descriptors, reports zero affected rows/warnings, increments only SQLite schema generation, and follows the limited user-transaction implicit commit subset; admits the limited comma-separated `ALGORITHM` / `LOCK` option tail; no temporary tables, repeated or mixed ALTER actions, storage-engine defragmentation guarantees, broader online-DDL algorithm/lock semantics, indexes, or privileges |
| `DISCARD TABLESPACE` | ❌ | Tablespace discard syntax |
| `IMPORT TABLESPACE` | ❌ | Tablespace import syntax |
| `ALGORITHM` | 🟡 | Limited comma-separated option tails on selected supported single-action persistent `ALTER TABLE` forms. `DEFAULT`, `INSTANT`, `INPLACE`, and `COPY` are parsed and validated as compatibility assertions for the admitted action matrix; unsupported action/algorithm pairs are rejected deterministically. No standalone option-only ALTER, full MySQL online DDL matrix, scheduler, metadata-lock, or concurrent DML guarantees |
| `LOCK` | 🟡 | Limited comma-separated option tails on selected supported single-action persistent `ALTER TABLE` forms. `DEFAULT`, `NONE`, `SHARED`, and `EXCLUSIVE` are parsed and validated as compatibility assertions, including MySQL-shaped rejection of `ALGORITHM=INSTANT` with non-default locks. No standalone option-only ALTER, no-comma lock tails, metadata-lock scheduling, or concurrent DML guarantees |

[Back to compatibility overview](../../COMPATIBILITY.md)
