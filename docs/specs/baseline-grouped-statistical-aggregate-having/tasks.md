# Baseline Grouped Statistical Aggregate HAVING Tasks

- [x] Record MySQL 8.4.9 expectations for selected statistical aggregate
  aliases and repeated selected statistical aggregate expressions in grouped
  `HAVING`.
- [x] Add runtime tests for population, sample, alias, repeated-expression,
  alias-function, variance, `IS NULL`, and row-scalar statistical `HAVING`
  cases.
- [x] Update compatibility documentation and related grouped statistical specs.
- [x] Run focused MySQL expectations, focused CTest, format/check gates, and
  release-gate review before commit.
