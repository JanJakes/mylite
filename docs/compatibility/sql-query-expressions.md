# SQL query expressions

Core query expression, SELECT, set operation, ordering, limiting, locking, modifier, and result metadata compatibility.

The limited row-scalar string-search, string-comparison, and regex family
includes `FIND_IN_SET()`, `STRCMP()`, and `REGEXP_LIKE()` for no-source,
`DUAL`, `DO`, and single-table row-scalar projections. `FIND_IN_SET()`,
`REGEXP_LIKE()`, the string length family, and the `SUBSTRING()` / `SUBSTR()` /
`MID()` family currently participate in narrow descriptor `WHERE` predicate
forms. It is ASCII-only, does not yet implement MySQL's `SET` column
member-ordinal behavior for `FIND_IN_SET()`, does not yet implement non-ASCII
collation parity for `STRCMP()` or substring predicates, and does not yet
implement full ICU regular-expression semantics for `REGEXP_LIKE()`.

The limited `WHERE` predicate operand set also admits statement-current
`CURRENT_TIMESTAMP` / `NOW()` values for compatible nonbinary string,
`DATETIME`, and `TIMESTAMP` descriptor comparisons.

The limited row-scalar control-flow family includes `IF()`, `IFNULL()`,
`COALESCE()`, `NULLIF()`, and `ISNULL()` in single-table projection lists over
admitted descriptor and scalar value operands. It does not make those functions
available in predicates, ordering/grouping expressions, or DML assignments.

The limited row-scalar comparison-extrema family includes `GREATEST()` and
`LEAST()` in no-source, `DUAL`, `DO`, and single-table projection lists over
flat all-string or all-integer argument lists. It uses the current ASCII
`utf8mb4_0900_ai_ci` collation subset for string comparisons. The related
`INTERVAL()` slice is limited to sorted integer-domain thresholds with a
literal or descriptor-column integer search value. These functions are not
available in predicates, ordering/grouping expressions, or DML assignments.

The limited table-backed signed integer arithmetic slice admits `+`, binary
`-`, and `*` projection expressions over signed integer descriptor columns,
integer/boolean/`NULL` literals, and parentheses in one-base-table `SELECT`
statements. It does not admit unsigned columns, division, modulo, functions,
casts, string or decimal coercion, expression predicates, expression ordering,
or DML assignment expressions.

The limited row-scalar `CAST()` / `CONVERT()` slice admits binary, character,
signed integer, unsigned integer, `USING BINARY`, and `USING
utf8mb4`/`utf8`/`utf8mb3`/`latin1` conversion projections over descriptor
integer-family columns, nonbinary string-family columns, scalar literals, and
supported folded session scalar values in one-base-table `SELECT` statements.
It uses MyLite-owned conversion and result metadata while SQLite scans the rows.
It does not admit binary string or `BIT` column operands, decimal/approximate/
temporal/JSON/spatial column operands, general expression operands, predicates,
ordering/grouping expressions, or DML assignments.

The limited user-variable surface exposes handle-local `@name` values in
no-source/`DUAL` scalar `SELECT` lists, `DO` expressions, and SQL-level
prepared statement source/`USING` positions. It does not make user variables
available directly in descriptor predicates, ordering, grouping, DML assignment
values, generated expressions, or arbitrary parameters.

| Feature | Status | Notes |
| --- | --- | --- |
| `SELECT` | 🟡 | Descriptor-driven table `SELECT *`, column-list reads, limited qualified wildcard projection, supported source qualifiers/table aliases, select-item aliases, selected no-op modifiers, limited `SQL_CALC_FOUND_ROWS`, simple no-op locking clauses, no-source/`DUAL` scalar projection over current scalar-function/operator subsets including mode-sensitive `PIPES_AS_CONCAT` `\|\|`, limited row-scalar control-flow, limited table-backed signed integer arithmetic, limited row-scalar `\|\|` concatenation under `PIPES_AS_CONCAT`, limited `UNIX_TIMESTAMP()` / `FROM_UNIXTIME()`, limited `TIMESTAMP()`, limited `GET_FORMAT()` / `STR_TO_DATE()`, and `JSON_ARRAY()` / `JSON_OBJECT()` / `JSON_SET()` / `JSON_INSERT()` / `JSON_REPLACE()` / `JSON_REMOVE()` projection over one descriptor-backed table, limited single-table rowset `SELECT DISTINCT` / `DISTINCTROW`, one-item aggregate forms, limited mutable `@@sql_select_limit` caps for supported top-level selects without explicit `LIMIT`, and limited grouped projection as `SELECT descriptor_projection [, descriptor_projection ...] [, aggregate ...] FROM source [WHERE ...] GROUP BY group_column [, group_column ...] [HAVING ...] [ORDER BY ...] [LIMIT ...]` for one to four integer or ASCII nonbinary string descriptor group columns or limited selected descriptor-column aliases that do not shadow source descriptor columns, selected descriptor columns that are grouped or determined by the complete grouped primary key of their own source, limited grouped wildcards that expand only such columns, and supported count/numeric/bitwise aggregate results over one base table or the current two-source inner/cartesian/left-outer joined source envelope. `SELECT *` and limited `table.*` / `schema.table.*` / `alias.*` projection expand visible descriptor columns only while explicit references may name invisible columns; grouped `GROUP_CONCAT()` remains one result and one-base-table only. Plain descriptor-backed `SELECT` additionally supports limited multi-source inner/cartesian/no-op `STRAIGHT_JOIN` join chains and pure comma lists, limited two-source right outer joins with the existing joined projection, `WHERE`, descriptor-column `ORDER BY`, limited single-table `ORDER BY FIELD(column, value[, ...])`, and `LIMIT` envelope. No joins beyond the limited descriptor-backed inner/cartesian/no-op `STRAIGHT_JOIN`/comma plain SELECT subset and two-source left/right outer SELECT subset, full grouping, mixed comma/explicit join precedence, multi-source outer joins, mixed unqualified wildcard select lists that expand nondependent grouped sources, general table-backed expression projection outside documented row-scalar slices, grouping expression aliases, aliases shadowed by source descriptor columns, full functional-dependence inference beyond complete primary-key coverage for the projected source, full modifier permutations/repetition, joined/grouped/scalar/expression full distinct rows, transaction lock behavior, `@@sql_select_limit` effects on internal DML/DDL source selects, or arbitrary SQLite pass-through |
| Query expression grammar | 🟡 | Limited top-level unparenthesized homogeneous chains of supported `SELECT` query blocks with `UNION [DISTINCT\|ALL]`, `INTERSECT [DISTINCT\|ALL]`, or `EXCEPT [DISTINCT\|ALL]`, plus the same unparenthesized `UNION`-only compound source envelope inside supported `INSERT ... SELECT`; limited top-level `TABLE table_name [ORDER BY ...] [LIMIT ...]` statement over currently supported visible table sources; limited standalone `VALUES ROW(...) [, ROW(...)] [ORDER BY ...] [LIMIT ...]` statement over literal rows; no parenthesized query expressions, query terms/primaries, compound-level `ORDER BY` / `LIMIT`, mixed set-operator precedence, `TABLE` or `VALUES` in set operations or nested query positions, or `INTERSECT` / `EXCEPT` in DML sources |
| `SYSDATE()` scalar projection | 🟡 | Limited zero-fractional execution-time timestamp in no-source `SELECT`, `SELECT ... FROM DUAL`, `DO`, and single-table row-scalar projection; ignores the limited session `timestamp` override, observes the limited session `time_zone` offset, supports `SYSDATE ()` only with `IGNORE_SPACE`, and returns existing scalar result conventions; no fractional precision, defaults, predicates, ordering expressions, grouping expressions, subqueries, arbitrary expression trees, or broader coercion targets |
| `RAND(seed)` scalar projection | 🟡 | No-source `SELECT`, `SELECT ... FROM DUAL`, and `DO` admit integer/boolean/`NULL` literals plus limited decimal, float, string, `NULLIF()`, and integer `CAST()` / `CONVERT()` seed coercion with MySQL-compatible deterministic sequences for the documented subset; table-backed `RAND(seed)` remains limited to the existing integer/boolean/`NULL` literal seed domain, and warning-producing table-backed seed expressions remain deferred |
| Projection list | 🟡 | Wildcard and limited qualified wildcard projection use visible descriptor columns in catalog ordinal order, including exact `DECIMAL` canonical text readback, limited text-family readback, and byte-safe binary string readback; explicit table projections and limited single-table rowset `SELECT DISTINCT` / `DISTINCTROW` resolve descriptor column names, including invisible columns and supported source-qualified references; no-source and `FROM DUAL` projection may return mixed session-scalar, user-variable, scalar-value, and limited `PIPES_AS_CONCAT` `\|\|` items over current scalar subsets; supported row-scalar projection slices include limited statement-current date/time/timestamp and UTC date/time/timestamp values, current flat string helpers including `INSERT()`, `REPLACE()`, and `QUOTE()`, current JSON helpers, limited string codepoint helpers, limited temporal extractors including `TIME()` / `TIME_TO_SEC()` / `TO_DAYS()` / `TO_SECONDS()` over admitted descriptor temporal and string columns, limited `SEC_TO_TIME()`, `FROM_UNIXTIME()`, `FROM_DAYS()`, `MAKEDATE()`, and `MAKETIME()` over integer-family descriptors and integer/boolean/`NULL` constructor operands, limited `UNIX_TIMESTAMP()` over temporal descriptors, limited `TIMESTAMP()` over temporal and nonbinary string descriptors plus canonical time operands, limited `DATE_ADD()` / `DATE_SUB()` / `ADDDATE()` / `SUBDATE()` interval-unit projection over temporal and nonbinary string descriptors with signed-64, exact quoted integer string, or `NULL` intervals, limited `GET_FORMAT()` format-string projection and as admitted format input to limited `DATE_FORMAT()` / `TIME_FORMAT()` / `STR_TO_DATE()`, limited `DATE_FORMAT()` / `TIME_FORMAT()` over temporal and string descriptors, limited `STR_TO_DATE()` over string descriptors and literal formats, limited `DATEDIFF()` and `TIMESTAMPDIFF()` over temporal and string descriptors, limited `TIMEDIFF()` over temporal descriptors, limited `DAYNAME()` / `MONTHNAME()` / `DAYOFWEEK()` / `DAYOFYEAR()` / `LAST_DAY()` / `WEEK()` / `WEEKDAY()` / `WEEKOFYEAR()` / `YEARWEEK()` over temporal and string descriptors, limited single-table control-flow `IF()` / `IFNULL()` / `COALESCE()` / `NULLIF()` / `ISNULL()` over admitted descriptor and scalar value operands, limited `GREATEST()` / `LEAST()` over flat all-string or all-integer descriptor/scalar value operands, limited `INTERVAL()` over an integer-family descriptor or scalar integer-domain search value and sorted scalar integer-domain thresholds, limited mode-sensitive `\|\|` concatenation over the current `CONCAT()` operand envelope, and limited signed integer `+`, binary `-`, and `*` arithmetic over signed integer descriptor columns and integer/boolean/`NULL` literals; supported non-wildcard items may use identifier, quoted-identifier, or string-literal aliases as result labels; limited one-item aggregate forms and WordPress-style multi-item `COUNT(NULLIF(column = literal,FALSE))` / `COUNT(NULLIF(column LIKE pattern,FALSE))` aggregates are supported over one table or the current joined source envelope; grouped aggregate projection is limited to one to four integer or ASCII nonbinary string descriptor group columns, selected descriptor columns that are grouped or determined by the complete grouped primary key of their own source, grouped wildcard expansion over only such visible columns, and optional supported count/numeric/bitwise aggregate results over one source or the current two-source joined envelope, with `GROUP_CONCAT()` grouped support restricted to one aggregate result over one base table; duplicate projected descriptor columns and duplicate labels are allowed for non-distinct selects, including limited `table.*` / `schema.table.*` / `alias.*` expansion; limited MySQL-shaped result-column metadata is available for descriptor columns, no-source/`DUAL` integer/boolean/`NULL`/string literal projections, selected session/system functions, current scalar JSON functions, and current single-table row-scalar descriptor/JSON functions; no general expression metadata, general table-backed expression projection, mixed unqualified wildcard grouped select lists that expand nondependent sources, full functional-dependence inference beyond complete primary-key coverage, unsigned/decimal/binary/unsupported-family distinct/order/group/aggregate comparison semantics, aggregate-argument string comparison semantics, or expression/multi-source distinct rows |
| Scalar subquery projection | 🟡 | Limited no-source and `FROM DUAL` scalar subqueries as projection operands and as current `CONCAT()` / `CONCAT_WS()` / `PIPES_AS_CONCAT` `\|\|` operands, with one inner scalar value from the supported `DATABASE()`/`SCHEMA()`, integer/boolean/`NULL`, and system-variable read subset; the same no-source/DUAL scalar-subquery subset is admitted in `FROM DUAL WHERE subquery IS [NOT] NULL` filters; limited uncorrelated table-backed scalar subqueries are also supported only as single-table `UPDATE` assignment values, with one descriptor source column and the current descriptor `SELECT` source envelope; multi-column inner projections return MySQL-compatible `1241 / 21000`, matched multi-row assignment subqueries return `1242 / 21000`, and same-table update sources return `1093 / HY000`; no correlated, derived-table, row, quantified, general DML-assignment, or arbitrary expression subqueries |
| Table aliases | 🟡 | Optional `AS alias` or bare `alias` on supported single persistent or shadowing session temporary base-table `SELECT` sources, limited multi-source descriptor-backed inner/cartesian/no-op `STRAIGHT_JOIN`/comma plain SELECT joins, and limited two-source descriptor-backed left/right outer joins with limited validated/no-op source index hints; selected columns, qualified wildcard projection, predicates, order keys, aggregate arguments on single-table paths, and joined-source columns may be qualified by that alias, while unaliased sources may use table or schema-table qualifiers; no derived tables or aliases outside the documented source subsets |
| `SELECT ... FROM DUAL` | 🟡 | Limited scalar/session-function projection, mixed session-scalar, user-variable, and scalar-value projection over current session scalar functions/system variables, decimal integer/string/`NULL`/boolean literal select items, scalar `IF()`/`IFNULL()`/`COALESCE()`/`NULLIF()`/`ISNULL()` values with signed-64 integer/boolean/`NULL` operands and supported nested scalar calls, limited signed-64 `+`, binary `-`, `*`, unary `+`, unary `-`, `%`, infix `MOD`, `MOD(left, right)`, and infix `DIV` scalar arithmetic projection plus limited top-level `/` scalar division, limited unsigned-64 numeric scalar bitwise projection for `~`, `&`, `\|`, `^`, `<<`, and `>>` over the current signed-64 scalar arithmetic operand domain, limited numeric `ABS()`, `SIGN()`, `CEIL()`/`CEILING()`/`FLOOR()`, limited integer-domain `ROUND()`, `BIT_COUNT()`, `BIN()`, `OCT()`, `CONV()`, `SQRT()`, `DEGREES()`, `RADIANS()`, `ACOS()`, `ASIN()`, `SIN()`, `COS()`, `TAN()`, `COT()`, `ATAN()`, `ATAN2()`, `EXP()`, `LN()`, `LOG()`, `LOG10()`, `LOG2()`, `POW()`, and `POWER()` over integer/boolean/`NULL`, signed-64 scalar arithmetic, and limited numeric bitwise operands, limited `CRC32()` over string/hex/integer/boolean/`NULL` literals, limited `HEX()` over string/hex/integer/boolean/`NULL` plus supported session scalar and binary cast/convert arguments, limited `QUOTE()` over string/integer/fixed-decimal/boolean/`NULL` scalar values and supported session scalar/system-variable values, and limited `FORMAT()` / `TRUNCATE()` over exact integer/fixed-decimal/boolean/`NULL` value literals with integer decimal-place literals, limited top-level `PI()` constant, zero-argument `RAND()`, and the documented no-source seeded `RAND(seed)` coercion subset, limited bare `CAST(value AS BINARY)`, `CAST(value AS CHAR)`, `CAST(value AS SIGNED [INTEGER\|INT])`, `CAST(value AS UNSIGNED [INTEGER\|INT])`, and corresponding `CONVERT(value, target)` forms over string/integer/boolean/`NULL` scalar values, limited `CONVERT('string' USING BINARY)` over ordinary string scalar literals, and limited `CONVERT(value USING utf8mb4\|utf8\|utf8mb3\|latin1)` over string/integer/boolean/`NULL` scalar values with `latin1` limited to ASCII scalar values, limited DATE interval-unit `DATE_ADD()` / `DATE_SUB()` / `ADDDATE()` / `SUBDATE()` scalar values, limited `ADDTIME(value, time_value)` / `SUBTIME(value, time_value)` scalar values over canonical datetime/time strings and `NULL`, limited `GET_FORMAT(class, format)` scalar values over supported temporal classes and literal format names, limited `DATE_FORMAT(value, format)` scalar values over supported canonical date/datetime strings, limited `TIME_FORMAT(value, format)` scalar values over supported canonical time/datetime strings, limited `STR_TO_DATE(value, format)` scalar values over supported string/NULL values and literal formats, limited `DATEDIFF(value1, value2)` and `TIMESTAMPDIFF(unit, value1, value2)` scalar values over supported canonical date/datetime strings and `NULL`, limited `TIMEDIFF(value1, value2)` scalar values over supported canonical time/datetime strings and `NULL`, limited `TIMESTAMP(value[, time_value])` scalar values over supported canonical date/datetime strings, canonical time strings, and `NULL`, limited `DAYNAME(value)` / `MONTHNAME(value)` / `DAYOFWEEK(value)` / `DAYOFYEAR(value)` / `LAST_DAY(value)` / `WEEK(value[, mode])` / `WEEKDAY(value)` / `WEEKOFYEAR(value)` / `YEARWEEK(value[, mode])` scalar values over supported canonical date/datetime strings and `NULL`, limited `TO_DAYS(value)` / `TO_SECONDS(value)` scalar values over supported canonical date/datetime strings and `NULL`, limited `UNIX_TIMESTAMP()` / `UNIX_TIMESTAMP(value)` over supported canonical date/datetime strings and `NULL`, limited `UTC_DATE` / `UTC_TIME` / `UTC_TIMESTAMP` statement values, limited temporal extractor and `TIME_TO_SEC()` scalar values over admitted canonical temporal strings, limited `SEC_TO_TIME()`, `FROM_UNIXTIME()`, `FROM_DAYS()`, `MAKEDATE()`, and `MAKETIME()` scalar values over signed-64 integer/boolean/`NULL` literals, and limited signed-64 plus numeric string/decimal/float scalar comparison projection for `=`, `<=>`, `<>`, `!=`, `<`, `<=`, `>`, and `>=`, limited keyword scalar logical projection for `NOT`, `AND`, `XOR`, and `OR`, limited scalar `IS` projection for `IS [NOT] NULL` / `TRUE` / `FALSE` / `UNKNOWN`, limited top-level searched/simple scalar `CASE` projection whose children may use existing non-`CASE` signed-64 scalar expressions, limited flat row-scalar `CONCAT()`, `CONCAT_WS()`, and mode-sensitive `\|\|` over string/integer/boolean/`NULL` literals and supported session scalar values, including `\|\|` as an operand to supported signed-64 arithmetic when the concatenated result is a decimal integer string, limited flat row-scalar `FIELD()` and `GREATEST()` / `LEAST()` over all-string or all-integer string/integer/boolean/`NULL` literals, limited flat row-scalar `INTERVAL()` over signed-64 integer/boolean/`NULL` search values and sorted signed-64 integer/boolean thresholds, limited string length, string codepoint, string slice, string search, string replacement/insertion, ASCII string case, `HEX()`, `JSON_ARRAY()`, `JSON_OBJECT()`, and `JSON_VALID()` functions over admitted scalar values, and one-item `COUNT(*)` / `COUNT(integer/NULL/boolean literal)` one-row semantics; optional `FROM DUAL WHERE` filters admit the scalar-literal predicate subset, scalar subquery `IS [NOT] NULL` over the current no-source/DUAL scalar-subquery value subset, plus `[NOT] EXISTS (...)`; no table-backed descriptor behavior or arbitrary expression evaluation |
| `WHERE` | 🟡 | Descriptor-backed boolean expressions over existing predicate atoms for filtered table `SELECT`, aggregate source filters, `DELETE`, and `UPDATE`: keyword `NOT`, `AND`/`&&`, `XOR`, `OR`, inactive-mode `\|\|`, and parentheses follow MySQL precedence, `SELECT` may use supported source-qualified references and limited plain joined `SELECT` same-scope `left_column = right_column` equality over same-family integer or ASCII string descriptor columns, while `DELETE` and `UPDATE` remain unqualified; limited scalar-literal truth predicates over signed-64 decimal integer, `TRUE`, `FALSE`, and `NULL`; limited scalar-literal comparisons, literal-left descriptor comparisons, descriptor `column op NULL`, and scalar-literal `IS [NOT] NULL` / `TRUE` / `FALSE` / `UNKNOWN`; integer comparisons with a non-`NULL` decimal integer or `TRUE`/`FALSE` right operand (`=`, `<=>`, `<>`, `!=`, `<`, `<=`, `>`, `>=`), ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string-literal comparisons for `=`, `<=>`, `<>`, `!=`, `<`, `<=`, `>`, and `>=`, limited string `BETWEEN` / `NOT BETWEEN`, limited string `IN` / `NOT IN` with string/`NULL` lists, limited `LIKE` / `NOT LIKE` pattern predicates, limited `REGEXP` / `RLIKE` / `NOT REGEXP` / `NOT RLIKE` ASCII regular-expression predicates, limited `REGEXP_LIKE(value, pattern[, match_type])` predicates with one nonbinary string descriptor value operand, scalar literal pattern, optional lowercase `c`/`i` match flags, integer/boolean comparison literals, and `IS NULL` / `IS NOT NULL`, limited single-source string-length function predicates (`LENGTH`, `OCTET_LENGTH`, `BIT_LENGTH`, `CHAR_LENGTH`, and `CHARACTER_LENGTH`) with truth, integer/boolean/`NULL` comparisons, and `IS [NOT] NULL`, limited single-source `SUBSTRING` / `SUBSTR` / `MID` predicates with ASCII string/`NULL` comparisons and `IS [NOT] NULL`, limited single-source numeric temporal extractor predicates with truth, integer/boolean/`NULL` comparisons, and `IS [NOT] NULL`, limited single-source `SELECT WHERE DATE_FORMAT(value, '%H.%i') = numeric_literal` predicates over supported `DATE_FORMAT()` value inputs and integer or exact-decimal numeric literals, limited `ENUM` label or ordinal equality/null-safe-equality/inequality predicates plus `<=> NULL`, `YEAR` comparisons with a non-`NULL` compatible `YEAR` literal, canonical `DATE`/`TIME` comparisons plus canonical and limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning `DATE`, `DATETIME`, and `TIMESTAMP` comparisons with a non-`NULL` string literal right operand, limited integer, `BIT`, `YEAR`, and temporal `BETWEEN` / `NOT BETWEEN` with two compatible bounds, limited integer, `BIT`, `YEAR`, and temporal `IN` / `NOT IN` with a nonempty compatible literal/`NULL` list, normal stored-value `IS NULL` and `IS NOT NULL` for integer, exact `DECIMAL`, approximate `FLOAT`/`DOUBLE`, `CHAR`, `VARCHAR`, baseline `TEXT` family, binary string, `BIT`, limited `ENUM`, `YEAR`, `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` columns, integer descriptor-column `IS [NOT] TRUE` / `FALSE` / `UNKNOWN` truth tests, limited `SELECT ... WHERE [NOT] EXISTS (...)` predicate subqueries over one outer base table with tableless/`DUAL` or one-table inner sources, optional limited inner predicates, simple correlated integer equality/null-safe equality, and `LIMIT row_count`, and limited `SELECT ... WHERE descriptor_column [NOT] IN (SELECT descriptor_column FROM descriptor_table [WHERE ...])` subqueries over compatible integer-family or ASCII nonbinary string-family columns with optional limited inner predicates and simple correlated integer equality/null-safe equality; admitted string comparisons, ranges, membership predicates, and substring predicates use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, admitted enum comparisons use the current ASCII case-insensitive enum label match, `TIME` comparisons and ranges use signed total-second semantics, `XOR` propagates `NULL` for the admitted predicate subset, `&&` and inactive-mode `\|\|` record MySQL-compatible deprecation warning 1287; no symbolic `!`, scalar expression `XOR`, nonliteral expression-left predicates outside documented function predicate atoms, descriptor-column bare truth tests, column-to-column operators other than limited plain joined `SELECT` same-scope `=`, mixed-type or unsupported-type column-to-column comparisons, string/decimal/float/hex/bit scalar-literal truth tests, enum ranges/membership/order predicates, binary string comparisons outside `BIT`, non-ASCII string predicate literals outside the current enum label-matching subset, full-Unicode string collation parity, decimal comparisons/ranges/membership/truth tests, relaxed temporal strings outside the documented `DATE`/`DATETIME`/`TIMESTAMP` predicate slices, `sql_auto_is_null` auto-increment lookup behavior, functions outside documented predicate atoms, parameters, decimal/float/hex predicate literals outside compatible `BIT` targets, subqueries outside the limited `EXISTS` and `IN` slices, row constructors, or general expression predicates |
| `GROUP BY` | 🟡 | Limited one to four integer or ASCII nonbinary string (`CHAR`, `VARCHAR`, baseline `TEXT` family) descriptor group columns, including unique selected descriptor-column aliases when no source descriptor column shadows the alias name, with selected descriptor columns that are grouped or determined by the complete grouped primary key of their own source, limited grouped `*` / qualified-wildcard expansion over only such visible columns, and optional supported count/numeric/bitwise aggregate results over one descriptor-backed persistent base table or the current two-source inner/cartesian/left-outer joined source envelope: `SELECT descriptor_projection [, descriptor_projection ...] [, aggregate ...] FROM source [WHERE ...] GROUP BY group_column_or_selected_descriptor_alias [, ...] [HAVING grouped_or_selected_aggregate_predicate] [ORDER BY grouped_or_primary_key_dependent_column_or_selected_count_min_max_sum_alias [ASC|DESC]] [LIMIT ...]`; grouped `GROUP_CONCAT()` support remains limited to one descriptor-backed base table, one aggregate result, and grouped-column `HAVING`; `NULL` key tuples form one group, optional `HAVING` filters groups after aggregation, optional ordering follows existing integer/`NULL` order semantics or MyLite's registered ASCII `utf8mb4_0900_ai_ci` string collation for group and primary-key-dependent descriptor columns and exact SQLite-backed order for selected `COUNT`, `MIN`, `MAX`, and `SUM` aggregate aliases, and limiting follows existing SELECT `LIMIT`/`OFFSET`; no grouping expression aliases, aliases shadowed by source descriptor columns, ordinals, expressions, rollup, unique-key/equality/transitive/view/constant functional-dependence inference, full Unicode collation parity, grouped `COUNT(DISTINCT)`, selected `AVG`/bitwise/`GROUP_CONCAT()` alias ordering, mixed grouped `GROUP_CONCAT()` projections, grouped `GROUP_CONCAT(DISTINCT)`, wider join sources for `GROUP_CONCAT()`, or full `ONLY_FULL_GROUP_BY` coverage |
| `WITH ROLLUP` | ❌ | Super-aggregate rows and GROUPING() behavior |
| `HAVING` | 🟡 | Limited post-group predicates on the existing grouped aggregate subset: one grouped descriptor integer column or its alias compared to an integer/boolean literal or tested with `IS NULL` / `IS NOT NULL`; one grouped ASCII nonbinary string column or alias for `IS NULL` / `IS NOT NULL`; or a selected `COUNT(*)`, `COUNT(column)`, `MIN(column)`, `MAX(column)`, `SUM(column)`, or `AVG(column)` aggregate expression or alias compared to an integer/boolean literal or tested with `IS NULL` / `IS NOT NULL`. Non-grouped single-table `SELECT` also supports descriptor-column `LIKE` / `NOT LIKE` filters over the existing row predicate subset and narrow `COUNT(*)` comparisons against integer/boolean literals for descriptor or row-scalar projection; no boolean composition, bare truth tests, string group-column comparisons, non-grouped aggregate predicates beyond `COUNT(*)`, aggregate expressions not selected in grouped queries, `COUNT(DISTINCT)`, bitwise aggregate result predicates, `GROUP_CONCAT()` predicates, arbitrary expressions, subqueries, or general alias ambiguity handling |
| Window definitions | ❌ | Named windows, frames, restrictions |
| `ORDER BY` | 🟡 | One or more integer, `BIT`, `YEAR`, `DATE`, `TIME`, `DATETIME`, `TIMESTAMP`, or ASCII nonbinary string (`CHAR`, `VARCHAR`, baseline `TEXT` family) descriptor columns for supported base-table `SELECT` and top-level `TABLE` paths; one such descriptor column for supported single-table `DELETE` and `UPDATE`; supported non-distinct single-table `SELECT` and row-scalar `SELECT` also admit exactly one hidden `FIELD(column, value[, ...])` order key over the current `FIELD()` string or integer candidate domain; each descriptor-column `SELECT` and `TABLE` key may use supported source-qualified references, `SELECT` may also use one unqualified select-item alias, while `DELETE` and `UPDATE` remain unqualified descriptor-only forms; aliases shadow descriptor column names and duplicate matching aliases are ambiguous; optional `ASC`/`DESC` per key; `ASC` is the default, `NULL` sorts before non-`NULL` ascending and after non-`NULL` descending, `BIT` values sort by fixed-width byte value, `YEAR` values sort by their canonical four-digit representation, `DATE`/`DATETIME`/`TIMESTAMP` values sort by canonical text, `TIME` values sort by signed total seconds, supported string values sort with MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, and supported `FIELD()` order keys sort by their integer first-match rank with `0` for `NULL`/unmatched values; limited single-table rowset `SELECT DISTINCT` requires each order key to resolve to a selected descriptor column or selected-column alias, and limited grouped aggregates may order by a grouped descriptor column, a descriptor column determined by the complete grouped primary key of its own source, a selected grouped projection alias, or one unique selected `COUNT`/`MIN`/`MAX`/`SUM` aggregate alias; no ordinals, general expressions, string-literal order keys, enum, set, JSON, binary string, decimal, approximate numeric, or non-ASCII/full-Unicode string order semantics, qualified wildcards, collations, multiple keys with `FIELD()`, multiple keys outside supported non-distinct `SELECT`/`TABLE` paths, aggregate expression ordering, selected `AVG` aggregate alias ordering, bitwise aggregate alias ordering, `GROUP_CONCAT()` aggregate alias ordering, or tie-order guarantee |
| `LIMIT` / `OFFSET` | 🟡 | Supported base-table `SELECT` and top-level `TABLE` forms are `LIMIT row_count`, `LIMIT row_count OFFSET offset`, and `LIMIT offset, row_count`; supported single-table `DELETE` and `UPDATE` admit only `LIMIT row_count`; all supported forms use unsigned decimal integer literals in the signed 64-bit range; `LIMIT 0` returns no rows for `SELECT`/`TABLE` and matches no rows for supported DML; no signed, string, decimal, float, hex, bit, parameter, or expression limits |
| `@@sql_auto_is_null` | 🟡 | Limited scalar reads expose the fixed disabled value `0`; no mutable session state, `LAST_INSERT_ID()`-driven special lookup behavior, or special `IS NULL` lookup behavior |
| `@@sql_big_selects` | 🟡 | Limited scalar reads expose the fixed enabled value `1`; no mutable session state, `max_join_size`, optimizer row-estimate aborts, or changed descriptor-backed `SELECT` execution |
| `@@sql_buffer_result` | 🟡 | Limited scalar reads expose the fixed disabled value `0`; no mutable session state, temporary result table materialization, lock-release behavior, or changed descriptor-backed `SELECT` execution |
| `@@sql_select_limit` | 🟡 | Limited scalar reads expose handle-local mutable session state, with global reads fixed at `18446744073709551615`; `SET` supports the documented baseline integer/boolean/default forms and top-level supported `SELECT` and `TABLE` statements without explicit `LIMIT` are capped by the session value. No mutable global state, safe-updates initialization, or implicit caps for internal DML/DDL source selects |
| `ALL` select modifier | 🟡 | Explicit `SELECT ALL` is accepted as the duplicate-preserving default for currently supported session scalar, scalar value projection, `DUAL`, descriptor-backed table column-list, table-backed wildcard, and aggregate select forms; no repeated `ALL`, mixed `ALL DISTINCT` / `ALL DISTINCTROW`, or other select modifiers |
| `DISTINCT` / `DISTINCTROW` | 🟡 | Limited `SELECT DISTINCT` and synonymous `SELECT DISTINCTROW` rowsets over one descriptor-backed persistent base table with optional source table alias, explicit descriptor columns, visible wildcard or qualified wildcard projection, source-qualified column references, baseline `WHERE`, optional `ORDER BY` resolving only to selected descriptor columns or selected-column aliases, and existing select `LIMIT`/`OFFSET`; supported selected columns are integer family, `YEAR`, canonical temporal types, and ASCII nonbinary `CHAR`/`VARCHAR`/baseline `TEXT` family columns using MyLite's registered `utf8mb4_0900_ai_ci` collation; successful supported statements return result rows, set `ROW_COUNT()` to `-1`, and produce zero warnings. No scalar/`DUAL` distinct, expressions, literals, unsupported descriptor families, joins, grouping, non-selected order keys, or protocol-grade metadata |
| `UNION` | 🟡 | Limited top-level unparenthesized chains of supported `SELECT` query blocks with `UNION`, `UNION DISTINCT`, and `UNION ALL`; the same unparenthesized compound source is also supported inside `INSERT ... SELECT` for current descriptor-backed single-table and no-source/`DUAL` row-scalar branches. `UNION` / `DISTINCT` remove duplicate rows with `NULL` equal to `NULL`, `UNION ALL` preserves duplicates, mixed chains follow MySQL's distinct-operator precedence over the accumulated left side, column labels come from the first branch for public result-set queries, and column-count mismatches return `1222 / 21000`; no parenthesized branches, global or branch-local `ORDER BY` / `LIMIT`, locking clauses, `SQL_CALC_FOUND_ROWS`, `UNION` inside `REPLACE ... SELECT` / CTAS / subqueries, joined/grouped/aggregate insert-source branches, type aggregation, collation aggregation, protocol-grade metadata merging, or streaming set-operation execution |
| `INTERSECT` | 🟡 | Limited top-level unparenthesized homogeneous chains of supported `SELECT` query blocks with `INTERSECT`, `INTERSECT DISTINCT`, and `INTERSECT ALL`; duplicate comparison uses `NULL`-equals-`NULL`, the current supported nonbinary ASCII string-collation subset, and bytewise binary values; result labels and metadata come from the first branch, and column-count mismatches return `1222 / 21000`. No mixed `UNION` / `INTERSECT` / `EXCEPT` precedence, parenthesized branches, global or branch-local `ORDER BY` / `LIMIT`, locking clauses, `SQL_CALC_FOUND_ROWS`, DML-source use, `TABLE` / `VALUES` operands, type aggregation, collation aggregation, protocol-grade metadata merging, or streaming set-operation execution |
| `EXCEPT` | 🟡 | Limited top-level unparenthesized homogeneous chains of supported `SELECT` query blocks with `EXCEPT`, `EXCEPT DISTINCT`, and `EXCEPT ALL`; duplicate comparison uses `NULL`-equals-`NULL`, the current supported nonbinary ASCII string-collation subset, and bytewise binary values; result labels and metadata come from the first branch, and column-count mismatches return `1222 / 21000`. No mixed `UNION` / `INTERSECT` / `EXCEPT` precedence, parenthesized branches, global or branch-local `ORDER BY` / `LIMIT`, locking clauses, `SQL_CALC_FOUND_ROWS`, DML-source use, `TABLE` / `VALUES` operands, type aggregation, collation aggregation, protocol-grade metadata merging, or streaming set-operation execution |
| `TABLE` | 🟡 | Limited top-level `TABLE table_name [ORDER BY descriptor_column ...] [LIMIT ...]` result-set statement over persistent or shadowing temporary base tables, translated to the descriptor-backed wildcard `SELECT` path; supports unqualified/schema-qualified names, visible-column projection, unqualified or table-qualified descriptor-column ordering over the current SELECT `ORDER BY` type subset, SELECT-style `LIMIT row_count`, `LIMIT row_count OFFSET offset`, and `LIMIT offset, row_count`, `@@sql_select_limit`, and `ROW_COUNT() = -1`; no `TABLE` in set operations, subqueries, DML sources, aliases, `WHERE`, locking clauses, modifiers, expression or ordinal ordering, `INTO OUTFILE`, or arbitrary SQLite pass-through |
| `VALUES` | 🟡 | Limited standalone `VALUES ROW(value[, ...]) [, ROW(value[, ...]) ...] [ORDER BY order_designator [ASC\|DESC] [, ...]] [LIMIT row_count] [LIMIT offset, row_count] [LIMIT row_count OFFSET offset]` result-set statement. Row values are signed decimal integer literals within MyLite's current signed-64 result range, ordinary string literals, `NULL`, `TRUE`, and `FALSE`; result labels are `column_0`, `column_1`, and so on; successful statements return rows through the public result API, set `ROW_COUNT()` to `-1`, report zero warnings, and do not require a selected schema. `ORDER BY` currently validates positive ordinals or implicit `column_N` labels but intentionally preserves constructor order for the observed MySQL 8.4.9 standalone behavior; `LIMIT` slices that constructor order. `DEFAULT`, empty rows, mismatched row widths, unknown order designators, signed or noninteger limits, aliases, `WHERE`, omitted `ROW`, arbitrary expressions, row constructors outside this top-level statement, nested/query-expression `VALUES`, and `VALUES` in set operations remain unsupported or rejected. | [baseline VALUES statement](../specs/baseline-values-statement/specs.md) |
| Locking clauses | 🟡 | Limited no-op `FOR UPDATE` and `FOR SHARE`, including optional `NOWAIT` or `SKIP LOCKED`, plus `LOCK IN SHARE MODE` on currently supported scalar, `DUAL`, descriptor table, distinct, aggregate, grouped, `SQL_CALC_FOUND_ROWS`, `INSERT ... SELECT`, and `REPLACE ... SELECT` paths; `CREATE TABLE ... SELECT` with a locking clause is rejected; no transaction lock behavior, `OF table`, repeated locking clauses, joins, subqueries, or privileges |
| `SQL_CALC_FOUND_ROWS` select modifier | 🟡 | Limited to existing non-distinct descriptor-backed single-base-table column-list and wildcard `SELECT` paths plus the current multi-source inner/cartesian/no-op `STRAIGHT_JOIN`/comma plain `SELECT` envelope and two-source left/right outer joined `SELECT` envelope, with supported aliases, baseline `WHERE`, descriptor-column `ORDER BY`, existing `LIMIT`/`OFFSET`, and simple no-op locking clauses where already admitted; records MySQL deprecation warning 1287 and updates `FOUND_ROWS()` state to the pre-limit matched row count; no scalar/`DUAL`, `DISTINCT`, grouped/aggregate-only, CTAS, `INSERT ... SELECT`, `REPLACE ... SELECT`, `UNION`, `TABLE`, CTE, subquery, locking options on joins, replication, or protocol-metadata behavior |
| SELECT modifiers | 🟡 | Explicit `ALL`, limited single-table rowset `DISTINCT` / `DISTINCTROW`, limited canonical no-op `HIGH_PRIORITY`, statement-level `STRAIGHT_JOIN`, `SQL_SMALL_RESULT`, `SQL_BIG_RESULT`, `SQL_BUFFER_RESULT`, deprecated no-op `SQL_NO_CACHE` with warning 1681, and limited `SQL_CALC_FOUND_ROWS`; table-reference `STRAIGHT_JOIN` is separately admitted as a no-op inner-join operator in the current joined source envelopes. No repeated modifiers, broad modifier permutations beyond the documented canonical sequence, `SQL_CACHE`, optimizer join-order forcing, or full modifier interactions |
| Expression metadata | ❌ | Type, flags, charset, origin |

[Back to compatibility overview](../../COMPATIBILITY.md)
