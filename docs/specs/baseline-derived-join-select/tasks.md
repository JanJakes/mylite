# Baseline Derived JOIN SELECT Tasks

- [x] Verify MySQL 8.4.9 behavior for WordPress-shaped left joins over grouped
      derived sources.
- [x] Plan grouped derived sources through the existing grouped aggregate path.
- [x] Expose derived source aliases and grouped aggregate output aliases to the
      outer joined query.
- [x] Add MySQL-runtime expectation coverage and focused MyLite runtime tests.
- [x] Document the supported derived-table source subset and remaining gaps.
