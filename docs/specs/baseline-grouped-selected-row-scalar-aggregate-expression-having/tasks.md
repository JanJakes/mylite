# Baseline Grouped Selected Row-Scalar Aggregate Expression HAVING Tasks

- [x] Record MySQL 8.4.9 expectations for selected row-scalar aggregate
  expressions repeated in grouped `HAVING`.
- [x] Add runtime tests for selected `COUNT()`, `SUM()`, `AVG()`, `MIN()`, and
  `MAX()` row-scalar aggregate expressions repeated in grouped `HAVING`.
- [x] Update compatibility documentation and related grouped `HAVING` specs.
- [x] Run focused MySQL expectations, focused CTest, format/check gates, and
  release-gate review before commit.
