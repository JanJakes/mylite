# String functions

String, byte, collation, pattern, base encoding, and regular-expression helpers.

| Function or operator | Status | Notes |
| --- | --- | --- |
| `ASCII()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/hex/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, string, baseline `TEXT`, binary string, `BIT`, `YEAR`, and temporal columns; returns the first byte value, returns `0` for empty non-`NULL` input, and returns `NULL` for `NULL`. No predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `BIT_LENGTH()` | 🟡 | Limited no-source, `DUAL`, `DO`, single-table row-scalar `SELECT` projection, and descriptor-backed `WHERE` truth/comparison/`IS NULL` predicate support over admitted string/integer/boolean/`NULL` literals, supported session scalar values, and descriptor-backed integer, exact `DECIMAL`, string, baseline `TEXT`, binary string, `BIT`, `YEAR`, and temporal columns; returns byte length multiplied by 8, with `NULL` yielding `NULL`. Predicate comparisons admit integer/boolean/`NULL` literals only. No DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `CHAR()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection over integer/boolean/`NULL` literals and descriptor-backed integer columns; returns default binary bytes, skips `NULL` arguments, and exposes embedded `NUL` through byte-safe result APIs. No `USING` charset forms, string/decimal/float coercion, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or full binary-string metadata |
| `CHAR_LENGTH()` | 🟡 | Limited no-source, `DUAL`, `DO`, single-table row-scalar `SELECT` projection, and descriptor-backed `WHERE` truth/comparison/`IS NULL` predicate support over admitted string/integer/boolean/`NULL` literals, supported session scalar values, and descriptor-backed integer, exact `DECIMAL`, string, baseline `TEXT`, binary string, `BIT`, `YEAR`, and temporal columns; returns UTF-8 character count for text values and byte count for binary string and `BIT` values, with `NULL` yielding `NULL`. Predicate comparisons admit integer/boolean/`NULL` literals only. No DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `CHARACTER_LENGTH()` | 🟡 | Synonym for the limited `CHAR_LENGTH()` subset, including the current descriptor-backed `WHERE` predicate support |
| `CHARSET()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted literals, `RAND()`, `DATABASE()` / `SCHEMA()`, binary cast/convert forms, `CONVERT(... USING utf8mb4)`, supported `CONCAT(...)`, and descriptor-backed column metadata. No full coercibility, explicit introducers, `COLLATE`, non-`utf8mb4` conversion, predicates, DML assignments, grouping, ordering expressions, parameters, or expression metadata |
| `COERCIBILITY()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, `RAND()`, `DATABASE()` / `SCHEMA()` / `VERSION()`, binary cast/convert forms, `CONVERT(... USING utf8mb4)`, supported scalar `CONCAT(...)`, and descriptor-backed column metadata. No explicit introducers, `COLLATE`, predicates, DML assignments, grouping, ordering expressions, parameters, broad expression metadata, or coercibility-driven comparison semantics |
| `COLLATION()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted literals, `RAND()`, `DATABASE()` / `SCHEMA()`, binary cast/convert forms, `CONVERT(... USING utf8mb4)`, supported `CONCAT(...)`, and descriptor-backed column metadata. No full coercibility, explicit introducers, `COLLATE`, non-`utf8mb4` conversion, predicates, DML assignments, grouping, ordering expressions, parameters, or expression metadata |
| `CONCAT()` / `\|\|` under `PIPES_AS_CONCAT` | 🟡 | Limited scalar projection support with one or more non-`CONCAT()` arguments over string, integer, boolean, `NULL`, existing session scalar values such as `DATABASE()` / `SCHEMA()` / supported `@@...` system variables, limited no-source/`DUAL` scalar subqueries, and descriptor-backed integer, exact `DECIMAL`, string, baseline `TEXT` family, and temporal columns in no-source, `DUAL`, and single-table row-scalar `SELECT` forms; any `NULL` argument returns `NULL`. The mode-sensitive `\|\|` form is also supported for `DO`. No nested `CONCAT()`, `CONCAT_WS()`, binary-string result typing, non-ASCII collation semantics, approximate numeric formatting, table-backed use outside the limited row-scalar SELECT path, DML assignments, predicates, ordering expressions, table-backed/DML subqueries, or expression metadata |
| `CONCAT_WS()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection over flat arguments; `NULL` separator returns `NULL`, later `NULL` values are skipped, and empty strings are retained. Supports string/integer/boolean/`NULL` literals, selected session scalar values, limited no-source/`DUAL` scalar subqueries, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns. No nested `CONCAT()`/`CONCAT_WS()`, binary-string result typing, binary string or `BIT` column operands, approximate columns, table-backed use outside row-scalar SELECT projection, DML assignments, predicates, ordering/grouping expressions, parameters, or expression metadata |
| `ELT()` | 🟡 | Limited no-source, `DUAL`, and `DO` scalar `ELT(index, value[, ...])` over signed-64 integer/boolean/`NULL` selector literals and ASCII string, signed-64 integer/boolean/`NULL` value literals; returns the selected 1-based value or `NULL` for `NULL`, nonpositive, or out-of-range selectors; no string/decimal/float selector coercion, binary or non-ASCII strings, table-backed expressions, predicates, ordering, DML assignments, defaults, nested functions, subqueries, parameters, or arbitrary expressions |
| `EXPORT_SET()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection over signed-64 integer/boolean/`NULL` bitmasks, admitted scalar and descriptor-backed string value arguments, and literal signed-64/boolean/`NULL` bit counts; returns bit-selected text from least-significant bit with MySQL's default/comma/count/null behavior. No warning-producing string/decimal/float bitmask conversion, binary result typing, predicates, DML assignments, ordering/grouping expressions, nested functions, parameters, arbitrary expressions, or expression metadata |
| `FIELD()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection over flat all-string or all-integer argument lists with `NULL`, ASCII string literals, signed-64 integer/boolean literals, and supported descriptor columns; limited supported single-table `SELECT ORDER BY FIELD(column, value[, ...])` uses the same flat string or integer candidate domain for one hidden order key; returns the 1-based first-match position or `0`; no mixed domains, binary strings, non-ASCII collation parity, predicates, DML assignments, grouped/aggregate ordering, multiple order keys with `FIELD()`, or general expression metadata |
| `FIND_IN_SET()` | 🟡 | Limited no-source, `DUAL`, `DO`, single-table row-scalar `SELECT` projection, and limited descriptor `SELECT`/`UPDATE`/`DELETE` predicate support over flat ASCII string/integer/boolean/`NULL` scalar values, supported session scalar/system-variable values, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `ENUM`, `YEAR`, and temporal columns; returns the 1-based first comma-list member position, `0`, or `NULL`. Empty members count as positions and ASCII matching is case-insensitive. No MySQL `SET` column member-ordinal behavior, binary strings, approximate columns, non-ASCII collation parity, nested functions, parameters, arbitrary expressions, ordering/grouping expressions, DML assignments, or expression metadata |
| `FROM_BASE64()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string, hex, integer, boolean, `NULL`, supported session scalar, supported system variable, binary cast/convert, and descriptor-backed integer, nonbinary string, baseline `TEXT`, binary string, and baseline BLOB-family values; decodes MySQL Base64 text, ignores ASCII space/tab/CR/LF, returns binary bytes, returns a non-`NULL` empty binary value for empty input, and returns `NULL` without warnings for supported invalid input. No predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or protocol binary metadata parity |
| `HEX()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string, hex, integer, boolean, `NULL`, supported session scalar, system variable, and binary cast/convert arguments, plus descriptor-backed integer, nonbinary string, baseline `TEXT`, binary string, and baseline `BLOB` columns; returns uppercase byte hex for string/binary values, unsigned 64-bit numeric hex for integer/boolean and supported numeric scalar/system-variable values, and `NULL` for `NULL`. No decimal/float/`BIT`/temporal row-backed conversion, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, binary result metadata, or expression metadata |
| `INSERT()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; performs UTF-8 character-position substring insertion/replacement and returns `NULL` when any argument is `NULL`. `pos` and `len` are limited to signed-64 decimal integer, boolean, and `NULL` literals. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, warning-producing string/decimal `pos`/`len` conversion, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `INSTR()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; returns the 1-based first ASCII case-insensitive substring position or `0`, and returns `NULL` for `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, non-ASCII collation parity, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `LCASE()` | 🟡 | Synonym for the limited `LOWER()` subset |
| `LEFT()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; length is limited to integer/boolean/`NULL` literals, and UTF-8 text is sliced by character position. No binary-string slicing, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `LENGTH()` | 🟡 | Limited no-source, `DUAL`, `DO`, single-table row-scalar `SELECT` projection, and descriptor-backed `WHERE` truth/comparison/`IS NULL` predicate support over admitted string/integer/boolean/`NULL` literals, supported session scalar values, and descriptor-backed integer, exact `DECIMAL`, string, baseline `TEXT`, binary string, `BIT`, `YEAR`, and temporal columns; returns byte length, with `NULL` yielding `NULL`. Predicate comparisons admit integer/boolean/`NULL` literals only. No DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `LOCATE()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; optional start position is limited to integer/boolean/`NULL` literals, matching is ASCII case-insensitive, empty substring follows MySQL's verified start-position behavior, and `NULL` arguments return `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, non-ASCII collation parity, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `LOWER()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over ASCII string/integer/boolean/`NULL` literals, supported session scalar values, system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; converts ASCII letters to lowercase and returns `NULL` for `NULL`. Non-ASCII text and embedded `NUL` values are rejected in this baseline; no Unicode/collation folding, binary-string result typing, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `LPAD()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; length is limited to integer/boolean/`NULL` literals, padding/truncation is by UTF-8 character count, and any `NULL` argument returns `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `LTRIM()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; removes leading ASCII space and returns `NULL` for `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `MAKE_SET()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection over signed-64 integer/boolean/`NULL` bitmasks and admitted scalar or descriptor-backed string value arguments; selected non-`NULL` values join with commas, `NULL` bitmask returns `NULL`, and selected `NULL` values are skipped. No warning-producing string/decimal/float bitmask conversion, binary result typing, predicates, DML assignments, ordering/grouping expressions, nested functions, parameters, arbitrary expressions, or expression metadata |
| `MID()` | 🟡 | Synonym for the limited `SUBSTRING()` subset, including the current descriptor-backed `WHERE` predicate support |
| `OCTET_LENGTH()` | 🟡 | Synonym for the limited `LENGTH()` subset, including the current descriptor-backed `WHERE` predicate support |
| `ORD()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/hex/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, string, baseline `TEXT`, binary string, `BIT`, `YEAR`, and temporal columns; returns MySQL-style left-to-right byte-packed first-character code for nonbinary UTF-8 text, first-byte behavior for binary values, `0` for empty non-`NULL` input, and `NULL` for `NULL`. No predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `POSITION()` | 🟡 | Limited `POSITION(substr IN str)` synonym for the supported two-argument `LOCATE()` subset, with the same scalar, row-scalar, ASCII matching, and unsupported-expression limits |
| `QUOTE()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection over string/integer/fixed-decimal/boolean/`NULL` scalar values, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; returns SQL-quoted text and returns the text `NULL` for SQL `NULL` input. Escapes only backslash, single quote, ASCII NUL, and Control+Z in the current subset. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `REGEXP_INSTR()` | ❌ | Starting index of substring matching regular expression |
| `REGEXP_LIKE()` | 🟡 | Limited no-source, `DUAL`, `DO`, single-table row-scalar `SELECT` projection, and descriptor-backed `SELECT`/`UPDATE`/`DELETE` predicate support over admitted ASCII string/integer/boolean/`NULL` scalar values, supported session scalar/system-variable value operands, and nonbinary string descriptor columns; patterns are scalar literals in MyLite's baseline ASCII regular-expression subset, optional `match_type` admits only lowercase `c` and `i`, and `NULL` operands return `NULL`. No binary arguments, ICU/full-Unicode regex semantics, `m`/`n`/`u` match flags, row-backed pattern or match-type operands, nested functions, parameters, arbitrary expressions, ordering/grouping expressions, DML assignments, or expression metadata |
| `REGEXP_REPLACE()` | ❌ | Replace substrings matching regular expression |
| `REGEXP_SUBSTR()` | ❌ | Return substring matching regular expression |
| `REPEAT()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; count is limited to integer/boolean/`NULL` literals, nonpositive counts return the empty string, and any `NULL` argument returns `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `REPLACE()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; performs exact case-sensitive non-overlapping byte-sequence replacement and returns `NULL` when any argument is `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `REVERSE()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; reverses UTF-8 text by character sequence and returns `NULL` for `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `RIGHT()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; length is limited to integer/boolean/`NULL` literals, and UTF-8 text is sliced by character position. No binary-string slicing, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `RPAD()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; length is limited to integer/boolean/`NULL` literals, padding/truncation is by UTF-8 character count, and any `NULL` argument returns `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `RTRIM()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; removes trailing ASCII space and returns `NULL` for `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `SOUNDEX()` | ❌ | Return a soundex string |
| `SPACE()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support; count is limited to integer/boolean/`NULL` literals, nonpositive counts return the empty string, `NULL` returns `NULL`, and the result contains ASCII spaces. No predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `STRCMP()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection over flat ASCII string/integer/boolean/`NULL` scalar values, supported session scalar/system-variable values, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, `DATE`, `DATETIME`, and `TIMESTAMP` columns; returns `-1`, `0`, `1`, or `NULL`, compares ASCII case-insensitively, and treats trailing spaces as significant. No binary strings, `TIME` columns, approximate columns, non-ASCII collation parity, nested functions, parameters, arbitrary expressions, predicates, ordering/grouping expressions, DML assignments, or expression metadata |
| `SUBSTR()` | 🟡 | Synonym for the limited `SUBSTRING()` subset, including the current descriptor-backed `WHERE` predicate support |
| `SUBSTRING()` | 🟡 | Limited no-source, `DUAL`, `DO`, single-table row-scalar `SELECT` projection, and descriptor-backed `WHERE` comparison/`IS NULL` predicate support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; supports comma and `FROM` / `FOR` forms with literal integer/boolean/`NULL` positions and optional lengths, with UTF-8 text sliced by character position. Predicate comparisons admit ASCII string or `NULL` literals and use the current ASCII case-insensitive collation subset. No binary-string slicing, string/numeric position coercion, bare substring truth predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `SUBSTRING_INDEX()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal value or delimiter columns; count is limited to integer/boolean/`NULL` literals, delimiter matching is exact, case-sensitive, and non-overlapping, and any `NULL` argument returns `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, string/numeric count coercion, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `TO_BASE64()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string, hex, integer, boolean, `NULL`, supported session scalar, supported system variable, binary cast/convert, and descriptor-backed integer, nonbinary string, baseline `TEXT`, binary string, and baseline BLOB-family values; encodes bytes with MySQL's Base64 alphabet, `=` padding, and newline wrapping after each complete 76-character output line that has more output, with `NULL` yielding `NULL`. No predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or charset/collation metadata parity |
| `TRIM()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string/integer/boolean/`NULL` literals, supported session scalar values and system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; supports default ASCII-space trimming, `remstr FROM`, and `LEADING` / `TRAILING` / `BOTH` forms with scalar remove strings, using exact repeated byte-sequence removal, and returns `NULL` when either value or remove string is `NULL`. No binary-string result typing, binary string or `BIT` row-backed inputs, approximate numeric row-backed inputs, row-backed remove strings, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `UCASE()` | 🟡 | Synonym for the limited `UPPER()` subset |
| `UNHEX()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over admitted string, hex, integer, boolean, `NULL`, supported session scalar, system variable, and binary cast/convert arguments, plus descriptor-backed integer, nonbinary string, baseline `TEXT`, binary string, and baseline `BLOB` columns; decodes ASCII hex input bytes to binary bytes, treats odd-length input as leading-zero padded, returns a non-`NULL` empty binary value for empty input, returns `NULL` for `NULL`, and returns `NULL` with warning `1411` for invalid non-hex input. No decimal/float/`BIT`/temporal row-backed conversion, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, complete binary metadata, or expression metadata |
| `UPPER()` | 🟡 | Limited no-source, `DUAL`, `DO`, and single-table row-scalar `SELECT` projection support over ASCII string/integer/boolean/`NULL` literals, supported session scalar values, system variables, and descriptor-backed integer, exact `DECIMAL`, nonbinary string, baseline `TEXT`, `YEAR`, and temporal columns; converts ASCII letters to uppercase and returns `NULL` for `NULL`. Non-ASCII text and embedded `NUL` values are rejected in this baseline; no Unicode/collation folding, binary-string result typing, predicates, DML assignments, ordering/grouping expressions, nested functions, subqueries, parameters, arbitrary expressions, or expression metadata |
| `WEIGHT_STRING()` | ❌ | Return weight string for a string |

[Back to compatibility overview](../../COMPATIBILITY.md)
