# SQL table DML

Table-shaped data modification, import, replace, and direct table access
statements.

| Feature | Status | Notes |
| --- | --- | --- |
| `DELETE` (single-table) | 🟡 | Limited persistent base-table `DELETE FROM table` with optional baseline integer/`NULL` `WHERE` predicate boolean expression, including keyword `NOT`, `AND`/`&&`, `XOR`, `OR`/`\|\|`, limited `BETWEEN` range predicates, limited `IN` membership predicates, descriptor-column `IS [NOT] TRUE` / `FALSE` / `UNKNOWN`, limited ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string-literal equality/null-safe equality/inequality predicates, and limited canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` string-literal predicates, one unqualified integer, `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` descriptor `ORDER BY` column, optional `ASC`/`DESC`, exact affected rows, and `LIMIT row_count` using unsigned decimal literals in signed 64-bit range; admitted string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, and `TIME` range predicates and ordering use signed total-second semantics; no aliases, partitions, modifiers, joined deletes, `USING`, string ranges/membership predicates, non-ASCII string predicate literals, full ordering, offset forms, triggers, cascades, foreign keys, or privilege semantics |
| `DELETE` (multi-table) | ❌ | Multi-table forms and affected rows |
| `HANDLER` | ❌ | HANDLER OPEN, READ, and CLOSE cursor-like table access |
| `INSERT ... VALUES` | 🟡 | Limited single- and multi-row `INSERT [LOW_PRIORITY \| HIGH_PRIORITY \| DELAYED] [IGNORE] [INTO] table_name ... VALUES ...` into persistent base tables with optional unqualified column lists, integer/fixed decimal/approximate numeric/`NULL`/`TRUE`/`FALSE`/`DEFAULT` values plus ordinary string literals for `CHAR`, `VARCHAR`, baseline `TEXT` family, and canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` targets, descriptor-driven assignment, omitted columns and explicit `DEFAULT` values filled from descriptor numeric/string/temporal defaults or effective `NULL` defaults, limited generated values for the current indexed auto-increment subset, strict range/nullability/no-default/string-length/temporal diagnostics including decimal rounding notes, approximate range checks, and default-mode `CHAR` trimming, duplicate enforcement for the current supported primary-key and unique-index subset, affected rows, all-or-nothing statement behavior, no-op `LOW_PRIORITY` / `HIGH_PRIORITY`, warning-producing deprecated `DELAYED`, and limited `IGNORE` warning demotion for omitted no-explicit-default columns, explicit `DEFAULT` for no-default columns, explicit `NULL` into numeric, string, or temporal `NOT NULL`, integer/decimal range clipping, invalid temporal-to-zero adjustment, `TIME` endpoint clipping, and duplicate key rows; no-column-list inserts target visible descriptor columns only, while explicit column lists may name invisible columns; nullable dropped-default columns warn and store `NULL`, numeric `NOT NULL` no-default columns warn and store target zero values, `DATE NOT NULL` no-default columns warn and store `0000-00-00`, `TIME NOT NULL` no-default columns warn and store `00:00:00`, `DATETIME NOT NULL` and `TIMESTAMP NOT NULL` no-default columns warn and store `0000-00-00 00:00:00`, and string `NOT NULL` no-default columns warn and store empty strings; non-ASCII values in admitted string key columns remain non-ignorable errors; no mixed-mode multi-row auto-increment gap emulation, `DEFAULT(col_name)`, nonspace overlength string truncation warning demotion, unsupported `ON DUPLICATE KEY UPDATE` shapes outside the limited duplicate-key tail, string-to-decimal/approximate/temporal conversion outside canonical strings, or `SELECT` form |
| `INSERT ... SET` | 🟡 | Limited one-row `INSERT [LOW_PRIORITY \| HIGH_PRIORITY \| DELAYED] [IGNORE] [INTO] table_name SET column_name = value[, ...]` into persistent base tables with unqualified assignment columns, supported decimal integer/fixed decimal/approximate numeric/`NULL`/`TRUE`/`FALSE`/`DEFAULT` values plus ordinary string literals for `CHAR`, `VARCHAR`, baseline `TEXT` family, and canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` targets, descriptor-driven assignment including invisible columns when explicitly named, omitted columns and explicit `DEFAULT` values filled from descriptor numeric/string/temporal defaults or effective `NULL` defaults, limited generated values for the current indexed auto-increment subset, strict required-column/range/nullability/string-length/temporal diagnostics including decimal rounding notes, approximate range checks, and default-mode `CHAR` trimming, duplicate enforcement for the current supported primary-key and unique-index subset, one affected row, all-or-nothing statement behavior, no-op `LOW_PRIORITY` / `HIGH_PRIORITY`, warning-producing deprecated `DELAYED`, and limited `IGNORE` warning demotion for omitted no-explicit-default columns, explicit `DEFAULT` for no-default columns, explicit `NULL` into numeric, string, or temporal `NOT NULL`, integer/decimal range clipping, invalid temporal-to-zero adjustment, `TIME` endpoint clipping, and duplicate key rows; nullable dropped-default columns warn and store `NULL`, numeric `NOT NULL` no-default columns warn and store target zero values, `DATE NOT NULL` no-default columns warn and store `0000-00-00`, `TIME NOT NULL` no-default columns warn and store `00:00:00`, `DATETIME NOT NULL` and `TIMESTAMP NOT NULL` no-default columns warn and store `0000-00-00 00:00:00`, and string `NOT NULL` no-default columns warn and store empty strings; non-ASCII values in admitted string key columns remain non-ignorable errors; no table-qualified assignment targets, expression assignments, `DEFAULT(col_name)`, nonspace overlength string truncation warning demotion, aliases, partitions, unsupported `ON DUPLICATE KEY UPDATE` shapes outside the limited duplicate-key tail, string-to-decimal/approximate/temporal conversion outside canonical strings, or `SELECT` form |
| `INSERT ... SELECT` | 🟡 | Limited descriptor-backed `INSERT [LOW_PRIORITY \| HIGH_PRIORITY \| DELAYED] [INTO] target [(column_name[, ...])] SELECT ... FROM source` for persistent base tables without primary-key descriptors, reusing the existing single-table source `SELECT` subset with optional table alias, `WHERE`, one-column integer, `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` `ORDER BY`, `LIMIT`, and simple no-op source locking clauses; target column lists are descriptor-driven, omitted targets receive descriptor numeric/string/temporal defaults or effective nullable `NULL` only for rows actually inserted, source/target `*` and omitted target lists use visible columns, explicit columns may name invisible columns, selected integer/`NULL`, compatible decimal text, compatible approximate `REAL` values, compatible canonical date/time/datetime/timestamp text admitted by the current limited SQL-mode temporal rules, and compatible string `TEXT` values are validated against target descriptors, `LOW_PRIORITY` / `HIGH_PRIORITY` are no-ops, and `DELAYED` records MySQL's deprecated-delayed warning; no key-bearing targets, `IGNORE`, partitions, `ON DUPLICATE KEY UPDATE`, `TABLE`, row constructors, joins, CTEs, literal/expression projection sources, auto-increment, triggers, generated columns, locking options, warning demotion, string/integer/decimal/approximate/temporal conversion outside compatible descriptors, or general expression conversion |
| `INSERT ... ON DUPLICATE KEY UPDATE` | 🟡 | Limited `INSERT [LOW_PRIORITY \| HIGH_PRIORITY \| DELAYED] [INTO] table_name ... VALUES ... ON DUPLICATE KEY UPDATE column_name = value` and one-row `INSERT ... SET ... ON DUPLICATE KEY UPDATE column_name = value` for persistent base tables, with one unqualified duplicate assignment target, supported descriptor insert value literals including `NULL`, `TRUE`, `FALSE`, `DEFAULT`, decimal/fixed decimal/string/temporal literals already admitted by `INSERT`, and deprecated same-target `VALUES(column_name)` references to the proposed row with warning `1287`; duplicate conflicts are handled for the current single-column primary-key or single unique-index subset, no-key tables insert normally, successful affected rows follow MySQL's inserted-row `1`, changed-duplicate `2`, unchanged-duplicate `0` behavior, generated auto-increment duplicate attempts advance the next generated value without replacing `LAST_INSERT_ID()`, and supported in-range duplicate updates preserve statement atomicity and file-backed persistence; `LOW_PRIORITY` and `HIGH_PRIORITY` are no-ops and `DELAYED` records warning `3005`; no `IGNORE`, `INSERT ... SELECT`, aliases, partitions, multiple assignments, qualified assignment targets, qualified or cross-column `VALUES()` references, key-column duplicate assignments, multiple or composite enforced keys, expression assignments, `DEFAULT(column_name)`, column-to-column assignments, general expression conversion, triggers, cascades, generated columns, or privilege semantics |
| `INSERT IGNORE` | 🟡 | Limited `INSERT IGNORE ... VALUES` and `INSERT IGNORE ... SET` warning demotion for descriptor integer/decimal/approximate/date/time/datetime/timestamp/`NULL`/`DEFAULT` inputs, admitted string null/default adjustments, generated values for the current indexed auto-increment subset, and duplicate rows for the current supported primary-key and unique-index subset; nonconflicting rows continue and affected rows count rows actually inserted. Omitted no-explicit-default columns and explicit `DEFAULT` for no-default columns warn and store target zero values for numeric `NOT NULL`, `0000-00-00` for `DATE NOT NULL`, `00:00:00` for `TIME NOT NULL`, `0000-00-00 00:00:00` for `DATETIME NOT NULL` or `TIMESTAMP NOT NULL`, empty string for string `NOT NULL`, or `NULL` for nullable dropped-default columns, explicit `NULL` into numeric `NOT NULL` stores the target zero value, explicit `NULL` into `DATE NOT NULL` stores `0000-00-00`, explicit `NULL` into `TIME NOT NULL` stores `00:00:00`, explicit `NULL` into `DATETIME NOT NULL` or `TIMESTAMP NOT NULL` stores `0000-00-00 00:00:00`, and explicit `NULL` into string `NOT NULL` stores empty string with warnings, out-of-range integer and decimal values clip to the supported descriptor range with warnings, out-of-range `TIME` values clip to `-838:59:59` or `838:59:59`, invalid temporal values store the target zero temporal value with warnings, and non-ignorable syntax/name/shape errors plus unsupported non-ASCII string-key values remain errors; no `INSERT IGNORE ... SELECT`, partitions, `DEFAULT(col_name)`, overlength string warning demotion, string-to-decimal/approximate/temporal conversion outside canonical strings, hex/bit conversion, general expression conversion, or full non-strict mode behavior |
| `INSERT DELAYED` | 🟡 | Limited modifier support on currently supported `INSERT ... VALUES`, `INSERT ... SET`, and descriptor-backed `INSERT ... SELECT` forms; executes as ordinary `INSERT` and records warning `3005`; `VALUES` and `SET` may combine `DELAYED IGNORE` with delayed warning before adjustment warnings; no delayed queues, background writes, storage scheduling, or `INSERT IGNORE ... SELECT` |
| `INSERT LOW_PRIORITY` / `HIGH_PRIORITY` | 🟡 | Limited no-op priority modifier support on currently supported `INSERT ... VALUES`, `INSERT ... SET`, and descriptor-backed `INSERT ... SELECT` forms; `VALUES` and `SET` may combine one priority modifier before `IGNORE`; no table-lock scheduling semantics, multiple modifiers, or `INSERT IGNORE ... SELECT` |
| `LOAD DATA INFILE` | ❌ | Server-side import syntax |
| `LOAD DATA LOCAL INFILE` | ❌ | LOCAL INFILE flow and security |
| `LOAD XML INFILE` | ❌ | XML import syntax |
| `LOAD XML LOCAL INFILE` | ❌ | Client-side XML import request behavior diagnostics |
| `REPLACE ... VALUES` | 🟡 | Limited no-key-descriptor `REPLACE [LOW_PRIORITY \| DELAYED] [INTO] table_name [(column_name[, ...])] VALUES (value[, ...])[, ...]` insert-equivalent path for persistent base tables without primary-key descriptors, with integer/fixed decimal/approximate numeric/`NULL`/`TRUE`/`FALSE`/`DEFAULT` values plus ordinary string literals for `CHAR`, `VARCHAR`, baseline `TEXT` family, and canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` targets, descriptor column resolution including explicitly named invisible columns, omitted-column and explicit-`DEFAULT` descriptor default filling, strict range/nullability/no-default/string-length/temporal diagnostics including decimal rounding notes, approximate range checks, and default-mode `CHAR` trimming, affected rows equal to inserted rows, all-or-nothing statement behavior, no-op `LOW_PRIORITY`, and warning-producing deprecated `DELAYED`; key-bearing targets are rejected until delete-insert semantics are specified, and there is no `VALUE` synonym, row constructors, partitions, `DEFAULT(col_name)`, generated values, warning demotion, nonspace overlength string truncation warning demotion, string-to-decimal/approximate/temporal conversion outside canonical strings, or insert ids |
| `REPLACE ... SET` | 🟡 | Limited no-key-descriptor one-row `REPLACE [LOW_PRIORITY \| DELAYED] [INTO] table_name SET column_name = value[, ...]` insert-equivalent path for persistent base tables without primary-key descriptors, with unqualified descriptor assignment targets, integer/fixed decimal/approximate numeric/`NULL`/`TRUE`/`FALSE`/`DEFAULT` values plus ordinary string literals for `CHAR`, `VARCHAR`, baseline `TEXT` family, and canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` targets, omitted-column and explicit-`DEFAULT` descriptor default filling, strict range/nullability/no-default/string-length/temporal diagnostics including decimal rounding notes, approximate range checks, and default-mode `CHAR` trimming, one affected row, all-or-nothing statement behavior, no-op `LOW_PRIORITY`, and warning-producing deprecated `DELAYED`; key-bearing targets are rejected until delete-insert semantics are specified, and there are no partitions, qualified assignment targets, expression assignments, `DEFAULT(col_name)`, `VALUES`, `SELECT` forms, nonspace overlength string truncation warning demotion, or string-to-decimal/approximate/temporal conversion outside canonical strings |
| `REPLACE ... SELECT` | 🟡 | Limited no-key-descriptor `REPLACE [LOW_PRIORITY \| DELAYED] [INTO] target [(column_name[, ...])] SELECT ... FROM source` insert-equivalent path for persistent base tables without primary-key descriptors, reusing the existing single-table source `SELECT` subset with optional table alias, `WHERE`, one-column integer, `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` `ORDER BY`, `LIMIT`, and simple no-op source locking clauses; target column lists are descriptor-driven, omitted targets receive descriptor numeric/string/temporal defaults or effective nullable `NULL` only for rows actually inserted, source/target `*` and omitted target lists use visible columns, explicit columns may name invisible columns, and selected integer/`NULL`, compatible decimal text, compatible approximate `REAL` values, compatible canonical date/time/datetime/timestamp text admitted by the current limited SQL-mode temporal rules, plus compatible string `TEXT` values are validated against target descriptors; key-bearing targets are rejected until delete-insert semantics are specified, and there are no partitions, `TABLE`, joins, CTEs, literal/expression projection sources, generated values, locking options, warning demotion, string/integer/decimal/approximate/temporal conversion outside compatible descriptors, or insert ids |
| `REPLACE LOW_PRIORITY` / `DELAYED` | 🟡 | Limited modifier support on currently supported no-key `REPLACE` forms: `LOW_PRIORITY` is accepted as a no-op and `DELAYED` executes as ordinary `REPLACE` with warning `3005`; no multiple modifiers, `HIGH_PRIORITY`, delayed queues, storage scheduling changes, or duplicate-key replacement semantics |
| `UPDATE` (single-table) | 🟡 | Limited persistent base-table `UPDATE table_name SET column_name = value` with one unqualified assignment column, supported decimal integer/fixed decimal/approximate numeric/`NULL`/`TRUE`/`FALSE`/`DEFAULT` assignment values plus ordinary string literals for `CHAR`, `VARCHAR`, baseline `TEXT` family, canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` targets admitted by the current limited SQL-mode temporal rules, and limited uncorrelated table-backed scalar subquery assignments of one descriptor source column copied through compatible descriptor families; optional baseline `WHERE` predicate boolean expression including keyword `NOT`, `AND`/`&&`, `XOR`, `OR`/`\|\|`, limited integer and canonical temporal string-literal `BETWEEN` range predicates, limited integer and canonical temporal string-literal `IN` membership predicates, limited ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string-literal equality/null-safe equality/inequality predicates, descriptor-column `IS [NOT] TRUE` / `FALSE` / `UNKNOWN` for integer columns, and `IS NULL` / `IS NOT NULL` for integer, decimal, approximate, string, or temporal columns, optional one-column integer, `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` `ORDER BY` with `ASC`/`DESC`, optional `LIMIT row_count` using unsigned decimal literals in signed 64-bit range, duplicate-key errors when updating the current supported primary-key or unique-index subset to an existing value, and changed-row affected counts after decimal/approximate/temporal canonicalization and default-mode `CHAR` trimming; scalar assignment subqueries reuse the current single-table descriptor `SELECT` source envelope, evaluate only when the outer update matches rows, return `NULL` for empty scalar results, return `1242 / 21000` for matched multi-row scalar results, and reject same-table source reads with `1093 / HY000`; admitted string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, `TIME` range predicates and ordering use signed total-second semantics rather than raw text ordering, assignment, predicate, and ordering columns resolve through descriptors and may explicitly name invisible columns, and non-ASCII values in admitted string key columns are rejected before mutation; no aliases, table-qualified assignment targets, multiple assignments, expression assignments outside the limited scalar subquery assignment subset, implicit non-`NULL` string/integer/decimal/approximate/temporal conversion outside compatible descriptors, `DEFAULT(col_name)`, partitions, modifiers, joined updates, multi-table updates, correlated subqueries, subquery predicates, derived tables, string ranges/membership/order predicates, non-ASCII string predicate literals, decimal or approximate ordering/comparison predicates, table-qualified/order expression/ordinal/multiple-key ordering, offset forms, triggers, cascades, foreign keys, generated columns, or privilege semantics |
| `UPDATE` (multi-table) | ❌ | Joined update semantics |
| SQL safe updates variable | 🟡 | Limited scalar `@@sql_safe_updates` reads report fixed disabled value `0`; no mutable safe-updates state, `sql_select_limit`, `max_join_size`, key-aware DML checks, or changed `UPDATE`/`DELETE` behavior |
| SQL warnings variable | 🟡 | Limited scalar `@@sql_warnings` reads report fixed disabled value `0`; limited `INSERT IGNORE ... VALUES` / `SET` adjustment warnings are still recorded in diagnostics like MySQL; no mutable warning-reporting state, broader warning-producing insert conversions, strict warning demotion beyond the limited `INSERT IGNORE` subset, or single-row insert information strings |

[Back to compatibility overview](../../COMPATIBILITY.md)
