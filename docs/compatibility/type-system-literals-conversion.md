# Type system, literals, and conversion

Ordinary non-strict DML currently has a narrow MySQL-compatible adjustment
surface: when neither `STRICT_TRANS_TABLES` nor `STRICT_ALL_TABLES` is active,
omitted no-explicit-default non-`AUTO_INCREMENT` `INSERT` / `REPLACE` target
columns and explicit `DEFAULT` values for no-default columns use the same
implicit descriptor values as the existing `INSERT IGNORE` subset, and matched
single-table `UPDATE` assignments of `NULL` to `NOT NULL` columns or `DEFAULT`
to no-default columns adjust with warnings. Omitted `AUTO_INCREMENT` columns
continue to generate values. Explicit `NULL` in ordinary `INSERT` / `REPLACE`
remains an error outside `IGNORE`, and broader non-strict string, numeric,
temporal, range, and expression coercions remain outside the current subset.

## Numeric types

| Feature | Status | Notes |
| --- | --- | --- |
| `TINYINT` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion with MyLite's current descriptor-range literal check, and single-column sort support; only signed `TINYINT(1)` is persisted/rendered with width, other widths normalize away with warning 1681; no `ZEROFILL`, repeated/combined attributes, aliases, general expression defaults beyond the limited parenthesized constant integer subset, general expression semantics, compact physical storage, or protocol-grade result metadata |
| `SMALLINT` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion with MyLite's current descriptor-range literal check, and single-column sort support; display widths normalize away with warning 1681; no `ZEROFILL`, repeated/combined attributes, aliases, general expression defaults beyond the limited parenthesized constant integer subset, general expression semantics, compact physical storage, or protocol-grade result metadata |
| `MEDIUMINT` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion with MyLite's current descriptor-range literal check, and single-column sort support; display widths normalize away with warning 1681; no `ZEROFILL`, repeated/combined attributes, aliases, general expression defaults beyond the limited parenthesized constant integer subset, general expression semantics, compact physical storage, or protocol-grade result metadata |
| `INT` / `INTEGER` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion, and single-column sort support; display widths normalize away with warning 1681; no general expression defaults beyond the limited parenthesized constant integer subset, general expression semantics, `ZEROFILL`, repeated/combined attributes, or protocol-grade result metadata |
| `BIGINT` | 🟡 | Limited DDL descriptors plus optional single explicit `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]` descriptor replacement, integer/`NULL` `INSERT`/`REPLACE` row and `UPDATE` assignment conversion, text readback, descriptor-driven filtered `SELECT`/`DELETE`/`UPDATE` predicate conversion, and single-column sort support; display widths normalize away with warning 1681; `BIGINT UNSIGNED` defaults and row values are capped at the signed 64-bit SQLite integer range in this slice; no `ZEROFILL`, repeated/combined attributes, general expression defaults beyond that integer subset, or protocol-grade result metadata |
| Integer type aliases | 🟡 | Limited `INT1`, `INT2`, `INT3`, `INT4`, and `INT8` aliases normalize to existing integer-family descriptors, with optional single `SIGNED` or `UNSIGNED`, deprecated display width `0..255`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, descriptor-driven DDL/DML/conversion/introspection coverage, and normalized type rendering; only signed `INT1(1)` persists/renders as `tinyint(1)`, other alias widths normalize away with warning 1681; no `ZEROFILL`, combined/repeated attributes, general expression defaults beyond that integer subset, casts, or protocol-grade metadata |
| `DECIMAL` / `NUMERIC` | 🟡 | Limited exact fixed-point descriptors, optional deprecated `UNSIGNED`, exact canonical `TEXT` storage/readback, decimal defaults, row DML conversion with scale padding, half-away-from-zero rounding, truncation notes, range diagnostics, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no decimal comparison/order/math, `ZEROFILL`, decimal primary keys, auto-increment, string/float/hex/bit conversion, casts, or protocol-grade numeric metadata |
| `FIXED` | 🟡 | Limited alias for the supported `DECIMAL` descriptor subset, normalized to `decimal(M,D)` metadata and storage; same gaps as `DECIMAL` |
| `FLOAT` | 🟡 | Limited approximate numeric descriptors stored as SQLite `REAL`, including bare `FLOAT`, `FLOAT(0..24)`, and `FLOAT4`, optional deprecated `UNSIGNED` with warning 1681, `DEFAULT NULL` or compatible numeric literal defaults, row DML conversion from integer/fixed-decimal/approximate/boolean/`NULL`/`DEFAULT` literals, single-precision rounding/readback text, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `FLOAT(M,D)`, `ZEROFILL`, `SIGNED`, string/hex/bit conversion, comparison/order/math, indexes, primary keys, auto-increment, casts, or protocol-grade numeric metadata |
| `DOUBLE` / `REAL` | 🟡 | Limited approximate numeric descriptors stored as SQLite `REAL`, including bare `DOUBLE`, `DOUBLE PRECISION`, default-mode `REAL`, `FLOAT(25..53)`, and `FLOAT8`, optional deprecated `UNSIGNED` with warning 1681, `DEFAULT NULL` or compatible numeric literal defaults, row DML conversion from integer/fixed-decimal/approximate/boolean/`NULL`/`DEFAULT` literals, double readback text, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, `REAL_AS_FLOAT` mapping of later `REAL` column definitions to the supported `FLOAT` descriptor subset, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `DOUBLE(M,D)`, `REAL(M,D)`, `ZEROFILL`, `SIGNED`, string/hex/bit conversion, comparison/order/math, indexes, primary keys, auto-increment, casts, or protocol-grade numeric metadata |
| `FLOAT4` / `FLOAT8` | 🟡 | Limited aliases: `FLOAT4` normalizes to the supported `FLOAT` descriptor subset and `FLOAT8` normalizes to the supported `DOUBLE` descriptor subset, with the same DDL, DML, metadata, and unsupported-operation limits |
| `BIT` | 🟡 | Limited `BIT` / `BIT(1..64)` descriptors stored as fixed-width big-endian `BLOB` values, with default width `1`, byte-safe result readback, compatible decimal integer/`TRUE`/`FALSE`/bit/string/hex row-value assignment, `NULL`, `DEFAULT`, strict width/range/nullability diagnostics, `INSERT IGNORE` over-width clipping and not-null adjustment, compatible descriptor defaults, `ALTER TABLE ... ADD COLUMN`, descriptor cloning/copying, limited scalar-subquery assignment, descriptor-backed comparison/`BETWEEN`/`IN`/`IS NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `BIT()`/`BIT(0)`/`BIT(65+)`, bit indexes, expression defaults, `ALTER MODIFY`/`CHANGE` conversion, bit arithmetic, casts, aggregate arguments, grouping/distinct, or protocol-grade metadata |
| `BOOL` / `BOOLEAN` | 🟡 | Bare column-type aliases normalize to signed `TINYINT(1)` descriptors with integer/`NULL`, optional `DEFAULT NULL`, in-range decimal integer/`TRUE`/`FALSE` defaults, or limited parenthesized constant integer/`NULL` expression defaults, plus limited `TRUE`/`FALSE` DDL/DML/conversion/introspection coverage and no display-width warning; no `BOOL(1)`, `BOOLEAN(1)`, `SIGNED`, `UNSIGNED`, `ZEROFILL`, general expression defaults beyond that integer subset, general expression truth semantics, casts, or protocol-grade metadata |
| `SERIAL` | 🟡 | Limited `CREATE TABLE` column-type alias expanding to `BIGINT UNSIGNED AUTO_INCREMENT` plus a generated unique key, with implicit `NOT NULL` unless explicit `NULL`; descriptor metadata renders the expanded type/key/auto-increment shape and DML uses the current indexed auto-increment allocator. No separate `SERIAL DEFAULT VALUE` attribute after explicit integer types, no ALTER column-attribute support, and `BIGINT UNSIGNED` row values remain capped at the current signed-64 physical range |

## Temporal types

| Feature | Status | Notes |
| --- | --- | --- |
| `DATE` | 🟡 | Limited canonical `YYYY-MM-DD` descriptors stored as `TEXT`, with valid range `1000-01-01` through `9999-12-31`, canonical date-string defaults, parenthesized generated current-date defaults, `INSERT`/`REPLACE`/`UPDATE` assignment including explicit zero-fractional current-date and UTC-date values, limited SQL-mode-aware full-zero, partial-zero, and `ALLOW_INVALID_DATES` conversion for row values/defaults/compatible descriptor copies/predicates, `INSERT IGNORE` invalid/zero/`NULL` adjustments to `0000-00-00`, readback, descriptor cloning/copying, `IS NULL` / `IS NOT NULL`, canonical plus limited datetime-shaped string-literal comparison/`BETWEEN`/`IN` predicates, single-column ordering with MySQL-compatible `NULL` placement, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no relaxed date strings outside the documented predicate slice, pre-1000 non-full-zero dates, `DATE '...'` temporal literals, time parts outside the admitted predicate literals, casts, date arithmetic, general expression defaults beyond admitted current-date defaults, current-date/UTC-date assignment to other target families, date primary keys, auto-increment, time-zone shifting for `DATE` predicate offsets, or protocol-grade temporal metadata |
| `TIME` | 🟡 | Limited canonical `[-]HH:MM:SS` / `[-]HHH:MM:SS` descriptors stored as `TEXT`, with valid range `-838:59:59` through `838:59:59`, canonical time-string defaults, parenthesized generated current-time defaults, `INSERT`/`REPLACE`/`UPDATE` assignment including explicit zero-fractional current-time and UTC-time values, `INSERT IGNORE` invalid/out-of-range/`NULL` adjustments to `00:00:00` or clipped endpoint values, nonempty `ALTER TABLE ... ADD TIME NOT NULL` zero-time backfill, readback, descriptor cloning/copying, `IS NULL` / `IS NOT NULL`, string-literal comparison/`BETWEEN`/`IN` predicates using signed total-second semantics, single-column ordering with MySQL-compatible `NULL` placement and signed time ordering, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering with `DATETIME_PRECISION = 0`; no fractional precision, relaxed time strings, standard `TIME '...'` literals, numeric time conversion, general expression defaults beyond admitted current-time defaults, current-time/UTC-time assignment to other target families, casts, time primary keys, auto-increment, or protocol-grade temporal metadata |
| `DATETIME` | 🟡 | Limited canonical `YYYY-MM-DD HH:MM:SS` descriptors stored as `TEXT`, with valid range `1000-01-01 00:00:00` through `9999-12-31 23:59:59`, canonical datetime-string and zero-fractional `CURRENT_TIMESTAMP` defaults, `ON UPDATE CURRENT_TIMESTAMP`, `INSERT`/`REPLACE`/`UPDATE` assignment including explicit current timestamp and UTC timestamp values, limited SQL-mode-aware full-zero, partial-zero, and `ALLOW_INVALID_DATES` conversion for row values/defaults/compatible descriptor copies/predicates, `INSERT IGNORE` invalid/zero/`NULL` adjustments to `0000-00-00 00:00:00`, readback, descriptor cloning/copying, `IS NULL` / `IS NOT NULL`, canonical plus limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning string-literal comparison/`BETWEEN`/`IN` predicates, single-column ordering with MySQL-compatible `NULL` placement, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering with `DATETIME_PRECISION = 0`; no fractional precision, relaxed datetime strings outside the documented predicate slice, pre-1000 non-full-zero datetimes, `DATE` / `TIMESTAMP` temporal literal introducers, mutable time-zone conversion beyond explicit-offset predicate conversion, temporal functions beyond the limited statement-current/UTC synonyms, casts, datetime arithmetic, datetime primary keys, auto-increment, or protocol-grade temporal metadata |
| `TIMESTAMP` | 🟡 | Limited fixed-UTC canonical `YYYY-MM-DD HH:MM:SS` descriptors stored as `TEXT`, with valid range `1970-01-01 00:00:01` through `2038-01-19 03:14:07`, canonical timestamp-string and zero-fractional `CURRENT_TIMESTAMP` defaults, `ON UPDATE CURRENT_TIMESTAMP`, `INSERT`/`REPLACE`/`UPDATE` assignment including explicit current timestamp and UTC timestamp values, limited SQL-mode-aware full-zero conversion for row values/defaults/compatible descriptor copies/predicates, `INSERT IGNORE` invalid/zero/out-of-range/`NULL` adjustments to `0000-00-00 00:00:00`, nonempty `ALTER TABLE ... ADD TIMESTAMP NOT NULL` zero backfill, readback, descriptor cloning/copying, `IS NULL` / `IS NOT NULL`, canonical plus limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning string-literal comparison/`BETWEEN`/`IN` predicates, single-column ordering with MySQL-compatible `NULL` placement, supported nonunique and unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering with `DATETIME_PRECISION = 0`; no fractional precision, relaxed timestamp strings outside the documented predicate slice, partial-zero timestamp storage, `ALLOW_INVALID_DATES` timestamp widening, temporal literal introducers, mutable time-zone storage/readback conversion, temporal functions beyond the limited statement-current/UTC synonyms, casts, timestamp arithmetic, timestamp primary keys, auto-increment, or protocol-grade temporal metadata |
| `YEAR` | 🟡 | Limited `YEAR` / deprecated `YEAR(4)` descriptors stored as canonical four-digit `TEXT`, with `YEAR(4)` warning 1287 and invalid explicit widths rejected with 1818, represented values `0000` and `1901..2155`, MySQL-compatible numeric, string, boolean, `NULL`, and `DEFAULT` row-value conversion including two-digit rules, canonical descriptor defaults, `INSERT`/`REPLACE`/`UPDATE` assignment, `INSERT IGNORE` invalid/out-of-range/`NULL`/missing-default adjustment to `0000`, nonempty `ALTER TABLE ... ADD YEAR NOT NULL` zero backfill, readback, descriptor cloning/copying, limited scalar-subquery assignment, `IS NULL` / `IS NOT NULL`, comparison/`BETWEEN`/`IN` predicates including MySQL-compatible multi-value `IN` list coercion, single-column ordering with MySQL-compatible `NULL` placement, supported nonunique and full-column unique secondary-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `YEAR()`/`YEAR(0)`/`YEAR(2)`/other widths, `YEAR UNSIGNED`/`SIGNED`/`ZEROFILL`, expression defaults, relaxed year strings, temporal functions, casts, year primary keys, auto-increment, or protocol-grade temporal metadata |

## String and binary types

| Feature | Status | Notes |
| --- | --- | --- |
| `CHAR` | 🟡 | Limited bare `CHAR`, `CHAR(0..255)`, `CHARACTER`, and `CHARACTER(0..255)` descriptors with descriptor-owned row-size checks for create/add-column paths, UTF-8 non-`NUL` `TEXT` storage/readback in MySQL's default trimmed `CHAR` shape, silent excess-trailing-space truncation, strict nonspace overlength errors, literal descriptor defaults, `NULL` and empty-string behavior including non-key `CHAR(0)`, optional explicit `utf8mb4` or `ascii` column charset/collation metadata on admitted create/add/modify/change paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique full/prefix secondary-index declaration/metadata, ASCII `CHAR(1..255)` primary-key participation in single-column or table-level composite primary keys plus single-column or table-level composite full-column unique-key participation and prefix unique-key participation including composite prefix key parts through MyLite's fixed ASCII subset of `utf8mb4_0900_ai_ci`, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no warning-producing overlength default truncation, `PAD_CHAR_TO_FULL_LENGTH`, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, `CHAR(0)` keys, primary prefix keys, non-ASCII primary/unique key values, full Unicode collation-aware index semantics, or protocol-grade metadata |
| `VARCHAR` | 🟡 | Limited `VARCHAR(0..16383)`, `CHARACTER VARYING(0..16383)`, and `CHAR VARYING(0..16383)` descriptors with descriptor-owned row-size checks for create/add-column paths, UTF-8 non-`NUL` `TEXT` storage/readback, `NULL` and empty-string behavior, strict overlength errors, literal descriptor defaults, optional explicit `utf8mb4` or `ascii` column charset/collation metadata on admitted create/add/modify/change paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique full/prefix secondary-index declaration/metadata, ASCII `VARCHAR(1..255)` primary-key participation in single-column or table-level composite primary keys plus single-column or table-level composite full-column unique-key participation and prefix unique-key participation including composite prefix key parts through MyLite's fixed ASCII subset of `utf8mb4_0900_ai_ci`, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no warning-producing overlength default truncation, lengths above 16383, charset conversion, full Unicode collation comparison, grouping/distinct, string aggregates, non-admitted column charset attributes, `VARCHAR(0)` keys, primary prefix keys, non-ASCII primary/unique key values, full Unicode collation-aware index semantics, or protocol-grade metadata |
| `CHARACTER` / `CHARACTER VARYING` | 🟡 | Limited aliases for the supported `CHAR` and `VARCHAR` descriptor subsets: `CHARACTER` / `CHARACTER(n)` normalize to `char(n)`, while `CHARACTER VARYING(n)` and `CHAR VARYING(n)` normalize to `varchar(n)`; same current CHAR length, wider VARCHAR length, DDL, DML, metadata, collation, and key limits as the normalized target descriptors |
| `NCHAR` / `NATIONAL CHAR` | 🟡 | Limited aliases for the supported `CHAR` descriptor subset: `NCHAR`, `NCHAR(n)`, `NATIONAL CHAR[(n)]`, and `NATIONAL CHARACTER[(n)]` store as SQLite `TEXT`, preserve a national logical descriptor, emit warning 3720 when explicitly declared, render as `char(n) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci` in `SHOW CREATE TABLE`, report utf8mb3 metadata in `SHOW FULL COLUMNS`, `INFORMATION_SCHEMA.COLUMNS`, and result-column metadata, and reuse current `CHAR` DDL/DML/default/key/clone/persistence behavior. No general column charset attributes, national string literals, charset conversion, utf8mb3 collation semantics, `PAD_CHAR_TO_FULL_LENGTH`, or protocol-grade metadata |
| `NVARCHAR` / `NATIONAL VARCHAR` | 🟡 | Limited aliases for the supported `VARCHAR` descriptor subset: `NVARCHAR(n)`, `NATIONAL VARCHAR(n)`, `NCHAR VARCHAR(n)`, `NCHAR VARYING(n)`, `NATIONAL CHAR VARYING(n)`, and `NATIONAL CHARACTER VARYING(n)` store as SQLite `TEXT`, preserve a national logical descriptor, emit warning 3720 when explicitly declared, render as `varchar(n) CHARACTER SET utf8mb3 COLLATE utf8mb3_general_ci` in `SHOW CREATE TABLE`, report utf8mb3 metadata in `SHOW FULL COLUMNS`, `INFORMATION_SCHEMA.COLUMNS`, and result-column metadata, and reuse current `VARCHAR` DDL/DML/default/key/clone/persistence behavior within the current `VARCHAR(0..16383)` envelope. No bare `NVARCHAR`, lengths above 16383, general column charset attributes, national string literals, charset conversion, utf8mb3 collation semantics, or protocol-grade metadata |
| `BINARY` | 🟡 | Limited `BINARY` / `BINARY(0..255)` descriptors stored as SQLite `BLOB`, with zero-byte right-padding, byte-safe result readback, string and hex row-value assignment, `NULL`, strict length diagnostics, `INSERT IGNORE` overlength truncation and not-null adjustment, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no defaults, binary comparisons, ordering/grouping/distinct, indexes, casts into table-backed expressions, streaming I/O, or protocol-grade metadata |
| `VARBINARY` | 🟡 | Limited `VARBINARY(0..65535)` descriptors stored as SQLite `BLOB`, with current row-size cap at `65532` for one wide column, byte-safe result readback, string and hex row-value assignment, `NULL`, strict length diagnostics, `INSERT IGNORE` overlength truncation and not-null adjustment, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no bare `VARBINARY`, defaults, binary comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `CHAR BYTE` | 🟡 | Limited alias behavior for `CHAR BYTE` and `CHAR(n) BYTE`, normalized to `BINARY(1)` and `BINARY(n)` descriptors; same DDL, DML, metadata, padding, and unsupported-operation limits as `BINARY` |

## Large object types

| Feature | Status | Notes |
| --- | --- | --- |
| `TINYBLOB` | 🟡 | Limited bare `TINYBLOB` descriptors stored as SQLite `BLOB`, with byte-safe result readback, string and hex row-value assignment, `NULL`, strict and `INSERT IGNORE` length/nullability handling, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no defaults, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `BLOB` | 🟡 | Limited bare `BLOB` plus `BLOB(M)` normalization to the smallest compatible BLOB family descriptor, with byte-safe result readback, string and hex row-value assignment, `NULL`, strict and `INSERT IGNORE` length/nullability handling, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and metadata rendering; no defaults, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `MEDIUMBLOB` | 🟡 | Limited bare `MEDIUMBLOB` descriptors stored as SQLite `BLOB`, with byte-safe result readback, string and hex row-value assignment, `NULL`, strict and `INSERT IGNORE` length/nullability handling, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and metadata rendering; no defaults, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `LONGBLOB` | 🟡 | Limited bare `LONGBLOB` descriptors stored as SQLite `BLOB`, with byte-safe result readback, string and hex row-value assignment, `NULL`, strict and `INSERT IGNORE` length/nullability handling, descriptor cloning/copying, `IS NULL` / `IS NOT NULL` predicates, and metadata rendering; no defaults, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |
| `TINYTEXT` | 🟡 | Limited bare `TINYTEXT` descriptors with descriptor-owned row-size checks for create/add-column paths, UTF-8 non-`NUL` `TEXT` storage/readback, strict 255-byte row-value checks, `NULL` and empty-string behavior, optional explicit `utf8mb4` or `ascii` column charset/collation metadata on admitted create/add paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique and unique prefix-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `TINYTEXT(M)`, explicit string defaults, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, full-column indexes, primary indexes, or protocol-grade metadata |
| `TEXT` | 🟡 | Limited bare `TEXT` descriptors with descriptor-owned row-size checks for create/add-column paths, UTF-8 non-`NUL` `TEXT` storage/readback, strict 65535-byte row-value checks, `NULL` and empty-string behavior, optional explicit `utf8mb4` or `ascii` column charset/collation metadata on admitted create/add paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique and unique prefix-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `TEXT(M)`, explicit string defaults, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, full-column indexes, primary indexes, or protocol-grade metadata |
| `MEDIUMTEXT` | 🟡 | Limited bare `MEDIUMTEXT` descriptors with descriptor-owned row-size checks for create/add-column paths, UTF-8 non-`NUL` `TEXT` storage/readback, strict 16777215-byte row-value checks, `NULL` and empty-string behavior, optional explicit `utf8mb4` or `ascii` column charset/collation metadata on admitted create/add paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique and unique prefix-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `MEDIUMTEXT(M)`, explicit string defaults, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, full-column indexes, primary indexes, or protocol-grade metadata |
| `LONGTEXT` | 🟡 | Limited bare `LONGTEXT` descriptors with descriptor-owned row-size checks for create/add-column paths, UTF-8 non-`NUL` `TEXT` storage/readback, strict 4294967295-byte row-value checks within process memory limits, `NULL` and empty-string behavior, optional explicit `utf8mb4` or `ascii` column charset/collation metadata on admitted create/add paths, admitted `INSERT`/`REPLACE`/`UPDATE` string assignments, ASCII comparison/range/membership, `LIKE`, `REGEXP`, `IS NULL`, and `IS NOT NULL` predicates, one-column ordering with MySQL-compatible `NULL` placement, descriptor cloning/copying, supported nonunique and unique prefix-index declaration/metadata, and `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `LONGTEXT(M)`, explicit string defaults, charset conversion, full Unicode collation comparison, grouping/distinct, non-admitted column charset attributes, full-column indexes, primary indexes, streaming I/O, or protocol-grade metadata |
| `LONG` / `LONG VARCHAR` | 🟡 | Limited aliases normalized to the supported `MEDIUMTEXT` descriptor path in create/add-column/clone/CTAS and row DML, with optional admitted `utf8mb4` or `ascii` column charset/collation metadata, inherited `MEDIUMTEXT` predicate and ordering subset, and normalized `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `LONG VARCHAR(length)`, `LONG TEXT`, `LONG BINARY`, explicit string defaults, expression defaults, alias spelling preservation, grouping/distinct, full-column indexes, primary indexes, streaming I/O, or protocol-grade metadata |
| `LONG VARBINARY` | 🟡 | Limited alias normalized to the supported `MEDIUMBLOB` descriptor path in create/add-column/clone/CTAS and row DML, with byte-safe readback and normalized `SHOW` / `INFORMATION_SCHEMA.COLUMNS` rendering; no `LONG VARBINARY(length)`, defaults, charset/collation attributes, alias spelling preservation, comparisons, ordering/grouping/distinct, indexes, streaming I/O, or protocol-grade metadata |

## Enumerated types

| Feature | Status | Notes |
| --- | --- | --- |
| `ENUM` | 🟡 | Limited descriptor-owned `ENUM('label'[, ...])` columns with valid non-`NUL` UTF-8 labels, trailing-space normalization at definition time, ASCII case-insensitive duplicate detection and label matching, physical `TEXT` storage of the canonical definition label, string-label defaults, nullable `NULL` defaults, omitted/default `NOT NULL` first-label behavior, strict `INSERT`/`REPLACE`/supported `UPDATE` assignment conversion from matching labels, in-range decimal ordinal literals, quoted ordinal fallback when no label matches, `NULL`, and `DEFAULT`, descriptor-backed `=`, `<=>`, `<>`, `!=`, `<=> NULL`, `IS NULL`, and `IS NOT NULL` predicates, `SHOW` / `INFORMATION_SCHEMA.COLUMNS` / result-column metadata, and descriptor cloning. No compact ordinal storage, invalid index-0 storage, non-strict or `IGNORE` invalid-value demotion, enum sorting/grouping/distinct, enum keys/indexes, enum foreign keys, expression conversion, `ALTER MODIFY`/`CHANGE`, `INSERT ... SELECT`, scalar subquery assignment, full collation semantics, or 65,535-label envelope |
| `SET` | 🟡 | Limited descriptor-owned `SET('member'[, ...])` columns with nonempty valid non-`NUL` UTF-8 members, trailing-space normalization at definition time, comma-containing member rejection, ASCII case-insensitive duplicate detection and member matching, physical `TEXT` storage of the canonical comma-separated definition-order display string, string-list defaults, nullable `NULL` defaults, catalog-only `ALTER COLUMN SET DEFAULT` and `ALTER COLUMN DROP DEFAULT`, strict `INSERT`/`REPLACE`/supported `UPDATE` assignment conversion from matching member lists, nonnegative decimal bitmap literals, quoted-bitmap fallback when no member matches, `NULL`, and `DEFAULT`, descriptor-backed `=`, `<=>`, `<>`, `!=`, `<=> NULL`, `IS NULL`, and `IS NOT NULL` predicates, `SHOW` / `INFORMATION_SCHEMA.COLUMNS` / result-column metadata, and descriptor cloning. No compact bitmap storage, empty-string set members, invalid value warning demotion, set sorting/grouping/distinct, set keys/indexes, set foreign keys, expression conversion, `ALTER MODIFY`/`CHANGE`, `INSERT ... SELECT`, scalar subquery assignment, full collation semantics, or more than 64 members |

## Structured and spatial types

| Feature | Status | Notes |
| --- | --- | --- |
| `JSON` | 🟡 | Limited descriptor-owned `JSON` columns stored as canonical SQLite `TEXT`, with validation/canonicalization for the verified subset of JSON `null`, booleans, strings, signed integer numbers, arrays, and ASCII-key objects; supports `CREATE TABLE`, `CREATE TABLE ... LIKE`, `CREATE TABLE ... SELECT`, `ALTER TABLE ... ADD COLUMN`, `INSERT`/`REPLACE`/supported `UPDATE` assignment, compatible `INSERT ... SELECT`/`REPLACE ... SELECT` and scalar-subquery `UPDATE`, SQL `NULL`, `DEFAULT`, `INSERT IGNORE` implicit JSON `null`, `IS NULL` / `IS NOT NULL`, limited `JSON_VALID()` checks, limited simple-path `JSON_EXTRACT()` / `JSON_UNQUOTE()` / `JSON_LENGTH()` / `JSON_TYPE()` / `->` / `->>` read projections, limited `JSON_ARRAY()` / `JSON_OBJECT()` construction, `SHOW COLUMNS`, `SHOW CREATE TABLE`, `INFORMATION_SCHEMA.COLUMNS`, and result-column metadata. No JSON mutation functions, full path grammar, comparison/order/grouping semantics, binary JSON storage, decimal/exponent/non-ASCII JSON normalization beyond the verified subset, expression defaults, generated/path indexes, direct JSON indexes, `ALTER MODIFY`/`CHANGE`, casts, or general expression conversion |
| `GEOMETRY` | ❌ | Base spatial type storage, SRID, validity, and metadata |
| `POINT` | ❌ | Point storage, SRID, coordinate access, and metadata |
| `LINESTRING` | ❌ | LineString storage, SRID, validity, and metadata |
| `POLYGON` | ❌ | Polygon storage, SRID, validity, and metadata |
| `MULTIPOINT` | ❌ | MultiPoint storage, SRID, validity, and metadata |
| `MULTILINESTRING` | ❌ | MultiLineString storage, SRID, validity, and metadata |
| `MULTIPOLYGON` | ❌ | MultiPolygon storage, SRID, validity, and metadata |
| `GEOMETRYCOLLECTION` | ❌ | Geometry collection storage, SRID, validity, and metadata |

## Literals

| Feature | Status | Notes |
| --- | --- | --- |
| Numeric literals | 🟡 | Decimal integer literals with optional unary sign as supported no-source/`DUAL` literal projection values, no-source/`DUAL` signed-64 arithmetic projection operands, limited no-source/`DUAL`/`DO` numeric functions, limited no-source/`DUAL` bare `CAST(value AS BINARY)` inputs, limited no-source/`DUAL`/`DO` `CONVERT(value, BINARY)` and `CONVERT(value USING utf8mb4)` inputs, limited no-source/`DUAL`/`DO` interval-second `DATE_ADD()` / `DATE_SUB()` / `ADDDATE()` / `SUBDATE()` operands, limited no-source/`DUAL`/`DO` and row-scalar control-flow, `CONCAT_WS()`, `FIELD()`, `GREATEST()`, `LEAST()`, `REPLACE()`, `JSON_ARRAY()`, `JSON_OBJECT()`, and `JSON_VALID()` integer arguments, limited no-source/`DUAL`/`DO` and row-scalar string length, string slice, string search, string replacement, string padding, ASCII string case, and `HEX()` function integer arguments, no-source/`DUAL` keyword scalar logical operands, limited `DO` scalar-expression operands including top-level `/` division, integer column default values and limited parenthesized constant integer expression defaults, `INSERT ... VALUES`, `INSERT ... SET`, `REPLACE ... VALUES`, `REPLACE ... SET`, single-table `UPDATE` assignment inputs, and limited unsigned same-column integer `UPDATE` arithmetic assignment deltas plus supported filtered `SELECT`/`DELETE`/`UPDATE` predicate right operands, scalar-literal truth/comparison/`IS` predicates, literal-left descriptor comparisons, `BETWEEN` bounds, and `IN` list values; fixed decimal literals with optional unary sign only for compatible decimal and approximate column defaults plus row DML assignments and limited no-source/`DUAL`/`DO` `FORMAT()` / `TRUNCATE()` value arguments, with exact decimal canonicalization for decimal targets and finite range checks for approximate targets; approximate numeric literals with optional unary sign are admitted only for compatible approximate column defaults and row DML assignments; `INSERT IGNORE ... VALUES` and `INSERT IGNORE ... SET` clip out-of-range descriptor integer and decimal inputs to the current supported range with warnings; unsigned decimal integer literals for supported `SELECT` `LIMIT`/`OFFSET` and `DELETE`/`UPDATE LIMIT`; hexadecimal literals are admitted only as binary-string row values and limited no-source/`DUAL`/`DO` `CRC32()` and `HEX()` inputs, not numeric values; no table-backed expression-level numeric semantics outside the limited same-column update arithmetic assignment subset and limited row-scalar control-flow value subset, non-decimal numeric formats, approximate scalar projection, approximate predicates beyond `IS NULL`, or bit literals |
| Boolean literals | 🟡 | `TRUE` and `FALSE` are accepted as `1` and `0` only in supported no-source/`DUAL` literal projection values, limited no-source/`DUAL` signed-64 scalar arithmetic projection operands including unary `+` and unary `-`, limited no-source/`DUAL` `ABS()`, `SIGN()`, `CEIL()`/`CEILING()`/`FLOOR()`, one-argument `ROUND()`, `BIT_COUNT()`, `BIN()`, `OCT()`, `CONV()`, `SQRT()`, `DEGREES()`, `RADIANS()`, `ACOS()`, `ASIN()`, `SIN()`, `COS()`, `TAN()`, `COT()`, `ATAN()`, `ATAN2()`, `EXP()`, `LN()`, `LOG()`, `LOG10()`, `LOG2()`, `POW()`, `POWER()`, `CRC32()`, `HEX()`, `FORMAT()`, and `TRUNCATE()` operands, limited no-source/`DUAL` bare `CAST(value AS BINARY)` inputs, limited no-source/`DUAL`/`DO` `CONVERT(value, BINARY)` and `CONVERT(value USING utf8mb4)` inputs, limited no-source/`DUAL`/`DO` and row-scalar control-flow, `CONCAT_WS()`, `FIELD()`, `GREATEST()`, `LEAST()`, `REPLACE()`, `JSON_ARRAY()`, `JSON_OBJECT()`, and `JSON_VALID()` boolean arguments, limited no-source/`DUAL`/`DO` and row-scalar string length, string slice, string search, string replacement, string padding, ASCII string case, and `HEX()` function boolean arguments, no-source/`DUAL` keyword scalar logical operands, limited `DO` scalar-expression operands including top-level `/` division, column default values, integer row-value, `INSERT ... SET`, `REPLACE ... VALUES`, `REPLACE ... SET`, single-table `UPDATE` assignment, filtered `SELECT`/`DELETE`/`UPDATE` predicate right-operand, scalar-literal truth/comparison/`IS` predicates, literal-left descriptor comparisons, `BETWEEN` bound, and `IN` list positions, `COUNT(TRUE)` / `COUNT(FALSE)` aggregate argument positions, and descriptor-column `IS [NOT] TRUE` / `FALSE` truth-test targets; no general boolean expressions outside the limited scalar and descriptor-backed subsets, scalar truth metadata, general expression evaluation, or `LIMIT TRUE` / `LIMIT FALSE` |
| String literals | 🟡 | Ordinary single- and double-quoted string literals with decoded quote and backslash escapes are supported only in admitted `CHAR`, `VARCHAR`, baseline `TEXT` family, limited `ENUM`, limited `SET`, limited `JSON`, and binary string row-value positions, limited ASCII string, `ENUM`, and `SET` equality predicate right operands, limited ASCII string range/membership predicate operands, limited ASCII `LIKE` and `REGEXP` pattern predicate right operands, canonical `DATE`, `TIME`, `DATETIME`, and `TIMESTAMP` defaults/row values plus canonical temporal predicates, limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning `DATE` predicates, and limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning `DATETIME` and `TIMESTAMP` predicates, plus literal `CHAR`/`VARCHAR`/`ENUM`/`SET` defaults, limited no-source/`DUAL` bare `CAST(value AS BINARY)` scalar inputs and limited no-source/`DUAL`/`DO` `CONVERT(value, BINARY)`, `CONVERT('string' USING BINARY)`, and `CONVERT(value USING utf8mb4)` scalar inputs, limited no-source/`DUAL`/`DO` `CRC32()` and `HEX()` string arguments, limited no-source/`DUAL`/`DO` interval-second `DATE_ADD()` / `DATE_SUB()` / `ADDDATE()` / `SUBDATE()` date/datetime string inputs, limited no-source/`DUAL`/`DO` and row-scalar `DATE_FORMAT()` value/format string arguments, limited no-source/`DUAL`/`DO` and row-scalar `DATEDIFF()` value string arguments, limited no-source/`DUAL`/`DO` and row-scalar `UNIX_TIMESTAMP()` value string arguments, limited no-source/`DUAL`/`DO` and row-scalar temporal extractor string arguments, limited no-source/`DUAL`/`DO` and row-scalar control-flow, `CONCAT_WS()`, `FIELD()`, `GREATEST()`, `LEAST()`, `REPLACE()`, `JSON_ARRAY()`, `JSON_OBJECT()`, `JSON_VALID()`, `JSON_TYPE()`, and `JSON_LENGTH()` string arguments, limited no-source/`DUAL`/`DO` and row-scalar string length, string slice, string replacement, and string padding function string arguments, limited ASCII NUL-free no-source/`DUAL`/`DO` and row-scalar string search function string arguments, limited ASCII no-source/`DUAL`/`DO` and row-scalar string case function string arguments, limited `HEX()` function string arguments, and existing alias contexts; `ANSI_QUOTES` makes later double-quoted tokens identifiers instead of strings, and `NO_BACKSLASH_ESCAPES` disables backslash escape decoding for later admitted string literals; admitted `CHAR` row values are canonicalized to MySQL's default trimmed readback shape, admitted `ENUM` row values canonicalize to the matching definition label, admitted `SET` row values canonicalize to comma-separated definition-order member display, admitted `JSON` row values canonicalize to the current verified JSON display subset, binary string row values may contain decoded embedded `NUL` bytes and are exposed through byte-safe result accessors, while other admitted row values preserve ordinary `\%` and `\_` backslash escapes like MySQL outside pattern matching; no introducers, national strings, adjacent literal concatenation, parameters, table-backed expression values outside admitted predicates and row-scalar control-flow projection, arbitrary scalar projection beyond listed slices, embedded `NUL` in nonbinary text storage outside scalar `HEX()` inputs and the explicit `DATEDIFF()` unsupported-literal diagnostic, non-ASCII string predicate, string search, string case, or `FIELD()`/`GREATEST()`/`LEAST()` literals outside the current enum/set label-matching subset, relaxed temporal strings outside the documented predicate slices, or broader SQL mode behavior |
| Temporal literals | 🟡 | Canonical quoted `YYYY-MM-DD`, `[-]HH:MM:SS` / `[-]HHH:MM:SS`, and `YYYY-MM-DD HH:MM:SS` strings are admitted only where the limited `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` descriptor subsets explicitly allow them plus limited `DATE`, `DATETIME`, and `TIMESTAMP` predicate strings with a `T` date/time separator, a numeric `+HH:MM` / `-HH:MM` offset, or a single trailing `Z` / `z` suffix with truncation warning, the limited no-source/`DUAL`/`DO` interval-second `DATE_ADD()` / `DATE_SUB()` / `ADDDATE()` / `SUBDATE()` scalar slice, limited `DATE_FORMAT()`, `DATEDIFF()`, and `UNIX_TIMESTAMP()` value arguments, and limited temporal extractor arguments; `DATEDIFF()` additionally admits verified complete date strings before year 1000 in its string-argument subset; `DATE` / `DATETIME` / `TIMESTAMP` descriptor positions apply the current limited SQL-mode zero-temporal rules; no `DATE '...'`, `TIME '...'`, `TIMESTAMP '...'`, relaxed temporal strings outside the documented predicate slices, broader trailing-garbage temporal truncation, pre-1000 non-full-zero temporal dates outside the verified `DATEDIFF()` string-argument subset, general intervals, fractional seconds, temporal offsets outside the documented temporal predicate slices, or temporal expression coercion |
| JSON path literals | 🟡 | Limited simple path string literals for `JSON_EXTRACT()`, `JSON_LENGTH()`, and `->` / `->>`: `$`, ASCII identifier members, quoted ASCII members, and nonnegative array indexes without leading zeros. No wildcards, recursive descent, array ranges, `last`, filters, non-ASCII path names, or general path expression support |

## Variables and conversion

| Feature | Status | Notes |
| --- | --- | --- |
| User variables | 🟡 | Limited handle-local `@name` variables with case-insensitive 1..64-character names, nonpersistent session retention, `SET @name = value` / `SET @name := value`, scalar `SELECT`/`DO` reads, uninitialized `NULL`, SQL-level prepared statement source/`USING` values, and text/`NULL` storage for admitted integer, string, boolean, system-variable, and user-variable assignment values. Existing limited scalar-expression functions may consume user variables when their current operand conversion accepts the stored text/`NULL` value. No table-backed predicates/order/DML values outside prepared execution expansion, metadata tables, binary values with embedded `NUL`, arbitrary expression assignment, direct parameters, subqueries, or durable storage |
| Local variables | ❌ | Stored-program variable typing, scope, and diagnostics |
| Type conversion | 🟡 | Limited strict conversion for supported integer/decimal/approximate/canonical `YEAR`/canonical temporal/binary string/limited `ENUM`/limited `SET`/`NULL`/`TRUE`/`FALSE` defaults plus inserted, replaced, and updated values, including exact decimal canonicalization, scale padding, half-away-from-zero rounding, truncation notes, range diagnostics for decimal descriptors, finite range checks and `FLOAT` single-precision rounding for approximate descriptors, canonical four-digit text storage for `YEAR` descriptors, canonical `YYYY-MM-DD` validation for `DATE` descriptors, canonical `[-]HH:MM:SS` / `[-]HHH:MM:SS` validation for `TIME` descriptors, canonical `YYYY-MM-DD HH:MM:SS` validation for `DATETIME` and `TIMESTAMP` descriptors, limited session SQL-mode zero-temporal behavior for full-zero, partial-zero, and `ALLOW_INVALID_DATES` `DATE` / `DATETIME` plus full-zero `TIMESTAMP`, binary string byte decoding, hex decoding, fixed-length padding, and byte-length checks, `ENUM` label/ordinal conversion to canonical definition-label text, and `SET` member-list/bitmap conversion to canonical definition-order display text; strict UTF-8 non-`NUL` string-literal conversion for admitted `CHAR`, `VARCHAR`, baseline `TEXT` family, binary string, `BIT`, `ENUM`, and `SET` row values; ASCII-only descriptor-driven string-literal conversion for admitted string, `ENUM`, and `SET` equality predicates; default-mode `CHAR` trailing-space canonicalization; descriptor resolution for limited DML `DEFAULT` keyword values in supported `INSERT`/`REPLACE` `VALUES` and `SET` paths plus supported `UPDATE` assignments; limited `INSERT IGNORE ... VALUES` / `SET` adjustment for omitted no-explicit-default columns, explicit `DEFAULT` for no-default columns, explicit `NULL` into numeric/string/`YEAR`/temporal/binary string `NOT NULL`, out-of-range descriptor integer, decimal, `YEAR`, and `TIME` inputs, invalid `YEAR` and temporal inputs, overlength string values, and overlength binary string values, descriptor-driven existing-row validation for supported integer `ALTER TABLE ... MODIFY [COLUMN]` and `CHANGE [COLUMN]`, descriptor-driven integer/boolean/`YEAR`/date/time/datetime/timestamp predicate conversion for `SELECT`/`DELETE`/`UPDATE` comparisons, literal-left descriptor comparisons, `BETWEEN` bounds, and non-`NULL` `IN` list values including limited `T`-separator, numeric-offset, and single trailing-`Z`/`z` truncation-warning `DATE`/`DATETIME`/`TIMESTAMP` predicate strings, signed-64 scalar-literal conversion for limited scalar truth/comparison/`IS` predicates, descriptor-preserving `NULL` comparison and `IN` list values, descriptor-driven ASCII `CHAR`/`VARCHAR`/baseline `TEXT` family plus limited `ENUM` and `SET` predicate conversion for `=`, `<=>`, `<>`, and `!=`, descriptor-driven ASCII `CHAR`/`VARCHAR`/baseline `TEXT` family predicate conversion for `<`, `<=`, `>`, `>=`, `BETWEEN`, and `IN`, and unsigned signed-64 range conversion for supported `SELECT` `LIMIT`/`OFFSET` plus `DELETE`/`UPDATE LIMIT` literals only |
| Aggregate numeric result envelope | 🟡 | Limited descriptor-backed integer `SUM(column)` results use MyLite's signed-64 text result envelope; MySQL's exact decimal widening for integer sums beyond signed 64 bits is not yet implemented |
| Collation coercibility | ❌ | Coercibility and diagnostics |

[Back to compatibility overview](../../COMPATIBILITY.md)
