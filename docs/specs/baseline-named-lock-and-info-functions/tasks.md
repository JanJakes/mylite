# Baseline named lock and information functions tasks

- [x] Verify MySQL 8.4.9 behavior for named locks, `ICU_VERSION()`, and
  `BENCHMARK()`.
- [x] Specify runtime scope, diagnostics, and embedded-design gaps.
- [x] Add MyLite named-lock registry helpers and SQLite callbacks.
- [x] Wire scalar evaluator support for named locks, `ICU_VERSION()`, and
  `BENCHMARK()`.
- [x] Add runtime and MySQL expectation tests.
- [x] Update compatibility docs.
- [x] Run focused tests, full checks, review, commit, and push.
- [x] Follow up with repeated scalar `BENCHMARK()` operand evaluation and split
  the row-scalar timing-fidelity residual from the green scalar compatibility
  row.
