# Tasks

- [x] Specify the row-backed `CRC32()`, `FORMAT()`, `TRUNCATE()`, and `PI()` subset.
- [x] Add private SQLite callbacks for row execution.
- [x] Wire row-scalar planning, SQL lowering, and parameter binding.
- [x] Add MySQL-runtime expectation coverage for row-backed forms.
- [x] Add focused MyLite runtime coverage for table-backed values and nesting.
- [x] Update compatibility documentation.
- [x] Run focused verification and release-gate checks.
