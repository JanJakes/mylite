# Control-flow functions

Conditional expression helpers.

| Function | Status | Notes |
| --- | --- | --- |
| `CASE` | 🟡 | Limited no-source and `FROM DUAL` top-level scalar searched and simple `CASE` operator projection plus limited `DO` expression execution; limited single-table searched `CASE` row-scalar projection, narrow joined simple `CASE value WHEN compare THEN result ELSE fallback END` row-scalar projection over integer descriptor/literal operands, and hidden `ORDER BY` rank keys over the current row-control-flow condition/value subset, including `LIKE` atoms combined with `AND`/`OR`; children may use existing non-`CASE` signed-64 scalar arithmetic/comparison/logical/`IS`/control-flow expressions where admitted by the scalar or row-scalar path; no nested `CASE` beyond the current row-control-flow depth, `CASE` as a larger scalar operand, predicates, DML assignments, strings outside the current row-scalar value subset, decimals, floats, hex, bit operands, subqueries, side effects beyond evaluated scalar warnings, full MySQL result type aggregation, or expression metadata |
| `COALESCE()` | 🟡 | Limited no-source and `FROM DUAL` scalar `SELECT COALESCE(value[, ...])` plus limited `DO` expression execution over signed-64 decimal integer, `TRUE`/`FALSE`, `NULL`, nested `COALESCE()` operands, supported nested `IFNULL()` operands, supported nested `IF()` operands, supported nested `NULLIF()` operands, and supported nested `ISNULL()` operands; limited single-table row-scalar projection over descriptor integer, nonbinary string, `YEAR`, and temporal columns, scalar literals, session scalar values, system variables, and one nested supported row control-flow layer; no predicates, ordering/grouping expressions, DML assignments, table-backed subqueries, binary string/`BIT`/approximate/JSON column operands, arithmetic row arguments, side effects, or expression metadata |
| `IF()` | 🟡 | Limited no-source and `FROM DUAL` scalar `SELECT IF(condition, true_value, false_value)` plus limited `DO` expression execution over signed-64 decimal integer, `TRUE`/`FALSE`, `NULL`, nested `IF()` operands, supported nested `IFNULL()` operands, supported nested `COALESCE()` operands, supported nested `NULLIF()` operands, and supported nested `ISNULL()` operands; limited single-table row-scalar projection where row conditions are descriptor integer columns, integer/boolean/`NULL` literals, or row-backed `ISNULL()` values and row values may use one nested supported row control-flow layer; no predicates, ordering/grouping expressions, DML assignments, string/decimal truth conversion, table-backed subqueries, binary string/`BIT`/approximate/JSON column operands, arithmetic row arguments, side effects, or expression metadata |
| `IFNULL()` | 🟡 | Limited no-source and `FROM DUAL` scalar `SELECT IFNULL(value, fallback)` plus limited `DO` expression execution over signed-64 decimal integer, `TRUE`/`FALSE`, `NULL`, nested `IFNULL()` operands, supported nested `IF()` operands, supported nested `COALESCE()` operands, supported nested `NULLIF()` operands, and supported nested `ISNULL()` operands; limited single-table row-scalar projection over descriptor integer, nonbinary string, `YEAR`, and temporal columns, scalar literals, session scalar values, system variables, and one nested supported row control-flow layer; no predicates, ordering/grouping expressions, DML assignments, table-backed subqueries, binary string/`BIT`/approximate/JSON column operands, arithmetic row arguments, side effects, or expression metadata |
| `NULLIF()` | 🟡 | Limited no-source and `FROM DUAL` scalar `SELECT NULLIF(left_value, right_value)` plus limited `DO` expression execution over signed-64 decimal integer, `TRUE`/`FALSE`, `NULL`, nested `NULLIF()` operands, supported nested `IF()` operands, supported nested `IFNULL()` operands, supported nested `COALESCE()` operands, and supported nested `ISNULL()` operands; limited single-table row-scalar projection over same-domain admitted integer or string-like values, with admitted string comparisons using MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation; no predicates, ordering/grouping expressions, DML assignments, mixed string/numeric row arguments, table-backed subqueries, binary string/`BIT`/approximate/JSON column operands, arithmetic row arguments, side effects, first-argument double-evaluation semantics, or expression metadata |

[Back to compatibility overview](../../COMPATIBILITY.md)
