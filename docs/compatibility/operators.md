# Operators

Operators, predicates, assignment forms, and SQL expression syntax that MySQL lists alongside built-ins.

| Function or operator | Status | Notes |
| --- | --- | --- |
| `&` | 🟡 | Limited no-source/`DUAL`, `DO`, single-table row-scalar projection, parenthesized comparison predicates, and direct `ORDER BY` keys over the documented integer-domain operands; no unparenthesized row predicate subjects, broader row coercion, or binary-string bit operations |
| `>` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string literal comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, string-family comparisons against signed integer RHS literals use numeric coercion, and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 plus numeric string/decimal/float scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `>>` | 🟡 | Limited no-source/`DUAL`, `DO`, single-table row-scalar projection, parenthesized comparison predicates, and direct `ORDER BY` keys over the documented integer-domain operands; shift counts greater than or equal to 64 produce `0`; no unparenthesized row predicate subjects, broader row coercion, or binary-string bit operations |
| `>=` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string literal comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, string-family comparisons against signed integer RHS literals use numeric coercion, and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 plus numeric string/decimal/float scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `<` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string literal comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, string-family comparisons against signed integer RHS literals use numeric coercion, and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 plus numeric string/decimal/float scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `<>, !=` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible `ENUM` labels or ordinals, compatible `SET` member lists or bitmaps, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string literal comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, string-family comparisons against signed integer RHS literals use numeric coercion, and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 plus numeric string/decimal/float scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `<<` | 🟡 | Limited no-source/`DUAL`, `DO`, single-table row-scalar projection, parenthesized comparison predicates, and direct `ORDER BY` keys over the documented integer-domain operands; shift counts greater than or equal to 64 produce `0`; no unparenthesized row predicate subjects, broader row coercion, or binary-string bit operations |
| `<=` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string literal comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, string-family comparisons against signed integer RHS literals use numeric coercion, and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 plus numeric string/decimal/float scalar comparison projection and limited `DO` expression execution; no table-backed expression support |
| `<=>` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible `ENUM` labels or ordinals, compatible `SET` member lists or bitmaps, string/`ENUM`/`SET` `NULL`, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands only; string literal comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, string-family comparisons against signed integer RHS literals use numeric coercion, and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 plus numeric string/decimal/float scalar NULL-safe equality projection and limited `DO` expression execution |
| `%, MOD` | 🟡 | Limited scalar/`DO`/constant-default modulo plus single-table row-scalar `%`, infix `MOD`, and `MOD(left, right)` projection, supported predicate/order/nested numeric contexts, numeric and string operands, MySQL string-coercion warnings, and zero-divisor `NULL` warnings; gaps: exact DECIMAL metadata, binary/JSON/temporal coercion, DML assignment expressions, and full expression metadata |
| `*` | 🟡 | Limited scalar/`DO`/constant-default multiplication plus single-table row-scalar projection, supported predicate/order/nested numeric contexts, numeric and string operands, MySQL string-coercion warnings, and checked integer overflow; gaps: exact DECIMAL metadata, binary/JSON/temporal coercion, broad DML assignment expressions, and full expression metadata |
| `+` | 🟡 | Limited scalar/`DO`/constant-default addition plus documented integer `UPDATE` slices and single-table row-scalar projection, supported predicate/order/nested numeric contexts, numeric and string operands, MySQL string-coercion warnings, and checked integer overflow; gaps: exact DECIMAL metadata, binary/JSON/temporal coercion, broad DML assignment expressions, and full expression metadata |
| `-` (binary) | 🟡 | Limited scalar/`DO`/constant-default subtraction plus documented integer `UPDATE` slices and single-table row-scalar projection, supported predicate/order/nested numeric contexts, numeric and string operands, MySQL string-coercion warnings, and checked integer overflow; gaps: exact DECIMAL metadata, binary/JSON/temporal coercion, broad DML assignment expressions, and full expression metadata |
| `-` (unary) | 🟡 | Limited no-source/`DUAL` signed-64 scalar unary arithmetic projection, limited `DO` expression execution, limited constant integer expression-default evaluation with unary `+` and unary `-`, limited source-free constant integer `UPDATE` assignment expressions for integer-family targets, single-table row-scalar projection over the documented table-backed arithmetic operand envelope, and unary descriptor-column predicate roots; no unsigned-width expression results, broad DML assignment expressions, or full expression metadata |
| `/` | 🟡 | Limited scalar/`DO` division plus single-table row-scalar projection, supported predicate/order/nested numeric contexts, numeric and string operands, MySQL string-coercion warnings, and zero-divisor `NULL` warnings; gaps: exact fixed-scale DECIMAL results/metadata, binary/JSON/temporal coercion, DML assignment expressions, and full expression metadata |
| `:=` | 🟡 | Limited user-variable assignment inside supported `SET @name := value` assignment lists and deprecated scalar `@name := expression` in no-source/`DUAL` scalar `SELECT` and `DO` surfaces with warning `1287`; table-row assignment-expression execution remains unsupported |
| `=` (assignment) | 🟡 | One unqualified descriptor-column single-table `UPDATE` assignment to a supported decimal integer literal, limited source-free constant integer arithmetic expression for integer-family targets, compatible bit literal for `BIT` targets, fixed decimal literal for decimal targets, compatible `ENUM` label or ordinal for enum targets, compatible `SET` member list or bitmap for set targets, compatible canonical `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` string literal for temporal targets, binary string or hex literal for binary string targets, `TRUE`, `FALSE`, `NULL`, descriptor-resolved `DEFAULT`, limited scalar subquery values, or limited same-column integer `+`/`-` arithmetic for non-key targets; limited supported system-variable `SET` assignments; limited user-variable `SET @name = value` assignments; general table-backed expression assignments remain unsupported |
| `=` (comparison) | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates with non-`NULL` decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible `ENUM` labels or ordinals, compatible `SET` member lists or bitmaps, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string right operands; limited descriptor `column = NULL`, signed-64 scalar-literal equality, signed-64 literal-left descriptor equality, documented single-table row-scalar function-subject comparisons, and documented row-scalar RHS values with row operands are also supported in `WHERE`; string literal comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, string-family comparisons against signed integer RHS literals use numeric coercion, and `TIME` comparisons use signed total-second semantics; limited no-source/`DUAL` signed-64 plus numeric string/decimal/float scalar comparison projection and limited `DO` expression execution; no general table-backed expression support beyond documented row-scalar predicates |
| `^` | 🟡 | Limited no-source/`DUAL`, `DO`, single-table row-scalar projection, parenthesized comparison predicates, and direct `ORDER BY` keys over the documented integer-domain operands; distinct from keyword logical `XOR`; no unparenthesized row predicate subjects, broader row coercion, or binary-string bit operations |
| `ABS()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `SIGN()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `CEIL()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/hex/bit/JSON/temporal coercion and exact expression metadata |
| `CEILING()` | 🟡 | Synonym for the limited `CEIL()` subset |
| `FLOOR()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/hex/bit/JSON/temporal coercion and exact expression metadata |
| `ROUND()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string value and places coercion emit MySQL warnings; gaps: exact-decimal rounding/coercion, binary/JSON/temporal coercion, and exact expression metadata |
| `BIN()` | 🟡 | Limited scalar/`DO` plus single-table row-scalar projection/nesting, comparison predicates, and `ORDER BY` expression keys over integer-domain operands; returns unsigned 64-bit base-2 text; gaps: string/decimal/float/hex/bit row coercion, non-ASCII collation parity, and exact expression metadata |
| `OCT()` | 🟡 | Limited scalar/`DO` plus single-table row-scalar projection/nesting, comparison predicates, and `ORDER BY` expression keys over integer-domain operands; returns unsigned 64-bit base-8 text; gaps: string/decimal/float/hex/bit row coercion, non-ASCII collation parity, and exact expression metadata |
| `CONV()` | 🟡 | Limited scalar/`DO` plus single-table row-scalar projection/nesting, comparison predicates, and `ORDER BY` expression keys over integer-domain value/base operands with absolute bases `2..36`; returns MySQL-style signed/unsigned base-conversion text; gaps: string/decimal/float/hex/bit row coercion, non-ASCII collation parity, and exact expression metadata |
| `SQRT()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; negative inputs return `NULL`; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `ACOS()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; values outside `-1..1` return `NULL`; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `ASIN()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; values outside `-1..1` return `NULL`; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `SIN()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `COS()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `TAN()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `COT()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; zero-valued inputs report `1690 / 22003`; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `ATAN()` / `ATAN2()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `DEGREES()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `RADIANS()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls; row string coercion emits MySQL warnings; gaps: binary/JSON/temporal coercion and exact expression metadata |
| `AND, &&` | 🟡 | Limited descriptor-backed `WHERE` boolean expressions over existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate atoms, including the limited scalar-literal predicate atoms; `AND` binds tighter than `XOR` and `OR`, `&&` is accepted with deprecation warning 1287, and limited no-source/`DUAL` scalar logical projection plus limited `DO` expression execution admit keyword `AND` only |
| `BETWEEN ... AND ...` | 🟡 | Limited descriptor-backed `WHERE` range predicates for existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate contexts with one descriptor column and two decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, or compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` string bounds; also supports WordPress-style row-scalar `CAST(... AS SIGNED)` quoted integer bounds, `CAST(... AS DECIMAL(M,D))` finite decimal string bounds, documented direct row-scalar value bounds with row operands, documented bare descriptor-column value bounds, and row-scalar arithmetic subjects with optional redundant parenthesized wrapper and nested arithmetic grouping in the existing descriptor-backed `SELECT` envelopes; also supported over one synthetic `INFORMATION_SCHEMA` metadata column with supported metadata literals; string ranges use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation for base tables and metadata-declared ASCII collations for `INFORMATION_SCHEMA`; `TIME` ranges use signed total-second semantics; no general expression operands, non-ASCII/full-Unicode string collation parity, row ranges, full fixed-precision decimal predicate arithmetic, or full expression-level operator support |
| `BINARY` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `SELECT` `CAST(value AS BINARY)`, `CONVERT(value, BINARY)`, `CONVERT(value USING BINARY)`, and no-source/`DUAL` unary `BINARY value` over admitted scalar values plus descriptor integer-family and nonbinary string-family row operands with bare `BINARY` only; no-source scalar `CAST(value AS BINARY(N))` and `CONVERT(value, BINARY(N))` truncate the current scalar text result without NUL padding; scalar `BINARY value REGEXP/RLIKE BINARY pattern` and comparison-left `CAST(... AS BINARY) REGEXP/RLIKE [BINARY] 'pattern'` predicates use case-sensitive ASCII regex matching in the current documented envelopes. No length-bearing row-scalar binary casts, binary string or `BIT` row operands, general binary comparisons, DML assignments, embedded-NUL scalar delivery, or full binary comparison semantics |
| `BIT_COUNT()` | 🟡 | Scalar plus row-backed numeric projection, supported comparison predicates, `ORDER BY` expression keys, arithmetic/control-flow operands, and nested covered numeric calls over integer-domain and nonbinary string operands; no binary-string bit counting |
| `CASE` | 🟡 | Limited no-source and `FROM DUAL` top-level scalar searched and simple `CASE` projection plus limited `DO` expression execution whose children may use existing non-`CASE` signed-64 scalar expressions; limited single-table searched row-scalar projection and narrow joined simple row-scalar projection over integer descriptor/literal operands are also admitted; no nested `CASE` beyond the current row-control-flow depth, `CASE` as a larger scalar operand, predicates, DML assignments, string/collation behavior, subqueries, full result type aggregation, or expression metadata |
| `DIV` | 🟡 | Limited scalar/`DO`/constant-default integer division plus single-table row-scalar projection, supported predicate/order/nested numeric contexts, numeric and string operands, MySQL string-coercion warnings, truncation toward zero, and zero-divisor `NULL` warnings; gaps: exact DECIMAL metadata, binary/JSON/temporal coercion, DML assignment expressions, and full expression metadata |
| `EXISTS()` | 🟡 | Limited descriptor-driven `SELECT ... WHERE EXISTS (subquery)` predicate slice for one outer base table and tableless/`DUAL` or one-table inner subqueries with optional limited predicate, simple correlated integer equality/null-safe equality, and `LIMIT row_count`; SELECT predicates also support limited descriptor-backed inner `INNER JOIN` sources with equality `ON` conditions and correlated integer equality to the outer row; also supports uncorrelated `[NOT] EXISTS` filters for no-source/`FROM DUAL` row-scalar `INSERT ... SELECT` sources; no scalar projection, other DML predicates, nested subqueries, derived tables, joined shapes outside the documented SELECT inner `INNER JOIN` subset, arbitrary expressions, or optimizer parity |
| `IN()` | 🟡 | Limited descriptor-backed `WHERE` membership predicates for existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate contexts with one descriptor column and a nonempty list of decimal integer, `TRUE`/`FALSE`, compatible `BIT` literals for `BIT` targets, ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string literals, compatible canonical `DATE` / `TIME` strings, compatible canonical plus limited `T`-separator/numeric-offset/single trailing-`Z`/`z` truncation-warning `DATE` / `DATETIME` / `TIMESTAMP` strings, `NULL`, documented bare descriptor-column value entries, or documented direct row-scalar value entries with row operands; also supports row-scalar arithmetic subjects with optional redundant parenthesized wrapper and nested arithmetic grouping in the documented single-table `SELECT` predicate envelope, limited descriptor-driven `SELECT ... WHERE descriptor_column IN (SELECT [DISTINCT] descriptor_column FROM descriptor_source [WHERE ...])` subqueries over compatible integer-family or ASCII nonbinary string-family columns in the current single-table or joined `SELECT` predicate envelope, limited SELECT-style scalar-subquery membership `(SELECT descriptor_column FROM descriptor_source [WHERE ...]) IN (literal_list)` using the inner descriptor to convert literal-list values, plus single-table `UPDATE` predicate subqueries and literal-list `INFORMATION_SCHEMA` metadata predicates; string membership uses MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation for base tables and metadata-declared ASCII collations for `INFORMATION_SCHEMA`; `TIME` membership uses signed total-second semantics; scalar-subquery membership does not yet enforce MySQL's multi-row scalar-subquery diagnostic; no metadata predicate subqueries, subqueries outside the documented predicate slices, row constructors, non-ASCII/full-Unicode string collation parity, or full expression-level operator support |
| `IS` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates for descriptor-column `IS NULL`, `IS TRUE`, `IS FALSE`, and `IS UNKNOWN`, comparison-result postfix `IS NULL` / `IS UNKNOWN` over documented comparison predicates, limited signed-64 scalar-literal `WHERE` truth tests for the same forms, and row-scalar arithmetic subjects with optional redundant parenthesized wrapper and nested arithmetic grouping in the documented single-table `SELECT` predicate envelope; limited no-source/`DUAL` signed-64 scalar projection and limited `DO` expression execution for `IS NULL`, `IS TRUE`, `IS FALSE`, and `IS UNKNOWN`; no broad table-backed expression projection, string truth conversion outside supported scalar/row arithmetic coercion, or unparenthesized scalar `IS` chaining |
| `IS NOT` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates for descriptor-column `IS NOT NULL`, `IS NOT TRUE`, `IS NOT FALSE`, and `IS NOT UNKNOWN`, comparison-result postfix `IS NOT NULL` / `IS NOT UNKNOWN` over documented comparison predicates, limited signed-64 scalar-literal `WHERE` truth tests for the same forms, and row-scalar arithmetic subjects with optional redundant parenthesized wrapper and nested arithmetic grouping in the documented single-table `SELECT` predicate envelope; limited no-source/`DUAL` signed-64 scalar projection and limited `DO` expression execution for `IS NOT NULL`, `IS NOT TRUE`, `IS NOT FALSE`, and `IS NOT UNKNOWN`; no broad table-backed expression projection, string truth conversion outside supported scalar/row arithmetic coercion, or unparenthesized scalar `IS` chaining |
| `IS NOT NULL` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates over admitted integer, decimal, string, binary string, `BIT`, limited `ENUM`, limited `SET`, and temporal descriptor columns, plus comparison-result postfix predicates over documented comparisons and limited signed-64 scalar-literal `WHERE` operands; limited no-source/`DUAL` scalar projection and limited `DO` expression execution over admitted signed-64 scalar operands |
| `IS NULL` | 🟡 | Descriptor-driven filtered table `SELECT`, `DELETE`, and `UPDATE` predicates over admitted integer, decimal, string, binary string, `BIT`, limited `ENUM`, limited `SET`, and temporal descriptor columns, plus comparison-result postfix predicates over documented comparisons and limited signed-64 scalar-literal `WHERE` operands; limited no-source/`DUAL` scalar projection and limited `DO` expression execution over admitted signed-64 scalar operands |
| `LIKE` | 🟡 | Limited descriptor-backed `WHERE` predicate for `CHAR`, `VARCHAR`, and baseline `TEXT` family columns with an ASCII string pattern literal or `NULL` in existing filtered `SELECT`, aggregate-source, `DELETE`, and `UPDATE` contexts, including `LIKE BINARY` case-sensitive matching for the same table-predicate subset, plus one-column synthetic `INFORMATION_SCHEMA` metadata predicates for ordinary `LIKE`; also supports WordPress-style `CAST(... AS DECIMAL(M,D)) LIKE 'pattern'` predicates with fixed-scale decimal text formatting in the existing descriptor-backed `SELECT` envelopes. Default mode supports `%`, `_`, and backslash wildcard escaping, while `NO_BACKSLASH_ESCAPES` disables backslash pattern escaping for this slice; metadata predicates use metadata-declared ASCII collations; no explicit `ESCAPE`, non-ASCII collation parity, general expression operands, parameter patterns, binary string storage predicates, metadata `LIKE BINARY`, or general expression-level pattern matching |
| `NOT, !` | 🟡 | Limited keyword `NOT` over descriptor-backed `WHERE` boolean expressions for existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate atoms; limited no-source/`DUAL` scalar logical projection and limited `DO` expression execution admit keyword `NOT`; symbolic `!` remains unsupported until MyLite owns its higher-precedence expression semantics and deprecation warning behavior |
| `NOT BETWEEN ... AND ...` | 🟡 | Limited negation of the descriptor-backed `BETWEEN` predicate subset, including the WordPress-style row-scalar `CAST(... AS SIGNED)`, `CAST(... AS DECIMAL(M,D))`, documented direct row-scalar value bounds with row operands, documented bare descriptor-column value bounds, and row-scalar arithmetic subjects with optional redundant parenthesized wrapper and nested arithmetic grouping in the documented single-table `SELECT` predicate envelope; no general expression operands, non-ASCII/full-Unicode string collation parity, row ranges, full fixed-precision decimal predicate arithmetic, or full expression-level operator support |
| `NOT EXISTS()` | 🟡 | Limited negation of the descriptor-driven `EXISTS` predicate slice through existing `NOT` predicate semantics |
| `NOT IN()` | 🟡 | Limited negation of the descriptor-backed `IN` predicate subset, including MySQL three-valued `NULL` list semantics for documented bare descriptor-column and direct row-scalar value entries with row operands, row-scalar arithmetic subjects with optional redundant parenthesized wrapper, nested arithmetic grouping, and subquery semantics for the documented `SELECT`, SELECT-style scalar-subquery membership, and single-table `UPDATE` predicate slices; no subqueries outside those slices, row constructors, non-ASCII/full-Unicode string collation parity, or full expression-level operator support |
| `NOT LIKE` | 🟡 | Limited negation of the descriptor-backed `LIKE` predicate subset, including `NOT LIKE BINARY`, in existing filtered `SELECT`, aggregate-source, `DELETE`, and `UPDATE` contexts, plus WordPress-style `CAST(... AS DECIMAL(M,D)) NOT LIKE 'pattern'` predicates in the existing descriptor-backed `SELECT` envelopes; `NULL` column values remain unmatched like MySQL |
| `NOT REGEXP` | 🟡 | Limited negation of the descriptor-backed `REGEXP` predicate subset in existing filtered `SELECT`, aggregate-source, `DELETE`, and `UPDATE` contexts, plus limited no-source/`DUAL` scalar `NOT REGEXP`/`NOT RLIKE` over the current scalar regexp operand subset and WordPress-style `CAST(... AS BINARY) NOT REGEXP [BINARY] 'pattern'` predicates in the existing descriptor-backed `SELECT` envelopes; `NULL` column values remain unmatched like MySQL |
| `OR, \|\|` | 🟡 | Limited descriptor-backed `WHERE` boolean expressions over existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate atoms, including the limited scalar-literal predicate atoms; `OR` binds looser than `XOR` and `AND`, and `\|\|` is accepted as logical OR with deprecation warning 1287 in predicate contexts when `PIPES_AS_CONCAT` is inactive. With `PIPES_AS_CONCAT`, limited `\|\|` string concatenation is supported for no-source/`DUAL` scalar projection, `DO`, and current single-table row-scalar `SELECT` projection; no predicate-level concatenation truth evaluation, scalar logical `\|\|` projection outside this mode, or general expression-level boolean/string semantics |
| `REGEXP` | 🟡 | Limited descriptor-backed `WHERE` predicate for `CHAR`, `VARCHAR`, and baseline `TEXT` family columns with an ASCII regular-expression string literal in existing filtered `SELECT`, aggregate-source, `DELETE`, and `UPDATE` contexts; includes fixed `{n}` repetition, one-level optional literal groups, and top-level nonempty alternation branches in the baseline ASCII subset; also supports limited no-source/`DUAL` scalar regexp over admitted string/integer/boolean/`NULL` literals and binary/character conversions, plus WordPress-style `CAST(... AS BINARY) REGEXP [BINARY] 'pattern'` predicates with case-sensitive ASCII matching in the existing descriptor-backed `SELECT` envelopes. Mixed binary/nonbinary scalar operands report MySQL-compatible `3995 / HY000` when the nonbinary side is non-`NULL`. No general table-backed expression operands, parameter patterns, non-ASCII pattern literals, grouped alternation, broad pattern-side binary semantics, or full regex syntax |
| `RLIKE` | 🟡 | Synonym for the limited `REGEXP` predicate subset, including the scalar and WordPress-style binary-cast row-scalar predicate forms |
| `SOUNDS LIKE` | 🟡 | Limited no-source, `DUAL`, `DO`, single-table row-scalar `SELECT` projection, and descriptor-backed `WHERE` predicate support over the current `SOUNDEX()` operand envelope; compares generated MySQL-style soundex values and returns `NULL` when either operand is `NULL`. No `NOT SOUNDS LIKE`, binary-string or `BIT` row-backed inputs, approximate numeric row-backed inputs, scalar decimal/float literals beyond the current `SOUNDEX()` scalar subset, DML assignments, ordering/grouping expressions, joins beyond the current row-scalar envelope, nested functions, parameters, or full expression metadata |
| `XOR` | 🟡 | Limited descriptor-backed `WHERE` boolean expressions over existing `SELECT`/aggregate-source/`DELETE`/`UPDATE` predicate atoms, including the limited scalar-literal predicate atoms; `XOR` binds looser than `AND` and tighter than `OR`, propagates `NULL` for the admitted predicate subset, and has limited no-source/`DUAL` scalar logical projection plus limited `DO` expression execution support |
| `\|` | 🟡 | Limited no-source/`DUAL`, `DO`, single-table row-scalar projection, parenthesized comparison predicates, and direct `ORDER BY` keys over the documented integer-domain operands; no unparenthesized row predicate subjects, broader row coercion, or binary-string bit operations |
| `~` | 🟡 | Limited no-source/`DUAL`, `DO`, single-table row-scalar projection, parenthesized comparison predicates, and direct `ORDER BY` keys over the documented integer-domain operands; no unparenthesized row predicate subjects, broader row coercion, or binary-string bit operations |

[Back to compatibility overview](../../COMPATIBILITY.md)
