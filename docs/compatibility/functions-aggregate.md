# Aggregate functions

Aggregate functions and grouping helpers.

Current ungrouped aggregate `SELECT` lists can mix supported aggregate items in
the documented one-table/no-source envelope with baseline `WHERE`, aliases, and
`LIMIT`; see [baseline multi-aggregate SELECT](../specs/baseline-multi-aggregate-select/specs.md).

Grouped `HAVING` support includes selected descriptor-column aggregate
expressions, selected bitwise aggregate aliases and repeated selected bitwise
aggregate expressions with supported nonnegative integer comparison literals,
selected statistical aggregate aliases and repeated selected statistical
aggregate expressions,
selected `GROUP_CONCAT()` aggregate aliases and repeated selected
`GROUP_CONCAT()` expressions with `IS NULL` / `IS NOT NULL`,
selected `ANY_VALUE()` aggregate aliases with supported null and comparison
predicates, grouped nonbinary string descriptor columns and selected aliases
with ordinary string-literal comparison predicates and supported literal-list
`IN` predicates,
and selected `COUNT()`, `SUM()`, `AVG()`, `MIN()`, and `MAX()` row-scalar
aggregate expressions in the documented envelopes; see
[baseline HAVING grouped aggregate](../specs/baseline-having-grouped-aggregate/specs.md)
and
[baseline grouped selected row-scalar aggregate expression HAVING](../specs/baseline-grouped-selected-row-scalar-aggregate-expression-having/specs.md)
and
[baseline grouped bitwise aggregate HAVING](../specs/baseline-grouped-bitwise-aggregate-having/specs.md)
and
[baseline grouped statistical aggregate HAVING](../specs/baseline-grouped-statistical-aggregate-having/specs.md)
and
[baseline grouped GROUP_CONCAT aggregate alias HAVING](../specs/baseline-grouped-group-concat-aggregate-alias-having/specs.md)
and
[baseline grouped selected GROUP_CONCAT aggregate expression HAVING](../specs/baseline-grouped-selected-group-concat-aggregate-expression-having/specs.md)
and
[baseline grouped ANY_VALUE aggregate alias comparison HAVING](../specs/baseline-grouped-any-value-aggregate-alias-comparison-having/specs.md)
and
[baseline grouped string comparison HAVING](../specs/baseline-grouped-string-comparison-having/specs.md)
and
[baseline grouped HAVING IN predicate](../specs/baseline-grouped-having-in-predicate/specs.md).

The parser also admits common MySQL aggregate-window `OVER` syntax. Projection
windows for `COUNT(*)`, `COUNT(value)`, and integer-domain `SUM()` / `AVG()` /
`MIN()` / `MAX()` execute in the documented core aggregate-window baseline, and
integer-domain `BIT_AND()` / `BIT_OR()` / `BIT_XOR()` windows execute in the
documented bitwise aggregate-window baseline. Integer-domain `STD()` /
`STDDEV()` / `STDDEV_POP()` / `STDDEV_SAMP()` / `VAR_POP()` / `VAR_SAMP()` /
`VARIANCE()` windows execute in the documented statistical aggregate-window
baseline.
`DISTINCT` aggregate windows return MySQL's `1235 / 42000` unsupported
diagnostic. `GROUP_CONCAT(...)`, JSON, custom, and broader aggregate-window
forms remain limited. See
[parser aggregate window syntax](../specs/parser-aggregate-window-syntax/specs.md),
[baseline core aggregate window functions](../specs/baseline-core-aggregate-window-functions/specs.md),
[baseline bitwise aggregate window functions](../specs/baseline-bitwise-aggregate-window-functions/specs.md),
[baseline statistical aggregate window functions](../specs/baseline-statistical-aggregate-window-functions/specs.md),
[parser corpus aggregate/window surfaces](../specs/parser-corpus-aggregate-window-surfaces/specs.md),
and
[parser corpus JSON/statistical aggregate window surfaces](../specs/parser-corpus-json-stat-aggregate-window-surfaces/specs.md).

| Function | Status | Notes |
| --- | --- | --- |
| `ANY_VALUE()` | 🟡 | Limited scalar and row-scalar transparent wrapper, mixed ungrouped `ANY_VALUE(column)`, and grouped descriptor-column forms with alias `HAVING`, alias comparison `HAVING`, alias ordering, and selected/hidden expression ordering. Direct grouped `HAVING ANY_VALUE(column)` operands reject with MySQL-compatible unknown-column diagnostics. Representative values remain arbitrary when a group has different candidates. No mixed/grouped expression arguments, `DISTINCT`, `*`, windows, DML/default/check/generated-column contexts, or deterministic representative-row selection |
| `AVG()` | 🟡 | Limited mixed ungrouped `AVG([DISTINCT] expr)` over one descriptor-backed persistent base table with optional source alias, source-qualified operands, baseline `WHERE`, aliases, `LIMIT`, literals, and supported row-scalar arguments, plus limited grouped `AVG([DISTINCT] expr)` over the current grouped source envelope and selected or hidden grouped aggregate-expression ordering; four-fractional-digit text results are produced while the intermediate signed-64 `SUM(expr)` stays in range, and selected grouped aliases/order expressions order by the exact signed rational `SUM()/COUNT()` value. Integer-domain projection windows execute in the core aggregate-window baseline. No exact decimal widening beyond the signed-64 intermediate envelope, broader windows, or full grouping. See [baseline grouped AVG aggregate alias order](../specs/baseline-grouped-avg-aggregate-alias-order/specs.md) and [baseline core aggregate window functions](../specs/baseline-core-aggregate-window-functions/specs.md) |
| `BIT_AND()` | 🟡 | Limited mixed ungrouped `BIT_AND(expr)` over one descriptor-backed persistent base table with optional source alias, source-qualified operands, baseline `WHERE`, aliases, `LIMIT`, literals, and supported row-scalar arguments, plus limited grouped and projection-window forms; numeric unsigned-64 semantics with neutral all-ones output for empty/all-`NULL` input and unsigned decimal text results. No `DISTINCT`, binary-string evaluation, negative bitwise `HAVING` literals, or full grouping. See [baseline bitwise aggregate windows](../specs/baseline-bitwise-aggregate-window-functions/specs.md) |
| `BIT_OR()` | 🟡 | Limited mixed ungrouped `BIT_OR(expr)` over one descriptor-backed persistent base table with optional source alias, source-qualified operands, baseline `WHERE`, aliases, `LIMIT`, literals, and supported row-scalar arguments, plus limited grouped and projection-window forms; numeric unsigned-64 semantics with neutral `0` output for empty/all-`NULL` input and unsigned decimal text results. No `DISTINCT`, binary-string evaluation, negative bitwise `HAVING` literals, or full grouping. See [baseline bitwise aggregate windows](../specs/baseline-bitwise-aggregate-window-functions/specs.md) |
| `BIT_XOR()` | 🟡 | Limited mixed ungrouped `BIT_XOR(expr)` over one descriptor-backed persistent base table with optional source alias, source-qualified operands, baseline `WHERE`, aliases, `LIMIT`, literals, and supported row-scalar arguments, plus limited grouped and projection-window forms; numeric unsigned-64 semantics with neutral `0` output for empty/all-`NULL` input and unsigned decimal text results. No `DISTINCT`, binary-string evaluation, negative bitwise `HAVING` literals, or full grouping. See [baseline bitwise aggregate windows](../specs/baseline-bitwise-aggregate-window-functions/specs.md) |
| Core aggregate windows | ✅ | Projection-only `COUNT(*)`, `COUNT(value)`, and integer-domain `SUM()` / `AVG()` / `MIN()` / `MAX()` windows execute over the current no-source, `DUAL`, one-table, named-window, partition/order/frame row-scalar window envelope; `DISTINCT` windows return MySQL `1235`. See [baseline core aggregate window functions](../specs/baseline-core-aggregate-window-functions/specs.md) |
| Bitwise aggregate windows | ✅ | Projection-only integer-domain `BIT_AND()` / `BIT_OR()` / `BIT_XOR()` windows execute over the current no-source, `DUAL`, one-table, named-window, partition/order/frame row-scalar window envelope. See [baseline bitwise aggregate window functions](../specs/baseline-bitwise-aggregate-window-functions/specs.md) |
| `COUNT()` | 🟡 | Limited `COUNT(*)`, `COUNT(integer/NULL/boolean literal)`, `COUNT(column)`, supported row-scalar `COUNT(expr)`, descriptor-column `COUNT(DISTINCT column)`, and single-expression `COUNT(DISTINCT literal_or_supported_row_scalar_expr)` in documented no-source, one-table, mixed ungrouped, joined-count-star, current joined count-expression, base-table grouped, multiple-key base-table grouped, and joined grouped forms; optional aliases, source aliases, source-qualified column arguments, baseline `WHERE`, selected grouped `HAVING`, selected or hidden grouped aggregate-expression ordering, and supported `LIMIT` envelopes are covered. Projection windows execute in the core aggregate-window baseline. The parser additionally admits unsupported multi-expression `COUNT(DISTINCT expr, expr...)` placeholders. No broad `COUNT(expr)`, broader joined non-star count forms, executable `DISTINCT` windows, or full grouping |
| `COUNT(DISTINCT)` | 🟡 | Limited descriptor-column `COUNT(DISTINCT column) [AS alias]` in the mixed ungrouped one-table and grouped aggregate envelopes, plus single-expression distinct literal and supported row-scalar arguments in the current count-expression and grouped aggregate envelopes with optional source alias, source-qualified operands, baseline `WHERE`, supported joined count-expression sources, selected grouped `HAVING`, and supported `LIMIT`; integer, nonbinary string, and binary string descriptor columns are covered, and supported nonbinary string row-scalar arguments use MyLite's registered MySQL-like string collation. The parser additionally admits multiple-expression placeholders and rejects unsupported execution deterministically. No executable multiple expressions, broader source forms, full Unicode collation parity, or executable `DISTINCT` windows |
| `GROUP_CONCAT()` | 🟡 | Limited mixed ungrouped and grouped `GROUP_CONCAT([DISTINCT] value [ORDER BY order_column [ASC\|DESC]] [SEPARATOR 'literal'])` and non-distinct `GROUP_CONCAT(value, value... ...)` over one descriptor-backed base table, where each value is a descriptor column or supported row-scalar expression and multiple values use per-row `CONCAT()` semantics before aggregation; selected grouped aliases support ordering and `HAVING` null predicates, and repeated selected aggregate expressions support ordering plus `HAVING` null predicates in the current grouped `GROUP_CONCAT()` envelope. The aggregate-local order key is a `NOT NULL` integer/`BIT`/`YEAR`/temporal descriptor column; aliases, source-qualified value/order columns, baseline `WHERE`, empty/all-`NULL` `NULL` results, and `@@group_concat_max_len` truncation warnings are covered for the supported subset. Multi-value `DISTINCT`, nullable/string/multiple order keys, binary result metadata, full grouping, joins, hidden aggregate order keys, executable aggregate windows, and metadata threshold behavior remain limited |
| `GROUPING()` | 🟡 | MySQL-runtime-verified `GROUPING(group_key)` markers work in select lists for the single-key `GROUP BY ... WITH ROLLUP` baseline, returning `0` for ordinary grouped rows and `1` for the final rollup row; multi-argument bitmasks, multi-key rollup subtotals, `HAVING`, `ORDER BY`, scalar, and DML contexts remain unsupported |
| `JSON_ARRAYAGG()` | 🟡 | Limited executable non-window aggregate over no-source/`DUAL`, one descriptor-backed table, and grouped aggregate envelopes using the current JSON constructor argument subset; SQL `NULL` values become JSON `null`, empty input returns SQL `NULL`, and JSON descriptor values are embedded as JSON documents. No window execution, deterministic unordered-source ordering, arbitrary expressions, broader source forms, or full JSON metadata |
| `JSON_OBJECTAGG()` | 🟡 | Limited executable non-window aggregate over no-source/`DUAL`, one descriptor-backed table, and grouped aggregate envelopes using the current JSON constructor key/value subset; duplicate keys keep the last value, SQL `NULL` keys report MySQL-compatible `3158`, empty input returns SQL `NULL`, and JSON descriptor values are embedded as JSON documents. No window execution, deterministic unordered-source duplicate-key ordering, arbitrary expressions, broader source forms, or full JSON metadata |
| `MAX()` | 🟡 | Limited mixed ungrouped `MAX([DISTINCT] expr)` over one descriptor-backed persistent base table with optional source alias, source-qualified operands, baseline `WHERE`, aliases, `LIMIT`, literals, and supported row-scalar arguments, plus limited grouped form over the current grouped source envelope and selected or hidden grouped aggregate-expression ordering; integer/`NULL` and nonbinary string result domains are covered for the documented envelope. Integer-domain projection windows execute in the core aggregate-window baseline. Full grouping, binary string comparison semantics, and broader windows remain limited |
| `MIN()` | 🟡 | Limited mixed ungrouped `MIN([DISTINCT] expr)` over one descriptor-backed persistent base table with optional source alias, source-qualified operands, baseline `WHERE`, aliases, `LIMIT`, literals, and supported row-scalar arguments, plus limited grouped form over the current grouped source envelope and selected or hidden grouped aggregate-expression ordering; integer/`NULL` and nonbinary string result domains are covered for the documented envelope. Integer-domain projection windows execute in the core aggregate-window baseline. Full grouping, binary string comparison semantics, and broader windows remain limited |
| `STD()` | 🟡 | Limited alias for `STDDEV_POP(expr)` in current mixed ungrouped, tableless, supported row-scalar, grouped, and projection-window aggregate forms over integer descriptor values, including selected or hidden grouped aggregate-expression ordering and selected grouped `HAVING`; empty/all-`NULL` inputs return `NULL`. No `DISTINCT`, string-to-double warnings, or full grouping. See [baseline statistical aggregate windows](../specs/baseline-statistical-aggregate-window-functions/specs.md), [baseline statistical aggregates](../specs/baseline-statistical-aggregates/specs.md), [baseline grouped statistical aggregate HAVING](../specs/baseline-grouped-statistical-aggregate-having/specs.md), [baseline grouped statistical aggregate alias order](../specs/baseline-grouped-statistical-aggregate-alias-order/specs.md), [baseline grouped selected statistical aggregate expression order](../specs/baseline-grouped-selected-statistical-aggregate-expression-order/specs.md), [baseline grouped selected row-scalar aggregate expression order](../specs/baseline-grouped-selected-row-scalar-aggregate-expression-order/specs.md), and [baseline hidden grouped aggregate order keys](../specs/baseline-hidden-grouped-aggregate-order-keys/specs.md) |
| `STDDEV()` | 🟡 | Limited alias for `STDDEV_POP(expr)` in current mixed ungrouped, tableless, supported row-scalar, grouped, and projection-window aggregate forms over integer descriptor values, including selected or hidden grouped aggregate-expression ordering and selected grouped `HAVING`; empty/all-`NULL` inputs return `NULL`. No `DISTINCT`, string-to-double warnings, or full grouping. See [baseline statistical aggregate windows](../specs/baseline-statistical-aggregate-window-functions/specs.md), [baseline statistical aggregates](../specs/baseline-statistical-aggregates/specs.md), [baseline grouped statistical aggregate HAVING](../specs/baseline-grouped-statistical-aggregate-having/specs.md), [baseline grouped statistical aggregate alias order](../specs/baseline-grouped-statistical-aggregate-alias-order/specs.md), [baseline grouped selected statistical aggregate expression order](../specs/baseline-grouped-selected-statistical-aggregate-expression-order/specs.md), [baseline grouped selected row-scalar aggregate expression order](../specs/baseline-grouped-selected-row-scalar-aggregate-expression-order/specs.md), and [baseline hidden grouped aggregate order keys](../specs/baseline-hidden-grouped-aggregate-order-keys/specs.md) |
| `STDDEV_POP()` | 🟡 | Limited population standard deviation in current mixed ungrouped, tableless, supported row-scalar, grouped, and projection-window aggregate forms over integer descriptor values, including selected or hidden grouped aggregate-expression ordering and selected grouped `HAVING`; single non-`NULL` inputs return `0`. No `DISTINCT`, string-to-double warnings, or full grouping. See [baseline statistical aggregate windows](../specs/baseline-statistical-aggregate-window-functions/specs.md), [baseline statistical aggregates](../specs/baseline-statistical-aggregates/specs.md), [baseline grouped statistical aggregate HAVING](../specs/baseline-grouped-statistical-aggregate-having/specs.md), [baseline grouped statistical aggregate alias order](../specs/baseline-grouped-statistical-aggregate-alias-order/specs.md), [baseline grouped selected statistical aggregate expression order](../specs/baseline-grouped-selected-statistical-aggregate-expression-order/specs.md), [baseline grouped selected row-scalar aggregate expression order](../specs/baseline-grouped-selected-row-scalar-aggregate-expression-order/specs.md), and [baseline hidden grouped aggregate order keys](../specs/baseline-hidden-grouped-aggregate-order-keys/specs.md) |
| `STDDEV_SAMP()` | 🟡 | Limited sample standard deviation in current mixed ungrouped, tableless, supported row-scalar, grouped, and projection-window aggregate forms over integer descriptor values, including selected or hidden grouped aggregate-expression ordering and selected grouped `HAVING`; fewer than two non-`NULL` inputs return `NULL`. No `DISTINCT`, string-to-double warnings, or full grouping. See [baseline statistical aggregate windows](../specs/baseline-statistical-aggregate-window-functions/specs.md), [baseline statistical aggregates](../specs/baseline-statistical-aggregates/specs.md), [baseline grouped statistical aggregate HAVING](../specs/baseline-grouped-statistical-aggregate-having/specs.md), [baseline grouped statistical aggregate alias order](../specs/baseline-grouped-statistical-aggregate-alias-order/specs.md), [baseline grouped selected statistical aggregate expression order](../specs/baseline-grouped-selected-statistical-aggregate-expression-order/specs.md), [baseline grouped selected row-scalar aggregate expression order](../specs/baseline-grouped-selected-row-scalar-aggregate-expression-order/specs.md), and [baseline hidden grouped aggregate order keys](../specs/baseline-hidden-grouped-aggregate-order-keys/specs.md) |
| `SUM()` | 🟡 | Limited mixed ungrouped `SUM([DISTINCT] expr)` over one descriptor-backed persistent base table with optional source alias, source-qualified operands, baseline `WHERE`, aliases, `LIMIT`, literals, and supported row-scalar arguments, plus limited grouped form over the current grouped source envelope and selected or hidden grouped aggregate-expression ordering; results are limited to MyLite's signed-64 text result envelope. Integer-domain projection windows execute in the core aggregate-window baseline. No exact decimal result widening beyond signed 64 bits, broader windows, or full grouping |
| `VAR_POP()` | 🟡 | Limited population variance in current mixed ungrouped, tableless, supported row-scalar, grouped, and projection-window aggregate forms over integer descriptor values, including selected or hidden grouped aggregate-expression ordering and selected grouped `HAVING`; single non-`NULL` inputs return `0`. No `DISTINCT`, string-to-double warnings, or full grouping. See [baseline statistical aggregate windows](../specs/baseline-statistical-aggregate-window-functions/specs.md), [baseline statistical aggregates](../specs/baseline-statistical-aggregates/specs.md), [baseline grouped statistical aggregate HAVING](../specs/baseline-grouped-statistical-aggregate-having/specs.md), [baseline grouped statistical aggregate alias order](../specs/baseline-grouped-statistical-aggregate-alias-order/specs.md), [baseline grouped selected statistical aggregate expression order](../specs/baseline-grouped-selected-statistical-aggregate-expression-order/specs.md), [baseline grouped selected row-scalar aggregate expression order](../specs/baseline-grouped-selected-row-scalar-aggregate-expression-order/specs.md), and [baseline hidden grouped aggregate order keys](../specs/baseline-hidden-grouped-aggregate-order-keys/specs.md) |
| `VAR_SAMP()` | 🟡 | Limited sample variance in current mixed ungrouped, tableless, supported row-scalar, grouped, and projection-window aggregate forms over integer descriptor values, including selected or hidden grouped aggregate-expression ordering and selected grouped `HAVING`; fewer than two non-`NULL` inputs return `NULL`. No `DISTINCT`, string-to-double warnings, or full grouping. See [baseline statistical aggregate windows](../specs/baseline-statistical-aggregate-window-functions/specs.md), [baseline statistical aggregates](../specs/baseline-statistical-aggregates/specs.md), [baseline grouped statistical aggregate HAVING](../specs/baseline-grouped-statistical-aggregate-having/specs.md), [baseline grouped statistical aggregate alias order](../specs/baseline-grouped-statistical-aggregate-alias-order/specs.md), [baseline grouped selected statistical aggregate expression order](../specs/baseline-grouped-selected-statistical-aggregate-expression-order/specs.md), [baseline grouped selected row-scalar aggregate expression order](../specs/baseline-grouped-selected-row-scalar-aggregate-expression-order/specs.md), and [baseline hidden grouped aggregate order keys](../specs/baseline-hidden-grouped-aggregate-order-keys/specs.md) |
| `VARIANCE()` | 🟡 | Limited alias for `VAR_POP(expr)` in current mixed ungrouped, tableless, supported row-scalar, grouped, and projection-window aggregate forms over integer descriptor values, including selected or hidden grouped aggregate-expression ordering and selected grouped `HAVING`; empty/all-`NULL` inputs return `NULL`. No `DISTINCT`, string-to-double warnings, or full grouping. See [baseline statistical aggregate windows](../specs/baseline-statistical-aggregate-window-functions/specs.md), [baseline statistical aggregates](../specs/baseline-statistical-aggregates/specs.md), [baseline grouped statistical aggregate HAVING](../specs/baseline-grouped-statistical-aggregate-having/specs.md), [baseline grouped statistical aggregate alias order](../specs/baseline-grouped-statistical-aggregate-alias-order/specs.md), [baseline grouped selected statistical aggregate expression order](../specs/baseline-grouped-selected-statistical-aggregate-expression-order/specs.md), [baseline grouped selected row-scalar aggregate expression order](../specs/baseline-grouped-selected-row-scalar-aggregate-expression-order/specs.md), and [baseline hidden grouped aggregate order keys](../specs/baseline-hidden-grouped-aggregate-order-keys/specs.md) |
| Statistical aggregate windows | ✅ | Projection-only integer-domain `STD()` / `STDDEV()` / `STDDEV_POP()` / `STDDEV_SAMP()` / `VAR_POP()` / `VAR_SAMP()` / `VARIANCE()` windows execute over the current no-source, `DUAL`, one-table, named-window, partition/order/frame row-scalar window envelope. See [baseline statistical aggregate window functions](../specs/baseline-statistical-aggregate-window-functions/specs.md) |

[Back to compatibility overview](../../COMPATIBILITY.md)
