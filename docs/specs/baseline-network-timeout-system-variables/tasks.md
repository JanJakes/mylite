# Baseline Network Timeout System Variables Tasks

- [x] Capture MySQL 8.4.9 defaults, scopes, ranges, diagnostics, warnings, and
      global/session behavior.
- [x] Write the independent feature specification.
- [x] Add MySQL-runtime expectation coverage.
- [x] Add runtime registry kinds and defaults.
- [x] Add session state, snapshot, scalar, `SHOW`, and `SET` behavior.
- [x] Add focused runtime C tests and CTest registration.
- [x] Update compatibility documentation and `SHOW VARIABLES` baseline tests.
- [x] Run focused MySQL and MyLite verification.
- [x] Run `git diff --check`, staged diff check, and
      `cmake --workflow --preset check`.
- [x] Perform release-gate review, fix findings, commit, and push.
