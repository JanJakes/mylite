# Temporal functions

Date, time, timestamp, interval, time-zone, and calendar functions.

Numeric temporal extractors also have a limited descriptor-source `WHERE` predicate
slice: top-level/truth, comparison against signed integer/boolean/`NULL`
literals, integer/boolean `BETWEEN` ranges, and `IS [NOT] NULL` over one
descriptor-backed table or joined source. This does not admit general
expression predicates, DML assignments, defaults, generated columns, or
grouping/having expressions.

| Function or operator | Status | Notes |
| --- | --- | --- |
| `ADDDATE()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `ADDDATE(value, INTERVAL signed_integer_or_exact_string_or_NULL unit)` for `YEAR`, `QUARTER`, `MONTH`, `WEEK`, `DAY`, `HOUR`, `MINUTE`, and `SECOND` over canonical date/datetime strings, descriptor `DATE` / `DATETIME` / `TIMESTAMP` / nonbinary string-family columns, and `NULL`; row-backed invalid temporal values return `NULL` with warning 1292 and row overflow returns `NULL` with warning 1441; the same core interval form is admitted in limited same-column single-table `UPDATE` assignments for compatible non-key DATE, DATETIME, and nonbinary string targets, where matched invalid values and overflow are errors; no `ADDDATE(date, days)`, `MICROSECOND`, composite units, interval expressions outside the documented update subset, predicates, defaults, generated columns, or general temporal arithmetic |
| `ADDTIME()` | 🟡 | Limited no-source, `FROM DUAL`, and `DO` scalar `ADDTIME(value, time_value)` over `NULL`, canonical `YYYY-MM-DD HH:MM:SS` datetime strings, and canonical `[-]HH:MM:SS` / `[-]HHH:MM:SS` time strings, returning text/`NULL` for in-range results; successful supported expressions produce no warnings; no row-scalar evaluation, numeric/relaxed/fractional/day-hour operands, result clamping, warning-returning invalid conversion, predicates, DML assignments, defaults, generated columns, or general temporal arithmetic |
| `CONVERT_TZ()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar projection over canonical datetime inputs and fixed-offset `+HH:MM` / `-HH:MM` zones; invalid datetimes return `NULL` with warning 1292 and invalid/named zones return `NULL`; no loaded named-zone rows, leap seconds, zoneinfo import, predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `CURDATE()` | 🟡 | Limited zero-fractional statement date in no-source, `FROM DUAL`, `DO`, row-scalar projection, compatible `DATE` DML assignment, and parenthesized generated `DATE` defaults; respects the limited session `timestamp` override and session `time_zone` offset; no loaded named-zone rows or general temporal expression evaluation |
| `CURRENT_DATE(), CURRENT_DATE` | 🟡 | Limited zero-fractional statement date synonyms for `CURDATE()` in the same supported positions, including parenthesized generated `DATE` defaults |
| `CURRENT_TIME(), CURRENT_TIME` | 🟡 | Limited zero-fractional statement time synonyms for `CURTIME()` in no-source, `FROM DUAL`, `DO`, row-scalar projection, compatible `TIME` DML assignment, and parenthesized generated `TIME` defaults, including explicit `CURRENT_TIME(0)` normalization; respects the limited session `timestamp` override and session `time_zone` offset; nonzero fractional precision is rejected, and `fsp > 6` uses MySQL's precision-limit diagnostic; no loaded named-zone rows or general temporal expression evaluation |
| `CURRENT_TIMESTAMP(), CURRENT_TIMESTAMP` | 🟡 | Limited zero-fractional statement timestamp synonym for `NOW()` in no-source, `FROM DUAL`, `DO`, row-scalar projection, supported temporal DML assignment, and `DATETIME` / `TIMESTAMP` defaults and `ON UPDATE`; respects the limited session `timestamp` override and session `time_zone` offset for materialized current values; no fractional precision, loaded named-zone rows, or TIMESTAMP row storage/retrieval conversion |
| `CURTIME()` | 🟡 | Limited zero-fractional statement time in the same supported positions as `CURRENT_TIME`, including parenthesized generated `TIME` defaults and explicit `CURTIME(0)` normalization; respects the limited session `timestamp` override and session `time_zone` offset; nonzero fractional precision is rejected |
| `DATE()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `DATE(value)` over `NULL`, canonical date/datetime strings including admitted zero-date strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning date text or `NULL`; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, relaxed temporal parsing, predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `DATE_ADD()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `DATE_ADD(value, INTERVAL signed_integer_or_exact_string_or_NULL unit)` for `YEAR`, `QUARTER`, `MONTH`, `WEEK`, `DAY`, `HOUR`, `MINUTE`, and `SECOND` over canonical date/datetime strings, descriptor `DATE` / `DATETIME` / `TIMESTAMP` / nonbinary string-family columns, and `NULL`, with MySQL-compatible whitespace handling under `IGNORE_SPACE`; date inputs return `DATE` text for date-bearing units and `DATETIME` text for time-bearing units; row-backed invalid temporal values return `NULL` with warning 1292 and row overflow returns `NULL` with warning 1441; the same core interval form is admitted in limited same-column single-table `UPDATE` assignments for compatible non-key DATE, DATETIME, and nonbinary string targets, where matched invalid values and overflow are errors; no `MICROSECOND`, composite units, interval expressions outside the documented update subset, warning-producing prefix string interval conversion, predicates, defaults, generated columns, or general temporal arithmetic |
| `DATE_FORMAT()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `DATE_FORMAT(value, format)` over `NULL`, canonical date/datetime strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, with a verified token subset, exact top-level numeric comparisons for `DATE_FORMAT(..., '%H.%i' or '%H.%i%s') [=, <>, <, <=, >, >=] numeric_literal`, limited single-table `SELECT WHERE DATE_FORMAT(..., '%H.%i' or '%H.%i%s') [=, <>, <, <=, >, >=] numeric_literal` predicates, and the narrow relaxed-mode grouped weekly archive projection; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, week/year-week tokens, locale/time-zone effects, relaxed temporal parsing, null-safe equality, other predicates, grouped projections outside the documented weekly archive shape, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `DATE_SUB()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `DATE_SUB(value, INTERVAL signed_integer_or_exact_string_or_NULL unit)` over the same current `DATE_ADD()` unit and input subset, with subtraction semantics and MySQL-compatible `DATE_SUB` whitespace/identifier handling under `IGNORE_SPACE`; row-backed invalid temporal values return `NULL` with warning 1292 and row overflow returns `NULL` with warning 1441; the same core interval form is admitted in limited same-column single-table `UPDATE` assignments for compatible non-key DATE, DATETIME, and nonbinary string targets, where matched invalid values and overflow are errors; no `MICROSECOND`, composite units, interval expressions outside the documented update subset, warning-producing prefix string interval conversion, predicates, defaults, generated columns, or general temporal arithmetic |
| `DATEDIFF()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `DATEDIFF(value1, value2)` over `NULL`, canonical date/datetime strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning signed whole-day differences or `NULL`; time portions are ignored, invalid string inputs return `NULL` with warning 1292, and stored zero temporal descriptor values return `NULL` without a fresh warning; limited descriptor-backed `WHERE` comparison, `IS [NOT] NULL`, and `[NOT] BETWEEN` row-scalar predicates are admitted; no `TIME` inputs, numeric temporal coercion, bare truth predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `DAY()` | 🟡 | Limited synonym for the current `DAYOFMONTH()` subset, including the limited single-table numeric extractor predicate slice |
| `DAYNAME()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `DAYNAME(value)` over `NULL`, canonical date/datetime strings including pre-1000 and admitted complete year-zero strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns; returns English weekday names or `NULL`, invalid inputs warn 1292, and result metadata follows the current connection collation; no `TIME` inputs, numeric temporal coercion, locale-sensitive names, relaxed/truncating temporal parsing, predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `DAYOFMONTH()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `DAYOFMONTH(value)` over `NULL`, canonical date/datetime strings including admitted zero-date strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning an integer part or `NULL`; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, relaxed temporal parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `DAYOFWEEK()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `DAYOFWEEK(value)` over `NULL`, canonical date/datetime strings including pre-1000 and admitted year-zero strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning Sunday=1 through Saturday=7 or `NULL`; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, numeric temporal coercion, relaxed temporal parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `DAYOFYEAR()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `DAYOFYEAR(value)` over `NULL`, canonical date/datetime strings including pre-1000 and admitted year-zero strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning 1 through 366 or `NULL`; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, numeric temporal coercion, relaxed temporal parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `EXTRACT()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `EXTRACT(unit FROM value)` over `NULL`, supported canonical temporal strings, descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns for `YEAR`, `QUARTER`, `MONTH`, `DAY`, and `YEAR_MONTH`, descriptor `TIME` / `DATETIME` / `TIMESTAMP` / string-family columns for `HOUR`, `MINUTE`, `SECOND`, `DAY_HOUR`, `DAY_MINUTE`, `DAY_SECOND`, `HOUR_MINUTE`, `HOUR_SECOND`, and `MINUTE_SECOND`, and `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` / string-family inputs for `MICROSECOND`; `EXTRACT(MICROSECOND FROM negative_time)` preserves the negative sign, and date-only string inputs return `0` with warning 1292; invalid inputs return `NULL` with warning 1292; no `WEEK`, microsecond composites, `DATE` descriptor time-unit coercion except `MICROSECOND`, `TIME` descriptor date-unit coercion, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `FROM_DAYS()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `FROM_DAYS(value)` over `NULL`, signed-64 integer and boolean literals, and integer-family descriptor columns physically stored in the current signed-64 range; returns MySQL-observed zero-date, date, and overflow-`NULL` results, including warning 1441 for the verified near-overflow window; no string/decimal/fractional arguments, predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `FROM_UNIXTIME()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `FROM_UNIXTIME(value)` over `NULL`, signed-64 integer and boolean literals, and integer-family descriptor columns physically stored in the current signed-64 range, returning datetime text or `NULL`; supported calls use the current fixed-offset session `time_zone`, negative seconds and seconds greater than `32536771199` return `NULL` without warnings, and successful in-range conversions produce no warnings; limited descriptor-backed `WHERE` comparison, `IS [NOT] NULL`, and `[NOT] BETWEEN` row-scalar predicates are admitted; no format argument, string/decimal/fractional seconds, bare truth predicates, DML assignments, defaults, generated columns, named time zones, daylight-saving behavior, or general temporal expression evaluation |
| `GET_FORMAT()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `GET_FORMAT(class, format)` for `DATE`, `TIME`, `DATETIME`, and `TIMESTAMP` classes with string, integer, boolean, or `NULL` literal format values; recognized `USA`, `JIS`, `ISO`, `EUR`, and `INTERNAL` names return MySQL format strings and unknown/non-string values return `NULL`; also admitted as a format input to the current `DATE_FORMAT()`, `TIME_FORMAT()`, and `STR_TO_DATE()` subsets. No expression-valued format names, predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `HOUR()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `HOUR(value)` over `NULL`, canonical time/datetime strings including negative and three-digit-hour time strings, and descriptor `TIME` / `DATETIME` / `TIMESTAMP` / string-family columns, returning an integer part or `NULL`; invalid temporal inputs return `NULL` with warning 1292; no date-only inputs, fractional seconds, relaxed temporal parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `LAST_DAY()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `LAST_DAY(value)` over `NULL`, canonical date/datetime strings including pre-1000 and admitted year-zero strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning month-end date text or `NULL`; accepts day-zero inputs only when year/month are otherwise valid; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, numeric temporal coercion, relaxed temporal parsing, predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `LOCALTIME(), LOCALTIME` | 🟡 | Limited zero-fractional statement timestamp synonym for `NOW()` in the same supported positions as `CURRENT_TIMESTAMP`; respects the limited session `time_zone` offset for materialized current values; no fractional precision, loaded named-zone rows, or TIMESTAMP row storage/retrieval conversion |
| `LOCALTIMESTAMP, LOCALTIMESTAMP()` | 🟡 | Limited zero-fractional statement timestamp synonym for `NOW()` in the same supported positions as `CURRENT_TIMESTAMP`; respects the limited session `time_zone` offset for materialized current values; no fractional precision, loaded named-zone rows, or TIMESTAMP row storage/retrieval conversion |
| `MAKEDATE()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `MAKEDATE(year, day_of_year)` over `NULL`, signed-64 integer and boolean literals, and integer-family descriptor columns physically stored in the current signed-64 range; implements MySQL-observed two-digit-year conversion, positive-day rollover, and `NULL` invalid/out-of-range results; limited descriptor-backed `WHERE` comparison, `IS [NOT] NULL`, and `[NOT] BETWEEN` row-scalar predicates are admitted; no string/decimal/fractional arguments, bare truth predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `MAKETIME()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `MAKETIME(hour, minute, second)` over `NULL`, signed-64 integer and boolean literals, and integer-family descriptor columns physically stored in the current signed-64 range; implements MySQL-observed `NULL` propagation, minute/second range checks, negative-hour results, and time-range clipping with warning 1292; limited descriptor-backed `WHERE` comparison, `IS [NOT] NULL`, and `[NOT] BETWEEN` row-scalar predicates are admitted; no fractional seconds, string/decimal arguments, bare truth predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `MICROSECOND()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `MICROSECOND(value)` over `NULL`, canonical time/datetime strings with optional fractional seconds, date-only strings, and descriptor `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` / string-family columns; short fractional parts are padded, more than six fractional digits are rounded to a six-digit microsecond part, date-only strings return `0` with warning 1292, invalid strings return `NULL` with warning 1292, and stored zero-fractional descriptor temporal values return `0`; no numeric temporal coercion, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, or general temporal expression evaluation |
| `MINUTE()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `MINUTE(value)` over the same current `HOUR()` input subset, returning an integer part or `NULL` |
| `MONTH()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `MONTH(value)` over the same current `YEAR()` / `DAYOFMONTH()` date-input subset, returning an integer part or `NULL` |
| `MONTHNAME()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `MONTHNAME(value)` over `NULL`, canonical date/datetime strings including pre-1000 and admitted complete year-zero strings, day-zero strings with a valid month, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns; returns English month names or `NULL`, invalid inputs warn 1292, and result metadata follows the current connection collation; no `TIME` inputs, numeric temporal coercion, locale-sensitive names, relaxed/truncating temporal parsing, predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `NOW()` | 🟡 | Limited zero-fractional statement timestamp in no-source, `FROM DUAL`, `DO`, row-scalar projection, supported temporal DML assignment, and `DATETIME` / `TIMESTAMP` defaults; respects the limited session `timestamp` override and session `time_zone` offset for materialized current values; no fractional precision, loaded named-zone rows, or TIMESTAMP row storage/retrieval conversion |
| `PERIOD_ADD()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar projection over signed integer/boolean/`NULL` operands and integer-family descriptor columns; implements MySQL period normalization, two-digit-year mapping, `NULL` propagation, and invalid-period error 1210; no string/decimal/floating coercion, predicates, DML assignments, defaults, generated columns, or general expression evaluation |
| `PERIOD_DIFF()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar projection over signed integer/boolean/`NULL` operands and integer-family descriptor columns; implements MySQL period normalization, two-digit-year mapping, signed month differences, `NULL` propagation, and invalid-period error 1210; no string/decimal/floating coercion, predicates, DML assignments, defaults, generated columns, or general expression evaluation |
| `QUARTER()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `QUARTER(value)` over `NULL`, canonical date/datetime strings including admitted zero and partial-zero date strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning `0`, `1`, `2`, `3`, `4`, or `NULL`; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, numeric temporal coercion, relaxed/fractional parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `SEC_TO_TIME()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `SEC_TO_TIME(value)` over `NULL`, signed-64 integer and boolean literals, and integer-family descriptor columns physically stored in the current signed-64 range, returning time text or `NULL`; constant calls are also admitted in descriptor-backed `INSERT`, `REPLACE`, duplicate-key update, and single-table `UPDATE` value positions before target-column storage conversion; values beyond MySQL's `TIME` range clip to `838:59:59` / `-838:59:59` with warning 1292. Limited descriptor-backed `WHERE` comparison, `IS [NOT] NULL`, and `[NOT] BETWEEN` row-scalar predicates are admitted. No string seconds, decimal/fractional seconds, bare truth predicates, DML assignments outside the constant descriptor-backed value subset, defaults, generated columns, or general temporal expression evaluation |
| `SECOND()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `SECOND(value)` over the same current `HOUR()` input subset, returning an integer part or `NULL` |
| `STR_TO_DATE()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `STR_TO_DATE(value, format)` over `NULL`, string literals, and string-family descriptor columns with `%Y`, `%y`, `%m`, `%c`, `%d`, `%e`, `%H`, `%k`, `%h`, `%I`, `%l`, `%i`, `%s`, `%S`, `%T`, `%r`, `%p`, and `%%`; constant calls are also admitted in descriptor-backed `INSERT`, `REPLACE`, duplicate-key update, and single-table `UPDATE` value positions before target-column storage conversion; returns date, time, or datetime text, maps invalid inputs to `NULL` with warning 1411, emits warning 1292 for trailing input, honors `NO_ZERO_DATE` / `NO_ZERO_IN_DATE` for zero-result parts, and honors `ALLOW_INVALID_DATES` for in-range but noncalendar date results. No locale names, fractional seconds, week/year-week tokens, numeric temporal coercion, row-backed format columns, predicates, DML assignments outside the constant descriptor-backed value subset, defaults, generated columns, or general temporal expression evaluation |
| `SUBDATE()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `SUBDATE(value, INTERVAL signed_integer_or_exact_string_or_NULL unit)` alias of the current `DATE_SUB()` interval slice for `YEAR`, `QUARTER`, `MONTH`, `WEEK`, `DAY`, `HOUR`, `MINUTE`, and `SECOND`; row-backed invalid temporal values return `NULL` with warning 1292 and row overflow returns `NULL` with warning 1441; the same core interval form is admitted in limited same-column single-table `UPDATE` assignments for compatible non-key DATE, DATETIME, and nonbinary string targets, where matched invalid values and overflow are errors; no non-interval forms, `MICROSECOND`, composite units, interval expressions outside the documented update subset, predicates, defaults, generated columns, or general temporal arithmetic |
| `SUBTIME()` | 🟡 | Limited no-source, `FROM DUAL`, and `DO` scalar `SUBTIME(value, time_value)` over `NULL`, canonical `YYYY-MM-DD HH:MM:SS` datetime strings, and canonical `[-]HH:MM:SS` / `[-]HHH:MM:SS` time strings, returning text/`NULL` for in-range results; successful supported expressions produce no warnings; no row-scalar evaluation, numeric/relaxed/fractional/day-hour operands, result clamping, warning-returning invalid conversion, predicates, DML assignments, defaults, generated columns, or general temporal arithmetic |
| `SYSDATE()` | 🟡 | Limited zero-fractional execution-time timestamp in no-source, `FROM DUAL`, `DO`, row-scalar projection, and compatible `DATETIME` / `TIMESTAMP` DML assignment; ignores the limited session `timestamp` override, observes the limited session `time_zone` offset, and supports `SYSDATE ()` only when `IGNORE_SPACE` is active; no fractional precision, defaults, broader temporal/string coercion targets, loaded named-zone rows, or TIMESTAMP row storage/retrieval conversion |
| `TIME()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `TIME(value)` over `NULL`, canonical time/datetime string literals, and descriptor `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` columns, returning time text or `NULL`; invalid literal inputs return `NULL` with warning 1292; no fractional seconds, numeric temporal coercion, date-only string coercion, string descriptor columns, predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `TIME_FORMAT()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `TIME_FORMAT(value, format)` over `NULL`, canonical time/datetime strings, descriptor `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` / string-family columns, and a verified time-token subset; invalid temporal inputs return `NULL` with warning 1292; no date/week format tokens, relaxed or numeric temporal parsing, fractional seconds beyond `%f = 000000`, predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `TIME_TO_SEC()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `TIME_TO_SEC(value)` over `NULL`, canonical time/datetime strings, descriptor `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` columns, and nonbinary string-family descriptor columns, returning signed whole seconds or `NULL`; `DATE` descriptor inputs return `0`, invalid admitted strings warn 1292, and negative time values return negative seconds; no numeric temporal coercion, fractional seconds, date-only string coercion parity, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `TIMEDIFF()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `TIMEDIFF(value1, value2)` over `NULL`, canonical `YYYY-MM-DD HH:MM:SS` datetime strings, canonical `[-]HH:MM:SS` / `[-]HHH:MM:SS` time strings, and descriptor `DATE` / `TIME` / `DATETIME` / `TIMESTAMP` columns, returning time text or `NULL`; supported mixed domains return `NULL`, invalid strings return `NULL` with warning 1292, and out-of-range results clip to `838:59:59` / `-838:59:59` with warning 1292; limited descriptor-backed `WHERE` comparison, `IS [NOT] NULL`, and `[NOT] BETWEEN` row-scalar predicates are admitted; no numeric temporal coercion, date-only string coercion parity, fractional seconds, string descriptor columns, bare truth predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `TIMESTAMP()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `TIMESTAMP(value)` / `TIMESTAMP(value, time_value)` over `NULL`, canonical date/datetime string literals, descriptor `DATE` / `DATETIME` / `TIMESTAMP` / nonbinary string-family first arguments, canonical time/day-time string literals, and descriptor `TIME` / nonbinary string-family second arguments; invalid first values warn 1292 as incorrect datetime values, invalid or clipped second values warn 1292 as truncated time values, and overflow returns `NULL` with warning 1441; limited descriptor-backed `WHERE` comparison, `IS [NOT] NULL`, and `[NOT] BETWEEN` row-scalar predicates are admitted; no numeric temporal coercion, compact/fractional temporal strings, `TIME` first arguments, bare truth predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `TIMESTAMPADD()` | 🟡 | Limited `SECOND` / `SQL_TSI_SECOND` whole-second arithmetic in no-source, `FROM DUAL`, `DO`, and single-table row-scalar `SELECT` over canonical date/datetime strings, descriptor `DATE` / `DATETIME` / `TIMESTAMP` / nonbinary string-family columns, and `NULL`; no broader units, expression intervals, fractional seconds, predicates, DML assignments, defaults, or generated columns |
| `TIMESTAMPDIFF()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `TIMESTAMPDIFF(unit, value1, value2)` over `NULL`, canonical date/datetime strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns for `YEAR`, `QUARTER`, `MONTH`, `WEEK`, `DAY`, `HOUR`, `MINUTE`, and `SECOND` plus matching `SQL_TSI_` aliases; returns signed whole-unit differences or `NULL`, treats `DATE` as midnight, invalid non-`NULL` strings warn 1292, and bare `MICROSECOND` is parsed but deferred. Limited descriptor-backed `WHERE` comparison, `IS [NOT] NULL`, and `[NOT] BETWEEN` row-scalar predicates are admitted. No fractional seconds, `SQL_TSI_MICROSECOND`, `TIME` inputs, numeric temporal coercion, bare truth predicates, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `TO_DAYS()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `TO_DAYS(value)` over `NULL`, canonical `YYYY-MM-DD` / `YYYY-MM-DD HH:MM:SS` strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning the verified MySQL day number or `NULL`; invalid string inputs warn 1292 and stored zero temporal descriptor values return `NULL` without a fresh warning. No numeric temporal coercion, relaxed/fractional parsing, `TIME` inputs, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `TO_SECONDS()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `TO_SECONDS(value)` over the current `TO_DAYS()` input subset, returning whole seconds since MySQL's year-zero day number or `NULL`; date-only inputs use midnight and datetime inputs add the admitted time portion. No numeric temporal coercion, relaxed/fractional parsing, `TIME` inputs, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `UNIX_TIMESTAMP()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, and single-table row-scalar `UNIX_TIMESTAMP()` / `UNIX_TIMESTAMP(value)` over `NULL`, canonical date/datetime string literals, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` columns, plus limited no-argument `UNIX_TIMESTAMP()` plus/minus signed integer or `NULL` deltas in supported `INSERT`, `REPLACE`, duplicate-key update, and single-table `UPDATE` assignment value positions for non-`AUTO_INCREMENT` integer-family and nonbinary string targets; no-argument form respects the current statement timestamp override, string/`DATE`/`DATETIME` values use the limited session fixed-offset `time_zone`, and `TIMESTAMP` descriptor values are treated as fixed UTC storage values; invalid string and full-zero-date inputs produce MySQL-compatible warning 1292; limited descriptor-backed `WHERE` comparison, `IS [NOT] NULL`, and `[NOT] BETWEEN` row-scalar predicates are admitted for the one-argument form; no fractional seconds, compact numeric temporal coercion, string descriptor columns, bare truth predicates, defaults, explicit `AUTO_INCREMENT` target arithmetic, generated columns, named time zones, or general temporal expression evaluation |
| `UTC_DATE(), UTC_DATE` | 🟡 | Limited zero-fractional statement UTC date in no-source, `FROM DUAL`, `DO`, row-scalar projection, and compatible `DATE` DML assignment; respects the limited session `timestamp` override but ignores session `time_zone`; no fractional precision, defaults, loaded named-zone rows, or general temporal expression evaluation |
| `UTC_TIME(), UTC_TIME` | 🟡 | Limited zero-fractional statement UTC time in no-source, `FROM DUAL`, `DO`, row-scalar projection, and compatible `TIME` DML assignment, including explicit `UTC_TIME(0)` normalization; respects the limited session `timestamp` override but ignores session `time_zone`; nonzero fractional precision is rejected, and `fsp > 6` uses MySQL's precision-limit diagnostic; no defaults, loaded named-zone rows, or general temporal expression evaluation |
| `UTC_TIMESTAMP(), UTC_TIMESTAMP` | 🟡 | Limited zero-fractional statement UTC timestamp in no-source, `FROM DUAL`, `DO`, row-scalar projection, and compatible `DATETIME` / `TIMESTAMP` DML assignment, including explicit `UTC_TIMESTAMP(0)` normalization; respects the limited session `timestamp` override but ignores session `time_zone`; nonzero fractional precision is rejected, and `fsp > 6` uses MySQL's precision-limit diagnostic; no defaults, loaded named-zone rows, or TIMESTAMP row storage/retrieval conversion |
| `WEEK()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `WEEK(value[, mode])` over `NULL`, canonical date/datetime strings including pre-1000 and admitted year-zero strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning a week number or `NULL`; omitted mode uses fixed MySQL mode 0 because `default_week_format` is not implemented, explicit mode is limited to signed integer, boolean, or `NULL` literals and normalized to MySQL's low three bits, invalid string inputs return `NULL` with warning 1292, and stored zero temporal descriptor values return `NULL` without a fresh warning; no `TIME` inputs, string/numeric mode coercion, numeric temporal coercion, relaxed temporal parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `WEEKDAY()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `WEEKDAY(value)` over the current `WEEK()` input subset, returning Monday=0 through Sunday=6 or `NULL`; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, numeric temporal coercion, relaxed temporal parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `WEEKOFYEAR()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `WEEKOFYEAR(value)` over the current `WEEK()` input subset, equivalent to `WEEK(value, 3)` for admitted values and returning 1 through 53 or `NULL`; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, numeric temporal coercion, relaxed temporal parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `YEAR()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `YEAR(value)` over `NULL`, canonical date/datetime strings including admitted zero-date strings, and descriptor `DATE` / `DATETIME` / `TIMESTAMP` / string-family columns, returning an integer part or `NULL`; invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, relaxed temporal parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |
| `YEARWEEK()` | 🟡 | Limited no-source, `FROM DUAL`, `DO`, single-table row-scalar projection, and limited descriptor-source numeric extractor predicates, including narrow `extractor +/- integer_literal` comparison forms, for `YEARWEEK(value[, mode])` over the current `WEEK()` input subset, returning `YYYYWW` or `NULL`; omitted mode uses mode 0 and is not affected by `default_week_format`, explicit mode is limited to signed integer, boolean, or `NULL` literals and normalized to MySQL's low three bits, and invalid temporal inputs return `NULL` with warning 1292; no `TIME` inputs, string/numeric mode coercion, numeric temporal coercion, relaxed temporal parsing, general predicates beyond the documented numeric extractor slice, DML assignments, defaults, generated columns, or general temporal expression evaluation |

[Back to compatibility overview](../../COMPATIBILITY.md)
