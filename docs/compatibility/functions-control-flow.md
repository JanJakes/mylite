# Control-flow functions

Conditional expression helpers.

| Function | Status | Notes |
| --- | --- | --- |
| `CASE` | 🟡 | Limited no-source/`DUAL` scalar searched and simple `CASE`, source-free/`DUAL` scalar `WHERE`/`ORDER BY` query clauses, `DO`, single-table searched row-scalar projection, supported single-table row-scalar `UPDATE` and duplicate-key assignment targets, narrow joined simple row-scalar projection over integer descriptor/literal operands, supported aggregate arguments, and hidden `ORDER BY` rank keys over the current row-control-flow condition/value subset, including supported row arithmetic, integer-domain `GREATEST()` / `LEAST()`, `LIKE`, `AND`, and `OR`; row values also admit current `GREATEST()` / `LEAST()` row expressions. No deeper nested `CASE`, `CASE` as an arbitrary larger scalar operand, grouping keys, unsupported value domains, string-domain extrema truth conversion, subqueries, side effects beyond evaluated expression warnings, full result type aggregation, or expression metadata |
| `COALESCE()` | 🟡 | Limited scalar, `DO`, `SET @user_variable`, source-free DML value, source-free/`DUAL` scalar `WHERE`/`ORDER BY` query clauses, descriptor-backed truth/comparison/`IS`/range/membership predicates, single-table row-scalar projection, non-grouped single-table `SELECT ORDER BY` expression support, supported aggregate arguments, and supported single-table row-scalar `UPDATE` and duplicate-key assignment targets over the documented scalar values, descriptor values, supported row arithmetic values, current `GREATEST()` / `LEAST()` row values, and one nested row-control-flow layer; no grouping-key expressions, table-backed subqueries, binary string/`BIT`/approximate/JSON column operands, side effects outside evaluated arithmetic warnings, or expression metadata |
| `IF()` | 🟡 | Limited scalar, `DO`, `SET @user_variable`, source-free DML value, source-free/`DUAL` scalar `WHERE`/`ORDER BY` query clauses, descriptor-backed truth/comparison/`IS`/range predicates, single-table row-scalar projection, non-grouped single-table `SELECT ORDER BY` expression support, supported aggregate arguments, and supported single-table row-scalar `UPDATE` and duplicate-key assignment targets; row conditions admit descriptor integer columns, integer/boolean/`NULL` literals, row-backed `ISNULL()`, supported row arithmetic truth values, integer-domain `GREATEST()` / `LEAST()`, and documented `LIKE`/logical atoms, while row values admit documented descriptor/scalar values, supported row arithmetic, current `GREATEST()` / `LEAST()` row values, and one nested row-control-flow layer; no grouping-key expressions, string/decimal truth conversion outside row arithmetic, string-domain extrema truth conversion, table-backed subqueries, unsupported column families, side effects outside evaluated arithmetic warnings, or expression metadata |
| `IFNULL()` | 🟡 | Limited scalar, `DO`, `SET @user_variable`, source-free DML value, source-free/`DUAL` scalar `WHERE`/`ORDER BY` query clauses, descriptor-backed truth/comparison/`IS`/range/membership predicates, single-table row-scalar projection, non-grouped single-table `SELECT ORDER BY` expression support, supported aggregate arguments, and supported single-table row-scalar `UPDATE` and duplicate-key assignment targets over the documented scalar values, descriptor values, supported row arithmetic values, current `GREATEST()` / `LEAST()` row values, and one nested row-control-flow layer; no grouping-key expressions, table-backed subqueries, binary string/`BIT`/approximate/JSON column operands, side effects outside evaluated arithmetic warnings, or expression metadata |
| `NULLIF()` | 🟡 | Limited scalar, `DO`, `SET @user_variable`, source-free DML value, source-free/`DUAL` scalar `WHERE`/`ORDER BY` query clauses, descriptor-backed truth/comparison/`IS`/range predicates, single-table row-scalar projection, non-grouped single-table `SELECT ORDER BY` expression support, supported aggregate arguments, and supported single-table row-scalar `UPDATE` and duplicate-key assignment targets over same-domain admitted integer/string-like values plus supported row arithmetic values and current `GREATEST()` / `LEAST()` row values; admitted string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation; no grouping-key expressions, mixed unsupported row domains, table-backed subqueries, binary string/`BIT`/approximate/JSON column operands, side effects outside evaluated arithmetic warnings, first-argument double-evaluation semantics, or expression metadata |

[Back to compatibility overview](../../COMPATIBILITY.md)
