# SQL query expressions

Core query expression, SELECT, set operation, ordering, limiting, locking, modifier, and result metadata compatibility.

| Feature | Status | Notes |
| --- | --- | --- |
| `SELECT` | 🟡 | Descriptor-driven single persistent or shadowing session temporary base-table `SELECT *`, `SELECT column [AS alias][, column [AS alias] ...] FROM table`, explicit `SELECT ALL` on supported non-distinct forms, limited canonical no-op `HIGH_PRIORITY`, statement-level `STRAIGHT_JOIN`, `SQL_SMALL_RESULT`, `SQL_BIG_RESULT`, `SQL_BUFFER_RESULT`, and deprecated no-op `SQL_NO_CACHE` with warning 1681, limited `SQL_CALC_FOUND_ROWS` on supported non-distinct descriptor-backed table forms, limited simple no-op locking clauses `FOR UPDATE`, `FOR SHARE`, and `LOCK IN SHARE MODE`, limited no-source and `FROM DUAL` mixed session-scalar plus scalar-value projection over current session scalar functions/system variables, decimal integer/`NULL`/boolean literal select items, scalar `IF()`/`IFNULL()`/`COALESCE()`/`NULLIF()`/`ISNULL()` values with signed-64 integer/boolean/`NULL` operands and supported nested scalar function calls, limited no-source/`DUAL` signed-64 scalar arithmetic projection for `+`, binary `-`, `*`, unary `+`, unary `-`, `%`, infix `MOD`, `MOD(left, right)`, and infix `DIV`, plus limited top-level `/` scalar division over signed-64 arithmetic operands, limited no-source/`DUAL` unsigned-64 numeric scalar bitwise projection for `~`, `&`, `\|`, `^`, `<<`, and `>>` over the current signed-64 scalar arithmetic operand domain, limited no-source/`DUAL` numeric `ABS()`, `SIGN()`, `CEIL()`/`CEILING()`/`FLOOR()`, one-argument `ROUND()`, `BIT_COUNT()`, `BIN()`, `OCT()`, `CONV()`, `SQRT()`, `DEGREES()`, `RADIANS()`, `ACOS()`, `ASIN()`, `SIN()`, `COS()`, `TAN()`, `COT()`, `ATAN()`, `ATAN2()`, `EXP()`, `LN()`, `LOG()`, `LOG10()`, `LOG2()`, `POW()`, and `POWER()` over integer/boolean/`NULL`, signed-64 scalar arithmetic, and limited numeric bitwise operands, limited `CRC32()` over string/hex/integer/boolean/`NULL` literals, and limited `FORMAT()` / `TRUNCATE()` over exact integer/fixed-decimal/boolean/`NULL` value literals with integer decimal-place literals, plus limited top-level `PI()` constant and zero-argument `RAND()` plus literal seeded `RAND(seed)` projection, limited no-source/`DUAL` bare `CAST(value AS BINARY)` over string/integer/boolean/`NULL` scalar values and limited `CONVERT('string' USING BINARY)` over ordinary string scalar literals, limited no-source/`DUAL` `DATE_ADD(date_or_datetime_string, INTERVAL signed_integer_or_NULL SECOND)` scalar values, limited `DATE_FORMAT(value, format)` scalar values over supported canonical date/datetime strings, and limited no-source/`DUAL` signed-64 scalar comparison projection for `=`, `<=>`, `<>`, `!=`, `<`, `<=`, `>`, and `>=`, limited no-source/`DUAL` keyword scalar logical projection for `NOT`, `AND`, `XOR`, and `OR`, limited no-source/`DUAL` scalar `IS` projection for `IS [NOT] NULL` / `TRUE` / `FALSE` / `UNKNOWN`, limited no-source/`DUAL` top-level searched/simple scalar `CASE` projection whose children may use existing non-`CASE` signed-64 scalar expressions, limited table-backed statement-time `CURRENT_DATE` / `CURRENT_TIME` / `CURRENT_TIMESTAMP` projection, limited flat row-scalar `CONCAT()` projection over string/integer/boolean/`NULL` literals, supported session scalar values, and descriptor columns in no-source, `DUAL`, and supported single-table row envelopes, and limited flat row-scalar `FIELD()` projection over all-string or all-integer string/integer/boolean/`NULL` literals and descriptor columns in the same envelopes, and limited flat row-scalar `DATE_FORMAT()` projection over supported canonical date/datetime/timestamp/string values in the same envelopes, and limited flat row-scalar string length and ASCII string case functions over admitted scalar and descriptor-backed values in the same envelopes, plus limited one-column `SELECT DISTINCT` / `DISTINCTROW column [AS alias] FROM table` with optional limited source table alias, descriptor-qualified column references, `WHERE`, single-column `ORDER BY`, and `LIMIT`/`OFFSET`, plus limited one-item `COUNT(*)` and `COUNT(integer/NULL/boolean literal)` with no source, `FROM DUAL`, or one descriptor-backed table with optional baseline table alias and `WHERE`, limited one-item `COUNT(column)` and `COUNT(DISTINCT column)` over one descriptor-backed table with optional baseline table alias, qualified column argument, and `WHERE`, limited one-item `MIN(column)` / `MAX(column)` / `SUM(column)` / `AVG(column)`, limited one-item `BIT_AND(column)` / `BIT_OR(column)` / `BIT_XOR(column)`, and limited one-item `GROUP_CONCAT(column [ORDER BY order_column [ASC\|DESC]] [SEPARATOR 'literal'])` over one descriptor-backed table with optional baseline table alias, qualified column argument, and `WHERE`; also supports limited grouped aggregates as `SELECT group_column, aggregate FROM source [WHERE ...] GROUP BY group_column [HAVING grouped_or_selected_aggregate_predicate] [ORDER BY group_column] [LIMIT ...]` for one descriptor group column and one supported aggregate result over one base table or the current two-source inner/cartesian/left-outer joined source envelope; supported non-wildcard select items may use identifier, quoted-identifier, or string-literal aliases as result labels; `SELECT *` expands visible descriptor columns only, while explicit column, predicate, order, distinct, group, having, and aggregate references may name invisible columns; top-level scalar integer literals keep the existing literal-projection envelope while scalar arithmetic operands and scalar function operands remain signed-64 except the direct unsigned-magnitude `ABS()`/`BIT_COUNT()`/`BIN()`/`OCT()`/`CONV()`/`SQRT()`/`DEGREES()`/`RADIANS()`/`ACOS()`/`ASIN()`/`SIN()`/`COS()`/`TAN()`/`COT()`/`ATAN()`/`ATAN2()`/`EXP()`/`LN()`/`LOG()`/`LOG10()`/`LOG2()`/`POW()`/`POWER()` value literal envelopes, direct exact-decimal `SIGN()` sign classification, plus direct 81-significant-digit `CEIL()`/`CEILING()`/`FLOOR()` and one-argument `ROUND()` integer identity, direct literal `CRC32()` byte checksums, and direct exact-decimal `FORMAT()` / `TRUNCATE()` text results; `SUM(column)` and `AVG(column)` are limited to MyLite's current signed-64 aggregate envelope, bitwise aggregates return unsigned-64 decimal text for integer descriptor columns, and `GROUP_CONCAT()` has the current limited value/order/separator envelope; no general expression projection, table-backed expression projection outside the limited statement-time current date/time/timestamp values, `CONCAT()` / `FIELD()` / `DATE_FORMAT()` / string length / ASCII string case function subsets, qualified wildcards, joins beyond the limited two-source descriptor-backed inner/cartesian/left-outer SELECT subset, full grouping, transaction lock behavior, locking options, full modifier permutations/repetition, mutable `sql_select_limit` row caps, `sql_buffer_result` temporary result buffering, `sql_auto_is_null` auto-increment lookup behavior, `sql_big_selects` row-estimate aborts, or arbitrary SQLite pass-through |
| Query expression grammar | ❌ | Query terms and primaries |
| Projection list | 🟡 | Wildcard uses visible descriptor columns in catalog ordinal order, including exact `DECIMAL` canonical text readback, limited `CHAR`, `VARCHAR`, and baseline `TEXT` family text readback, and byte-safe binary string readback; explicit table projections and limited one-column `SELECT DISTINCT` / `DISTINCTROW` resolve descriptor column names, including invisible columns and supported source-qualified references; no-source and `FROM DUAL` projection may return mixed session-scalar plus scalar-value item lists over current session scalar functions/system variables, decimal integer, `NULL`, `TRUE`, and `FALSE` literal select items, scalar `IF()`/`IFNULL()`/`COALESCE()`/`NULLIF()`/`ISNULL()` values with signed-64 integer/boolean/`NULL` operands and supported nested scalar calls, limited signed-64 `+`, binary `-`, `*`, unary `+`, unary `-`, `%`, infix `MOD`, `MOD(left, right)`, and infix `DIV` arithmetic plus limited top-level `/` division over the current scalar-value domain, limited unsigned-64 numeric scalar bitwise projection for `~`, `&`, `\|`, `^`, `<<`, and `>>` over the current signed-64 scalar arithmetic operand domain, limited numeric `ABS()`, `SIGN()`, `CEIL()`/`CEILING()`/`FLOOR()`, one-argument `ROUND()`, `BIT_COUNT()`, `BIN()`, `OCT()`, `CONV()`, `SQRT()`, `DEGREES()`, `RADIANS()`, `ACOS()`, `ASIN()`, `SIN()`, `COS()`, `TAN()`, `COT()`, `ATAN()`, `ATAN2()`, `EXP()`, `LN()`, `LOG()`, `LOG10()`, `LOG2()`, `POW()`, and `POWER()` over integer/boolean/`NULL`, signed-64 scalar arithmetic, and limited numeric bitwise operands, limited `CRC32()` over string/hex/integer/boolean/`NULL` literals, and limited `FORMAT()` / `TRUNCATE()` over exact integer/fixed-decimal/boolean/`NULL` value literals with integer decimal-place literals, limited top-level `PI()` constant and zero-argument `RAND()` plus literal seeded `RAND(seed)` projection, limited bare `CAST(value AS BINARY)` over string/integer/boolean/`NULL` scalar values and limited `CONVERT('string' USING BINARY)` over ordinary string scalar literals, limited `DATE_ADD(date_or_datetime_string, INTERVAL signed_integer_or_NULL SECOND)` scalar values, limited `DATE_FORMAT(value, format)` scalar values over supported canonical date/datetime strings, and limited signed-64 scalar comparison projection for `=`, `<=>`, `<>`, `!=`, `<`, `<=`, `>`, and `>=`, limited keyword scalar logical projection for `NOT`, `AND`, `XOR`, and `OR`, limited scalar `IS` projection for `IS [NOT] NULL` / `TRUE` / `FALSE` / `UNKNOWN`, limited top-level searched/simple scalar `CASE` projection whose children may use existing non-`CASE` signed-64 scalar expressions, limited table-backed statement-time `CURRENT_DATE` / `CURRENT_TIME` / `CURRENT_TIMESTAMP` projection, limited flat row-scalar `CONCAT()` over string/integer/boolean/`NULL` literals, supported session scalar values, and descriptor columns in supported single-table row envelopes, and limited flat row-scalar `FIELD()` over all-string or all-integer string/integer/boolean/`NULL` literals and descriptor columns in the same envelopes, and limited flat row-scalar `DATE_FORMAT()` over supported canonical date/datetime/timestamp/string values in the same envelopes, and limited flat row-scalar string length and ASCII string case functions over admitted scalar and descriptor-backed values in the same envelopes; top-level scalar integer literals keep the existing literal-projection envelope while scalar arithmetic operands and scalar function operands remain signed-64 except the direct unsigned-magnitude `ABS()`/`BIT_COUNT()`/`BIN()`/`OCT()`/`CONV()`/`SQRT()`/`DEGREES()`/`RADIANS()`/`ACOS()`/`ASIN()`/`SIN()`/`COS()`/`TAN()`/`COT()`/`ATAN()`/`ATAN2()`/`EXP()`/`LN()`/`LOG()`/`LOG10()`/`LOG2()`/`POW()`/`POWER()` value literal envelopes, direct exact-decimal `SIGN()` sign classification, plus direct 81-significant-digit `CEIL()`/`CEILING()`/`FLOOR()` and one-argument `ROUND()` integer identity, direct literal `CRC32()` byte checksums, and direct exact-decimal `FORMAT()` / `TRUNCATE()` text results; supported non-wildcard items may use identifier, quoted-identifier, or string-literal aliases as result labels; limited one-item `COUNT(*)`, `COUNT(column)`, `COUNT(integer/NULL/boolean literal)`, integer-only `COUNT(DISTINCT column)`, integer-only `MIN(column)`, `MAX(column)`, signed-64-range `SUM(column)`, signed-64-intermediate `AVG(column)`, unsigned-64-text `BIT_AND(column)` / `BIT_OR(column)` / `BIT_XOR(column)`, and limited `GROUP_CONCAT(column [ORDER BY order_column [ASC\|DESC]] [SEPARATOR 'literal'])` aggregate forms are supported; grouped aggregate projection is limited to one integer descriptor group column and one aggregate result over one source or the current two-source joined envelope, with `GROUP_CONCAT()` grouped support restricted to one base table; duplicate projected descriptor columns and duplicate labels are allowed for non-distinct selects; no expression metadata, table-backed expression projection outside the limited statement-time current date/time/timestamp values, `CONCAT()` / `FIELD()` / `DATE_FORMAT()` / string length / ASCII string case function subsets, qualified wildcards, decimal/string/binary distinct/order/group/aggregate comparison semantics, or full multi-expression distinct rows |
| Scalar subquery projection | 🟡 | Limited no-source and `FROM DUAL` scalar subqueries as projection operands and as current `CONCAT()` operands, with one inner scalar value from the supported `DATABASE()`/`SCHEMA()`, integer/boolean/`NULL`, and system-variable read subset; limited uncorrelated table-backed scalar subqueries are also supported only as single-table `UPDATE` assignment values, with one descriptor source column and the current descriptor `SELECT` source envelope; multi-column inner projections return MySQL-compatible `1241 / 21000`, matched multi-row assignment subqueries return `1242 / 21000`, and same-table update sources return `1093 / HY000`; no correlated, predicate, derived-table, row, quantified, general DML-assignment, or arbitrary expression subqueries |
| Table aliases | 🟡 | Optional `AS alias` or bare `alias` on supported single persistent or shadowing session temporary base-table `SELECT` sources and limited two-source descriptor-backed inner/cartesian/left-outer joins; selected columns, predicates, order keys, aggregate arguments on single-table paths, and joined-source columns may be qualified by that alias, while unaliased sources may use table or schema-table qualifiers; no qualified wildcards, derived tables, index hints, or aliases outside the documented source subsets |
| `SELECT ... FROM DUAL` | 🟡 | Limited scalar/session-function projection, mixed session-scalar plus scalar-value projection over current session scalar functions/system variables, decimal integer/`NULL`/boolean literal select items, scalar `IF()`/`IFNULL()`/`COALESCE()`/`NULLIF()`/`ISNULL()` values with signed-64 integer/boolean/`NULL` operands and supported nested scalar calls, limited signed-64 `+`, binary `-`, `*`, unary `+`, unary `-`, `%`, infix `MOD`, `MOD(left, right)`, and infix `DIV` scalar arithmetic projection plus limited top-level `/` scalar division, limited unsigned-64 numeric scalar bitwise projection for `~`, `&`, `\|`, `^`, `<<`, and `>>` over the current signed-64 scalar arithmetic operand domain, limited numeric `ABS()`, `SIGN()`, `CEIL()`/`CEILING()`/`FLOOR()`, one-argument `ROUND()`, `BIT_COUNT()`, `BIN()`, `OCT()`, `CONV()`, `SQRT()`, `DEGREES()`, `RADIANS()`, `ACOS()`, `ASIN()`, `SIN()`, `COS()`, `TAN()`, `COT()`, `ATAN()`, `ATAN2()`, `EXP()`, `LN()`, `LOG()`, `LOG10()`, `LOG2()`, `POW()`, and `POWER()` over integer/boolean/`NULL`, signed-64 scalar arithmetic, and limited numeric bitwise operands, limited `CRC32()` over string/hex/integer/boolean/`NULL` literals, and limited `FORMAT()` / `TRUNCATE()` over exact integer/fixed-decimal/boolean/`NULL` value literals with integer decimal-place literals, limited top-level `PI()` constant and zero-argument `RAND()` plus literal seeded `RAND(seed)` projection, limited bare `CAST(value AS BINARY)` over string/integer/boolean/`NULL` scalar values and limited `CONVERT('string' USING BINARY)` over ordinary string scalar literals, limited `DATE_ADD(date_or_datetime_string, INTERVAL signed_integer_or_NULL SECOND)` scalar values, limited `DATE_FORMAT(value, format)` scalar values over supported canonical date/datetime strings, and limited signed-64 scalar comparison projection for `=`, `<=>`, `<>`, `!=`, `<`, `<=`, `>`, and `>=`, limited keyword scalar logical projection for `NOT`, `AND`, `XOR`, and `OR`, limited scalar `IS` projection for `IS [NOT] NULL` / `TRUE` / `FALSE` / `UNKNOWN`, limited top-level searched/simple scalar `CASE` projection whose children may use existing non-`CASE` signed-64 scalar expressions, limited flat row-scalar `CONCAT()` over string/integer/boolean/`NULL` literals and supported session scalar values, limited flat row-scalar `FIELD()` over all-string or all-integer string/integer/boolean/`NULL` literals, limited string length and ASCII string case functions over admitted scalar values, and one-item `COUNT(*)` / `COUNT(integer/NULL/boolean literal)` one-row semantics; no table-backed descriptor behavior or arbitrary expression evaluation |
| `WHERE` | 🟡 | Descriptor-backed boolean expressions over existing predicate atoms for filtered table `SELECT`, aggregate source filters, `DELETE`, and `UPDATE`: keyword `NOT`, `AND`/`&&`, `XOR`, `OR`/`\|\|`, and parentheses follow MySQL precedence, `SELECT` may use supported source-qualified references, while `DELETE` and `UPDATE` remain unqualified; integer comparisons with a non-`NULL` decimal integer or `TRUE`/`FALSE` right operand (`=`, `<=>`, `<>`, `!=`, `<`, `<=`, `>`, `>=`), ASCII `CHAR`, `VARCHAR`, and baseline `TEXT` family string-literal comparisons for `=`, `<=>`, `<>`, `!=`, limited `LIKE` / `NOT LIKE` pattern predicates, and limited `REGEXP` / `RLIKE` / `NOT REGEXP` / `NOT RLIKE` ASCII regular-expression predicates, limited `ENUM` label or ordinal equality/null-safe-equality/inequality predicates plus `<=> NULL`, `YEAR` comparisons with a non-`NULL` compatible `YEAR` literal, canonical `DATE`, `TIME`, `DATETIME`, and `TIMESTAMP` comparisons with a non-`NULL` canonical string literal right operand, limited integer, `BIT`, `YEAR`, and temporal `BETWEEN` / `NOT BETWEEN` with two compatible bounds, limited integer, `BIT`, `YEAR`, and temporal `IN` / `NOT IN` with a nonempty compatible literal/`NULL` list, normal stored-value `IS NULL` and `IS NOT NULL` for integer, exact `DECIMAL`, approximate `FLOAT`/`DOUBLE`, `CHAR`, `VARCHAR`, baseline `TEXT` family, binary string, `BIT`, limited `ENUM`, `YEAR`, `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` columns, integer descriptor-column `IS [NOT] TRUE` / `FALSE` / `UNKNOWN` truth tests, and limited `SELECT ... WHERE [NOT] EXISTS (...)` predicate subqueries over one outer base table with tableless/`DUAL` or one-table inner sources, optional limited inner predicates, simple correlated integer equality/null-safe equality, and `LIMIT row_count`; admitted string comparisons use MyLite's registered ASCII `utf8mb4_0900_ai_ci` collation, admitted enum comparisons use the current ASCII case-insensitive enum label match, `TIME` comparisons and ranges use signed total-second semantics, `XOR` propagates `NULL` for the admitted predicate subset, `&&` and `\|\|` record MySQL-compatible deprecation warning 1287; no `WHERE TRUE`, symbolic `!`, scalar expression `XOR`, literal-left comparisons, expression-left truth tests, `col = NULL`, string ranges/membership/truth tests outside the current `LIKE` / `REGEXP` subset, enum ranges/membership/order predicates, binary string comparisons outside `BIT`, non-ASCII string predicate literals outside the current enum label-matching subset, decimal comparisons/ranges/membership/truth tests, relaxed temporal strings, `sql_auto_is_null` auto-increment lookup behavior, functions, parameters, decimal/float/hex predicate literals outside compatible `BIT` targets, subqueries outside the limited `EXISTS` slice, row constructors, or general expression predicates |
| `GROUP BY` | 🟡 | Limited single integer descriptor group column with one supported aggregate result over one descriptor-backed persistent base table or the current two-source inner/cartesian/left-outer joined source envelope: `SELECT group_column, aggregate FROM source [WHERE ...] GROUP BY group_column [HAVING grouped_or_selected_aggregate_predicate] [ORDER BY group_column [ASC|DESC]] [LIMIT ...]`; `GROUP_CONCAT()` grouped support is limited to one descriptor-backed base table and grouped-column `HAVING`; `NULL` keys form one group, optional `HAVING` filters groups after aggregation, optional ordering follows existing integer/`NULL` order semantics, and limiting follows existing SELECT `LIMIT`/`OFFSET`; no string grouping, multiple grouping keys, grouping aliases, ordinals, expressions, rollup, functional-dependence analysis, aggregate-only grouped projection, grouped `COUNT(DISTINCT)`, grouped `GROUP_CONCAT(DISTINCT)`, wider join sources for `GROUP_CONCAT()`, or full `ONLY_FULL_GROUP_BY` coverage |
| `WITH ROLLUP` | ❌ | Super-aggregate rows and GROUPING() behavior |
| `HAVING` | 🟡 | Limited post-group predicates on the existing grouped aggregate subset only: one grouped descriptor integer column or its alias, or the selected `COUNT(*)`, `COUNT(column)`, `MIN(column)`, `MAX(column)`, `SUM(column)`, or `AVG(column)` aggregate expression or alias, compared to an integer/boolean literal or tested with `IS NULL` / `IS NOT NULL`; no non-grouped `HAVING`, boolean composition, bare truth tests, aggregate expressions not selected, `COUNT(DISTINCT)`, bitwise aggregate result predicates, arbitrary expressions, subqueries, or general alias ambiguity handling |
| Window definitions | ❌ | Named windows, frames, restrictions |
| `ORDER BY` | 🟡 | One integer, `BIT`, `YEAR`, `DATE`, `TIME`, `DATETIME`, or `TIMESTAMP` descriptor column for supported base-table `SELECT`, `DELETE`, and `UPDATE`; `SELECT` may use supported source-qualified references or one unqualified select-item alias, while `DELETE` and `UPDATE` remain unqualified descriptor-only forms; aliases shadow descriptor column names and duplicate matching aliases are ambiguous; optional `ASC`/`DESC`; `ASC` is the default, `NULL` sorts before non-`NULL` ascending and after non-`NULL` descending, `BIT` values sort by fixed-width byte value, `YEAR` values sort by their canonical four-digit representation, `DATE`/`DATETIME`/`TIMESTAMP` values sort by canonical text, and `TIME` values sort by signed total seconds; limited `SELECT DISTINCT column [AS alias]` still requires the order key to resolve to the selected integer column or its alias, and limited grouped aggregates may order only by the grouped integer descriptor column or its alias; no ordinals, expressions, string-literal order keys, enum or string descriptor order keys outside current temporal descriptors, qualified wildcards, collations, multiple keys, aggregate alias ordering, or tie-order guarantee |
| `LIMIT` / `OFFSET` | 🟡 | Supported base-table `SELECT` forms are `LIMIT row_count`, `LIMIT row_count OFFSET offset`, and `LIMIT offset, row_count`; supported single-table `DELETE` and `UPDATE` admit only `LIMIT row_count`; all supported forms use unsigned decimal integer literals in the signed 64-bit range; `LIMIT 0` returns no rows for `SELECT` and matches no rows for supported DML; no signed, string, decimal, float, hex, bit, parameter, or expression limits |
| `@@sql_auto_is_null` | 🟡 | Limited scalar reads expose the fixed disabled value `0`; no mutable session state, `LAST_INSERT_ID()`-driven special lookup behavior, or special `IS NULL` lookup behavior |
| `@@sql_big_selects` | 🟡 | Limited scalar reads expose the fixed enabled value `1`; no mutable session state, `max_join_size`, optimizer row-estimate aborts, or changed descriptor-backed `SELECT` execution |
| `@@sql_buffer_result` | 🟡 | Limited scalar reads expose the fixed disabled value `0`; no mutable session state, temporary result table materialization, lock-release behavior, or changed descriptor-backed `SELECT` execution |
| `@@sql_select_limit` | 🟡 | Limited scalar reads expose the fixed no-limit value `18446744073709551615`; no mutable session state and no implicit row caps for descriptor-backed `SELECT` |
| `ALL` select modifier | 🟡 | Explicit `SELECT ALL` is accepted as the duplicate-preserving default for currently supported session scalar, scalar value projection, `DUAL`, descriptor-backed table column-list, table-backed wildcard, and aggregate select forms; no repeated `ALL`, mixed `ALL DISTINCT` / `ALL DISTINCTROW`, or other select modifiers |
| `DISTINCT` / `DISTINCTROW` | 🟡 | Limited one-column `SELECT DISTINCT column [AS alias] FROM table` and synonymous `SELECT DISTINCTROW column [AS alias] FROM table` over one descriptor-backed persistent base table with optional source table alias, source-qualified column references, baseline `WHERE`, optional `ORDER BY` resolving to the same selected column or its alias, and existing select `LIMIT`/`OFFSET`; integer/`NULL` descriptor columns only, no wildcard, multiple select items, expressions, literals, full multi-column duplicate rows, grouping, joins, or protocol-grade metadata |
| `UNION` | ❌ | ALL/DISTINCT, metadata, ordering |
| `INTERSECT` | ❌ | set operator semantics |
| `EXCEPT` | ❌ | set operator semantics |
| `TABLE` | ❌ | Table-value statement syntax and ordering/limit behavior |
| `VALUES` | ❌ | Standalone values statement and row constructor behavior |
| Locking clauses | 🟡 | Limited no-op `FOR UPDATE`, `FOR SHARE`, and `LOCK IN SHARE MODE` on currently supported scalar, `DUAL`, descriptor table, distinct, aggregate, grouped, `SQL_CALC_FOUND_ROWS`, `INSERT ... SELECT`, and `REPLACE ... SELECT` paths; `CREATE TABLE ... SELECT` with a locking clause is rejected; no transaction lock behavior, `OF table`, `NOWAIT`, `SKIP LOCKED`, repeated locking clauses, joins, subqueries, or privileges |
| `SQL_CALC_FOUND_ROWS` select modifier | 🟡 | Limited to existing non-distinct descriptor-backed single-base-table column-list and wildcard `SELECT` paths with optional source alias, baseline `WHERE`, one-column `ORDER BY`, existing `LIMIT`/`OFFSET`, and simple no-op locking clauses; records MySQL deprecation warning 1287 and updates `FOUND_ROWS()` state to the pre-limit matched row count; no scalar/`DUAL`, `DISTINCT`, grouped/aggregate-only, CTAS, `INSERT ... SELECT`, `REPLACE ... SELECT`, join, `UNION`, `TABLE`, CTE, subquery, locking options, replication, or protocol-metadata behavior |
| SELECT modifiers | 🟡 | Explicit `ALL`, limited one-column `DISTINCT` / `DISTINCTROW`, limited canonical no-op `HIGH_PRIORITY`, statement-level `STRAIGHT_JOIN`, `SQL_SMALL_RESULT`, `SQL_BIG_RESULT`, `SQL_BUFFER_RESULT`, deprecated no-op `SQL_NO_CACHE` with warning 1681, and limited `SQL_CALC_FOUND_ROWS`; no repeated modifiers, broad modifier permutations beyond the documented canonical sequence, `SQL_CACHE`, join-level `STRAIGHT_JOIN`, or full modifier interactions |
| Expression metadata | ❌ | Type, flags, charset, origin |

[Back to compatibility overview](../../COMPATIBILITY.md)
